# 开发 Prompt · v1.1.0 完整开发规格（轻量多设备 4 件事 + 架构重构）

> 给开发者的实施规格。v1.1.0 共 5 项交付，本文档覆盖全部：
> ① 经验共享（跨设备 knowledge/ + think.md 同步）　② 自迭代周报（daemon 汇总 lessons-missteps）　③ 权限作用域化（permission.local.json override）　④ daemon 主动巡检（4 项定时巡检）　⑤ 架构重构（11 包拆分：harness/ontology/scoring/core/orchestrator/daemon/ab-testing/work模板市场/think/skillopt/audit 收敛/mcp 更新，CLI 子命令全量迁移新包二进制）
>
> 关联 changelog：`docs/changelog/v1.1.0.md`（设计背景、边界表、技术依赖）
> 提出人：孔放勋 · 2026-07-14
>
> ⚠️ **版本说明**：v1.1.0 尚未发版，开发中。v1.0.9 仅交付 MCP compose tool（编排经 MCP 暴露），编排的**物理归位**与本次轻量多设备四件事都并入 v1.1.0。

---

## 总览：5 项交付与依赖

| # | 交付 | 改动范围 | 破坏性 | 前置依赖 |
|---|------|---------|:--:|---------|
| ① | 经验共享 | `.sofagent/knowledge` 共享层 + 加载链注入顺序 | 否 | ⑤（harness 独立后统一注入）|
| ② | 自迭代周报 | daemon weekly cron + LLM 汇总 | 否 | ①（复用 lessons 提取）|
| ③ | 权限作用域化 | `permission.local.json` 叠加解析 | 否 | — |
| ④ | daemon 主动巡检 | daemon daily/weekly cron × 4 | 否 | ②（巡检结果写入 lessons-missteps）|
| ⑤ | 架构重构 | 11 包拆分（harness/ontology/scoring/core/orchestrator/daemon/ab-testing/work模板市场/think/skillopt/audit 收敛/mcp 更新） | ✅ CLI 全量迁移（子命令→新包二进制） | — |

**建议执行顺序**：⑤ 先行打地基（harness 独立后 ①/② 的加载链注入才好落）→ ③ 可与 ⑤ 并行起步 → ① → ② → ④。

---

## 交付 ① — 经验共享：跨设备 knowledge/ + think.md 同步

### ①.1 目标与现状

- **现状**：每个设备的 `.sofagent/knowledge` 与 `think.md` 是孤岛。A 设备踩的坑，B 设备重踩。
- **目标**：跨设备同步 `knowledge/`（实体 / 概念 / 对比页）与「从 think.md 提取的可公开经验摘要」`knowledge/shared/lessons-{date}.md`；`think.md` 原始内容**不**共享。

### ①.2 设计决策（不可变，详见 changelog）

- 物理同步用 **git submodule 或 NAS 挂载**，sofagent 只管语义合并——不搞 P2P 协议、不搞消息队列。
- `think.md` 原始不共享（可能含设备特定路径 / 密钥片段 / 调试细节）；daemon 从 `think.md` 提取可公开经验写入 `knowledge/shared/lessons-{date}.md`。
- **加载链注入顺序**：先 `knowledge/shared/`（跨设备经验），再本机 `think.md`。
- **合并策略**：同名文件冲突 → `git merge`（人工 resolve，不自动覆盖）；`entities/concepts/comparisons` 页面用 frontmatter `relations` 字段做关联图合并；`lessons-*.md` 按日期追加，不合并。

### ①.3 具体任务

**任务 1.1 — knowledge 共享目录约定 + 同步引导文档**
- 在 `.sofagent/` 下明确 `knowledge/shared/` 子目录（语义层，存放 `lessons-*.md`）。
- 写 `docs/guides/knowledge-sync.md`：说明 git submodule / NAS 挂载两种物理同步方式、`ignore` 规则（`think.md`、`task/logs/` 不进共享层）。
- 改动文件：`.gitignore` / `.sofagent/.gitignore`（若本地 repo）；新增 `docs/guides/knowledge-sync.md`。
- 验证：`cat docs/guides/knowledge-sync.md` 结构完整；`git check-ignore .sofagent/think.md` 命中（确认不进共享层）。

**任务 1.2 — 加载链注入顺序调整（依赖 ⑤ harness）**
- 在 `@sofagent/harness` 的 `buildConstrainedSystemPrompt()` 中，将 `knowledge/shared/` 的读取放在本机 `think.md` **之前**。
- 改动文件：`sofagent/harness/src/constraint-loader.ts`（交付 ⑤ 抽到此包后改；改造前 `grep "think.md" src/` 确认实际位置）。
- 验证（单测）：给定 `knowledge/shared/lessons-x.md` 与本机 `think.md` 都含「路径穿越」经验，注入结果中 shared 段落排在前。

**任务 1.3 — lessons 提取（daemon 子任务，供 ② 复用）**
- 提取逻辑独立成 `sofagent/daemon/src/lessons-extract.ts`（daemon 独立包，见交付 ⑤ ⑤.5.6），从 `think.md` 抽取「踩坑 / 下次怎么办」段落写入 `knowledge/shared/lessons-{date}.md`。
- 验证：`node -e` 跑一个 `think.md` 样本，确认生成 lessons 文件且**不含**密钥片段 / 设备绝对路径。

### ①.4 硬约束

1. **不共享原始反思**：`think.md`、`task/logs/` 永不进入共享层（物理同步层 ignore 强制）。
2. **不内置传输**：物理同步机制不内置于 sofagent 二进制——只提供引导文档 + ignore 模板，避免引入 P2P / 传输依赖。
3. **shared 优先**：加载链必须 `knowledge/shared/` 优先于本机 `think.md`，防本机旧经验覆盖跨设备新经验。

### ①.5 验收标准

- [ ] `knowledge/shared/` 在 2+ 设备经 git submodule 同步后双方可见
- [ ] 加载链注入顺序单测通过（shared 在前）
- [ ] lessons 提取不泄露 `think.md` 原始私密段落（人工 review 样本）

---

## 交付 ② — 自迭代周报：daemon 汇总 think.md 生成 lessons-missteps

### ②.1 目标与现状

- **现状**：`think.md` 只在本机积累，无跨周期复盘。
- **目标**：daemon 定时汇总本周 `think.md`（及 ① 的 lessons），LLM 生成 `knowledge/shared/lessons-missteps-{YYYY-WW}.md`，下期加载链注入。

### ②.2 设计决策（不可变）

- 周报 frontmatter：`type: lessons` / `period: YYYY-WW` / `device` / `generated`。
- 三段结构：**反复出现的问题（≥3 次）** / **新发现问题** / **已改善**。
- 复用 ①.3 的 lessons 提取作为素材来源。

### ②.3 具体任务

**任务 2.1 — daemon 周报 weekly cron 任务**
- 在 daemon 调度里新增 `weekly` 任务，调用 lessons 汇总（LLM）。
- 改动文件：`sofagent/daemon/src/scheduler.ts`（daemon 独立包，见交付 ⑤ ⑤.5.6）；新增 `sofagent/daemon/src/weekly-report.ts`。
- 验证：`DEBUG=daemon` 手动触发一次，确认生成 `lessons-missteps-{YYYY-WW}.md`。

**任务 2.2 — LLM 汇总 prompt + 格式校验**
- 汇总 prompt 产出规定 frontmatter；加格式校验（`period` 正则、三段齐全）。
- 验证：给定 3 周 `think.md` 样本，输出符合周报格式 schema（可用 `ajv` 或最小正则校验）。

**任务 2.3 — 加载链注入周报（依赖 harness）**
- 在 harness 加载链中，将最新一期 `lessons-missteps-{YYYY-WW}.md` 注入（位于 `knowledge/shared/` 之后、本机 `think.md` 之前或并列）。
- 改动文件：`sofagent/harness/src/constraint-loader.ts`。

### ②.4 硬约束

1. **只读聚合**：周报不写回本机 `think.md`（不改反思源）。
2. **失败降级**：LLM 调用失败必须降级为「跳过本期周报 + 写 daemon 日志」，不得阻塞 daemon 主循环。

### ②.5 验收标准

- [ ] 周报格式稳定（跑 ≥2 期，schema 校验通过）
- [ ] 加载链注入周报单测通过
- [ ] LLM 失败降级路径单测 / 手动验证

---

## 交付 ③ — 权限作用域化：项目级 permission override

### ③.1 目标与现状

- **现状**：全局 `permission.json` 一份管所有场景。
- **目标**：项目级 `permission.local.json` override，优先级 **项目级 > 组织默认层**，**无自动降级**。

### ③.2 设计决策（不可变）

- 目录：`.sofagent/projects/{project-name}/permission.local.json`。
- 叠加规则：优先级覆盖；项目级窄权限凭证调用失败（401/403），系统**不自动降级**到默认层宽权限——宁可失败，绝不隐形越权。
- 项目级只声明 override 项，未声明的继承默认层。
- 频道级（Cloudtag 第三层）不在 v1.1.0，留给 v1.2.x。

### ③.3 具体任务

**任务 3.1 — 权限叠加解析器**
- 新增 `sofagent/audit/src/permission/resolve.ts`：读取默认 `permission.json` + 项目级 override，做深度合并（项目级覆盖同键，未声明继承）。
- 改动文件：原 permission 读取入口（`grep "permission.json" src/` 确认）。
- 验证（单测）：默认 `allowedPaths=[src,docs]`，项目级加 `infra/terraform` → 结果合并；项目级 `forbiddenPaths` 加 `prod/**` → 结果合并。

**任务 3.2 — 无降级硬规则**
- 在 resolve 阶段：项目级声明的凭证 / 路径被拒绝时，直接抛 `PermissionDenied`（带 context），**不得 fallback** 到默认层。
- 验证（单测）：项目级 `forbiddenPaths` 含 `infra/terraform/prod/**`，尝试写入该路径 → 拒绝，且对照「默认层允许」不触发。

**任务 3.3 — CLI / 文档校验**
- 在 `verify` / `doctor` 中校验 `permission.local.json` 格式合法（JSON schema）。
- 改动文件：`sofagent/audit/src/verify/checks.ts` 增一项；`docs/ARCHITECTURE.md` 权限章节更新。

### ③.4 硬约束

1. **永不自动降级**到默认层宽权限。
2. **override 缺失 / 格式错误**时，回退到默认层（不报错），但 daemon 告警提示。

### ③.5 验收标准

- [ ] 合并单测通过（覆盖 / 继承 / 冲突）
- [ ] 无降级单测通过（401/403 不越权）
- [ ] `permission.local.json` schema 校验接入 `verify`

---

## 交付 ④ — daemon 主动巡检：从被动监工到主动告警

### ④.1 目标与现状

- **现状**：daemon 每 30s 检查 `think.md/fde.md` 文件 hash 变化并通知——被动响应。
- **目标**：新增 4 项定时主动巡检，结果写入 `lessons-missteps` 或 daemon 通知。

### ④.2 设计决策（不可变）

| 巡检项 | 触发条件 | 告警方式 |
|--------|---------|---------|
| **审计历史分析** | 每日定时跑 | 扫描最近 7 天 audit history，同类规则连续触发 ≥3 次 → 写入 `lessons-missteps.md` |
| **doctor 健康度** | 每日定时跑 | `sofagent-core --doctor`（doctor 已迁 core，见交付 ⑤ ⑤.5.4），发现问题写入 daemon 通知 |
| **知识库新鲜度** | 每周定时跑 | `knowledge/` 最新文件 mtime > 30 天 → 提醒 FDE 更新 |
| **Skill 过时检测** | 每周定时跑 | `scoring.md` 最近 10 次评分趋势下降 → 提醒 Skill 需要优化 |

**不做的**：实时告警（IM 推送 / 邮件通知）——留给 v1.2.x 多设备协同时。

### ④.3 具体任务

**任务 4.1 — 巡检调度框架**
- daemon scheduler 支持 `daily` / `weekly` 两类 cron；新增 `sofagent/daemon/src/inspectors/` 目录（daemon 独立包，见交付 ⑤ ⑤.5.6），每巡检项一个模块。
- 改动文件：`sofagent/daemon/src/scheduler.ts`。

**任务 4.2 — 审计历史分析 inspector**
- 读近 7 天 audit history（`grep` audit log 目录确认路径），统计同规则触发计数 ≥3 → 写 `knowledge/shared/lessons-missteps-{YYYY-WW}.md`（追加「反复出现的问题」）。

**任务 4.3 — doctor 健康度 inspector**
- 调 `sofagent-core --doctor`（doctor 已迁 `@sofagent/core`，见交付 ⑤ ⑤.5.4；或内部 API），问题写入 daemon 通知队列。

**任务 4.4 — 知识库新鲜度 inspector**
- 扫 `knowledge/` 最新 mtime > 30 天 → daemon 通知「提醒 FDE 更新」。

**任务 4.5 — Skill 过时检测 inspector**
- 读 `scoring.md` 近 10 次评分，趋势下降 → 通知「Skill 需优化」。

### ④.4 硬约束

1. **隔离失败**：巡检失败不影响 daemon 主循环（各自 try/catch，错误进 daemon 日志）。
2. **不引入外部通道**：仅 daemon 本地通知 / 日志，IM 推送留给 v1.2.x。

### ④.5 验收标准

- [ ] 4 项巡检均可配置开关（config 字段 `inspect.auditHistory` / `doctor` / `knowledgeFreshness` / `skillStaleness`）
- [ ] 各 inspector 单测：给定样本输入，产出预期通知 / 写入
- [ ] daemon 主循环在 inspector 抛错时持续运行

---

## 交付 ⑤ — 架构重构：11 包拆分（audit 只做 audit）🔧

> ⚠️ 本项原在 v1.0.9 第 4 项规划、建议以 v1.0.10 发版。经评估后并入 **v1.1.0**（v1.1.0 尚未发版，开发中）。v1.0.9 仅交付 MCP compose tool（编排经 MCP 暴露），编排的**物理归位**与本次轻量多设备四件事都并入 v1.1.0。
>
> 终态以 [`v1.1.0.md`](./v1.1.0.md)「目标包结构（终态）」为准：11 包分层（基础层 harness/ontology/scoring/core · 运行层 orchestrator/daemon/ab-testing/work模板市场/think/skillopt · 协议层 mcp · 纯审计叶子 audit）。

### ⑤.1 背景与目标

`@sofagent/audit` 名义叫"审计"，实际是整个 sofagent **运行时**——审计规则 + 编排引擎（subagents/）+ 四层约束加载链（launcher.ts）+ 本体模型（ontology/）+ 后台引擎（daemon/）+ A/B 自进化（ab-testing/）+ Work模板市场 分发（commands/hub）+ Skill 优化（skill-safety-check/skillopt-integration）+ 反思生成（think-generator）+ 评分引擎（eval/）+ 自检（verify/doctor/hitl）+ 一堆运行时基础设施（config/run-envs/env-check/model-client/log-*）。一个包里塞了至少 11 个领域，形成"上帝包"。

**目标**：audit 只做审计。其余 10 个领域拆成独立包，按"基础层 / 运行层 / 协议层"清晰分层。各包依赖方向严格单向，严禁循环。

### ⑤.2 目标架构（11 包终态，含关键文件）

```
基础层（叶子，不依赖任何 sofagent 包）
  @sofagent/harness       src/constraint-loader.ts  buildConstrainedSystemPrompt()
                          （SKILL.md + fde.md + think.md + knowledge + persona 四层约束）
  @sofagent/ontology      src/{merge-engine,ontology-view,types}.ts
                          （OntologyObject / Action / Constraint / MergedOntology）
  @sofagent/scoring          src/{types,eval-scorer,eval-runner,eval-reporter}.ts
                          （ScoreBreakdown 评分引擎）
  @sofagent/core          src/{verify/,doctor.ts,hitl/,config/,run-envs,env-check,
                          cost-baseline,model-client,diff-parser,reporter,log-*,
                          utils,shared/}  运行时基础设施 + 自检  bin: sofagent-core

运行层（单向依赖基础层或彼此）
  @sofagent/orchestrator   src/{composer,launcher,registry,builtin-agents,
                          audit-sub-agent}.ts + DAG executor + orchestrator-compare.ts
                          bin: sofagent-orchestrator / -orchestrator-compare        → harness
  @sofagent/daemon        src/{cron,fs-watch,run-fs-audit,snapshot,lessons-extract,
                          weekly-report,inspectors/}.ts   bin: sofagent-daemon    → audit, harness, ontology, core
  @sofagent/ab-testing    src/{ab-runner,ab-promoter,types}.ts   bin: sofagent-ab  → orchestrator, harness, scoring
  @sofagent/work模板市场           src/hub.ts（list / deploy）  bin: sofagent-work模板市场          → orchestrator, ontology, scoring（可选→skillopt）
  @sofagent/think         src/think-generator.ts   bin: sofagent-think            → audit（AuditResult）, core（diff-parser）
  @sofagent/skillopt      src/{skill-safety-check,skillopt-integration}.ts
                          bin: sofagent-skillopt                                    → ontology, core

协议层
  @sofagent/mcp           src/mcp-server.ts（sofagent_compose → orchestrator；可暴露 work模板市场 / ab）
                                                                                    → orchestrator, work模板市场

纯审计（叶子，只被依赖）
  @sofagent/audit        rules/ webhook/ filesystem/ audit-{history,log,regression,
                          root-cause}.ts fix-suggestions evidence-mode sensitive-env
                          qa-boundary-verify permission/   bin: sofagent-audit        → ontology, core
```

### ⑤.3 依赖铁律（11 包单向依赖 + 禁止循环）

- **叶子层**（绝不 import 任何 sofagent 包）：`harness` / `ontology` / `scoring` / `core`。
- **单向依赖方向**（只能按箭头方向 import）：
  - `orchestrator → harness`
  - `daemon → audit + harness + ontology + core`
  - `ab-testing → orchestrator + harness + eval + core`
  - `work模板市场 → orchestrator + ontology + eval`（可选 `→ skillopt`）
  - `think → audit + core`
  - `skillopt → ontology + core`
  - `mcp → orchestrator + work模板市场`
  - `audit → ontology + core`（知识感知规则处 import ontology；基础设施处 import core）
- **关键解耦**：
  - `audit` 绝不反向依赖 `think`：`generateThinkEntry` 的调用从 `audit/src/index.ts` 移除，改由 daemon 跑完 fs-audit 后 / 或独立 `sofagent-think` CLI 触发。
  - `skillopt` 绝不反向依赖 `work模板市场`：hub 可选依赖 skillopt 提供 `sofagent-work模板市场 skillopt` 包装，反之不可。
  - `core` 不依赖任何运行包（本版不引入 ontology 依赖，保持纯叶子）。

### ⑤.4 现状证据（改造前必须重新 grep 确认实际行号）

- `sofagent/audit/package.json` 的 `bin` 同时含 `sofagent-audit` 和 `sofagent-orchestrator-compare`（编排 A/B 二进制错放在 audit 包）。
- `sofagent/audit/src/subagents/` 含 5 个文件：`composer.ts` `launcher.ts` `registry.ts` `builtin-agents.ts` `audit-sub-agent.ts`。
- `audit/src/index.ts` 是"上帝入口"，集中 dispatch 了全部子命令与动态 import（当前实测行号，开发者动手前请重新 `grep` 确认）：
  - `:40` `import { generateThinkEntry } from './think-generator'`（think 独立后移除）
  - `:41` `import { runFilesystemAudit } from './daemon/run-fs-audit'`（daemon 独立后改 `@sofagent/daemon`）
  - `:166` `compose` 子命令分支；`:184` `subagent run` 子命令分支（整段移除）
  - `:294` `SUBCOMMANDS = ['hub','skillopt-run','compose','subagent','ontology']`
  - `:501-502 / :794` `import('./daemon/snapshot')`（daemon 包）
  - `:540` `import('./skillopt-integration')`（skillopt 包）
  - `:612` `import('./commands/init')`（`commands/init` 保留在 audit，不迁）
  - `:621 / :625` `import('./commands/doctor')`（core 包）
  - `:637` `import('./subagents/composer')`；`:662` `import('./subagents/registry')`；`:671` `import('./subagents/launcher')`（orchestrator 包）
  - `:686 / :691` `import('./commands/hub')`（work模板市场 包）
  - `:701` `import('./ontology/ontology-view')`（ontology 包）
  - `:721` `import('./daemon/fs-watch')`；`:732` `import('./daemon/cron')`（daemon 包）
  - `:722` `import('./config/watch-config')`（core 包）
  - `:833-834` `import('./eval/...')`（eval 包）
  - `:847-849` `import('./ab-testing/...')`（ab-testing 包）
  - `:1022` `generateThinkEntry(diffFiles, results, args.task)`（think 独立后移除调用）
- `audit/src/ab-testing/ab-runner.ts:131` `const { buildConstrainedSystemPrompt } = await import('../subagents/launcher')`（→ `@sofagent/harness`）；`:11-14` import `../eval/types` `../eval/eval-scorer` `../model-client` `../model-client` ModelMessage（→ `@sofagent/scoring` / `@sofagent/core`）。
- `audit/src/commands/doctor.ts:14-18` import `../config-loader` `../cost-baseline` `../skillopt-integration` `../subagents/launcher`（→ `@sofagent/core` / `@sofagent/skillopt` / `@sofagent/orchestrator`）。
- `audit/src/think-generator.ts:9-10` import `./diff-parser` `./reporter`（→ `@sofagent/core` / `@sofagent/audit`）。
- `audit/src/skill-safety-check.ts:19-28` import `./rules/skill-safety-*`（`rules/` 随 audit 保留；skillopt 包经 `@sofagent/audit` 复用这些规则类型，或把 `rules/skill-safety-*` 也迁到 skillopt——见 ⑤.5.10 待确认）。
- `audit/src/daemon/run-fs-audit.ts:17-21` import `../diff-parser` `../audit-history` `../rules/runner` `../config-loader`（→ `@sofagent/core` / `@sofagent/audit`）。
- `@sofagent/mcp`：`src/mcp-server.ts:503` `const cmd = ['compose', '--task', args.task]`；`:512` `execFileSync('sofagent-audit', cmd, ...)` 直接调 audit 的 compose CLI。
- 全量改写锚点：`grep -rn "from '\\(\\.\\./\\|\\./\\)\\(subagents\\|ontology\\|eval\\|diff-parser\\|reporter\\|verify\\|hitl\\|config\\|run-envs\\|env-check\\|cost-baseline\\|model-client\\|log-\\|utils\\|shared\\|daemon\\|ab-testing\\|work模板市场\\|skill-safety-check\\|skillopt-integration\\|think-generator\\)" sofagent/audit/src`
- FDE 文档：`FDE/FDE.md`（行 286、309、367 等）与 `FDE/templates/*` 多处写 `sofagent-audit compose --task` 或 `ao compose`。
- 自检脚本：`src/verify.ts:195`「运行 ao compose 测试」、`src/verify/checks.ts:51`「DeepAgents compose 可用」、`FDE/fde-install.sh`「ao compose 可用 / agency-orchestrator」。

> 注：以上行号基于当前 dev 快照，开发者动手前请重新 `grep` 确认（审计规则 A1–A17 编号可能有微调，index.ts 分支行号随版本浮动）。

### ⑤.5 各包迁移任务（按依赖顺序 / Wave）

> 通用约定：每个新包 `package.json` 统一 `version: "1.1.0"`、`scripts: { build: "tsc", test: "vitest run" }`、纳入根 `package.json` 的 npm workspace；`tsconfig.json` 沿用 audit 包配置风格（含 `paths` 别名便于本地联调）。验证默认动作：各包 `npm test` + `npm run build` 全绿 + 对应新二进制手动验证。

#### ⑤.5.1 harness 独立 → `@sofagent/harness`（基础层叶子）

- **背景**：四层约束加载链 `buildConstrainedSystemPrompt()` 是 sofagent 核心 IP，被 orchestrator 与 ab-testing 共用，必须独立成包，不允许留在 orchestrator 内 fallback。
- **现状证据**：`audit/src/subagents/launcher.ts:210` 定义 `buildConstrainedSystemPrompt()`，被 `audit/src/ab-testing/ab-runner.ts:131` 动态 import 引用。
- **新包目录**：`sofagent/harness/`（与 `audit/` `mcp/` 同级）。
- **package.json**：`name: "@sofagent/harness"`，`version: "1.1.0"`，无 `bin`，`dependencies: {}`（不依赖任何 sofagent 包），`scripts: { build: "tsc", test: "vitest run" }`。
- **迁出文件**：从 `audit/src/subagents/launcher.ts` 抽取 `buildConstrainedSystemPrompt()` 及其 SKILL.md/fde.md/think.md/knowledge/persona 加载逻辑 + 相关类型，落 `sofagent/harness/src/constraint-loader.ts` 并 `export`。
- **audit 内需删除**：无独立目录（约束层原寄居 `launcher.ts`，`launcher.ts` 整体随 subagents 迁 orchestrator，但该函数留在 harness；ab-testing 的引用改写见下）。
- **跨包 import 改写点**：
  - `audit/src/ab-testing/ab-runner.ts:131` `const { buildConstrainedSystemPrompt } = await import('../subagents/launcher')` → `await import('@sofagent/harness')`（ab-testing 迁到 ab-testing 包后同样改 `@sofagent/harness`）。
  - `orchestrator/src/launcher.ts`（迁后）`spawnSubAgent()` 内调用 `buildConstrainedSystemPrompt` → `from '@sofagent/harness'`。
- **硬约束**：🔴 必须独立成包；`harness` 不得 import 任何 sofagent 包（含 orchestrator/audit）。
- **验证**：`cd sofagent/harness && npm test && npm run build`；`node -e "const h=require('@sofagent/harness'); console.log(typeof h.buildConstrainedSystemPrompt)"` 输出 `function`。

#### ⑤.5.2 ontology 独立 → `@sofagent/ontology`（基础层叶子）

- **背景**：本体模型（知识图谱合并 / 视图 / 类型）是 harness 加载知识图谱、daemon 做新鲜度与关联图合并、knowledge 共享层 `relations` 合并的共用基础，需独立。
- **现状证据**：`audit/src/ontology/{merge-engine.ts, ontology-view.ts, types.ts}`，定义 `OntologyObject/Action/Constraint/MergedOntology`。
- **新包目录**：`sofagent/ontology/`。
- **package.json**：`@sofagent/ontology` v1.1.0，无 bin，`dependencies: {}`，`scripts` 同上。
- **迁出文件**：`audit/src/ontology/*` → `sofagent/ontology/src/*`。
- **audit 内需删除**：`src/ontology/` 目录。
- **跨包 import 改写点**：全量 `grep -rn "from '.*ontology" sofagent/audit/src`（含 `../ontology/ontology-view` `./ontology/...`）：
  - `audit/src/index.ts:701` `import('./ontology/ontology-view')` → `import('@sofagent/ontology')`。
  - 迁到 daemon / harness 的相关引用改为 `from '@sofagent/ontology'`。
- **硬约束**：`ontology` 不得 import 任何 sofagent 包。
- **验证**：ontology `npm test` + `npm run build`；导出 `merge-engine` `ontology-view` `types` 三件套。

#### ⑤.5.3 scoring 独立 → `@sofagent/scoring`（基础层叶子）

- **背景**：评分引擎 `ScoreBreakdown` 被 ab-testing 依赖，属基础能力，独立成包。
- **现状证据**：`audit/src/eval/{types.ts:47 ScoreBreakdown, eval-scorer.ts, eval-runner.ts, eval-reporter.ts}`，被 `audit/src/ab-testing/ab-runner.ts:11` `import type { ScoreBreakdown, TestCase } from '../eval/types'` 依赖。
- **新包目录**：`sofagent/scoring/`。
- **package.json**：`@sofagent/scoring` v1.1.0，无 bin，`dependencies: {}`，`scripts` 同上。
- **迁出文件**：`audit/src/eval/*` → `sofagent/scoring/src/*`。
- **audit 内需删除**：`src/eval/` 目录。
- **跨包 import 改写点**：
  - `audit/src/ab-testing/ab-runner.ts:11` `import type { ScoreBreakdown, TestCase } from '../eval/types'` → `from '@sofagent/scoring'`；`:12` `import { scoreCase } from '../eval/eval-scorer'` → `from '@sofagent/scoring'`。
  - ab-testing 迁到 ab-testing 包后同样改 `@sofagent/scoring`。
  - `audit/src/__tests__/eval.test.ts` 等 eval 相关测试随包迁移。
- **硬约束**：`scoring` 不得 import 任何 sofagent 包。
- **验证**：eval `npm test` + `npm run build`；导出 `scoreCase` `ScoreBreakdown` 等。

#### ⑤.5.4 core 独立 → `@sofagent/core`（基础层边界项收敛）

- **背景**：运行时基础设施 + 自检（verify/doctor/hitl/config/run-envs/env-check/cost-baseline/model-client/log-*/diff-parser/reporter/utils/shared）不是审计逻辑，也不是独立领域，归 core 最干净。doctor / verify 子命令迁此包，对应 `sofagent-core doctor` / `sofagent-core verify`。
- **现状证据**：`audit/src/` 下散落 `verify/` `commands/doctor.ts` `hitl/` `config/` `config-loader.ts` `config-template.ts` `config-suggestion.ts` `run-envs.ts` `env-check.ts` `cost-baseline.ts` `model-client.ts` `log-checker.ts` `log-reader.ts` `diff-parser.ts` `reporter.ts` `shared/` `utils/` 等。
- **新包目录**：`sofagent/core/`。
- **package.json**：`@sofagent/core` v1.1.0，`bin: { "sofagent-core": "dist/index.js" }`，`dependencies: {}`（本版不引入 ontology），`scripts` 同上。
- **迁出文件清单（具体子路径）**：
  - `audit/src/verify/` → `core/src/verify/`
  - `audit/src/commands/doctor.ts` → `core/src/doctor.ts`（`commands/init.ts` 保留在 audit，不迁）
  - `audit/src/hitl/` → `core/src/hitl/`
  - `audit/src/config/` + `audit/src/config-loader.ts` + `audit/src/config-template.ts` + `audit/src/config-suggestion.ts` + `audit/src/config/watch-config.ts`（若存在）→ `core/src/config/`
  - `audit/src/run-envs.ts` `env-check.ts` `cost-baseline.ts` `model-client.ts` `log-checker.ts` `log-reader.ts` `diff-parser.ts` `reporter.ts` → `core/src/`
  - `audit/src/shared/` → `core/src/shared/`（atomic-write / constants 等）
  - `audit/src/utils/` → `core/src/utils/`
- **audit 内需删除**：上述目录 / 文件（audit 内仅保留对其的 `@sofagent/core` 引用）。
- **跨包 import 改写点**（全量 `grep -rn "from '\(\./\| \.\./\)\(verify\|hitl\|config\|run-envs\|env-check\|cost-baseline\|model-client\|log-\|diff-parser\|reporter\|shared\|utils\)" sofagent/audit/src`）：
  - `audit/src/index.ts:28` `./diff-parser`、`:30` `./log-checker`、`:31` `./reporter`、`:32` `./config-loader`、`:35` `./config-suggestion`、`:42` `./shared/constants.js` → 统一 `@sofagent/core`。
  - `audit/src/index.ts:621/625` `import('./commands/doctor')` → `import('@sofagent/core')`（core 暴露 runDoctor）。
  - `audit/src/index.ts:722` `import('./config/watch-config')` → `@sofagent/core`。
  - `audit/src/commands/doctor.ts:14` `../config-loader`、`:16` `../cost-baseline` → `@sofagent/core`（doctor 整体迁 core，本项自然消解）。
  - `audit/src/daemon/run-fs-audit.ts:17` `../diff-parser`、`:21` `../config-loader` → `@sofagent/core`（daemon 包内）。
  - `audit/src/audit-history.ts:27` `./config-loader`、`:28` `./shared/atomic-write`；`audit/src/audit-log.ts:12` `./shared/constants.js`；`audit/src/env-check.ts:9` `./shared/constants.js` → `@sofagent/core`（audit 保留这些审计文件，仅改 import）。
- **硬约束**：core 不依赖任何运行包（保持纯叶子）；`AuditResult` 类型定义在 `reporter.ts` 随 core 迁移，audit 通过 `export type { AuditResult } from '@sofagent/core'` 重新导出，以保持 `think → audit` 的依赖方向（见 ⑤.5.9）。
- **验证**：core `npm test` + `npm run build`；`sofagent-core doctor` / `sofagent-core verify` 可用（手动）。

#### ⑤.5.5 orchestrator 独立 → `@sofagent/orchestrator`（运行层）

- **背景**：编排引擎（compose / subagent / DAG）长期寄居 audit，导致 `sofagent-audit compose` 语义错配。本次物理归位。
- **现状证据**：`audit/src/subagents/{composer,launcher,registry,builtin-agents,audit-sub-agent}.ts` + `orchestrator-compare.ts`；`compose`/`subagent` 子命令在 `index.ts:166/184`；编排 A/B 二进制 `sofagent-orchestrator-compare` 错放 audit 的 bin。
- **新包目录**：`sofagent/orchestrator/`。
- **package.json**：`@sofagent/orchestrator` v1.1.0，`bin: { "sofagent-orchestrator": "dist/index.js", "sofagent-orchestrator-compare": "dist/orchestrator-compare.js" }`，`dependencies: { deepagents, js-yaml, "@sofagent/harness" }`，`scripts` 同上。
- **迁出文件**：`audit/src/subagents/*` → `orchestrator/src/`；`audit/src/orchestrator-compare.ts` → `orchestrator/src/orchestrator-compare.ts`。
- **audit 内需删除**：`src/subagents/` 目录、`src/orchestrator-compare.ts`。
- **跨包 import 改写点**：
  - `orchestrator/src/launcher.ts` 内 `buildConstrainedSystemPrompt` 调用 → `from '@sofagent/harness'`（见 ⑤.5.1）。
  - `audit/src/index.ts:637` `import('./subagents/composer')`、`:662` `import('./subagents/registry')`、`:671` `import('./subagents/launcher')` → 移除（compose/subagent 子命令分支 166/184 整段删）。
  - `orchestrator-compare.ts` 内 `import { composeWithDeepAgents } from './subagents/composer.js'` → `./composer`（已同包）。
  - `audit/src/commands/doctor.ts:18` `import { readRuntimeState } from '../subagents/launcher'` → `from '@sofagent/orchestrator'`（readRuntimeState 随 launcher 迁 orchestrator）。
- **硬约束**：orchestrator 仅依赖 harness；不得反向依赖 audit。`composeWithDeepAgents`/`spawnSubAgent` 行为不变（只物理归位）。
- **验证**：orchestrator `npm test` + `npm run build`；`sofagent-orchestrator compose --task "..."` 输出合法 YAML；`sofagent-orchestrator subagent run fde --task "..."` 起 Sub Agent（约束经 harness 注入）；`sofagent-orchestrator-compare` 可用。

#### ⑤.5.6 daemon 独立 → `@sofagent/daemon`（运行层）

- **背景**：常驻引擎 / 定时巡检（cron / fs-watch / run-fs-audit / snapshot）长期寄居 audit。轻量多设备 ①/②/④ 的新增代码（lessons-extract / weekly-report / inspectors）本就该落在 daemon 包，不先拆则继续在 audit 内长肉。
- **现状证据**：`audit/src/daemon/{cron.ts, fs-watch.ts, run-fs-audit.ts, snapshot.ts, run-fs-audit.test.ts}`。
- **新包目录**：`sofagent/daemon/`。
- **package.json**：`@sofagent/daemon` v1.1.0，`bin: { "sofagent-daemon": "dist/index.js" }`，`dependencies: { "@sofagent/audit", "@sofagent/harness", "@sofagent/ontology", "@sofagent/core" }`，`scripts` 同上。
- **迁出文件**：`audit/src/daemon/*` → `daemon/src/*`。**注意**：①/②/④ 规划的 `lessons-extract.ts` `weekly-report.ts` `inspectors/`（原 dev-prompt 误写在 `audit/src/daemon/` 下）直接落到 `daemon/src/`（见 ①.3 / ②.3 / ④.3 路径修正）。
- **audit 内需删除**：`src/daemon/` 目录（含 run-fs-audit.test.ts 一并迁 daemon）。
- **跨包 import 改写点**：
  - `audit/src/index.ts:41` `import { runFilesystemAudit } from './daemon/run-fs-audit'` → 移除（fs-audit 子命令入口迁 daemon 包，audit 不再 dispatch）。
  - `audit/src/index.ts:501-502/794` `import('./daemon/snapshot')`、`:721` `import('./daemon/fs-watch')`、`:732` `import('./daemon/cron')` → 移除（daemon 子命令整体迁 daemon 包）。
  - `daemon/src/run-fs-audit.ts:17` `../diff-parser` → `@sofagent/core`；`:18-21` `../audit-history` `../rules/runner` `../config-loader` → `@sofagent/audit` / `@sofagent/core`（daemon → audit + core 依赖方向）。
  - `daemon/src/fs-watch.ts:21` `import { loadWatchConfig } from '../config/watch-config'` → `@sofagent/core`。
  - daemon 在跑完 fs-audit 后**调用** `@sofagent/think`（think-generator），但 think 不归属 daemon 包（见 ⑤.5.9）。
- **硬约束**：daemon 依赖 audit + harness + ontology + core，不得反向；think 的生成由 daemon 触发但 belong to think 包。
- **验证**：daemon `npm test` + `npm run build`；`sofagent-daemon` 启动 cron/fs-watch；手动触发 fs-audit 后 lessons 提取可用。

#### ⑤.5.7 ab-testing 独立 → `@sofagent/ab-testing`（运行层）

- **背景**：A/B 自进化（ab-runner / ab-promoter）依赖 eval 与编排，独立成包。
- **现状证据**：`audit/src/ab-testing/{ab-runner.ts, ab-promoter.ts, types.ts}`，`ab-runner.ts:11-14` import `../eval/*` `../model-client` `../subagents/launcher(buildConstrainedSystemPrompt)`。
- **新包目录**：`sofagent/ab-testing/`。
- **package.json**：`@sofagent/ab-testing` v1.1.0，`bin: { "sofagent-ab": "dist/index.js" }`，`dependencies: { "@sofagent/orchestrator", "@sofagent/harness", "@sofagent/scoring", "@sofagent/core" }`，`scripts` 同上。
- **迁出文件**：`audit/src/ab-testing/*` → `ab-testing/src/*`（含 `ab-runner.ts` `ab-promoter.ts` `types.ts` 及 `__tests__/ab-testing.test.ts`）。
- **audit 内需删除**：`src/ab-testing/` 目录。
- **跨包 import 改写点**：
  - `ab-testing/src/ab-runner.ts:11` `import type { ScoreBreakdown, TestCase } from '../eval/types'` → `@sofagent/scoring`；`:12` `import { scoreCase } from '../eval/eval-scorer'` → `@sofagent/scoring`；`:13` `import { callModelAPI } from '../model-client'` → `@sofagent/core`；`:14` `import type { ModelMessage } from '../model-client'` → `@sofagent/core`；`:131` `buildConstrainedSystemPrompt` → `@sofagent/harness`。
  - `audit/src/index.ts:847-849` `import('./ab-testing/...')` → 移除（ab-testing 子命令迁 ab-testing 包，对应 `sofagent-ab`）。
- **硬约束**：ab-testing 依赖 orchestrator + harness + eval + core，不得反向依赖 audit。
- **验证**：ab-testing `npm test` + `npm run build`；`sofagent-ab` 可用。

#### ⑤.5.8 work模板市场 独立 → `@sofagent/work模板市场`（运行层）

- **背景**：Work模板市场 模板分发（list / deploy）本质是把模板部署出去（底层调 orchestrator 跑编排），与编排是"消费者"关系，独立成包。
- **现状证据**：`audit/src/commands/hub.ts`（`sofagent-audit hub list/deploy`），仅 import `fs`/`path`，部署底层将调 orchestrator。
- **新包目录**：`sofagent/work模板市场/`。
- **package.json**：`@sofagent/work模板市场` v1.1.0，`bin: { "sofagent-work模板市场": "dist/index.js" }`，`dependencies: { "@sofagent/orchestrator", "@sofagent/ontology", "@sofagent/scoring", "@sofagent/skillopt?" }`（可选 skillopt），`scripts` 同上。
- **迁出文件**：`audit/src/commands/hub.ts` → `work模板市场/src/hub.ts`（及 `work模板市场 list/deploy` 子命令入口）。
- **audit 内需删除**：`src/commands/hub.ts`。
- **跨包 import 改写点**：
  - `audit/src/index.ts:686/691` `import('./commands/hub')` → 移除（work模板市场 子命令迁 hub 包，对应 `sofagent-work模板市场 list/deploy`）。
  - work模板市场 部署时调用 `@sofagent/orchestrator` 的 `composeWithDeepAgents` / `spawnSubAgent`（文档表达"work模板市场 调用 sofagent-orchestrator 部署 Workflow"，不写成 work模板市场 自己生成）。
- **硬约束**：work模板市场 是编排消费者，可选依赖 skillopt 提供 `sofagent-work模板市场 skillopt` 包装，skillopt 绝不反向依赖 work模板市场。
- **验证**：work模板市场 `npm test` + `npm run build`；`sofagent-work模板市场 list` / `sofagent-work模板市场 deploy <模板>` 可用。

#### ⑤.5.9 think 独立 → `@sofagent/think`（运行层 · 审计后反思生成）

- **背景**：`think-generator` 是"审计跑完之后的后处理"（基于 diff + AuditResult 写 think.md），与 daemon 的"定时/文件监听触发审计"是不同关注点。硬塞进 daemon 会让 daemon 既管调度又管内容生成，违背单一职责。
- **现状证据**：`audit/src/think-generator.ts:9-10` `import type { DiffFile } from './diff-parser'` `import type { AuditResult } from './reporter'`；`audit/src/index.ts:40` `import { generateThinkEntry } from './think-generator'`、`:1022` `generateThinkEntry(diffFiles, results, args.task)` 调用。
- **新包目录**：`sofagent/think/`。
- **package.json**：`@sofagent/think` v1.1.0，`bin: { "sofagent-think": "dist/index.js" }`，`dependencies: { "@sofagent/audit", "@sofagent/core" }`，`scripts` 同上。
- **迁出文件**：`audit/src/think-generator.ts` → `think/src/think-generator.ts`（及 `think-generator.test.ts`）。
- **audit 内需删除**：`src/think-generator.ts`（及测试）。
- **跨包 import 改写点**：
  - `think/src/think-generator.ts:9` `import type { DiffFile } from './diff-parser'` → `from '@sofagent/core'`；`:10` `import type { AuditResult } from './reporter'` → `from '@sofagent/audit'`（AuditResult 由 audit 从 core 重导出，保持 `think → audit` 依赖）。
  - `audit/src/index.ts:40` `import { generateThinkEntry } from './think-generator'` → **移除**；`:1022` `generateThinkEntry(...)` 调用 → **移除**（改由 daemon 跑完 fs-audit 后调用 `@sofagent/think`，或独立 `sofagent-think` CLI 触发）。
  - `audit/src/think-generator.test.ts:11` `import type { AuditResult } from './reporter'` → `@sofagent/audit`（随包迁移）。
- **硬约束**：think 是 audit 输出（AuditResult）的消费者，不是 audit 的一部分；audit 保持纯叶子，绝不反向依赖 think。
- **验证**：think `npm test` + `npm run build`；`sofagent-think` 基于给定 diff + AuditResult 生成 think.md 条目（幂等）。

#### ⑤.5.10 skillopt 独立 → `@sofagent/skillopt`（运行层 · Skill 优化）

- **背景**：Skill 优化（读取 SKILL.md → 安全校验 → 调 skillopt 引擎就地演化 → 产出候选）与 Hub 的 Workflow 模板分发完全无关，独立成包。原 `skillopt-run` 子命令 → `sofagent-skillopt`。
- **现状证据**：`audit/src/skill-safety-check.ts`（import `./rules/skill-safety-*`）+ `audit/src/skillopt-integration.ts`（import `child_process`/`fs`），对应 `sofagent-audit skillopt-run` 子命令。
- **新包目录**：`sofagent/skillopt/`。
- **package.json**：`@sofagent/skillopt` v1.1.0，`bin: { "sofagent-skillopt": "dist/index.js" }`，`dependencies: { "@sofagent/ontology", "@sofagent/core", "@sofagent/audit?" }`（复用 audit 的 `rules/skill-safety-*` 类型，见待确认），`scripts` 同上。
- **迁出文件**：`audit/src/skill-safety-check.ts` `audit/src/skillopt-integration.ts` → `skillopt/src/`（及 `__tests__/skillopt-integration.test.ts` `skill-safety-check.test.ts`）。
- **audit 内需删除**：`src/skill-safety-check.ts` `src/skillopt-integration.ts`（及测试）。
- **跨包 import 改写点**：
  - `audit/src/index.ts:540` `import('./skillopt-integration')` → 移除（skillopt-run 子命令迁 skillopt 包，对应 `sofagent-skillopt`）。
  - `audit/src/commands/doctor.ts:17` `import { isSkillOptAvailable } from '../skillopt-integration'` → `from '@sofagent/skillopt'`（doctor 迁 core 后，core 内 import `@sofagent/skillopt`；**注意依赖方向**：core 是叶子，不应依赖 skillopt —— 建议把 `isSkillOptAvailable` 的探测逻辑下放到 skillopt 包，doctor 仅做可选调用，**待确认**见 修订记录）。
- **硬约束**：skillopt 依赖 ontology + core，绝不反向依赖 work模板市场。
- **验证**：skillopt `npm test` + `npm run build`；`sofagent-skillopt --input <path>` 可用（原 `sofagent-audit skillopt-run`）。

#### ⑤.5.11 audit 收敛为纯审计

- **背景**：audit 在拆出 10 个包后，仅保留审计与治理逻辑，成为纯审计叶子。
- **现状证据**：见 ⑤.4 各跨包引用——audit 当前仍承载全部 10 个领域。
- **移除（audit 内删除）**：
  - 目录：`src/subagents/` `src/orchestrator-compare.ts` `src/ab-testing/` `src/ontology/` `src/daemon/` `src/eval/` `src/commands/hub.ts` `src/think-generator.ts` `src/skill-safety-check.ts` `src/skillopt-integration.ts` `src/verify/` `src/commands/doctor.ts` `src/hitl/` `src/config/` `src/run-envs.ts` `src/env-check.ts` `src/cost-baseline.ts` `src/model-client.ts` `src/log-checker.ts` `src/log-reader.ts` `src/diff-parser.ts` `src/reporter.ts` `src/shared/` `src/utils/`（及对应 `*.test.ts`）。
  - 子命令：`compose` `subagent` `work模板市场` `ab-testing` `skillopt-run` `doctor` `verify`（迁对应新包二进制）。
- **保留（audit 仅留）**：`rules/`（A1–A17）`webhook/`（审计通知）`filesystem/`（文件系统审计）`audit-{history,log,regression,root-cause}.ts` `fix-suggestions.ts` `evidence-mode.ts` `sensitive-env*` `qa-boundary-verify.ts` `permission/`（权限作用域化，见交付 ③，不拆出）。
- **依赖方向收敛**：audit 仅（知识感知规则处）import `@sofagent/ontology`、（基础设施处）import `@sofagent/core`；不依赖 orchestrator/daemon/ab-testing/work模板市场/think/skillopt。`AuditResult` 经 `export type { AuditResult } from '@sofagent/core'` 重导出供 think 消费。
- **index.ts 收口**（集中改动）：`:40` 移除 think-generator import；`:166/184` 移除 compose/subagent 分支；`:294` `SUBCOMMANDS = ['hub','skillopt-run','compose','subagent','ontology']` → 收敛为 `['ontology']`（仅保留 ontology view 子命令；其余已全部迁出）；`:1022` 移除 generateThinkEntry 调用；`--help` 文案（约 `:237-265`）删除 compose/subagent/work模板市场/ab-testing/skillopt-run/doctor/verify 行，保留 ontology。
- **硬约束**：audit 不反向依赖任何运行包；`sofagent-audit` 仅暴露审计 / 治理子命令（ontology view 等）。
- **验证**：audit `npm test` + `npm run build`；`sofagent-audit --help` 不再含 compose/subagent/work模板市场/ab-testing/skillopt-run/doctor/verify；`sofagent-audit ontology view` 保留。

#### ⑤.5.12 mcp 更新引用

- **背景**：`@sofagent/mcp` 的 `sofagent_compose` tool 直接 spawn `sofagent-audit compose`，编排归位后必须改调 `@sofagent/orchestrator`。
- **现状证据**：`sofagent/mcp/src/mcp-server.ts:503` `const cmd = ['compose', '--task', args.task]`；`:512` `execFileSync('sofagent-audit', cmd, ...)`；`:380` 日志文本 `[sofagent-audit]`；`:494-495` doc 注释。
- **改动**：
  - 将 `execFileSync('sofagent-audit', ['compose', ...])` 改为 `execFileSync('sofagent-orchestrator', ['compose', ...])`（或推荐 `import { composeWithDeepAgents } from '@sofagent/orchestrator'` 进程内调用，少一层子进程）。
  - `--agent` / `--run` 参数透传。
  - 工具 description / 日志文本 `sofagent-audit` → `sofagent-orchestrator`；doc 注释「调用 CLI compose 逻辑」→「调用 @sofagent/orchestrator 编排引擎」。
- **package.json**：`mcp/package.json` 增 `@sofagent/orchestrator`（及按需 `@sofagent/work模板市场` `@sofagent/ab-testing`）依赖。
- **硬约束**：mcp 仅依赖 orchestrator + work模板市场（及可选的 ab-testing），不得依赖 audit 的编排能力。
- **验证**：mcp `npm test` + `npm run build`；经 MCP 客户端调用 `sofagent_compose` 返回一致 YAML（底层走 orchestrator）。

#### ⑤.5.13 FDE / 文档 / 脚本引用全量替换

> 随 11 包拆分，下列引用需全量替换（原 `sofagent-audit <子命令>` → 新包二进制），与 ⑤.5.5/⑤.5.8/⑤.5.11/⑤.5.12 的二进制迁移配套。

- `sofagent-audit compose` → `sofagent-orchestrator compose`；`ao compose` / `agency-orchestrator` → `sofagent-orchestrator compose`
- `sofagent-audit hub list/deploy` → `sofagent-work模板市场 list/deploy`
- `sofagent-audit ontology view` → `sofagent-ontology view`
- `sofagent-audit ab-testing` / `skillopt-run` → `sofagent-ab` / `sofagent-skillopt`
- `sofagent-audit doctor` → `sofagent-core doctor`；`sofagent-audit verify` → `sofagent-core verify`
- 涉及文件：
  - `FDE/FDE.md`（行 286、309、367 等 + 所有 "ao compose"）、`FDE/templates/README.md`、`FDE/templates/nodes/node-template.md`、`FDE/templates/deployment-plan.md`
  - `FDE/fde-install.sh`（"ao compose 可用" / "agency-orchestrator" → "sofagent-orchestrator 可用"）
  - `sofagent/audit/src/verify.ts:195`、`sofagent/audit/src/verify/checks.ts:51`（"DeepAgents compose 可用" / "运行 ao compose 测试" → sofagent-orchestrator compose；verify 迁 core 后路径为 `sofagent/core/src/verify/checks.ts`，见待确认）
  - `agents/README.md`、`docs/ARCHITECTURE.md`（River/Workflow/Subagent 章节把 compose 归到 orchestrator）
  - `docs/guides/team-deploy.md`（如有引用）
  - 桌面交付物 `~/Desktop/river-dingtalk-deploy-guide.md`：`sofagent-audit compose` → `sofagent-orchestrator compose`（三处：2.4 图、4.2 节、附录）

> 替换时注意语义：FDE 是编排的**消费者**，文档应表达「FDE 调用 sofagent-orchestrator 拆任务」，不要写成 FDE 自己生成 Workflow。

### ⑤.6 跨包硬约束汇总（不可违反）

1. **🔴 禁止循环依赖**：依赖方向严格单向——`harness/ontology/scoring/core` 为叶子（绝不 import 任何 sofagent 包）；`orchestrator → harness`；`daemon → audit+harness+ontology+core`；`ab-testing → orchestrator+harness+eval+core`；`work模板市场 → orchestrator+ontology+eval`（可选→skillopt）；`think → audit+core`；`skillopt → ontology+core`；`mcp → orchestrator+work模板市场`；`audit → ontology+core`。任何 `audit → think` / `orchestrator → audit` / `harness → *` / `core → 运行包` 的反向 import 一律禁止。
2. **语义归位**：编排 / 拆解 / Sub Agent 调度只在 orchestrator；daemon 只管调度与触发；think 只做反思生成；skillopt 只做 Skill 优化；audit 只做审计与治理。各二进制不得越界暴露他包子命令。
3. **🔴 harness 必须独立成包**：`buildConstrainedSystemPrompt()` 必须落在 `@sofagent/harness`，不允许留在 orchestrator 内 fallback。
4. **think 触发解耦**：`generateThinkEntry` 调用从 `audit/src/index.ts` 移除，改由 daemon 跑完 fs-audit 后 / 或 `sofagent-think` CLI 触发；audit 不反向依赖 think。
5. **skillopt 与 work模板市场 单向**：work模板市场 可选依赖 skillopt 提供 `sofagent-work模板市场 skillopt` 包装，skillopt 绝不反向依赖 work模板市场。
6. **向后兼容 / 破坏性标注**：`sofagent-audit compose/subagent/work模板市场/ontology/ab-testing/skillopt-run/doctor/verify` 全部移除或改指向新包二进制（仅 `sofagent-audit ontology view` 保留），必须在 changelog / Release Notes 标注迁移命令（见 v1.1.0.md「破坏性变更清单」）。
7. **测试不丢**：原 composer / launcher / 约束加载链 / eval / ab-testing / 各包测试的覆盖率不下降，迁移后 11 包 `npm test` 全绿。
8. **不改动编排行为**：本次只做物理归位，不改 `composeWithDeepAgents` 的 YAML 输出格式、不改四层约束加载逻辑、不改 Sub Agent 行为、不改审计规则 A1–A17。

### ⑤.7 总体验收标准（Definition of Done）

- [ ] 11 个包（harness/ontology/scoring/core/orchestrator/daemon/ab-testing/work模板市场/think/skillopt/audit/mcp）均独立 `npm run build` + `npm test` 通过
- [ ] 依赖方向无循环（harness/ontology/scoring/core 为叶子；audit 仅依赖 ontology+core）
- [ ] `sofagent-audit compose/subagent/work模板市场/ab-testing/skillopt-run/doctor/verify` 全部移除或改指向新包（仅 `sofagent-audit ontology view` 保留）
- [ ] 新二进制可用：`sofagent-orchestrator` / `-orchestrator-compare` / `-daemon` / `-ontology` / `-work模板市场` / `-ab` / `-think` / `-skillopt` / `-core`
- [ ] `@sofagent/mcp` 的 `sofagent_compose` tool 经新包返回一致结果
- [ ] FDE / ARCHITECTURE / verify / fde-install.sh / River 指南 全部引用已切换到新包命令
- [ ] `sofagent-audit --help` 不含 compose/subagent/work模板市场/ab-testing/skillopt-run/doctor/verify
- [ ] `pre-push-check.sh` 全绿；`check-version.sh` 通过
- [ ] changelog 与 Release Notes 标注破坏性变更及迁移命令

### ⑤.8 版本与发布

- 本重构纳入 **v1.1.0** 发版（v1.1.0 尚未发版，开发中）。v1.0.9 已交付 MCP compose tool（编排经 MCP 暴露），但编排的**物理归位**（独立包）推迟到 v1.1.0。
- 移除 `sofagent-audit compose` / `subagent` / `work模板市场` / `ab-testing` / `skillopt-run` / `doctor` / `verify` 子命令属 **CLI 破坏性变更**，Release Notes 必须标注迁移命令（见 v1.1.0.md「破坏性变更清单」子命令迁移表）。
- `bump-version.sh <旧> <新> --dry-run` 先核验影响面，再执行。
- **建议分波**（每波可独立验证、独立回归）：
  - Wave 1 基础层：harness + ontology + eval + core 独立（无依赖）
  - Wave 2 运行层：orchestrator + daemon 独立（audit 去掉对应代码）
  - Wave 3 实验层：ab-testing + work模板市场 + think + skillopt 独立
  - Wave 4 功能：轻量多设备 ①–④（落在新包上）+ audit 最终收敛
  - Wave 5 收口：全量回归 + 文档（ARCHITECTURE/DEVELOPMENT/HANDBOOK/verify/fde-install/River）+ Release Notes 破坏性变更标注

---

## 参考文件（改造前必读）

- `docs/changelog/v1.1.0.md`（本项目背景，11 包终态 + 边界表 + 破坏性变更清单）
- `sofagent/audit/src/index.ts`（收敛后仅 ontology view 派发；原 compose/subagent/work模板市场/ab-testing/skillopt-run/doctor/verify 子命令已迁出，见 ⑤.4 / ⑤.5.11）
- `sofagent/harness/src/constraint-loader.ts`（buildConstrainedSystemPrompt，从 audit/src/subagents/launcher.ts:210 迁出，见 ⑤.5.1）
- `sofagent/orchestrator/src/{composer,launcher,registry,builtin-agents,audit-sub-agent}.ts` + `orchestrator-compare.ts`（从 audit/src/subagents/ 与 audit/src/orchestrator-compare.ts 迁出，见 ⑤.5.5）
- `sofagent/orchestrator/src/launcher.ts`（spawnSubAgent 内 buildConstrainedSystemPrompt → @sofagent/harness）
- `sofagent/ab-testing/src/ab-runner.ts`（约束加载 + eval 引用改写，见 ⑤.5.7）
- `sofagent/core/src/doctor.ts`（从 audit/src/commands/doctor.ts 迁出；readRuntimeState 来自 @sofagent/orchestrator，见 ⑤.5.4 / ⑤.5.5）
- `sofagent/daemon/src/`（cron / fs-watch / run-fs-audit / snapshot / lessons-extract / weekly-report / inspectors — 交付 ①②④ 改动点，见 ⑤.5.6）
- `sofagent/think/src/think-generator.ts`（从 audit/src/think-generator.ts 迁出，见 ⑤.5.9）
- `sofagent/skillopt/src/{skill-safety-check,skillopt-integration}.ts`（从 audit/src/ 迁出，见 ⑤.5.10）
- `sofagent/work模板市场/src/hub.ts`（从 audit/src/commands/hub.ts 迁出，见 ⑤.5.8）
- `sofagent/ontology/src/{merge-engine,ontology-view,types}.ts`（从 audit/src/ontology/ 迁出，见 ⑤.5.2）
- `sofagent/scoring/src/{types,eval-scorer,eval-runner,eval-reporter}.ts`（从 audit/src/eval/ 迁出，见 ⑤.5.3）
- `sofagent/audit/src/permission/`（权限解析 — 交付 ③ 改动点，**保留在 audit 包，不拆出**）
- `sofagent/mcp/src/mcp-server.ts`（sofagent_compose tool 改调 @sofagent/orchestrator，行 494–525，见 ⑤.5.12）
- `FDE/FDE.md` / `FDE/templates/*`（compose 引用，全量替换见 ⑤.5.13）
- `docs/ARCHITECTURE.md`（River—Workflow—Subagent 三层）

---

## 修订记录

> 本次修订由架构师（高见远）基于 [`v1.1.0.md`](./v1.1.0.md) 的 **11 包终态**对 `v1.1.0-dev-prompt.md` 做扩写与路径修正。**仅改此一份 dev-prompt 文档，未改动任何产品代码，未 commit。**

### 修订范围

1. **交付 ⑤ 由「orchestrator + harness 两项」扩写为「11 包完整迁移任务清单」**
   - 原 ⑤ 只有 orchestrator（5.1–5.8 风格）与 harness 两包的迁移任务；现按依赖顺序补齐全部 11 包，每个包一节，沿用「背景 / 现状证据 / 新包目录 / package.json / 迁出文件 / audit 内需删除 / 跨包 import 改写点（真实 grep 锚点）/ 硬约束 / 验证」结构：
     - ⑤.5.1 harness（基础层叶子）
     - ⑤.5.2 ontology（基础层叶子）
     - ⑤.5.3 eval（基础层叶子）
     - ⑤.5.4 core（基础层边界项收敛，含 doctor/verify 迁此包）
     - ⑤.5.5 orchestrator（运行层）
     - ⑤.5.6 daemon（运行层）
     - ⑤.5.7 ab-testing（运行层）
     - ⑤.5.8 work模板市场（运行层）
     - ⑤.5.9 think（运行层 · 审计后反思生成；`generateThinkEntry` 从 audit/src/index.ts 移除，改由 daemon 或 `sofagent-think` CLI 触发）
     - ⑤.5.10 skillopt（运行层 · Skill 优化；原 `skillopt-run` → `sofagent-skillopt`）
     - ⑤.5.11 audit 收敛为纯审计
     - ⑤.5.12 mcp 更新引用（compose tool 改调 `@sofagent/orchestrator`）
     - ⑤.5.13 FDE / 文档 / 脚本引用全量替换
   - ⑤.1 目标架构 ASCII 图替换为 **11 包完整图（含各包关键文件）**；新增 ⑤.3 依赖铁律（11 包单向依赖 + 禁止循环）；⑤.4 现状证据用**当前 dev 快照的真实行号**重写（原行号基于 v1.0.9 快照、已过时）；⑤.6 跨包硬约束汇总扩写覆盖全部 11 包依赖方向；⑤.7 验收、⑤.8 版本补充 11 包与分波建议。

2. **① / ② / ④ 过时路径修正（共 6 处 `audit/src/daemon` → `daemon/src`）**
   - ①.3 任务 1.3：`sofagent/audit/src/daemon/lessons-extract.ts` → `sofagent/daemon/src/lessons-extract.ts`
   - ②.3 任务 2.1：`sofagent/audit/src/daemon/scheduler.ts` / `weekly-report.ts` → `sofagent/daemon/src/scheduler.ts` / `weekly-report.ts`
   - ④.3 任务 4.1：`sofagent/audit/src/daemon/inspectors/` / `scheduler.ts` → `sofagent/daemon/src/inspectors/` / `scheduler.ts`
   - ④.2 / ④.3 的 `sofagent-audit --doctor` → `sofagent-core --doctor`（doctor 已迁 core，见 ⑤.5.4）
   - 参考文件清单末尾：`sofagent/audit/src/daemon/` → `sofagent/daemon/src/`，并补 `sofagent/{harness,ontology,eval,core,think,skillopt,work模板市场,orchestrator,ab-testing,daemon}/src/` 新包路径
   - **注意**：③ 的 `permission/` 路径**保持不变**（权限作用域化仍留在 audit 包，不拆出）。

3. **文首总览表 + ⑤ 硬约束同步**
   - 总览表交付⑤「改动范围」由「orchestrator / harness 独立包」扩展为「11 包拆分（harness/ontology/scoring/core/orchestrator/daemon/ab-testing/work模板市场/think/skillopt/audit 收敛/mcp 更新）」，破坏性列注明「CLI 全量迁移（子命令→新包二进制）」。
   - 文首导语 ⑤ 描述同步为 11 包拆分。

### 全局一致性自查

- ✅ 全文已无「`@sofagent/audit` 保留 daemon/ab-testing/ontology/hub」类与 11 包冲突表述（旧 ⑤.1 ASCII 图中 audit 含 daemon/ab-testing/ontology/hub 的画法已替换为 11 包终态图）。
- ✅ 所有 `audit/src/daemon` 引用已改为 `daemon/src`（permission 除外，③ 路径保持不变）。
- ✅ ①~④ 业务逻辑与原有设计决策（不共享原始反思、不内置传输、shared 优先、只读聚合、失败降级、永不自动降级、隔离失败、不引入外部通道等）均保留，仅做路径与包归属措辞修正。

### 待确认项（无法独自决定，需主理人 / 团队裁定）

1. **`permission/` 目录当前在源码中并不存在**。`v1.1.0.md` 与本文 ③ 均将权限作用域化落在 audit 的 `permission/` 下，但 `sofagent/audit/src/` 实际无 `permission/` 目录（仅 `commands/` 下有 `init.ts`/`doctor.ts`）。请确认：权限解析器（`resolve.ts` 等）是本次 v1.1.0 新建、还是早已在别处（如 `core/` 或顶层）实现？本文按"保留在 audit、路径不变"的指令处理，但源码现实需要补全。
2. **`skill-safety-check.ts` 复用 audit 的 `rules/skill-safety-*`**：skillopt 包依赖 ontology + core，但 `skill-safety-check.ts` 实际 import `./rules/skill-safety-*`（属 audit 的 A 规则体系）。两种方案待定：(a) 把 `rules/skill-safety-*` 也一并迁到 skillopt 包；(b) skillopt 经 `@sofagent/audit` 复用这些规则类型。本文 ⑤.5.10 暂以 `@sofagent/audit?` 标注。
3. **`core` 是叶子，却要依赖 `skillopt`（`isSkillOptAvailable`）**：原 `doctor.ts:17` import `../skillopt-integration`，而 doctor 迁 core 后若保留该调用会破坏"core 不依赖运行包"铁律。建议把探测逻辑下放到 skillopt 包，doctor 仅做可选调用。本文已标注待确认，未强行改。
4. **`verify/` 引用在 ③.3 中仍写作 `sofagent/audit/src/verify/checks.ts`**：因本次任务 B 的修正范围仅限 ①/②/④（③ 仅"permission 路径不变"），③.3 的 verify 引用未改。但 verify 已整体迁 core（⑤.5.4），该路径在拆分后将失效。建议同步将 ③.3 改为 `sofagent/core/src/verify/checks.ts`，待主理人确认是否纳入本次修订。
5. **`AuditResult` 类型归属**：定义在 `reporter.ts`，随 core 迁移。本文采取"audit 通过 `export type { AuditResult } from '@sofagent/core'` 重导出，think 从 `@sofagent/audit` 导入"的方案以维持 `think → audit` 依赖方向。若团队倾向 think 直接从 core 取 AuditResult，则可去掉 audit 的重导出层——属风格选择，已落地一种可行解。
