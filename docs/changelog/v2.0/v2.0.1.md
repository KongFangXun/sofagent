# v2.0.1 开发日志 — 网络出口治理面（host 白名单 + 出站裁决审计 + 拦截器接口）

> 📋 **已排期。** 前置依赖：v2.0.0（宣告完成后交付——本章原为 v2.0.0 第二章，2026-09-22 用户拍板定位重写时迁出为独立补丁位；治理件本身与判定底座零依赖，可独立验收）+ 既有 G10 读取审计（对称参照——「管进」已就绪，本版补「管出」）+ v1.5.4（HMAC 挂链链路复用）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-09-16（原 v2.0.0 第二章收编落盘，Guardian 启发 · 用户拍板——「管进」G10 对称补「管出」，边界=策略契约+审计挂链不自建拦截器）· **2026-09-22 迁版（用户拍板）：v2.0.0 重定位为决策模型宣告版（零新能力纪律），本章作为纯治理件迁至 v2.0.1——内容零丢失，仅版本归属变更；新增与判定档的衔接说明**

## 定位

> 🌐 **网络出口治理面**——数据主权「管出」翼：Agent 能访问哪些 host 无人管时，数据外泄通道敞开。本版给网络出站访问建三件套——**host 白名单声明面**（默认空 = 全拒，opt-in）+ **出站裁决审计挂链**（与 G10 读取审计对称）+ **拦截器接口**（裁决契约留约束层，egress proxy / OS 沙箱实现出约束层）。在 V2.0.0 宣告的 GDM 身份下，本版是判定底座的第一个**新增判定消费场景**：出站裁决事件可由判定档语义判定（host 与任务的相关性 / 出站必要性），且 GDM 边界纪律不变——策略契约与执行动作留在确定性代码，判定只出建议概率。

---

## 一、网络出口治理面（Codex Guardian 网络审批启发 · 2026-09-16 用户拍板排期）

> **形态归属**：`[主干]`（判定·出站裁决 + 留痕挂链；边界自述「策略契约 + 审计，不自建拦截器」）**+ `[通道]` 成分**（拦截器接口——裁决契约留约束层、egress proxy / OS 沙箱实现出约束层，章节原文自陈「对齐 TrainChannel 通道形态」）｜ L1 关档

> 📋 **研究收编**（codex-rs [protocol/src/approvals.rs](https://github.com/openai/codex/blob/main/codex-rs/protocol/src/approvals.rs) 网络审批类型）：Codex 给网络访问定义了 host 粒度的审批面——`NetworkApprovalContext{host, protocol}` 按 host 裁决 Allow/Deny，配 `NetworkPolicyAmendment` 增量修正。与本版主题同源：数据主权不只要「管进」（G10 授权读取白名单——读什么要授权），也要「管出」（Agent 能访问哪些 host 无人管时，数据外泄通道敞开）。
>
> ⚠️ **马鞍边界（四问过筛已过，边界在此）**：只做**策略契约 + 审计挂链**（host 白名单声明面、裁决事件落审计链）——不自建 egress proxy / 网络拦截器（对齐「router 本体由工程交付、约束层不自研」既有边界，拦截器实现是执行面，开源生态已有成熟轮子）。

### 交付

| 组件 | 能力 |
|------|------|
| **host 白名单声明面** | workflow/节点声明允许出站的 host 清单（默认空 = 全拒，opt-in——对齐 G10 模式） |
| **出站裁决审计** | 出站访问裁决（host×Allow/Deny）落审计链（HMAC 挂链——与 G10 读取审计对称，「管进」+「管出」两翼齐备） |
| **拦截器接口** | 裁决接口开放给外部拦截器（egress proxy / OS 沙箱，对齐 TrainChannel 通道形态——接口+事件契约+审计留约束层，实现在外） |
| **判定档衔接（GDM 新增）** | 出站裁决事件可作为判定底座输入：白名单外的 host 请求触发 `Noul` 判定（该出站与当前任务的相关性概率）——**判定只出建议**，Allow/Deny 执行决策仍由策略契约持有（确定性规则在前，语义判定加签） |

### 验收标准

- [ ] host 白名单声明面可用（默认空全拒，声明后放行）
- [ ] 出站裁决进审计链（与 G10 读取审计对称）
- [ ] 外部拦截器可接入裁决接口（通道形态，不绑实现）
- [ ] 判定档衔接可用：白名单外请求可触发语义判定并留痕（建议不执法，行为锁）
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v2.0.1 的什么 |
|------|------|
| v2.0.x / v2.1.x（AI 节点落位版） | **治理挂接件**——AI 节点（DeepSeek harness 造的外部 Agent）经本版出站裁决与审计挂接纳入治理面（探索方向「AI 节点」的触发前提之一） |
| v3.x | 出站治理面随分层模型架构沿用（本地档出站白名单与云端档审批联动） |
