# v2.0.0 开发日志 — 数据主权大版本（离线 USB 节点合体）

> 📋 **已排期。** 前置依赖：v1.4.4（本地权重部署链路）+ v1.4.7（workflow 烧进 USB + G8 cron job 包）+ v1.5.0（双时态本体——时点快照基础）+ v1.5.3（模型路由层 + Vault + 多实例表决——三项已前移，本版直接复用）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-29（v1.5.0 系列规划首版落盘）· **2026-08-29 拆分（用户拍板）：模型路由层 / Vault / 多实例自验证三项不依赖精调模型、依赖全就绪，前移当时编号的 v1.5.4（现 v1.5.3）——本版收窄为离线 USB 节点合体一件核心**

## 定位

> 🏰 **数据主权大版本**——敏感数据不出本地是硬承诺的落地版：离线 USB 节点 = 本地权重（v1.4.4）+ workflow 烧录（v1.4.7）+ 审计模块（既有）+ 路由底座（v1.5.3 前移件）四位一体，完全离线运行。v3.x-v4.x+ 剩企业专属小模型精调（QLoRA distill 轻量化）。

---

## 一、离线 USB 节点合体（本地权重 + workflow 烧录 + 路由底座）

> 📋 **ROADMAP 提前路线**：v1.4.4 本地权重部署 + v1.4.7 workflow 烧录底座已就绪，v2.x 合体成完全离线节点。

### 交付

| 组件 | 能力 |
|------|------|
| **合体封装** | USB 节点 = 本地权重（v1.4.4 链路）+ workflow 烧录（v1.4.7）+ 审计模块（既有）+ 模型路由（v1.5.3 前移——本地权重直接接入路由插槽）四位一体，完全离线运行 |
| **离线激活** | 无网络环境激活流程（本地 license 校验 + 离线 ontology 导入） |
| **离线审计回传** | 审计日志本地滞留，联网后批量回传同步（联邦模式复用）；离线期凭证经 Vault（v1.5.3 前移）托管 |

### 验收标准

- [ ] 断网环境 USB 节点完整运行（激活→执行→审计全离线）
- [ ] 联网后审计日志批量回传
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v2.0.0 的什么 |
|------|------|
| v3.x | 离线节点（分层模型架构的载体——企业专属小模型精调 QLoRA 在其上迭代） |
