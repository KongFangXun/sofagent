# v0.95 开发日志

> TBD · 审计配置化 + 推送层 + 编排对接
>
> v0.94 完成审计独立化（沉默审计 + LogFormat 接口）后，v0.95 做 v0.94 推迟的功能。核心判断：这些功能依赖"审计可信了"才有意义。

---

## 概述

v0.95 做六件事：

1. **审计配置化**——`.sofagent/config.yml`，LOW_RISK_PATTERNS / testPatterns / 规则阈值全部可配
2. **MCP server MVP**——任务完成推送到企业平台
3. **Agency Agent 模板对接**——编排引擎角色注入
4. **OpenClaw 预装集成**——install.sh 自动装底座
5. **合规三件套增强**——脱敏 + 审计报告 + 保留策略
6. **CLI 体验补全**——`--json` 输出 + pre-commit hook 模板

---

## 一、P0：审计配置化（.sofagent/config.yml）

> 来源：DeepSeek PR #2。不同项目的低风险文件不同——Java 项目不关心 package.json，Python 项目不在乎 tsconfig.json。

### 背景

v0.94 前 `LOW_RISK_PATTERNS`、`testPatterns`、规则阈值全部硬编码在 TypeScript 源码里。团队接入需要改源码 + 重新编译——这在企业部署中不可接受。

### 设计

```yaml
# .sofagent/config.yml
audit:
  low_risk_patterns:
    - package-lock.json
    - yarn.lock
    - "*.generated.*"
    - "docs/**"
  test_patterns:
    - "npm test"
    - "npm run test"
    - "pytest"
    - "go test ./..."
  careful_modify_threshold: 0.2  # 超过 20% 无关文件 → WARN
  strict_mode: false             # 默认不强制写日志
```

### 加载优先级

1. 项目根目录 `.sofagent/config.yml`（最高）
2. 用户目录 `~/.sofagent/config.yml`
3. 内置默认值（当前硬编码值，作为 fallback）

### 收益

- FDE 部署者可为每个企业定制审计规则（不碰源码）
- 不同语言项目可用不同配置（Java/Python/Go 各一份）
- 为 v0.96 GitHub Actions 模板打基础

---

## 二、P1：MCP server MVP

> 从 v0.94 推迟。依赖 JSONL 结构化日志先落地（v0.94 已做）。

### 功能范围

- watch `.sofagent/task/logs/` 目录（优先 JSONL，fallback MD）
- 任务完成（日志文件写入）立即检测
- 首次推送：任务摘要（任务名、模型、耗时、铁律违反数）
- webhook 客户端框架（预留企业平台接入接口）
- 对标 Claude Code 的 Slack 集成——但跨平台

### 技术选型

- Node.js + `chokidar`（文件监听） + 内置 `fetch`（webhook 推送，不引入 axios）
- 轻量级——状态用 `.sofagent/mcp-state.json` 记录

---

## 三、P1：Agency Agent 模板对接

> 从 v0.94 推迟。

### 对接方式

- `install.sh --with-agency` flag，安装时自动 clone 模板库
- `task-orchestrate.sh` 在 AO compose 拆解后，从模板库匹配角色并注入
- 模板库作为 git submodule（保持仓库轻量）
- Apache-2.0 与 MIT 兼容

---

## 四、P1：OpenClaw 预装集成

> 从 v0.94 推迟。对 SMB 场景优先级低于 FDE Skill，但 FDE 部署时需要。

- macOS：`brew install openclaw`
- Linux：检测包管理器（apt/yum/dnf/pacman）
- 安装后自动注册 systemd/launchd 服务
- `install.sh --no-oc` 跳过

---

## 五、P2：合规三件套增强

1. **脱敏增强**：sanitize() 新增正则（JWT token / internal IP / 内网域名）
2. **审计报告**：`sofagent-audit --report` 周报 Markdown（任务数 / 铁律违反趋势 / 高危 Agent 标记）
3. **保留策略强制执行**：cleanup.sh `--enforce` 模式

---

## 六、P2：CLI 体验补全

- `--json` 输出（CI 系统解析结构化结果）
- `pre-commit hook` 模板（`.git/hooks/pre-commit`，不用手动配 CI）
- `--help` 重构（子命令分组：审计 / 安全 / 验证）

---

## 七、文档降温（续 v0.94）

- DEVELOPMENT.md 重构：绿灯路径独立为新节（§九：AI 的"最低成本通过"本能）
- 状态账本从概念声明升级为具体模板（四字段：看到/改了/验证了/还剩）
- demo.gif 录制（v0.94 实验数据完整后）

---

## 八、发布检查清单

- [ ] `.sofagent/config.yml` 配置加载 + 三级 fallback
- [ ] MCP server MVP：watch JSONL + webhook 推送
- [ ] Agency Agent：`--with-agency` + 角色匹配注入
- [ ] OpenClaw 预装：macOS/Linux 自动安装 + 系统服务注册
- [ ] 合规三件套：脱敏增强 + 审计报告 + 保留策略强制执行
- [ ] CLI：`--json` + pre-commit hook 模板 + `--help` 重构
- [ ] DEVELOPMENT.md §九 绿灯路径 + §十 状态账本模板
- [ ] demo.gif 录制并替换 TODO
- [ ] `npm test` 全量通过
- [ ] `CHANGELOG.md` 索引添加 v0.95 条目
- [ ] `ROADMAP.md` 更新版本状态
