# v1.6.1 开发日志 — 判定底座 · 数据集三元组与三源采集（判据样本化 + 标注来源三档）

> 📋 **已排期。** 前置依赖：v1.6.0（判据 schema 定稿——本版消费其判据清单与正负样例）+ v1.5.14（域验证器三档 `deterministic` / `model-judge` / `human-only`——本版标注来源分档与之一致）。
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-09-20（判定底座施工期拆版落盘，用户拍板——原 v1.6 的数据集部分独立成版）。设计依据见维护者侧判定底座设计文档（仓外）。

## 定位

> 🧱 **判定底座 · 数据集三元组与三源采集**——阶段一第二版。v1.6.0 定义了「要判断什么」，本版解决「**拿什么训练**」：把判据样本化为统一三元组，并从约束层**已有的运行痕迹**里采集标注，而不是靠人工从零标注。**关键洞察**：约束层自己就在生产训练数据——history 轨迹、HITL 人工裁决、PR 审阅结论都是天然标签，本版只是把它们接起来。**但采到不等于能用**——本版同时把数据可用性升级为四道可断言的机器门，并把样本挑选从被动汇聚升级为主动选样（§三）：校准能否达成取决于**困难样本**有没有被采到，而困难样本在任何自然分布里都是少数。

---

## 一、数据集三元组格式（状态 / 问题集 / 标注答案）

> **形态归属**：`[主干]`（判定·数据格式）｜ **不可拔**（格式是数据集契约，拔掉则标注无法被校验）

### 交付

| 组件 | 能力 |
|------|------|
| **三元组定义** | `(state, questions, answers)`——`state` 为非结构化输入快照、`questions` 为限定原语类型的问句集、`answers` 为类型化答案（含概率目标）。与 v1.6.0 三原语类型同构，**不另立格式** |
| **标注来源三档** | `label.source` 三档与 v1.5.14 域验证器口径一致：`deterministic`（自动判据/测试断言 → 廉价可信）/ `model-judge`（模型评分 → 有成本）/ `human`（HITL 裁决、PR 审阅 → 最贵最可信）。**分档随样本走**，训练时可按档加权 |
| **概率目标表达** | `answers` 须能表达**概率目标**（choice 的分布 / score 的区间 / noul 的真值概率），而非单一硬标签——这是阶段二校准训练的前置条件。只有硬标签的数据集**训不出校准**，格式层必须预留 |
| **血缘锚点** | 每条样本记录反向锚点（轨迹 id / instinct id / PR 号 / HITL 会话 id），与 v1.5.15 数据集血缘同族——训练产物可反向追溯至样本源头 |
| **租户隔离** | 样本按 `enterpriseId` 分区（对齐 v1.4.7 多租户路径纪律），跨租户混入默认禁止 |
| **格式校验器** | 数据集加载时断言：三元组齐全、问句类型合法、答案类型与问句匹配、概率目标合法（分布归一 / 区间有序 / 概率 ∈ [0,1]）；不合规 → **拒绝加载** |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/decision-dataset-schema.ts` | 新建 | 三元组格式 + 概率目标表达 + 校验（fail-closed） |
| `engine/orchestrator/src/__tests__/decision-dataset-schema.test.ts` | 新建 | 格式校验单测（概率目标非法 / 类型不匹配 → 拒绝） |
| `docs/ARCHITECTURE.md` | 修改 | 判定数据集格式说明（与 TrainChannel 数据面衔接） |

### 验收标准

- [ ] 三元组格式在位，与 v1.6.0 三原语类型同构（零格式分叉）
- [ ] `label.source` 三档可标记且随样本持久化（训练侧可按档加权）
- [ ] **概率目标可表达**（分布 / 区间 / 真值概率三种形态各有构造器与校验）
- [ ] 格式校验器 fail-closed（概率目标非法即拒绝加载，不静默裁剪）
- [ ] 血缘锚点齐全（样本 → 轨迹/PR/HITL 可反向追溯）
- [ ] 租户隔离生效（跨企业混入默认禁止）
- [ ] `npm test` 全绿

---

## 二、三源采集器与八类补充采集点（history / HITL / PR + 困难样本）

> **形态归属**：`[主干]`（判定·样本来源判定与来源真实性门）**+ `[通道]` 成分**（采集与落盘的执行动作）｜ —

### 交付

| 组件 | 能力 |
|------|------|
| **history 采集** | 从历史执行轨迹（含 v1.5.0 交付的 trace 与因果链）抽取 `(state, questions, answers)`——状态从轨迹最小可判定前缀取，答案是**当时实际发生的处置** |
| **HITL 采集** | 从人工裁决记录（v1.3.7 起 HITL 通道）抽取——人工批准/拒绝是最高可信档 `human` 标签，**直接可作校准的真值来源** |
| **PR 采集** | 从 PR 审阅结论（v1.3.x 起 pr-store 的审阅结论与 suggestion 采纳情况）抽取——采纳/拒绝构成标签 |
| **来源真实性门** | 只收**经审计链锚定的真实运行痕迹**派生的样本；合成 / Agent 自述 / 未建链来源标记后**默认拒收**（与 v1.5.15 防自证污染同源纪律：数据来源未标记 ⇒ 按不可信处理 fail-closed） |
| **去重与泄漏防护** | 采集侧做近重复检测；**同一轨迹不得同时进训练集与验证集**（数据泄漏防护）——切分按轨迹/租户粒度而非样本粒度 |
| **采集可观测** | 各源采集量 / 各档占比 / 拒收原因分布可查询（数据面健康度可见，数据本地不出门） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/collectors/history-collector.ts` | 新建 | 轨迹源采集 |
| `engine/orchestrator/src/router/collectors/hitl-collector.ts` | 新建 | HITL 裁决源采集 |
| `engine/orchestrator/src/router/collectors/pr-collector.ts` | 新建 | PR 审阅源采集 |
| `engine/orchestrator/src/router/collectors/index.ts` | 新建 | 三源统一入口 + 去重 + 切分 |
| `engine/orchestrator/src/__tests__/decision-collectors.test.ts` | 新建 | 三源抽取 / 来源门 / 去重 / 泄漏防护单测 |

### 验收标准

- [ ] 三源各自可抽取三元组，产出格式与 §一 校验器兼容
- [ ] 来源真实性门生效：未建链 / 合成来源 fail-closed 拒收，拒收原因可查
- [ ] HITL 源样本标记为 `human` 档（最高可信档）
- [ ] **同一轨迹不跨训练/验证集**（按轨迹粒度切分，泄漏防护有单测）
- [ ] 采集量 / 档位占比 / 拒收原因可观测
- [ ] 租户隔离生效（跨企业样本不混入）
- [ ] `npm test` 全绿

### 风险与前置假设

> ⚠️ **本版产出的样本量可能不足**——三源采集依赖真实运行积累，早期客户环境样本稀疏。若样本量不足以支撑阶段二校准，则**下调阶段二目标（只出判定不出校准）并把校准后移**，不硬训。样本量实测结论作为 v1.6.2 的基线输入之一。
>
> ⚠️ **语言面是隐性稀疏项**——判定模型普遍以英文为主训练，中文判据样本即使在三源里也天然稀少（真实运行痕迹的业务语言分布决定）。本版采集侧须**分别落数中英文样本占比**，不合并成一个「样本总量」——否则「样本够」的结论会掩盖「中文不够」，而中文正是 v1.6.2 中文基线的输入。

---

## 三、采集质量门与主动学习选样（数据可用性的前置判定）

> **形态归属**：`[主干]`（判定·数据质量）｜ **不可拔**（质量门是样本可信的前提——不过门的样本进训练等于把噪声固化成能力）

采到了不等于能用。本版把「数据可用性」从人工判断升级为**四道可断言的机器门**，并把「样本怎么挑」从被动汇聚升级为**主动选样**——两者的共同前提是同一个事实：**校准能否达成，取决于困难样本有没有被采到**，而困难样本在任何自然分布里都是少数。

### 交付

**四道质量门**：

| 门 | 判据 | 处置 |
|----|------|------|
| **门一 · 来源真实性** | 只收经审计链锚定的真实运行痕迹 | 合成 / Agent 自述 / 未建链来源标记后**默认拒收**（与 v1.5.15 防自证污染同源） |
| **门二 · 跨集泄漏** | 切分按**轨迹**粒度而非样本粒度；样本带**私有哈希** | 同一轨迹不得同时进训练集与验证集；近重复检测**跨集**执行；与公开基准 / 厂商评测集**零交集**（有交集则测到的是记忆而非能力）。另须防**回放泄漏**——同一轨迹内，**切分点之后才产生的信息**（事后复盘、已结案工单、事后标签、结论性备注）不得进入切分点之前的输入，否则评测会系统性高估判定能力（生态实证：这是回放式评测最主要的效度威胁）。切分点须显式定义并留痕 |
| **门三 · 标签噪声对账** | 同一样本多档标签是否一致 | `deterministic` 与 `human` 冲突 ⇒ 进人工复核队列，**不取多数票静默裁决** |
| **门四 · 校准标注** | 样本能否表达概率目标 | 只有能表达概率目标的样本进**校准集**；硬标签样本归训练集并标 `no-probability-target`，不参与 ECE 计算 |

**主动学习四类选样**（被动汇聚会让样本集中在易判样本上，校准曲线反而失真）：

| 选样 | 判据 |
|------|------|
| **不确定优先** | 概率接近 0.5 的样本优先——最接近决策边界，标注收益最大 |
| **分歧优先** | 模型判定与人工裁决不一致的优先（对齐分歧样本） |
| **代表优先** | 覆盖判据域稀疏区域的优先——防样本集中在少数判据上 |
| **漂移优先** | 跨时间重复采样中标定失准的判据优先补样 |

**集分档**（三集分工，不混用——同一个集既挑模型又调阈值，等于用同一把尺子量两件不同的事）：

| 集 | 用途 | 构造纪律 |
|----|------|----------|
| **候选集**（挑模型用） | 比较基座 / 实现路线 / 训练档的优劣 | 覆盖真实流量分布 + **必须含边缘与对抗样本**；**评测集训练痕迹重则泛化差**——公开基准上的高分不构成选用依据 |
| **刻度集**（调阈值用） | 定分流门槛的位置 | **必须来自生产流量分布**：门槛设在**数据实际峰值所在、且达到精度目标**的位置，而非「所有任务上看都还过得去」的位置 |
| **免调集**（不该继续调的任务） | 登记为不值得继续优化的任务并**移出循环** | 须写明「为什么移出」，避免它反复回到排期里空耗注意力 |

**门槛是分布的函数**：换基座或数据漂移时须**重新校准门槛**，不得沿用旧值——门槛绑定的从来不是任务名，而是**该任务在当前分布下的概率峰位**。

**集的两条纪律**：

- **冻结即单调**：候选集一经冻结不得追加（分数**只能降不能升**）——冻结的意义正是堵住「偷塞简单样本抬分」这条路。样本增补走**新建集**，并声明新旧集**不可横比**。
- **私有锚定**：集内样本带**私有哈希**，与任何公开基准 / 厂商评测集**零交集**——公开样本会被厂商纳入评测训练，届时测到的是记忆而非能力。

**优先序纪律**（选样不是等权，谁先补有次序）：

| 纪律 | 内容 |
|------|------|
| **中文优先** | 中文判据面近乎无人占据——先补中文，英文判据面竞争充分且非我方差异点 |
| **人审 > 合成** | `human` 档样本不可替代；合成样本只用于**扩面**，不用于**定标** |
| **对抗必备** | 对抗样本不是可选项——state 被当数据不当敌意内容是已知失灵面，无对抗样本则校准数字无解释力 |
| **弃权 / 分歧 > 一致** | 一致样本信息量低；弃权与分歧样本才是校准曲线的形状来源 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/collectors/quality-gates.ts` | 新建 | 四道质量门（拒收原因可查，fail-closed） |
| `engine/orchestrator/src/router/collectors/active-sampling.ts` | 新建 | 四类主动选样 + 选样理由留痕 |
| `engine/orchestrator/src/__tests__/collector-quality.test.ts` | 新建 | 质量门 / 选样 / 中英文占比统计单测 |
| `docs/ARCHITECTURE.md` | 修改 | 判定数据集质量门与选样口径（与 TrainChannel 数据面衔接） |

### 验收标准

- [ ] 四道质量门各自有单测（真实来源 / 轨迹级切分 / 噪声对账 / 概率目标区分）
- [ ] 主动学习四类选样可配置，且**选样理由落痕**（不是黑箱挑样）
- [ ] **中英文样本占比分别落数**（不合并为总量）
- [ ] 拒收原因分布可查（与 §二 采集可观测同口径）
- [ ] **三集分工在位**（候选集 / 刻度集 / 免调集），用途不混用
- [ ] 候选集冻结后分数**只能降**（无追加路径，有单测）；样本增补走新集并声明新旧集不可横比
- [ ] 门槛来自生产流量分布，且**换基座 / 数据漂移时门槛重标**有可触发路径
- [ ] 样本**私有哈希**在位，与公开基准零交集（有断言）
- [ ] `npm test` 全绿
