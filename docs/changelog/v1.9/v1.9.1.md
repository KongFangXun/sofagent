# v1.9.1 开发日志 — 判定底座 · 判据面扩展与四层性能标准（含第四方对照 · 带叙事章）

> 📋 **已排期。** 前置依赖：v1.9.0（既有判定面收敛）+ v1.8.1（延迟与吞吐落数）+ v1.7.2（三态对照口径）。
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-09-20（判定底座施工期拆版落盘，用户拍板）。设计依据见维护者侧判定底座设计文档（仓外）。**本版带叙事章。**

## 定位

> 📊 **判定底座 · 判据面扩展与四层性能标准**——阶段四第二版，回答「**怎么证明它真的有用**」。判定能力的正确性不由通用榜单定义，而由**自有判据集上的分层性能**定义——本版把判据面从审计规则扩到 workflow / 本体数据 / 路由判定。同时给出**四层性能标准**与**第四方对照**，把判定能力的证据链首次公开。**纪律**：性能以自有判据集为准，**不比通用 benchmark**（通用榜单与判定任务不同构，比了也无解释力）。第四章补上这套证据链**失效时**的探测器——判定坏掉时它不会报错，只会安静地答错。

---

## 一、判据面扩展（从审计规则扩到三类判定域）

> **形态归属**：`[主干]`（判定·判据面）｜ **不可拔**（判据面是能力的边界声明）

### 交付

| 组件 | 能力 |
|------|------|
| **workflow 判据面** | 编排任务的判定判据（该不该触发 / 该走哪条支路 / 该不该升级）——判据来自 v1.5.1 事件驱动与 v1.5.6 判定分层的既有语义 |
| **本体数据判据面** | 本体实体/关系操作的判定判据（该不该建 / 该不该合 / 该不该失效）——对接 v1.5.0 双时态与 Validation Engine 既有语义 |
| **路由判定判据面** | 请求难度与敏感度的判定判据（该走哪档）——对接 v1.5.6 路由层与 v1.8.1 判定档 |
| **判据集分层声明** | 各判据域的能力边界显式声明：**哪些域已校准、哪些域只是接口在**——未校准域的输出不得进决策路径（沿用 v1.7.1 门禁） |
| **域的适用边界** | 明确判定底座**未验证的域**（开放式写作、主观偏好、长链推理）——不在无验证器的域宣称判定能力（对齐「验证差距」纪律：验证比生成便宜才有自动化的前提）。判据面的**扩展**须以**分布外表现**为前置输入——生态实测的泛化落差约 24 个百分点（已训练任务族可用不等于新任务类型可用），口径见 [v1.6.2](./v1.6.2.md) 分布内外分列 |
| **判定不适用面（不是所有决策都该交给判定）** | 判定**不是决策层的默认件**，有明确不该交给它的情形：① **要生成散文** → 用生成模型，判定不写字 ② **需多轮推理或工具调用** → 用 Agent，判定只做单跳判断 ③ **决策是结构化输入的纯函数、能用确定性代码直接写出** → **去写代码**；判定只处理「代码已经开始变形」的那一类（正则与手工阈值越缠越深的地方），代码清楚的场景交给判定是白增一层不可审计的间接 ④ **延迟预算在十毫秒量级** → 判定件本身在数百毫秒档，这种场合**答案是小分类器**。一句话：**判定的价值来自「输入没法写死、但答案空间可以写死」**——两侧任一不成立，即回到确定性代码或生成模型 |
| **三层级联（判定在系统中的位置）** | 判定**既不替换**生成模型、**也不替换**确定性代码，而是坐在两者之间：**判定先做便宜的判断与分类 → 确定性代码处理它能处理的部分 → 生成模型只接剩下的难例**。这与「判断与生成分离」是同一件事的两面——**判定管内环**（分类 / 重排 / 门控 / 路由，输出供代码分支），**生成模型管外环**（理解用户 / 调工具 / 综合答复，输出供人阅读），两者**不是竞争者，是不同层**。生态共识表述为「**判定只判断，执行归代码**」：门槛、风险否决与执行动作**一律留在确定性代码**，判定只提供带概率的判断 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/criteria/workflow/*` | 新建 | workflow 判据集 |
| `engine/orchestrator/src/router/criteria/ontology/*` | 新建 | 本体数据判据集 |
| `engine/orchestrator/src/router/criteria/routing/*` | 新建 | 路由判据集 |
| `engine/orchestrator/src/__tests__/criteria-domains.test.ts` | 新建 | 判据集加载 / 域边界声明 / 未校准域拒绝入决策路径 |

### 验收标准

- [ ] 三类判据域各自判据集在位，加载校验通过
- [ ] 各域**校准状态显式声明**（已校准 / 仅接口）
- [ ] 未校准域的输出**不进决策路径**（门禁断言在位）
- [ ] 未验证域（开放式 / 主观 / 长链推理）显式声明为不宣称能力
- [ ] **判定不适用面在建档时显式声明**（生成类 / 多轮推理类 / 可用纯函数写出类 / 十毫秒级延迟类）
- [ ] 判据登记时有一个入口判据回答「这件事是否该交给判定」（不适用的落到代码或生成模型，且**理由留痕**）
- [ ] 三层级联的接缝在类型层体现：判定输出**只供代码分支**，执行动作与门槛**不经判定**（有断言）
- [ ] `npm test` 全绿

---

## 二、四层性能标准（判据级 / 校准级 / 分流级 / 端到端）

> **形态归属**：`[主干]`（判定·性能标准）｜ **不可拔**（性能标准是能力声明的依据）

### 交付

| 层 | 度量 | 说明 |
|----|------|------|
| **判据级** | 逐判据准确率 / 一致率 | 与 v1.6.2 / v1.7.2 同数据集同评测器口径（对照不漂移） |
| **校准级** | ECE / 分桶可靠图 | 概率可信度独立度量，**不与判据级合并** |
| **分流级** | 三段门控（高自动 / 中复核 / 低转人工）的实际错误率 | 门槛对应的**真实错误率**落数（这是门槛是否有意义的唯一证据） |
| **端到端** | 判定延迟（单次 / 批量 / 并发劣化）+ 判定失败率 + 降级次数 | 一体机实测（v1.8.1 落数的正式化） |
| **判定调用密度** | 每任务的**判定调用次数**（调用总量 / 任务数）——判定不是免费的：调用密度既是成本指标，也是**设计质量的信号**（一次任务调二十次判定，通常意味着问句该合并、或该拆到确定性代码里去）。成熟形态的量级是「单任务打分次数仅数次」 |
| **打包并发的成本杠杆** | 同一份 `state` 下的多个问句**须打包成一次请求**，不得串行多次调用——这是判定层**最大的单点成本优化**：生态实测把五次串行调用合并为一次打包后，**延迟降约五倍、成本降约一个数量级**（另一组十三问口径给出同量级结论）。因此**调用密度指标须区分「串行调用次数」与「打包请求数」**——只报前者会把打包成效记成假象 |

### 交付要点

- 四层**分别落数、分开报告**（禁止压成单一分数——合并会掩盖「判得准但概率不可信」「概率可信但太慢」这类危险组合）
- 每层给出**阈值**与**未达阈的处置**（未达阈不得对外宣称该层能力）
- 性能标准写入 `docs/VALIDATION.md`，作为对外能力声明的唯一依据
- **可复算产物**：四层落数的每个数字须附生成脚本与结果文件入仓；不可复算的数字标注「单次运行 / 私有数据」口径，**不与可复算数字并列展示**
- **成本的主体是输入而非输出**：判定件的输出通常计价自由或近乎自由，账单几乎全部由**输入 token** 构成——成本优化的着力点是**问句与 state 的尺寸**（合并问句 / 裁剪 state / 复用前缀缓存），而不是压缩输出。生态共识：**贵的是你的提示词，不是模型**
- **延迟的用途是职责边界**：端到端延迟不只报毫秒，须同时给出「该延迟下判定可承担的职责层级」（与 [v1.8.1](../v1.8/v1.8.1.md) 职责频率分层同口径）

### 验收标准

- [ ] 四层各自落数并与阈值比较（指标口径固定，可复算）
- [ ] 四层**分开报告**（无单一合成分数）
- [ ] 未达阈项显式标注且不进对外声明
- [ ] 端到端延迟与并发劣化曲线来自一体机实测（非推算）
- [ ] `npm test` 全绿

---

## 三、第四方对照（不是自说自话）

> **形态归属**：`[主干]`（判定·外部对照——本版为**对照实验**）｜ —

### 交付

| 对照方 | 说明 |
|--------|------|
| **高能力本地模型档** | 同级可本机部署的通用模型（如 30B 级 MoE 档） |
| **大参数量云端模型档** | 大参数开源模型存档（用于「小判定件 vs 大通用件」的性价比对照） |
| **云端快档通用模型** | 云端快档（对照「判定件 vs 通用快档」的延迟/成本/准确率三角） |
| **同类开源判定件** | 同期开源判定模型的公开数字（**引用其自报口径并标注口径差异**，不直接横比） |
| **公开独立实测存档** | 已公开的**第三方实测**数字，作为「不自己评自己」的对照锚：① 某独立测试机构对商业判定件的实测（37 份文档 / 21 个问题 / 777 次判断在 0.7 s 内完成，成本约四分之一美分，**准确率比前沿模型低一档**）② 某开源复刻作者在**一体机同款机型**上做的对照（扩散路线 198/201 vs 商业件 191/201，且更快）。两条的评测集均系对方自建，**与我们不同构，只可并列不可排名** |
| **同集可比对（唯一可横比的一对）** | 某商业判定件在其**公开评测的严格公共子集（343 对）**上 86.9%，同集上某开源复刻 76.7%、同集上的通用模型 73.8%。**这是全部公开数字里唯一同集同口径的一对，因而是唯一可横比的**——其余各条均系他方自建评测集，只并列不排名 |

### 对照纪律

- **同判据集、同评测器**（口径一致是可比的前提；口径不可比时**只列数字不排名**）
- 公开报告须同时给出**准确率 / 校准 / 延迟 / 成本**四维（只报有利维度即失去意义）
- 引用第三方数字时**标注来源与口径**（自报 vs 独立实测须区分）
- **分布内外分列**：本版落数须沿用 v1.6.2 的分布内外口径——在**已训练任务族内**与**从未见过的任务类型**上分别报数（生态实测的落差量级约 24 个百分点，合并会掩盖判据面扩展的真实风险）
- **对手格式破坏率的机理解释须一并入档**：跨厂商对照中对手的格式失败率可达**两位数百分比**（实测两例为 17.00% 与 45.50%），这**不是对手能力差**，而是**对照通道本身的偏置**——判定件走类型化接口（结构由契约保证），对手走文本通道，其结构约束只能靠提示与重试维持。**对手方适配器亦自承该对照「并不构成一次公平测试」**（拿不到对手的原始输出分布，只能请求字符串、不中重问）。这一条须写进对照说明，**否则格式失败会被误读为能力差距**
- **同一对象的多源数字冲突时并列各源原值，禁自行拼合**：同一判定件的公开数字在不同来源间可相差一档（实测：同一发布物在不同来源的基准口径下出现过**十个百分点级**的准确率差异，另有一来源给出完全不同的精度口径）——**各源各自是有效读数，拼合出的第三个数不是任何一个来源的读数**。处置：**逐源列出原值 + 出处 + 口径**，交由读者裁定，**不得合成一个取舍后的数字**
- **公开实测存档须含「判定件被推到能力边界」的实例**：生态里判定件已在两类与审计同源的场景留下可引用数字——① **单据与表单分类**（实测 261 份表单严格准确率 100%，单页成本约千分之一美元，比其原有生成模型管线**便宜三十余倍、快数倍**）② **高频决策回路**（实测每约三百毫秒做一次带概率判断，且**阈值、风险否决与执行全部留在确定性代码**）。引用时**只作形态参照**——其评测集均系对方自建，**只可并列不可排名**
- **不利结论照录**：第三方实测已显示判定件的准确率可能低于前沿生成模型一档——这类结论**进 VALIDATION 而非删去**，判定件的卖点是延迟与成本，不是准确率

### 验收标准

- [ ] 三方（或以上）对照实验落数，同判据集同评测器
- [ ] 四维并列（准确率 / 校准 / 延迟 / 成本），无选择性报告
- [ ] 第三方数字标注来源与口径（自报 / 独立实测区分）
- [ ] 口径不可比的项目**只列不排名**
- [ ] **分布内外分列落数**（与 v1.6.2 同口径，落差显式声明）
- [ ] 结论进 `docs/VALIDATION.md`（**含不利结论**——准确率可能低于前沿生成模型一档这类结论照录）
- [ ] 每个数字附**可复算产物**或标注「单次运行 / 私有数据」口径
- [ ] 判定调用密度落数（每任务判定次数），**且区分串行调用与打包请求**，与四层标准并列报告
- [ ] 跨厂商对照中**对手格式破坏率与通道偏置说明**一并入档（不使格式失败被读成能力差距）
- [ ] 同一对象出现多源数字时**逐源并列入档**（含出处与口径），无拼合后的单一数字
- [ ] 公开实测存档条目均标注**评测集归属**（对方自建 ⇒ 只并列不排名）

### 风险与前置假设

> ⚠️ **对照可能显示判定件并不占优**——同类开源判定件（基于 9B 基座做微调，参考标签一致率从基座 66.36% 提升到 90.12%，其闭源参照为 93.21%）提示：判定能力对**数据质量**的敏感度远高于参数量。若我们的数字落后，如实落数并定位原因（判据质量 / 数据量 / 训练方法），**不通过更换评测口径来美化**。
>
> ⚠️ **本版带叙事章，但叙事只讲形态与验证方式，不讲「比 X 好」**。性能对比归 VALIDATION 的落数，叙事不做结论性宣称。

---

## 四、静默失败监控与判定使用度量

> **形态归属**：`[主干]`（判定·可观测性）｜ **不可拔**（静默失败是判定层最危险的失效形态——无报错、无异常、答案照出，监控是唯一防线）

前三章回答「判定有多好」，本章回答「**判定坏掉时我们怎么知道**」。判定层的失败多数**不抛异常**：类型对了、结构对了、答案出来了——只是答错了。因为**类型安全意味着没有解析器会替你报警**：一个返回了合法 `Choice` 分布的模型，从类型系统看与一个答对的模型完全一样。这一章是判定层与「能跑」之间最后一道缝。

### 交付

| 组件 | 能力 |
|------|------|
| **静默失败四探针** | 产出级探测（与 v1.4.5 的 `providerStatus` 产出级探测同族，探测对象从「进程活着」深化为「判定还有效」）：① **输出分布漂移**（某判据的选项分布突变为单峰或均匀）② **概率分布异常**（长期贴 0.5 或长期贴 1.0——两者都是未校准或模式坍塌的特征）③ **弃权率突变**（骤升或骤降）④ **输入长度与延迟的异常相关**（提示触发了截断或降级）。超阈**告警不阻断**，但告警必须**可见**（面板 + 周报），不允许只落日志 |
| **判定使用度量** | 判定被调用后下游**采纳了多少**：判定值采纳率 / ASK 率 / DENY 率 / **被人工推翻率**（推翻率是判定质量最直接的外部信号）。**「已上线但无人消费」与「上线即被推翻」必须能区分**——前者是集成问题，后者是质量问题，混成一个数字则两者都无法处置。**红旗群**：ASK 率趋近于零 · **获批率恒为 100%** · **审批时延落在提示后 1–2 秒** · **人均审批量异常高**——四者同时出现即门控退化为形式（与 v1.7.1 门控健康度同族，此处是观测侧的落地） |
| **设计有效性与运行有效性分列** | 判据齐全、正负样例在位是**设计有效性**；实际判过、留痕在位是**运行有效性**——**两者须分列报告，并显式报告两者之差**。生态实证：对 Agent 的控制只能从**运行记录**测其运行有效性，而**运行证据缺失本身就是一条 finding**——「无法证明它没乱来」对一个自治系统而言与「它乱来了」是同级治理缺陷。这与本版「判定质量 / 概率可信度分列」同族：**禁合并** |
| **压缩留痕完整性** | 上下文压缩（v1.4.8 自动压缩，实现于 `engine/inject/src/load-chain/compactor.ts`）是**有损**的，但判定依据的留痕必须**无损**：压缩后判定当时依据的证据须仍可在审计链上还原——留痕保存**压缩前的判据输入引用**而非压缩后摘要。**为什么这是判定层的问题而非文档问题**：被压掉的很可能正是判定所依据的那条证据，留痕若只剩摘要，审计就无法回答「当时凭什么这么判」 |
| **四层性能标准的持续采集面** | §二 的四层落数依赖**稳定采集**而非一次性跑分——本版为其提供持续采集与趋势视图（判据级 / 校准级 / 分流级 / 端到端各自的时序曲线），使「能力退化」在版本之间就可见，而不是等下一次评测才发现 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/judge-observability.ts` | 新建 | 静默失败四探针 + 判定使用度量采集 |
| `engine/inject/src/load-chain/compactor.ts` | 修改 | 压缩时保留判据输入引用（压缩前证据可还原） |
| `tools/dashboard/` | 修改 | 判定使用度量面板（采纳率 / 推翻率 / 探针趋势） |
| `engine/orchestrator/src/__tests__/judge-observability.test.ts` | 新建 | 探针触发 / 度量口径 / 压缩留痕完整性单测 |

### 验收标准

- [ ] 静默失败四探针在位且各有触发用例（分布漂移 / 概率异常 / 弃权突变 / 长度-延迟相关）
- [ ] 探针超阈**告警不阻断**，告警在面板与周报可见（非仅日志）
- [ ] 判定使用度量四指标齐备，**「无人消费」与「被推翻」可区分**
- [ ] 压缩留痕完整性：压缩后判定依据仍可从审计链还原（有单测，含引用不丢）
- [ ] 四层性能标准有持续采集与趋势视图（非一次性跑分）
- [ ] `npm test` 全绿

> ⚠️ **本章的指标都是「坏消息探测器」，不是成绩单**。采纳率高不等于判得准（可能只是没人复核），弃权率低不等于判得好（可能是阈值失效）。落数时须与 §二 四层标准并列阅读，**不得把本文指标单独用作能力声明**。
