# v1.5.3 开发日志 — 执行模块 · 路由与验证（模型路由层 + 凭证隔离 Vault + 多实例自验证 + 全节点执行状态机 · 自 v2.0.0 前移）

> 📋 **已排期。** 前置依赖：v1.3.6（model_register / model_switch / 分级降级梯队 + routeReason 可解释）+ v1.3.5（run_ab_test 多实例基础设施）+ v1.3.7（虚拟 key 凭证边界——Vault 的部分覆盖基础）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-29（自 v2.0.0 拆分前移，用户拍板——三项均**不依赖企业专属精调模型**，无需等 v2.0.0 数据主权窗口）· **2026-08-31 版本号顺延：原 v1.5.4 本版改号 v1.5.3（前序版本号撤销引起顺延，见 [v1.4.5 第七章](../v1.4/v1.4.5.md#七进化模块实证收口自-v151-整版前移--2026-08-31-用户拍板)）** · **2026-09-05 收编：router 网关化拆件两件收进第一章**（敏感度实时分类器——敏感度路由的输入前置件；权重灰度发布 AB——本地权重部署链路的灰度面），用户拍板落盘

> 🔗 **拆分依据（2026-08-29 拍板）**：v2.0.0 原含四件，其中模型路由层（ROADMAP 明示「路由层可提前到 v2.x 做（不依赖精调模型）」）、Vault（v1.3.7 虚拟 key 已部分覆盖）、多实例自验证（run_ab_test 基础设施现成）三件的依赖全部就绪——前移当时的 v1.5.4（现 v1.5.3），v2.0.0 收窄为数据主权核心（离线 USB 节点合体）。

## 定位

> ⚡ **执行模块 · 路由与验证**——把 v2.0.0 里「不依赖精调模型」的三件提前交付：模型路由层（云端规划/本地执行/管道分层 + 敏感度 fail-closed）+ 凭证隔离 Vault（Agent 代码碰不到 token）+ 多实例自验证（并发小模型交叉表决）+ **全节点执行状态机（SKILL.state 收编，2026-09-03 拍板新增并同日拍板全面推广不试点——全部长任务节点切换执行协议，全局降级开关兜底）**。v2.0.0 届时只剩离线 USB 节点合体一件核心，交付面收窄、发版确定性提高。

---

## 一、模型路由层（Harness 路由 · 云端/本地分流）

> 📋 **ROADMAP 分层模型架构**：云端 32B+ 负责规划推理 → 本地 7B 执行多步 workflow → 本地 0.5B 跑管道层。**引擎层只做模型路由（model-router.ts 已有四档插槽），精调 pipeline 属模型层非开源范围。路由层不依赖精调模型——对接现有云端 API + 本地推理服务（Ollama 等）即可交付。**

### 交付

| 组件 | 能力 |
|------|------|
| **路由策略引擎** | 任务分级路由：规划类→云端大模型 / 执行类→本地 7B / 管道类（模板/格式/字段提取）→本地 0.5B——按任务类型+数据敏感度双维决策 |
| **敏感度实时分类器（router 网关化拆件 · 2026-09-05 收编）** | 请求数据敏感度的实时分级（公开/内部/敏感）——敏感度路由的输入前置件：先分类、再路由，分类结果随 routeReason 入审计留痕；本地权重部署链路（v1.4.4）的权重灰度 AB 亦复用该分类结果决定权重可服务的请求档位 |
| **敏感度路由** | 数据分级（公开/内部/敏感）自动决定可否走云端——敏感数据强制本地（fail-closed） |
| **权重灰度发布 AB（router 网关化拆件 · 2026-09-05 收编）** | 本地权重部署链路的灰度面：新旧 LoRA 适配器按流量比例灰度（复用 v1.3.5 run_ab_test 基础设施），灰度指标回退保护对齐 v1.4.5 持续后训练回退闸 |
| **session 全量记录与蒸馏对接（router 网关化拆件 · 2026-09-05 收编）** | router 记录过站 session 内容（请求/响应/路由决策），定期对接 v1.4.5 持续后训练数据回流通道（蒸馏素材源）；存储**本地优先**（数据主权铁律 3——记录不出企业边界），记录读写全程审计留痕。商业平台的集中式云存储是部署形态选项，不是引擎能力 |
| **路由可解释** | 每次路由决策附 routeReason（复用 v1.3.6 EndpointProfile + route-policy 模式） |
| **降级梯队** | 云端不可用→本地承接（复用 v1.3.6 分级降级梯队） |
| **本地端点注册** | model-registry 注册本地推理服务端点（Ollama 兼容协议），云端/本地统一注册面 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/policy-engine.ts` | 新建 | 任务×敏感度双维路由决策 |
| `engine/orchestrator/src/model-registry.ts` | 修改 | 本地模型端点注册（7B/0.5B 档位） |

### 验收标准

- [ ] 三级任务路由可用（规划→云/执行→本地7B/管道→本地0.5B）
- [ ] **敏感度实时分类器可用**：请求入站先分类（公开/内部/敏感），结果随 routeReason 留痕
- [ ] **权重灰度 AB 可用**：新旧适配器按比例分流，指标回退自动切回旧权重
- [ ] **session 全量记录可用**：过站记录本地落盘 + 审计留痕 + 对接持续后训练回流通道
- [ ] 敏感数据强制本地路由（fail-closed，配置错误时拒绝执行）
- [ ] 每次路由附 routeReason
- [ ] 云端不可用自动降级本地
- [ ] `npm test` 全绿

---

## 二、凭证隔离 Vault（OMA 启发）

> 📋 **探索方向表收编**：沙箱发 HTTP 请求时动态注入凭证、Agent 代码碰不到 token——执行层安全基础设施，v1.3.7 虚拟 key 凭证边界已部分覆盖，完整 Vault 前移本版。

### 交付

| 组件 | 能力 |
|------|------|
| **Vault 服务** | 凭证托管：Agent 运行时凭证不出 Vault（动态注入请求头，Agent 代码/日志/审计面均见不到 token 明文） |
| **轮换与吊销** | 凭证可轮换（定时/事件触发）+ 单凭证吊销（泄露响应） |

### 验收标准

- [ ] Agent 沙箱 HTTP 请求经 Vault 动态注入凭证，代码层拿不到 token
- [ ] 凭证轮换/吊销可用
- [ ] `npm test` 全绿

---

## 三、多实例自验证（并发小模型交叉表决）

> 📋 **探索方向表 2026-08-24 新增**（方向参考不引数字）：同一任务并发跑多个小模型实例 + 交叉验证多数表决，替代单次大模型调用。方向上与 AB 测试基础设施（v1.3.5 run_ab_test）可复用。

### 交付

| 组件 | 能力 |
|------|------|
| **表决编排** | 同任务 N 实例并发（复用 run_ab_test 多实例基础设施）+ 结果交叉比对 + 多数表决产出 |
| **分歧路由** | 实例分歧超阈值 → 路由人工介入队列（HITL 复用）或升级大模型重跑 |

### 验收标准

- [ ] N 实例并发 + 多数表决可用
- [ ] 分歧超阈值路由 HITL
- [ ] `npm test` 全绿

---

## 四、全节点执行状态机（SKILL.state 机制收编 · arXiv:2608.26263 · EMNLP · 2026-09-03 用户拍板：全面推广不试点）

> SKILL.state 把技能执行从聊天记录改成状态机：每步模型只收 P（不可变技能说明）+ Σt（结构化状态 JSON）+ ot（最新观察），输出推理轨迹 Rt / 状态补丁 ΔΣt / 动作 at；运行时确定性校验合并 Σt+1 = Σt ⊕ ΔΣt 后**推理轨迹永久丢弃**——单步 prompt O(1)、累计 token O(T)。论文实测：16.2 倍 token 压缩（1,062,387→65,408）、抗噪 ≥0.97（ReAct 崩到 0.53）、状态恢复零步数、InterCode CTF 54.2%（+7.8 且省 60.4% token）；**预算对齐控制实验实锤赢在结构化不在压缩**（同预算下：滑窗截断 0.18 / LLMLingua 0.22 / SKILL.state 0.94）。
>
> **sofagent 的增量空间在节点内部**：编排层（LoopGraphState + artifacts 浅合并 reducer）已是 sofagent 版的 Σt⊕ΔΣt，checkpoint 已支持状态恢复；但 `node-executor.ts` 用 createReactAgent，节点内部是消息历史式滚雪球（无 trimMessages），长任务 token O(T²) 与上下文中毒风险与论文基线相同。
>
> **sofagent 天生的落地底气**：wrapToolCall 运行时审计在每步动作发生时实时落记录——推理轨迹可弃而行为仍可溯，**轨迹可弃、行为可溯**，这是论文没有、sofagent 独有的审计前提。
>
> **全面推广判据（2026-09-03 用户拍板，不做单点试点）**：① 风险已被 sofagent 自己消解——wrapToolCall 审计通道在位，轨迹丢弃的审计风险天然兜底，无需靠试点验证安全性；② 论文已在三个基准族（仓库管理 SkillExecBench/InterCode CTF/τ-Bench）跨域验证协议通用性，「先试一个域再推广」的验证价值已被论文做完；③ 兜底不靠试点靠**全局降级开关**——所有节点默认带降级路径，任何节点出问题单独回退消息历史式，不影响其他节点，等效于每节点自带「试点失败保险」，且比先试一点的方案更快拿到全量收益。

### 交付

**执行协议规范（SKILL.state 采纳形态——先定协议，再写代码）**

| 协议要素 | sofagent 落地形态 |
|------|------|
| **P（技能说明，不可变）** | 现有 system prompt / SKILL.md 注入内容，执行期只读恒定 |
| **Σt（执行状态 JSON）** | 每个节点类型定义结构化状态（见下方基础 schema + 领域扩展），随执行逐步演化，是唯一跨步携带的记忆 |
| **ot（最新观察）** | 上一步工具返回（截断至 token 预算内），只看最新一条，历史观察不进 prompt |
| **ΔΣt（状态补丁）** | 模型每步随动作输出 JSON Patch 子集（`{op: add/remove/update, path, value}`），**由运行时代码执行合并** Σt+1 = Σt ⊕ ΔΣt，schema 校验不过即拒绝该补丁——状态合并不靠模型自觉，靠代码保证 |
| **Rt（推理轨迹，即弃）** | 步内 CoT 完整保留供本步推理，状态转移验证通过后**不进下一步 prompt**；丢弃前先落审计摘要（动作 + ΔΣt + 因果边）进 wrapToolCall 通道 |
| **降级路径（全局开关）** | 每节点带 `executionMode: stateful | legacy` 配置，补丁连续校验失败 N 次自动降级 `legacy` 并审计告警；环境变量 `SOFAGENT_STATEFUL_EXEC=off` 一键全回退——单节点故障不扩散，协议层 bug 不卡死任何任务 |

**基础状态 schema（全节点通用底座，六字段）**：

```json
{
  "goal": "本节点任务目标",
  "done": ["已完成步骤（带结果摘要）"],
  "todo": ["待办步骤"],
  "facts": ["工具观察到的关键事实（不含原始输出）"],
  "files": ["已改动文件路径"],
  "blockers": ["阻塞项与绕行决策"]
}
```

**节点类型 schema 扩展表**（基础六字段 + 各节点领域字段）：

| 节点 | 领域扩展字段 | 说明 |
|------|------|------|
| engineer | `tests: {pass, fail, skipped}`、`reviewComments: [{file, line, status}]` | 代码修复循环——测试状态与审查意见回链 |
| checker | `checkRuns: [{rule, verdict, evidence}]`、`falsePositives: []` | 审查循环——规则判定留状态不留原始日志 |
| reviewer | `issuesBySeverity: {p0, p1, p2}`、`coveredFiles: []` | 审查循环——覆盖度与问题分级实时账 |
| refine-agent | `qualityScores: [{round, score}]`、`triedStrategies: []` | 优化循环——已试策略防重复（对齐 failure-ledger 精神） |
| 通用长任务 | 基础六字段即可 | 文档批处理/数据处理等默认形态 |

**推广范围**：`node-executor.ts` 全部 createReactAgent 节点（engineer/checker/reviewer/parallel_wave 单元）+ refine-agent 优化循环 + loop-agent driver——**所有长任务节点一次性切换到执行协议**，短平快节点（单次工具调用）不改造（改造收益低于成本）。

| 组件 | 能力 |
|------|------|
| **执行状态机执行器** | `node-executor.ts` 改造为协议执行器：每步产出 ΔΣt，代码确定性合并，下一步 prompt 只带 P+Σt+ot，推理轨迹合并后即弃；`executionMode` 双模式在位（stateful 默认 / legacy 保底） |
| **审计闸门前置** | Rt 丢弃前落一条审计摘要（动作 + 状态补丁 + 因果边）——wrapToolCall 通道接线，原则「轨迹可弃、行为可溯」写进 DEVELOPMENT.md 执行层章节 |
| **节点 schema 注册表** | 节点类型 → schema 映射注册表（基础六字段 + 领域扩展），新节点类型注册即用——schema 是领域资产沉淀进 SKILL/ 设计文档，非任务级配置 |
| **全局度量** | 每节点 token 曲线（对齐论文 O(1) 平线）/ 轮次稳定性 / 结果质量对比（vs legacy 模式）落 `data/evolution/stateful-metrics.jsonl`，Dashboard 可见 |

### 涉及文件（预估）

`engine/orchestrator/src/node-executor.ts`（协议执行器改造）/ `engine/orchestrator/src/refine-agent/`（优化循环接协议）/ `engine/orchestrator/src/loop-agent/driver.ts`（driver 接协议）/ `engine/orchestrator/src/middleware/`（审计摘要接线）/ `engine/orchestrator/src/execution-state/`（schema 注册表新建）/ `SKILL/`（schema 设计文档）/ `docs/DEVELOPMENT.md`（执行层章节——「轨迹可弃、行为可溯」原则 + SKILL.state 蒸馏小节）。

### 验收标准

- [ ] 执行协议六要素落地（P 恒定/Σt 唯一记忆/ot 仅最新/ΔΣt 代码合并校验/Rt 即弃/降级开关）
- [ ] 全部长任务节点运行在执行协议上（engineer/checker/reviewer/parallel_wave/refine-agent/loop-driver），单次工具调用节点不改造
- [ ] 节点 schema 注册表在位（基础六字段 + 四类领域扩展）
- [ ] 审计摘要先于轨迹丢弃落盘（动作+状态补丁可溯）
- [ ] `executionMode` 降级路径可用：单节点自动降级 + `SOFAGENT_STATEFUL_EXEC=off` 全局一键回退
- [ ] 全局度量落盘：每节点 token 曲线 + 稳定性 + 质量对比（stateful vs legacy）
- [ ] 五文档挂点同步：DEVELOPMENT.md 补 SKILL.state 蒸馏小节（与九B WikiSkill 对称）+ PHILOSOPHY §五自进化谱系挂「执行协议层」+ ARCHITECTURE 数据流图补「节点内部状态机」
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.3 的什么 |
|------|------|
| v2.0.0 | 模型路由层（离线 USB 节点直接复用路由底座——本地权重接入路由插槽）；Vault（USB 离线场景的凭证托管）；多实例表决（离线节点的轻量验证通道）；执行状态机（离线节点的长任务执行层——token 效率在离线算力受限场景更重要） |
