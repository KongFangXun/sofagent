# v1.5.1 开发日志 — 编排模块 · 事件驱动（业务事件触发 + 理解债务应对）

> 📋 **已排期。** 前置依赖：v1.5.0（决策因果链——异常判定依据 + trace 对账差异事件——异常分类路由的输入之一）+ v1.3.8（cron 三档糖 + 依赖图 + 死循环检测）+ v1.4.9（G9 设备注册/心跳——G12 升级面向已注册设备）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-29（v1.5.0 系列规划首版 · 探索方向「事件驱动执行触发」+「理解债务应对」收编，用户拍板落盘）· 2026-08-29 二次收编：探索方向「AI 异常处理总线」迁入第三章（原候选 v2.x，用户拍板前移 v1.5.x）· **2026-08-31 版本号顺延：原 v1.5.2 本版改号 v1.5.1（前一个 v1.5.1「进化模块实证收口」整体前移 v1.4.5 第七章，版本号撤销，顺延说明见 [v1.4.5 第七章头部](../v1.4/v1.4.5.md#七进化模块实证收口自-v151-整版前移--2026-08-31-用户拍板)）** · 2026-09-01 三次收编：**G12 设备 OTA 远程升级**迁入第四章（原候选 v2.x 设备联邦同期，用户拍板前移 v1.5.x——升级指令下发复用本版事件总线，设备注册/心跳依赖 v1.4.9 G9）· 2026-09-18 收编：**T8/T9 生产管线接线**自 v1.5.0 顺延入第六章（v1.5.0 开发批收口未含，用户拍板顺延）· 2026-09-20 收编：**存量断链修复**（`--legacy` 退役调用方清零）立为第九章（v1.5.0 发版后全仓独立审查实证——本版 `[bugfix]` 章，开工顺序优先于第三、五章）

## 定位

> ⚡ **编排模块 · 事件驱动**——业务节点从「指令驱动」升级为「事件驱动」：上游节点产出、外部事件（邮件到达/表单提交/定时器）自动触发下游节点，编排模块从「人来派活」变成「事件来了自己跑」。与 v1.3.8 cron + 依赖图同向但更彻底：触发源泛化到业务事件。外加理解债务应对——审计已覆盖「发生了什么」，补「为什么这么做」。

---

## 一、事件驱动执行触发

> **形态归属**：`[主干]`（编排·路由规则 + 事件留痕）**+ `[通道]` 成分**（webhook / IM 外部入站适配器——IO 接入算子，四问②答「是」）｜ L1 关档
>
> 📋 **探索方向表 2026-08-24 新增**：与 v1.3.8 cron + 依赖图同向但更彻底——触发源泛化到业务事件。等 v1.4.x 后训模块稳定后评估 → 本版收编。

### 交付

| 组件 | 能力 |
|------|------|
| **事件源适配器** | 三类触发源：上游节点产出事件（workflow 内部）/ 外部事件（webhook 入站：邮件到达/表单提交/IM 消息）/ 定时器（v1.3.8 cron 既有） |
| **事件路由规则** | workflow 节点声明 `on:` 事件订阅（YAML 内声明式配置）——事件到达自动派发下游节点 |
| **事件总线** | 轻量事件总线（进程内 pub/sub + 落盘事件队列），事件投递全程审计留痕（每条事件可追溯触发链） |
| **死信与重试** | 事件处理失败进死信队列（复用 v1.3.1 stop_reason 分类 + 退避），死信可重放 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/events/bus.ts` | 新建 | 事件总线（pub/sub + 落盘队列） |
| `engine/orchestrator/src/events/adapters.ts` | 新建 | 三类事件源适配器 |
| `engine/orchestrator/src/events/router.ts` | 新建 | 事件→节点路由（读 workflow `on:` 声明） |
| `engine/mcp/src/tools/workflow-submit.ts` | 修改 | workflow schema 支持 `on:` 事件订阅字段 |

### 验收标准

- [x] 上游节点产出自动触发下游节点（workflow 内部事件链）
- [x] webhook 入站事件（≥2 类：表单提交/IM 消息）可触发节点〔边界声明：本版「通道」章只出接入/执行侧；运行期宿主装配不在本版落点（订阅登记入口/适配器已交付并测试背书）〕
- [x] 事件投递全程审计留痕（每条事件触发链可追溯）
- [x] 失败事件进死信队列，可重放
- [x] `npm test` 全绿

---

## 二、理解债务应对（Comprehension Debt Spiral · S2 级故障）

> **形态归属**：`[主干]`（判定信息呈现·因果链解释）｜ L1 关档
>
> 📋 **探索方向表收编**（loop-engineering 启发）：审计已覆盖「发生了什么」，需补「为什么这么做」（auto-PR 要求 Agent 解释决策）+「本周摘要」（daemon 周报）。

### 交付

| 组件 | 能力 |
|------|------|
| **auto-PR 决策解释** | AI 节点产出 PR 时强制附决策解释块（为什么这么做——引 decision-log 因果链，v1.5.0 字段） |
| **daemon 周报** | 每周自动生成本周摘要：节点执行统计 + 决策高亮 + 异常与介入汇总（写入 dashboard 周报导出源） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/runtime/pr-explainer.ts` | 新建 | auto-PR 决策解释块生成 |
| `engine/daemon/src/inspectors/weekly-digest.ts` | 新建 | 周报巡检 |

### 🔴 增量边界声明（相对既有周报面——不得产出第二份内容重叠的周报）

> 落点 `{dataDir}/dashboard/digest-<ISO 周>.json`（dashboard 数据目录即周报导出源；不改 dashboard 前端）。

| 既有面 | 它的内容 | 本章增量（不重叠处） |
|---|---|---|
| `engine/audit/src/governance.ts` `formatGovernanceWeekly`（dashboard 导出按钮消费） | 治理 KPI markdown：安全边界/覆盖率/HITL 时延中位数/周环比/重复执行率/trace 对账六卡 + 规则 Top5 + 数据集审阅 + 决策高亮平铺表 | 不产第二份 KPI 周报、不复制任何卡；HITL 只取「待办/已决计数」，时延中位数仍归它 |
| `engine/audit/src/report-generator.ts` `generateWeeklyReport` | 数据主权 ISO 周报 | 数据源不同（本节不读主权记录） |
| `engine/daemon/src/inspectors/data-sovereignty-weekly.ts`（L2 @weekly） | 数据主权档 | 同上 |
| `engine/daemon/src/inspectors/commons-health.ts`（L2 @weekly） | 公地清单完整性/退役候选 | 零交集 |
| **`engine/daemon/src/inspectors/trend-aggregator.ts`（L2 @weekly，实查补入）** | daily-snapshot 聚合的**安全边界趋势**：违规/告警/阻断/任务计数 + 规则 Top5 + 周环比，写 `dashboard/weekly-YYYY-WNN.json`；dashboard sustain 面板按 `startsWith('weekly-')` 取最近文件当 `weeklyReport`（`tools/dashboard/serve-dashboard.mjs:490`） | 本节取**执行与理由面**（worklog 节点执行统计 / decision-log 因果链高亮 / 异常与介入汇总），不碰违规计数与规则榜；**且文件名刻意避开 `weekly-` 前缀**（用 `digest-*`）——同前缀会被 sustain 面板读走且 schema 不兼容 |
| `engine/orchestrator/src/worklog/attribution.ts` 周报 Top5（高影响/低效决策） | 归因链接 Top5 | 实查为**零生产消费的孤儿模块**（未从包索引导出、`attribution.jsonl` 无生产写入方）⇒ 改「**参考**（不直接调用）」其「按影响排序而非按时间罗列」口径，数据源改用有生产写入方的 decision-log |

> 决策高亮与 governance 的差异化：governance 是「带 causedBy 决策的时间倒序平铺表」；本节是「本周窗口 + **按因果链深度排序** + 渲染**链式叙事**（根因→结果，经 `traceDecisionChain`）」，最多 5 条，载体是 JSON 摘要而非 markdown KPI 周报。

### 验收标准

- [x] auto-PR 附决策解释块（引因果链）
- [x] daemon 周报自动生成（统计+决策+异常）
- [x] `npm test` 全绿

---


## 三、AI 异常处理总线（自探索方向迁入 · 2026-08-29 用户拍板）

> **形态归属**：`[主干]`（判定·三分类路由）｜ L1 关档
>
> 📋 **探索方向表迁入**（DSH 社区实践观察 · 2026-08-24 新增，原候选 v2.x——事件总线落地后依赖就绪，前移本版）：Agent 执行异常不再各节点自兜底，统一进异常总线。与 v1.3.1 错误处理（stop_reason + 退避）衔接，升级为跨节点的总线形态。

### 交付

| 组件 | 能力 |
|------|------|
| **异常分类路由** | 异常统一分类（可重试 / 需人工 / 需回滚）→ 路由到对应处理器（重试队列 / HITL 审批队列 / 回溯能力）——复用第一章事件总线的死信通道作为异常入口 |
| **全程审计留痕** | 异常进总线即留 decision-log（挂 v1.4.4 causedBy 因果边——异常处理决策可追溯） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/events/error-bus.ts` | 新建 | 异常分类路由（三分类 → 重试队列 / HITL 队列 / 回溯能力），复用第一章死信通道作异常入口 |
| `engine/orchestrator/src/loop-agent/error-localizer.ts` | 修改 | 节点内兜底出口接总线（既有 stop_reason 分类逻辑保持原样，只改出口） |

### 验收标准

- [x] 三类异常（可重试/需人工/需回滚）各自路由到对应处理器
- [x] 异常处理决策挂因果边入 decision-log（可追溯）
- [x] **三类异常在 decision-log 中可区分**：断言三条记录的 `kind` 互不相同且 `why` 分别带 `retryable` / `needs-human` / `needs-rollback` 标签（防三类共用同一 kind 的静默退化）
- [x] `npm test` 全绿

---

## 四、设备 OTA 远程升级（G12 · 升级指令走事件总线 + 设备 daemon 拉取验签）

> **形态归属**：`[通道]`（设备侧执行面——**验签判定留主干**，本版只出执行侧）｜ L1 关档
>
> 📋 **自 v2.x 设备联邦前移**（2026-09-01 用户拍板）：G9/G10/G11 管「数据通道」，G12 管「运维通道」——一体机/节点设备卖出去后引擎怎么升级（发新版/打补丁/规则更新）。不掌握升级通道的设备会逐渐脱管，G12 是设备生命周期的最后一块拼图。**升级指令下发复用本版第一章事件总线**（升级 = 一类事件），**设备身份/健康复用 v1.4.9 G9**（升级面向已注册设备，心跳离线的设备挂起等待）。

### 交付

| 组件 | 能力 |
|------|------|
| **升级事件类型** | 事件总线新增 `device.upgrade` 事件类型（payload：目标版本 + 组件清单 + 灰度策略 + 截止窗口），平台/租户管理员发布 |
| **skill/模板下发（`device.deploy`）** | 事件类型扩展 `device.deploy`（与 upgrade 同构的平台→设备推送面）——商业平台 §2.4 执行链第一步「平台下放 skill」的引擎侧通道：payload 携带 skill/workflow 模板包（G1 五件套格式），设备 daemon 订阅 → 验签 → 落盘注册（缺口 C，2026-09-01 对账补）——平台→设备的「内容下发」与「版本升级」共用一套推送底座 |
| **daemon 升级执行器** | 设备 daemon 订阅升级事件 → 拉取新版包（npm registry / 制品库）→ 验签（复用 v1.4.9 G9 Ed25519 身份码验签链，包签名校验 fail-closed）→ 灰度执行（先升级非核心组件，健康探针通过再升级核心）→ 失败自动回滚 |
| **升级窗口策略** | 设备侧配置升级窗口（业务低峰时段执行；窗口外挂起等待）+ 手动确认模式（高风险版本需设备侧 HITL 确认） |
| **升级审计留痕** | 升级全生命周期进 audit history（谁发布/哪台设备/从什么版本到什么版本/验签结果/回滚记录）——HMAC 链覆盖 |
| **版本清单上报** | 升级完成后设备版本清单上报（G11 通道回传，平台设备面板可见） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/events/types.ts` | 新建 | 事件类型加 `device.upgrade` |
| `engine/daemon/src/ota/upgrade-executor.ts` | 新建 | 拉取 + 验签 + 灰度 + 回滚执行器 |
| `engine/daemon/src/ota/upgrade-policy.ts` | 新建 | 升级窗口/手动确认策略解析 |
| `engine/daemon/src/__tests__/ota-upgrade.test.ts` | 新建 | 单测（验签失败拒绝 / 灰度次序 / 回滚触发 / 窗口外挂起） |

### 验收标准

- [x] 升级事件发布后，在线注册设备按灰度次序完成升级（非核心→核心）〔边界声明：本版「通道」章只出接入/执行侧；运行期宿主装配不在本版落点（订阅登记入口/适配器已交付并测试背书）〕
- [x] 伪造签名升级包被拒（fail-closed）
- [x] 升级失败自动回滚到前一版本（设备可继续服务）
- [x] 窗口外 / 离线设备挂起，恢复后自动补升
- [x] 升级全程审计留痕（HMAC 链可追溯）
- [x] `device.deploy` 下发 skill/workflow 模板到设备（验签 + 落盘注册可用）〔边界声明：本版「通道」章只出接入/执行侧；运行期宿主装配不在本版落点（订阅登记入口/适配器已交付并测试背书）〕
- [x] `npm test` 全绿

---

## 五、任务下发通道二期——推送/实时（2026-09-08 拍板排入）

> **形态归属**：`[通道]`（设备侧执行面）｜ L1 关档
>
> 📋 **来源**：商业平台任务下发通道的二期形态——一期心跳捎带（v1.4.9，零新增连接）解决「指令能到」，二期解决「实时到达」：设备常驻在线后，任务不再等下一次心跳轮询，经本版事件总线**推送直达**。复用第一章事件总线（任务下发 = 一类事件，与 `device.upgrade` / `device.deploy` 同构）+ v1.4.9 G9 设备注册/心跳（离线设备回落心跳捎带通道）。

### 交付

| 组件 | 能力 |
|------|------|
| **任务下发事件类型** | 事件总线新增任务下发事件（payload：任务清单 + 目标设备 + 时效），派单方发布 |
| **设备侧订阅消费** | 设备 daemon 订阅任务事件 → 领任务回执进审计链 |
| **双通道回落** | 设备在线走推送；离线设备自动回落 v1.4.9 心跳捎带通道（上线补收） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/events/types.ts` | 修改 | 加任务下发事件类型（第四章新建该文件，本章同批扩展） |
| `engine/mcp/src/tools/device-register.ts` | 修改 | 设备侧订阅消费 + 领任务回执 + 离线回落心跳捎带（既有注册/心跳逻辑保持原样，只加订阅与回执出口） |

### 验收标准

- [x] 在线设备任务推送实时到达（不等心跳轮询）〔边界声明：本版「通道」章只出接入/执行侧；运行期宿主装配不在本版落点（订阅登记入口/适配器已交付并测试背书）〕
- [x] 离线设备回落心跳捎带通道，上线后补收不丢〔边界声明：本版「通道」章只出接入/执行侧；运行期宿主装配不在本版落点（订阅登记入口/适配器已交付并测试背书）〕
- [x] 领任务回执进审计链（HMAC 留痕）〔边界声明：本版「通道」章只出接入/执行侧；运行期宿主装配不在本版落点（订阅登记入口/适配器已交付并测试背书）〕
- [x] `npm test` 全绿

---

## 六、T8/T9 生产管线接线（自 v1.5.0 顺延 · 2026-09-18 用户拍板）

> **形态归属**：`[主干]`（判定接线·敏感识别与灰度插槽的管线消费）｜ L1 关档
>


> 📋 **来源与顺延说明**：v1.4.9 交付的 T8 敏感识别三层插槽（DetectorRegistry/classifySensitivity）与 T9 权重灰度 AB（canary 四导出）当前为 SDK 面先行——导出符号与测试就绪，零生产消费。原承诺排期 v1.5.0（CHANGELOG v1.4.9 行 + ROADMAP v1.5.0 行双承诺），v1.5.0 开发批收口时未含该章（十章均已排满），顺延至本版。**接线对象明确**：`device_data_push`（设备数据上行）与 `router_session_push`（router 过站 session 上行）两条旧通道升级为新插槽管线——上行数据先过三层敏感检测再落盘/上行，采样数据过灰度分流；判定逻辑零改动，纯管线消费接线。

### 交付

| 组件 | 能力 |
|------|------|
| **上行脱敏插槽接线** | `device_data_push` / `router_session_push` 上行管线内嵌三层敏感检测（L0 正则 → L1 词典 → L2 NER 逐层降漏；置信度三层 fail-closed 语义沿用 v1.4.9 定义）——检测命中项走既有脱敏管线后落盘/上行 |
| **灰度分流接线** | 上行采样数据经 canary hash 稳定分流（新版 LoRA 灰度流量比例消费 v1.4.9 权重灰度导出）——分流决策入审计链（routeReason 可解释） |
| **接线回归测试** | 三层检测各一用例（L0 命中 / L1 命中 / L2 外挂 mock）+ 灰度分流一用例（hash 稳定性 + 劣化回退）——共 4 用例 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/mcp/src/tools/device-data-push.ts` | 修改 | 上行管线接三层检测 + 灰度分流（工具入参/返回结构不动，只在落盘/上行前插管线） |
| `engine/mcp/src/tools/router-session-push.ts` | 修改 | 过站 session 同管线（与设备上行同语义） |
| `engine/core/src/export/sensitivity-classifier.ts` | 复用（不修改） | 分类判定本体——`classifySensitivity` 直接调用 |
| `engine/train/src/weight-canary.ts` | 复用（不修改） | 灰度分流判定本体——`canaryRouteRequest` 直接调用 |
| `engine/core/src/export/detector-remote.ts` | 复用（不修改） | L2 外挂 NER 客户端（fail-closed 语义已内建） |

### 验收标准

- [x] `device_data_push` 上行数据过三层检测，命中项脱敏后落盘（原始敏感值不入盘，断言）
- [x] `router_session_push` 过站 session 同管线处理（与设备上行同语义）
- [x] L2 NER 外挂不可用时 fail-closed（降级 L1 词典并留痕，不静默放行）
- [x] 灰度分流 hash 稳定（同一 session 多次上行同侧）+ 劣化触发回退可用
- [x] 接线回归测试 **6** 用例全绿 + `npm test` 全绿

---

## 七、审计输入双通道（调用意图 + 结果文本 · 2026-09-16 用户拍板排期本版）

> **形态归属**：`[主干]`（判定·审计输入面扩容——规则判定逻辑零改动）｜ L1 关档
>
> 📋 **本版事件总线的第一个消费方**：现有审计输入面是 **git diff（结果文本）** 单一通道——但 Agent 的危险动作往往在**参数级**（`rm -rf` 的 path、写文件的落点、外发请求的目标 host），diff 只能看到事后的残留产物，看不到「调用意图」。本版借事件总线把审计输入扩为**双通道：调用意图 + 结果文本**。
>
> **与 v1.5.0 第十章的边界**：第十章做的是**接线**（把宿主事件位接到既有 bridge API，判定逻辑零改动）；本章做的是**消费**（把意图流做成审计输入面的第二通道，供规则判定与留痕举证使用）。审计仍零执行权限——事件流只读消费 + 落盘留痕；短路拦截属宿主事件位能力（第十章），不是审计越权。
>
> **不新增规则**：本章只扩输入面，规则判定逻辑零改动；新规则走 v1.5.2 第十 / 十一章。

### 交付

| 组件 | 能力 |
|------|------|
| **意图流接入** | 订阅 `tools/pre-execute` + `tools/result` 事件，落 `intent.jsonl`（tool 名 + 参数摘要 + 会话标识 + 时间戳；**参数经既有脱敏管线**处理后才落盘） |
| **双通道输入面** | 规则判定入口接受「意图 + 结果」两路输入——意图通道供**事前**判定（越界参数在执行前暴露），结果通道维持既有 git diff 事后判定 |
| **通道声明语义** | 规则显式声明取哪个通道（默认结果通道，意图通道 opt-in）——消灭「规则自己猜输入源」整类 bug（对齐探索项「审计范围语义一等公民化」的最小版本） |
| **同链留痕** | 意图流与结果流进同一 HMAC 链（条目类型区分）——举证时可对照「当时想干什么」与「实际改了什么」 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/intent-channel.ts` | 新建 | 意图流接入与落盘（复用脱敏管线，不落原始参数） |
| `engine/audit/src/rules/types.ts` | 修改 | 规则输入面加通道声明（默认结果通道） |
| `engine/audit/src/chain-kernel.ts` | 修改 | 链条目类型区分（intent / result），哈希输入扩展同批核 |
| `engine/audit/src/__tests__/intent-channel.test.ts` | 新建 | 单测（意图落盘 / 脱敏生效 / 双通道判定 / 同链留痕 / 默认通道回归） |

### 验收标准

- [x] `tools/pre-execute` 事件落 `intent.jsonl`（参数经脱敏管线，**断言原始参数不入盘**）
- [x] 规则可显式声明输入通道（默认结果通道；意图通道 opt-in，未声明规则行为零变化）
- [x] 意图与结果同链留痕（条目类型可区分），举证时可对照
- [x] 审计零执行权限不变（只读消费意图流 + 落盘；拦截能力在宿主事件位，非审计新增权限）
- [x] 链完整性回归：既有 golden vector 与验链双实现差分测试全绿（哈希输入扩展后须重跑）
- [x] `npm test` 全绿

---

## 八、`sofagent demo` 五分钟戏剧弧（增长杠杆 · 自探索方向提级 · 2026-09-19 用户拍板）

> **形态归属**：`[通道]`（演示编排——串接既有能力的脚本面，零新判定逻辑）｜ L4 卸包（demo 不属生产安装面，独立可卸）

> 📋 **来源**：探索方向表提级（原「增长杠杆而非工程项」）。裁定提级理由：文档再好不如亲眼看一次拦截发生——它是新用户转化漏斗第一环，且工作量小（一个脚本串既有能力，零新引擎改动）。**五幕对应五能力**：约束注入 → 审计拦截（重头戏）→ 快照回滚 → 沉淀（报告生成）→ 进化（结尾点排期）。

### 交付

| 组件 | 能力 |
|------|------|
| **demo 沙箱编排** | 一条命令在 `/tmp` 建 demo 仓库（虚构电商「示例优选」：订单脚本 + 客户数据 + .env），真实文件零接触 |
| **五幕脚本** | ① 进场写规则（生成 workflow.yml + 本体数据）② 注入约束（加载链 system prompt 摘要）③ **拦截重头戏**：模拟 AI 提交三类违规（密钥泄漏 A2 / 越界编辑 A3 / 隐私出界（A1 不碰敏感——规则号以实现时实测命中为准）），hook 真实拦截 commit 拒绝 ④ 回滚（一个「漏网」污染 + 快照复原前后 diff）⑤ 举证导出（HMAC 链验证 + demo-report.md） |
| **`--speed fast`** | 60 秒精简版（跳幕①②直入拦截） |
| **确定性** | 固定种子数据，跑一万次结果一致（可重复演示、CI 可跑） |
| **零依赖** | 纯终端文本输出（无 TUI 库），截图即可传播 |

### 存在形式与分发设计（实现者照此零决策）

> **挂点**：`sofagent-audit` 子命令（`cli-quick.ts` 加 `demo` 分支），**不新增 bin**——不碰「13 bin → 1」收敛叙事，且传播命令与试用命令同体（看完 demo 去掉 `demo` 参数就是真审计，转化零摩擦）。**主分发形态是 npx**：`npx -y -p @sofagent/audit sofagent-audit demo`——一条可粘贴进 README/推文/群聊的命令。demo 是既有能力的串接（审计 hook / 快照 / 验链全为现成 API），零新引擎逻辑——对应「通道形态 L4 可卸」：卸载 demo 不影响任何真实能力。

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/cli/demo.ts` | 新建 | 五幕编排（~400 行）：沙箱构建 → 幕①②生成与注入 → 幕③真实 commit 触发 hook 拦截 → 幕④ snapshot_restore → 幕⑤ HMAC 验链 + demo-report.md 生成 |
| `engine/audit/src/cli/demo-fixtures/` | 新建 | 种子数据（虚构电商订单脚本 / 客户数据 / .env / workflow.yml 模板）——固定种子保证确定性 |
| `engine/audit/src/cli-quick.ts` | 修改 | 加 `demo` 子命令分支（`--speed fast` 透传） |
| `engine/audit/src/cli/demo.test.ts` | 新建 | 三个钉戏单测：① 三类违规必真实命中（断言 exit code 与规则号）② HMAC 链验证必过且报告落盘 ③ 沙箱退出后 `/tmp` 清理干净（防「改引擎忘改 demo」的静默退化） |
| `.github/workflows/verify.yml` | 修改 | CI 定期跑 demo，五幕输出存 artifact（README「看 demo 输出」的链接源） |

### 验收标准

- [x] `sofagent demo` 一条命令跑完五幕，全程真实引擎（零 mock 拦截、零预录输出）
- [x] `npx -y -p @sofagent/audit sofagent-audit demo` 可跑（主分发形态，无需安装）
- [x] 三类违规全部真实命中（A1/A2 commit 被拒；A3 按「能力拐杖」档 WARN 放行，由幕④快照回滚兜底）
- [x] 幕④快照复原前后 diff 可见；幕⑤ HMAC 链验证通过且报告落盘
- [x] 沙箱隔离（断言不触碰用户真实文件）；退出清理干净
- [x] `--speed fast` 60 秒内完成
- [x] 三个钉戏单测在位（拦截必发生 / 链必可验 / 沙箱必清）
- [ ] CI artifact 产出（五幕输出可链接）〔**未实测故不勾选**：job 已就位、action pin 已对账、各 step 命令本地等价复现全过，但从未在真实 GitHub Actions 上执行——artifact 是否存在只能等首次 schedule 触发（2026-09-21 11:00 GMT+8）。在触发前本项达成状态为**未知**，不按「大概率达成」勾选〕
- [x] `npm test` 全绿

---

## 九、存量断链修复（`--legacy` 调用方清零 · v1.5.0 遗留）

> **形态归属**：`[主干]`（判定·修 v1.5.0 遗留生产缺陷，不产新能力）｜ L1 关档
>
> 📋 **来源**：v1.5.0 发版后全仓独立审查（23 视角 fresh-eyes）实证。v1.5.0 退役 `--legacy` 旗标时**只改了退役侧与 acceptance 断言面，未扫生产调用方**——本仓库历史高频事故形态（物理搬迁 / 改名 / 退役残留）。
>
> ⚠️ **为什么必须排在第三、五章之前**：第三章「异常处理总线」与第五章「任务下发二期」都要动 `cron.ts` / 调度面。若不断链先修，新功能会叠加在一个**每 5 分钟 100% 失败、且零告警**的代码路径上。

### 交付

| 组件 | 能力 |
|------|------|
| **退役旗标调用方清零** | orchestrator 侧 `loop --legacy` 已于 v1.5.0 退役收口（显式拒绝 → `process.exit(2)`，fail-closed 正确）；本版清除 daemon 侧两处仍在传该实参的调用点 |
| **防复发机械断言** | 新增 acceptance 断言：扫描全仓（含 `tools/` 与全部 `.sh`），断言**无任何生产调用方仍在传已退役 flag / 命令**，失败即红——「退役 commit 必扫生产调用方」纪律的机械落点 |

**缺陷实证**（发布态 dist，非源码推测）：

| 位置 | 现状 | 性质 |
|---|---|---|
| `engine/orchestrator/src/cli.ts`（`loop` 分支） | `args.includes('--legacy')` → 打印退役公告 + `process.exit(2)` | 退役侧 **fail-closed，正确，保持原样不动** |
| `engine/daemon/src/cron.ts`（scheduler-consume 到期任务分支） | `execFileSync(…, [orchCli, 'loop', '--legacy', '--task', task.prompt])` | **仍在传** |
| `engine/daemon/src/cli.ts`（`scheduler trigger` 手动触发路径） | 同上 | **仍在传** |

- **后果**：每 5 分钟一次的到期任务消费 **100% 拿到 exit 2**；失败被 `output.trim()` 塞进 `data/scheduler/history/`，而 `daemon-health.json` 的 `lastError` 仍为 `null` —— **零告警的活死循环**。
- **掩盖它的测试盲区**：`playbook/acceptance-test.sh` 的 S407 **只断言「CLI 会拒绝 `--legacy`」**，没有任何断言检查「是否仍有生产调用方在传」——测试了守门人的拒绝，没测有没有人在撞门。
- **复现**：`node node_modules/@sofagent/orchestrator/dist/cli.js loop --legacy --task "ping"` → `EXIT=2`。
- **反讽对照**：`engine/daemon/src/cron.ts` 的 subagent 巡检分支注释显示作者刚在同一文件修好「`execFileSync` 静默 exit 0」的同类问题——断链修了一半，另一半在裸奔。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/cron.ts` | 修改 | 移除 `'--legacy'` 实参，改为 `loop --task <prompt>` |
| `engine/daemon/src/cli.ts` | 修改 | 同上 |
| `playbook/acceptance-test.sh` | 修改 | 新增防复发断言——本版真正防复发的一步，比改两行代码更重要 |

> 🔴 **改造保留声明**：两处**只删 `'--legacy'` 字符串实参**，不动 `execFileSync` 调用方式、`cwd`、输出处理、调度记历史等既有逻辑；同文件刚修过路径断链，属已知敏感带。
> 🔴 **调用方清零证明**：`grep -rn -- "'--legacy'" engine/ --include='*.ts' | grep -v '\.test\.ts' | grep -v '__tests__'` 应**只剩退役侧一条命中**。

### 验收标准

- [x] 全仓非测试命中仅剩退役侧（`engine/orchestrator/src/cli.ts`）；两处调用方 `--legacy` 实参已清除
- [x] **新增 acceptance 断言**：扫描全仓（含 `tools/` 与所有 `.sh`），断言无任何生产调用方仍在传已退役 flag / 命令，失败即红
- [x] 到期任务消费路径可用：建一个 `@daily` 任务 → 等 5 分钟 → 调度历史落盘且该次运行**非 exit 2**〔实测口径：历史落在 `data/scheduler/history/<任务 id>/<时间戳>.json`（scheduler.ts 既有结构），非该目录下扁平文件；行为已实测非 exit 2〕
- [x] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.1 的什么 |
|------|------|
| v1.5.2 | 事件总线（audit 数据对外的事件订阅推送复用） |
| v2.0.0 | 事件总线（异常处理总线的事件源基础）+ G12 升级执行器（设备联邦批量运维基础——离线 USB 节点滞留升级合体） |

---

**防膨胀瘦身判据记录（v1.5.1）**

[瘦身判据记录 v1.5.1]

- ①冗余：本版新增 8 个验收场景（S432 发版态自洽 + S433 第九章存量断链残余面 + S434-S439 六章行为锁）逐条比对 `check-version` / `check-test-count` / `check-review-system` 扫描范围——八面均为 dist 直调行为锁（逐场景经反向探针取证），既有门禁不可覆盖 → 无可删场景
- ②重叠：八场景分属发版态 / 存量断链 / 事件触发 / 异常总线 / 理解债务 / OTA+任务推送 / 上行管线 / 输入双通道八个不同交付面，逐条内容复核无 ≥2 场景做同一主题
- ③增长性质：八场景均为本版首版覆盖（v1.5.0 发版态 357 场景中无对应锚点），属新审查面，非「同模式第 N 次重复」
- ④归并配额：新增 8 场景 → 本版真实归并 **23 处**——`acceptance-test.sh` 中 S416 / S418-S439 族逐场景重复的三行样板（探针调用 + `^OK` 判定 + pass・fail 出口）**实际移入**该文件新增的 `probe_assert` 单点实现，调用壳四行→两行，断言本体、✗ 取证输出与计数语义零删减（`git diff` 可查）；归并数 **23 ≥ 新增 8** ✓
- 净增行数：`acceptance-test.sh` **4461**（v1.5.0 发版态）→ **4492**（本次归并后），净增 **+31** ≤ 警戒线余量 **39** ✓。修复前该文件为 **4531**（净增 +70 > 余量 39，越线）——归并是本次消化手段
- 关联清理：S414 块 ✗ 取证标签误写 `S415`（该编号早已归并折入 S414，标签未随归并去向更新）——随本批改回 `S414`，避免排障时按标签找错场景
- 结论：**警戒线未上调**——`acceptance-test.sh` ≤ 4500 保持唯一 SSOT（S426 双值锚不动），避开「只调警戒线不归并 = 不合格」；场景数 **365 不变**，SSOT 各处声明无需联动
