# v1.5.4 开发日志 — 执行模块 · 路由决策链与凭证验证

> 📋 **已排期。** 前置依赖：v1.3.6（model_register / model_switch / 分级降级梯队 + routeReason 可解释）/ v1.4.9（router 伴生 exporter——数据承接面，本版补决策下达面与之构成双向闭环）/ v1.3.7（虚拟 key 凭证边界——Vault 的部分覆盖基础）/ v1.3.5（run_ab_test 多实例基础设施）（全为已发版外部依赖，无块内前置）。2026-09-22 归位再分配新增块内前置：v1.5.1（AI 节点事件挂接的通道复用——事件总线）+ v1.5.2（第五章出口治理——出站管控消费方的前置）+ v1.4.9（治理面注册的身份模式参照——G9 Ed25519，另一消费面）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.3 第一章（模型路由层）与第七章（本地槽位排队与判定分层）——两章同属一条决策链的相邻段，同版交付保依赖闭环（第七章修改第一章新建件 `policy-engine.ts`），内容零改写** · 原依据链：2026-08-29 自 v2.0.0 拆分前移（用户拍板——不依赖企业专属精调模型，无需等 v2.0.0 数据主权窗口）· 2026-09-05 router 网关化拆件两件收编 · 2026-09-08 三件前移 v1.4.9（一体机数据飞轮整链在 v1.5.0 前成型）· 2026-09-18 用户拍板本地槽位排队与判定分层独立成章
> 状态（续）：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.3 第二章（凭证隔离 Vault）与第三章（多实例自验证），内容零改写**
> 状态（三续）：已排期 · **2026-09-22 归位再分配（用户授权主理人裁定）：AI 节点治理接入自 v1.5.9 迁入本版第五章——按主题亲和归位（AI 节点属路由决策链面：治理面注册的编排栈声明、workflow 引用契约、出站管控消费都是「谁执行、怎么判」的路由域问题），章文零改写迁入；出口治理提供方同步迁 [v1.5.2 第五章](./v1.5.2.md)——消费方排在提供方之后，时序自洽** · 原依据链：2026-08-29 自 v2.0.0 拆分前移（用户拍板——三项均不依赖企业专属精调模型） · **本版由旧 v1.5.6、旧 v1.5.7 撤并而来**
> 状态（四续）：已排期 · **2026-09-22 探索方向清仓（用户拍板「逐项裁定、排进 V2.0 前」）：授权面与凭证面对账自探索方向迁入本版第七章——改判理由是版本边界纪律（属 V2.0 前治理面能力，V2.0 后无承接窗口），且本版第三章 Vault 正是两处证据之一，时机亲和；非触发条件已满足**

## 定位


> ⚡ **执行模块 · 路由决策链**——把一条决策链的两段一次交付：**往哪走**（模型路由层——云端规划 / 本地执行 / 管道分层 + 敏感度 fail-closed + routeReason 留痕）与**去不了怎么办、怎么判**（本地槽位排队与判定分层——本地并发槽位信号量 3–5 + 排队超时按合规升级云端 + L0 声明式映射 / L1 embedding 分类 / L2 难例兜底，判定链不占本地主模型槽 + 决策下达面补齐 v1.4.9 承接面的反向通道）。

> ⚡ **执行模块 · 凭证与验证**——执行层的两条安全线：**凭证不出 Vault**（Agent 代码 / 日志 / 审计面均见不到 token 明文，轮换与吊销在位）与**结论要多方表决**（同任务 N 实例并发 + 交叉比对 + 多数表决，分歧超阈值路由 HITL 或升级大模型重跑）。

> 🤝 **执行模块 · AI 节点治理接入（2026-09-22 归位再分配迁入）**——外部 Agent 节点纳入治理面：节点本体用 DeepSeek harness 加 plugin 构建、编排用 LangChain/LangGraph，本仓不做运行时合体，只做治理接入（注册/事件挂接/出站管控消费/引用契约）。分工三句：DeepSeek harness 造节点（生成侧），LangChain/LangGraph 编排（工作流侧），本仓治理节点（判定与审计侧）。

> 🧾 **执行模块 · 授权与凭证对账（自探索方向迁入 · 2026-09-22 用户拍板）**——两处证据到位后做交叉对账：**授权记录**（谁批的 / 批了什么——[v1.5.2 第七章](./v1.5.2.md) 的 mandate 签发面）与**凭证签发记录**（发了什么范围——本版第三章 Vault）分属两处，两者不一致即风险（批了一件小事却发下大范围凭证）。本章把两面对起来：范围不一致即判定为越权风险并留痕。

---

## 一、模型路由层（Harness 路由 · 云端/本地分流）


> **形态归属**：`[主干]`（判定·分流 + fail-closed + routeReason 留痕）｜ L1 关档
>
> 📋 **ROADMAP 分层模型架构**：云端档负责规划推理 → 本地执行档跑多步 workflow → 本地管道档跑固定管道。**引擎层只做模型路由（`model-router.ts` 已有云端两档 / 本地两档插槽 + 判定档 + 拦截出口），通用大模型精调属模型层非开源范围；决策模型为约束层自有底座，随本仓开源。路由层不依赖精调模型——对接现有云端 API + 本地推理服务（Ollama 等）即可交付。**

### 交付

| 组件 | 能力 |
|------|------|
  | **路由策略引擎** | 任务分级路由：规划类→云端强档 / 执行类→本地执行档 / 管道类（模板/格式/字段提取）→本地管道档——按任务类型+数据敏感度双维决策 |
| **敏感度路由** | 数据分级（公开/内部/敏感）自动决定可否走云端——敏感数据强制本地（fail-closed）；**分类输入前置件已前移 [v1.4.9 第八章](../v1.4/v1.4.9.md)（2026-09-08 拍板）**，本版消费分类结果做调用侧分流 |
| **路由可解释** | 每次路由决策附 routeReason（复用 v1.3.6 EndpointProfile + route-policy 模式；v1.4.9 session 承接的路由决策回流经本层全链路可查） |
| **降级梯队** | 云端不可用→本地承接（复用 v1.3.6 分级降级梯队） |
| **本地端点注册** | model-registry 注册本地推理服务端点（Ollama 兼容协议），云端/本地统一注册面 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
  | `engine/orchestrator/src/router/policy-engine.ts` | 新建 | 任务×敏感度双维路由决策 |
  | `engine/orchestrator/src/model-registry.ts` | 修改 | 本地模型端点注册（执行档 / 管道档） |

### 验收标准

- [ ] 三级任务路由可用（规划→云端强档 / 执行→本地执行档 / 管道→本地管道档）
- [ ] 敏感数据强制本地路由（fail-closed，配置错误时拒绝执行）
- [ ] 每次路由附 routeReason
- [ ] 云端不可用自动降级本地
- [ ] `npm test` 全绿

---

## 二、本地槽位排队与判定分层（一体机在线服务面 · 2026-09-18 用户拍板独立成章）


> **形态归属**：`[主干]`（判定·槽位/分层/决策下达）｜ L1 关档
>
> **形态归属**：`[主干]`（判定·槽位/分层/决策下达）｜ L1 关档
>
> 📋 **来源**：排期准入判据 **W1 具名消费方**（企业内网私有化部署的一体机形态——模型全部跑在客户自有硬件上，本地推理槽位是稀缺资源，真实客户诉求）+ **W2 链路缺口**（一体机**在线服务链路**缺这一环；数据飞轮链路 session→分拣→脱敏→数据集→训练→灰度已在 v1.4.9 整链成型）。
>
> **与第一章的关系**：第一章「模型路由层」解决**往哪走**（云端/本地/管道分流决策）；本章解决**去不了怎么办**（本地并发槽位满了如何排队与升级）与**怎么判**（判定链如何不占本地推理槽）。两章同属一条决策链的相邻段，分章为保交付面清晰。
>
> **与 v1.4.9 第七章的关系（双向闭环）**：v1.4.9 已交付 router 伴生 exporter——**数据承接面**（router → 引擎：过站 session / usage / 路由决策推送 + HMAC 验签 + 幂等）。本章补**决策下达面**（引擎 → router）——引擎判定出的去向决策下达给 router 执行，两向合起才是完整回路。
>
> **纪律承继（不自研 router 本体）**：v1.4.9 已拍板「router 基于开源自建、引擎不自研 router 本体、key 发放/吊销/管理归 router」——本章**不碰转发实现、不碰 key 生命周期**，只交付决策产出、槽位管理与决策下达协议。

### 交付

| 组件 | 能力 |
|------|------|
| **本地并发槽位信号量** | 本地推理槽位上限可配（一体机默认 3–5）；超限请求排队（FIFO + 优先级），前台可见排队位置与预估等待；**排队是治理策略不是模型能力**——vLLM 双实例无全局视图，router 无 GPU 槽位概念，引擎是唯一看得到全局的层 |
| **排队超时升级** | 排队等待达阈值 → 按合规路由决策**自动提议转云端**（合规允许出门时）或继续等待（信创全封场景）；升级动作留痕 routeReason，可查可举证 |
| **判定分层编排（L0/L1/L2）** | **默认不用本地主模型做判定**（占槽 / 循环依赖 / 判定延迟放大 / 判错路由错下游放大四问题）：**L0 声明式映射**——请求来自已定义 workflow 的 Agent 节点，直接查节点级 modelPreference 映射（复用 v1.4.8 已交付 schema，零模型成本）→ **L1 语义分类**——自由文本走 embedding 相似度分类（复用本地 RAG 的 embedding 模型，零新增组件，不占本地推理槽）→ **L2 难例兜底**——置信度低于阈值时按配置分流（判定特征走云端强 API / 信创场景本地低峰复核） |
| **L1 判定通道接口（DecisionChannel）** | L1 语义分类的**可插拔判定源协议**（研究收编：System One 决策模型 Jev 启发，2026-09-19 拍板）：`judge(state, questions) → typed answers + 概率` 标准接口，Noul/Choice/Score 三问题原语语义对齐。**通道而非依赖**——引擎定义接口与编排，实现由用户按 TrainChannel 同款纪律自带（云端 provider 走用户 key opt-in，默认关闭；本地部署走开源并行约束解码方案——KV cache 广播 + 单次前向出全部字段，开源 RLCD 小权重可跑一体机，数据主权不破）。判定结果带概率落 decision-log 留痕（类型化输出天然可审计）。**边界**：决策模型不进信任地基（其可被 state 内容操纵的失灵面已知）——只坐确定性规则之后的语义兜底层，24 条审计规则判定面零改动 |
| **决策下达面** | 引擎产出去向决策（目标模型 / 降级路径 / 槽位归属）按标准 schema 下达 router 执行（与 v1.4.9 exporter 同族 schema，反向）；schema 校验 fail-closed，坏格式拒绝下达而非静默放行 |
| **槽位与排队可观测** | MCP tool 查询槽位占用 / 排队深度 / 各请求等待时长（运维可见，数据本地不出门） |
| **三层队列边界** | 明确不重叠的分工：router 层管 per-key 限速与云侧 cooldown（防单 key 打爆配额）· **引擎层管本地 GPU 槽位**（本章核心）· vLLM 实例层管实例内 continuous batching 微调度 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/slot-manager.ts` | 新建 | 本地槽位信号量 + 排队 + 优先级 + 超时升级判定 |
| `engine/orchestrator/src/router/intent-triage.ts` | 新建 | 判定分层编排（L0 映射 → L1 embedding → L2 兜底） |
| `engine/orchestrator/src/router/policy-engine.ts` | 修改 | 第一章新建件——决策产出对接槽位管理与下达面 |
| `engine/orchestrator/src/router/decision-dispatch.ts` | 新建 | 决策下达协议（schema 校验 fail-closed + HMAC 挂链） |
| `engine/train/src/router-exporter.ts` | 修改 | 与承接面 schema 同族对齐（双向 schema 一致性校验） |
| `engine/mcp/src/tools/router-slots.ts` | 新建 | 槽位/排队状态查询 tool |
| `engine/orchestrator/src/__tests__/router-slot.test.ts` | 新建 | 单测（槽位上限/排队顺序/优先级/超时升级/判定链不调主模型） |

### 验收标准

- [ ] 本地槽位上限可配（一体机默认 3–5），超限排队，前台可见排队位置与预估等待
- [ ] 排队超时达阈值按合规决策升级云端（或在全封场景继续等待），升级留痕 routeReason
- [ ] 判定三层链可用：L0 命中零模型调用 / L1 embedding 分类不占本地推理槽 / L2 难例按配置兜底
- [ ] DecisionChannel 接口在位：`judge(state, questions)` 三原语语义可插拔替换实现，云端 provider 默认关闭 opt-in，判定结果带概率落 decision-log
- [ ] L1 语义分类器的置信度阈值有校准依据（声称 X% 准确的阈值须有实测验证支撑，不接受拍脑袋数字——RLCD 校准判据）
- [ ] **判定链不调用本地主模型**（行为锁：判定期间无本地主模型推理请求）
- [ ] 决策下达 schema 校验 fail-closed（坏格式拒绝下达，不静默放行）
- [ ] 与 v1.4.9 承接面构成双向闭环（下达的决策在后续承接过站数据中可对账）
- [ ] 槽位与排队状态可观测（MCP tool 查询）
- [ ] 门禁断言同步：`playbook/acceptance-test.sh` 新增槽位/判定链行为锁场景
- [ ] `npm test` 全绿

---

## 三、凭证隔离 Vault（OMA 启发）


> **形态归属**：`[通道]`（**执行层安全基础设施**——凭证托管/轮换/吊销是通用轮子，换 HashiCorp Vault 价值不受损；**「Agent 代码碰不到 token」的拦截约束留主干**）｜ L1 关档
>
> 📋 **探索方向表收编**：沙箱发 HTTP 请求时动态注入凭证、Agent 代码碰不到 token——执行层安全基础设施，v1.3.7 虚拟 key 凭证边界已部分覆盖，完整 Vault 前移本版。

### 交付

| 组件 | 能力 |
|------|------|
| **Vault 服务** | 凭证托管：Agent 运行时凭证不出 Vault（动态注入请求头，Agent 代码/日志/审计面均见不到 token 明文） |
| **轮换与吊销** | 凭证可轮换（定时/事件触发）+ 单凭证吊销（泄露响应） |
| **凭证范围声明（闭合审计盲区）** | Vault 侧掌握「这条凭证是什么、属于谁、本任务申请了什么范围」——除托管 / 轮换 / 吊销外，**产出一条凭证范围声明**（本任务申请范围 + 实际签发范围 + 比对结果）并挂审计链。**为什么**：审计面的**凭证范围**盲区（凭证是否为本任务的**最小授权**）本质是**证据面缺口**——A22 查「有没有提权」查的是权限本身，而「权限与任务的匹配度」需要 Vault 侧的事实才可观测。**事实不可观测时加规则无用**：只会得到永远弃权或永远放行的假规则 |

### 验收标准

- [ ] Agent 沙箱 HTTP 请求经 Vault 动态注入凭证，代码层拿不到 token
- [ ] 凭证轮换/吊销可用
- [ ] `npm test` 全绿

---

## 四、多实例自验证（并发小模型交叉表决）


> **形态归属**：`[主干]`（判定·多数表决 + 分歧路由 HITL）｜ L1 关档
>
> 📋 **探索方向表 2026-08-24 新增**（方向参考不引数字）：同一任务并发跑多个小模型实例 + 交叉验证多数表决，替代单次大模型调用。方向上与 AB 测试基础设施（v1.3.5 run_ab_test）可复用。

### 交付

| 组件 | 能力 |
|------|------|
| **表决编排** | 同任务 N 实例并发（复用 run_ab_test 多实例基础设施）+ 结果交叉比对 + 多数表决产出 |
| **分歧路由** | 实例分歧超阈值 → 路由人工介入队列（HITL 复用）或升级大模型重跑 |

### 验收标准

- [ ] N 实例并发 + 多数表决可用
- [ ] 分歧超阈值路由 HITL
- [ ] `npm test` 全绿

---


## 五、AI 节点治理接入（外部 Agent 节点纳入治理面 · DeepSeek harness 造节点形态 · 2026-09-22 归位 v1.5.4）

> **形态归属**：`[主干]`（判定·治理面注册与事件挂接）｜ L1 关档
>
> 📋 **重定位依据（2026-09-22 用户拍板）**：原「离线 USB 节点合体」（本地权重 + workflow 烧录 + 审计 + 路由四位一体）废弃——「相当于我建了一个 Agent，但这个已经比较老了」。新形态：**未来这个 Agent 直接用 DeepSeek harness 快速搭建**——① 编排：主要使用 LangChain 和 LangGraph；② 实现：整个 Agent 的实现基于 DeepSeek harness 加上对应的 plugin 来构建 AI 节点。本仓对它只做**治理接入**。
>
> ⚠️ **马鞍边界（分工三句）**：DeepSeek harness 造节点（生成侧能力），LangChain/LangGraph 做编排（工作流侧能力），本仓治理节点（判定与审计侧能力）——三者互补不重叠，正是「判定只判断、执行归代码」在部署形态上的落法。本仓**不做节点本体、不做运行时合体**。

### 交付

| 组件 | 能力 |
|------|------|
| **治理面注册** | 外部 Agent 节点登记为被治理对象：节点身份（Ed25519，对齐 G9 设备注册模式——节点是「软件公民」，注册面轻于设备面：无心跳/派单，只登记身份 + 编排栈声明（LangChain/LangGraph 等）+ 事件端点 + 能力面声明） |
| **事件挂接** | 节点运行事件（工具调用 / 出站请求 / 任务完成）进审计链——复用 v1.5.1 事件总线，规则判定逻辑零改动（只扩输入面，对齐 v1.5.1 双通道同款纪律） |
| **出站管控消费** | 节点出站流量过 [v1.5.2 第五章](./v1.5.2.md) 白名单裁决——**本版第五章（AI 节点）是 v1.5.2 第五章（出口治理）的第一个实装消费方**（AI 节点 = 网络出口治理的首个治理对象，出站裁决与审计挂接是节点纳入治理面的最小闭环） |
| **workflow 引用契约** | 编排层可把已注册外部节点引用为 workflow 节点类型（引用契约 + 治理状态前置检查：未注册节点可被引用但不入治理面，引用时告警留痕）——对齐 v1.4.8 节点级 modelPreference 先例，引用契约不绑定具体编排栈 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/ai-node-registry.ts` | 新建 | 治理面注册（身份验签 + 编排栈声明 + 事件端点） |
| `engine/orchestrator/src/__tests__/ai-node-registry.test.ts` | 新建 | 注册验签 / 未注册引用告警 / 事件进链单测 |

### 验收标准

- [ ] 治理面注册可用（外部节点登记 + Ed25519 身份验签，fail-closed）
- [ ] 节点事件进审计链（复用事件总线，规则判定逻辑零改动）
- [ ] 节点出站过白名单裁决（与 [v1.5.2 第五章](./v1.5.2.md) 对账，裁决留痕）
- [ ] workflow 可引用已注册外部节点类型（引用契约不绑编排栈实现）
- [ ] `npm test` 全绿

---

## 六、PHILOSOPHY × FDE 叙事章（万物皆可被 FDE 文档重构 · R4 · 2026-09-22 用户拍板）

> **形态归属**：`[非功能]`（文档治理·重构——[v1.5.1 第九章](./v1.5.1.md#九断言承载面普查万物皆可被-fde-文档重构--r1-前置--2026-09-22-用户拍板)升级清单消费）｜ —
>
> 📋 PHILOSOPHY.md 补「FDEing 能力底座」叙事章：万物皆可被 FDE 的理念展开（任何业务对象、流程、节点都能走一遍「梳理 → 判定 → 交付 → 养护」）、与既有「从 FDE 到 FDEing」叙事（PHILOSOPHY.md:156 既有锚）的衔接。**时机亲和**：本版第五章（AI 节点治理接入）是「外部节点纳入 FDE 治理面」的第一实装——理念章引用实装证据。

### 交付

| 组件 | 能力 |
|------|------|
| **FDEing 叙事章** | PHILOSOPHY.md 新增叙事章：万物皆可被 FDE 的理念基础（AI 不是效率工具是硅基员工 → 岗位随人走、能力随交付物留 → 万物皆可被 FDE 的组织含义）+ 与既有 FDEing 段落的衔接（不重复，补「理念 → 打法 → 场景」链条） |
| **三因子位置声明** | 叙事章内声明三因子各归其位（FDEing = 能力底座 / S1M = 本体 / harness = 治理环境），与 [FDE/README.md](../../../FDE/README.md)「自研 S1M，FDE 是打法」强调段同口径 |
| **升级清单销账** | R1 普查清单 PHILOSOPHY 条目逐条销账 |

### 验收标准

- [ ] 叙事章在位且与既有 FDEing 段零重复（新章补「理念 → 打法 → 场景」链，不复述既有内容）
- [ ] 三因子口径与 FDE/README.md 强调段一致（无第二口径）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms`

---

## 七、授权面与凭证面对账（两处证据交叉核验 · 自探索方向迁入 · 2026-09-22 用户拍板）


> **形态归属**：`[主干]`（判定·授权范围与凭证范围一致性）｜ L1 关档
>
> 📋 **为什么现在排**（2026-09-22 用户拍板 · 版本边界纪律）：本项原为探索方向，触发条件写的是「凭证范围声明与门禁三态记录均落地后」。改判理由是**版本边界纪律**——本项属 V2.0 前治理面能力，V2.0 后无承接窗口；而**两处前置证据在本版内全部就位**（本版第三章 Vault 出凭证范围声明 + [v1.5.2 第七章](./v1.5.2.md) mandate 出授权三元素），时机上「等」已无意义。
>
> 📋 **为什么必须对账**（两处记录各自的盲区）：授权记录只说「批了什么」，凭证记录只说「发了什么」——**单看任一侧都自洽**。风险恰恰在两侧之间：批了一件小事却发下大范围凭证，两张表分开看都「合法」。对账是把两个自洽面之间的**缝**变成一条可判定的命题。

### 交付

| 组件 | 能力 |
|------|------|
| **范围一致性判定** | 以授权三元素（范围 / 时效 / 审批人）与凭证范围声明逐项比对：凭证范围 ⊄ 授权范围 / 凭证时效 > 授权时效 / 无对应授权记录 ⇒ **越权风险**判定 |
| **证据取用不改写** | 两侧记录均为**只读取用**（本版不改 mandate 与 Vault 的落盘 schema），对账结论另立记录并挂 HMAC 链 |
| **错位即留痕** | 不一致即进 decision-log 并挂链（谁在何时用哪个授权发了哪个范围凭证）——对齐本版第五章出站裁决留痕形态，不另立通道 |
| **对账面可关** | 默认关（L1）——关闭后不产生对账判定与记录，两侧行为与今日一致，不留半开状态 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/mandate-credential-reconcile.ts` | 新建 | 授权记录 × 凭证签发记录交叉对账判定 |
| `engine/audit/src/decision/` | 修改 | 对账结论事件类型注册（挂链留痕） |
| `engine/audit/src/__tests__/mandate-credential-reconcile.test.ts` | 新建 | 单测（范围超集 / 时效超期 / 缺授权三类错位 + 关闭后行为一致） |

### 验收标准

- [ ] 范围 / 时效 / 缺授权三类错位均可判定为越权风险
- [ ] 错位进 decision-log 且挂 HMAC 链可举证
- [ ] 两侧记录只读取用（对账不改 mandate / Vault 落盘 schema）
- [ ] 关闭（L1）后行为与今日一致（无半开状态）
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.4 的什么 |
|------|------|
| v2.0.0 | 模型路由层——外部 AI 节点直接复用路由底座（节点编排栈接入路由插槽）；Vault——节点的凭证托管；多实例表决——节点的轻量验证通道 |
| v2.0.0（对外叙事） | **节点形态口径**——「DeepSeek harness 造节点 + 本仓治理节点」的分工形态在本版实装完成，宣告版引用实装证据而非规划语言 |

> 🔗 **拆分依据（2026-08-29 拍板）**：v2.0.0 原含四件，其中模型路由层（ROADMAP 明示「路由层可提前到 v2.x 做（不依赖精调模型）」）、Vault（v1.3.7 虚拟 key 已部分覆盖）、多实例自验证（run_ab_test 基础设施现成）三件的依赖全部就绪——前移当时的 v1.5.3，经地基期拆版落于本版与 [v1.5.4](./v1.5.4.md) / [v1.5.5](./v1.5.5.md)，v2.0.0 收窄为数据主权核心（离线 USB 节点合体）。
> 📋 **前移收编说明（2026-09-08）**：原「router 网关化拆件」三件（敏感度实时分类器 / 权重灰度发布 AB / session 全量记录与蒸馏对接）已整体前移 [v1.4.9 第七/八/九章](../v1.4/v1.4.9.md)——一体机数据飞轮（session 承接→分拣→脱敏→数据集→训练→灰度上线）在 v1.5.0 前成型，用户拍板。本版保留模型路由层（调用侧分流消费 v1.4.9 分类结果）。
