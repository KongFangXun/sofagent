# v1.5.2 开发日志 — 审计模块 · 开放与治理面（约束导出与证据链外部可验 + 双规则统一 + 规则自测 schema + A24 规则 + 能力面使用率治理 + 审计结论失效语义）

> 📋 **已排期。** 前置依赖：v1.5.0（治理 KPI 面板——SMB 场景 KPI 复用）+ v1.5.1（事件总线——数据处理触发源）+ v1.3.9（Agentic Browser 多模态链路）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-29（v1.5.0 系列规划首版 · 探索方向「SMB 场景审计扩展」+「UI 层审计」收编，用户拍板落盘）· 2026-08-29 二次收编：探索方向「MCP 暴露 audit 数据对外」迁入第三章（用户拍板前移 v1.5.x）· **2026-08-31 版本号顺延：原 v1.5.3 本版改号 v1.5.2；「FDE 进场记忆目录工程化」前移 [v1.4.5 第八章](../v1.4/v1.4.5.md#八fde-进场记忆目录工程化自-v153-第四章前移--2026-08-31-用户拍板)（用户拍板）；「运行时 should-run 判定链」保留本版第四章** · 2026-09-01 收编：**约束导出通道**（AIR §2.5 缺口①——24 条规则导出为机器可读训练信号）迁入第八章（用户拍板排期 v1.5.2——与第三章 audit 数据对外同属「审计模块对外开放面」主题）· 2026-09-15 研究收编（OpenAI Agents API 公测 + codex 仓源码 + Google Agentic Skills 综述，用户拍板）：**审计规则自测 schema**（Codex execpolicy 正负样例加载断言）第十章 + **A24 交付物落点审计规则**（+1 条成 25）第十一章 + **技能库健康度治理**（SkillOps 五维）第十二章 · 2026-09-16 收编：**审计结论失效语义**（Codex Guardian 启发）第十三章（用户拍板）

## 定位

> 🔍 **审计模块 · 开放与治理面**——审计对外可消费（规则导出 + 证据链外部可验 + audit 数据对外查询）与对内治理（双规则引擎统一 + 规则自测 schema + 能力面使用率治理 + 结论失效语义）。原「SMB 场景审计扩展」「UI 层审计前置评估」两章按[章数上限判据](../../ROADMAP.md#排期准入判据)移出本版，退回[探索方向表](../../ROADMAP.md#探索方向)（2026-09-16 用户拍板）。

---

## 一、SMB 场景审计扩展（→ 已移出本版 · 章数上限收敛）

> 📋 **2026-09-16 移出本版**：按[排期准入判据](../../ROADMAP.md#排期准入判据)「每版交付章上限 10 章」，本章权重 **W5**（内部灵感——指认不出具名消费方：非 G 编号、非模型层缺口、非数据飞轮链路缺口），超限时优先移出，退回[探索方向表](../../ROADMAP.md#探索方向)。原内容要点留存：数据产物审计规则（数值勾稽/来源可溯/口径一致三规则）+ `DecisionKind` 加 `DATA_PRODUCT`（⚠️ `@public` 契约，落地须同步 `tools/check/public-api-baseline.json`）+ SMB onboarding 模板 + 治理 KPI 复用（v1.5.0 面板）。

---

## 二、UI 层审计前置评估（→ 已移出本版 · 章数上限收敛）

> 📋 **2026-09-16 移出本版**：按同一判据，本章权重 **W5**（**自述「不实做」**——产出是可行性报告而非交付能力，实做候选已定 v2.x），超限时优先移出，退回[探索方向表](../../ROADMAP.md#探索方向)。

---

## 三、MCP audit 数据对外（自探索方向迁入 · 2026-08-29 用户拍板）

> 📋 **探索方向表迁入**（DataFlow 启发）：v1.3.6 已排期 ontology/workflow 暴露面；audit 数据对外暴露（读 history.jsonl / decision-log 的 MCP 接口）作为 meta-harness（v1.3.9）的延伸能力——外部 SIEM / 商业平台可订阅审计流。

### 交付

| 组件 | 能力 |
|------|------|
| **audit 查询 tool** | MCP `audit_query`：按时间/规则/exitCode 过滤读 history.jsonl（只读，HMAC 链零破坏）+ decision-log 因果链查询（消费 v1.4.4 causedBy 字段） |
| **订阅推送** | 审计事件流对外订阅（复用 webhook 三态推送通道，对齐 v1.5.2 事件总线出站面） |

### 验收标准

- [ ] audit_query 只读查询可用（时间/规则/exitCode 过滤）
- [ ] 只读验证：查询前后 history.jsonl 字节级一致
- [ ] `npm test` 全绿

---

## 四、FDE 进场记忆目录工程化（→ 已前移 v1.4.5 第八章）

> 📋 **2026-08-31 拍板前移**：FDE 进场记忆目录工程化（session 目录 + session-stop 自动捕获 + 跨 session 恢复）已整体前移 [v1.4.5 第八章](../v1.4/v1.4.5.md#八fde-进场记忆目录工程化自-v153-第四章前移--2026-08-31-用户拍板)（SSOT）——FDE 六引擎 v1.4.2 已交付，FDE Harness 层主题延续到 v1.4.5 自然收口，纯工程化无硬依赖。

---

## 五、运行时 should-run 判定链（自探索方向迁入 · 2026-08-29 用户拍板）

> 📋 **探索方向表迁入**（Loop Engineering 控制面启发 · 2026-08-19 新增）：每轮开工前统一问一遍的判定链：健康 → 人审 gate → 证据等待 → 专注等待 → 配额，全部通过才执行——断路器（v1.3.7）两态的丰富形态。

### 交付

| 组件 | 能力 |
|------|------|
| **五问判定链** | 节点执行前统一五问（健康/人审 gate/证据等待/专注等待/配额），任一不通过则挂起等待——与 v1.5.1 事件总线的派发前置检查集成 |
| **挂起原因可查** | 判定不过时挂起原因落 decision-log（对齐「只提示不阻断」——挂起非失败，条件满足自动恢复） |

### 验收标准

- [ ] 五问判定链集成事件总线派发前置
- [ ] 挂起原因可查（decision-log 记录）
- [ ] 条件满足自动恢复执行
- [ ] `npm test` 全绿

---

## 六、OWASP Agentic Top 10 补条（自探索方向收编 · 2026-08-31 用户拍板）

> 📋 **探索方向表收编**（Microsoft AGT 启发）：当前 4/10（v1.3.7 补 ASI08+ASI10，v1.3.9 补 ASI01+ASI04）——审计场景扩展版顺手补 1-2 条 ASI 规则，按每版补条节奏走向 10/10 全覆盖。具体补哪几条以本版开发时 OWASP Agentic Top 10 与规则引擎现状差距为准（候选：ASI03 提示注入防护 / ASI06 记忆投毒检测——发版前评估定案）。

### 验收标准

- [ ] 补 1-2 条 ASI 规则（24+3 条基线上增量，含测试）
- [ ] ROADMAP 探索方向行更新覆盖率进度
- [ ] `npm test` 全绿

---

## 七、doctor 修复闭环（Omarchy refresh 模式启发 · 自探索方向收编 · 2026-08-31 用户拍板）

> 📋 **探索方向表收编**：「检查→一键修复」的自然演进——doctor 已有 repairHint 提示（v1.4.3 补 Ontology 完整性检查后检查项已稳定），本版补 refresh 模式「备份+一键重置到默认」。对标 `omarchy refresh`（自动备份时间戳 + 拷贝默认 + 按需重启）。

### 交付

| 组件 | 能力 |
|------|------|
| **doctor refresh** | 检查发现可修复项 → `sofagent doctor --refresh`：自动备份当前配置（时间戳目录）→ 重置到默认 → 报告前后 diff——修复动作全程审计留痕 |
| **备份保留** | refresh 备份目录保留最近 N 份（默认 3），可回滚 |
| **旧包退役收尾** | 移除 `@sofagent/evolve` 的 workspace 引用（v1.4.8 已 npm deprecate 并指向 `@sofagent/evolve`）——**breaking 收尾，随本项「备份+重置」语义一并收**：移除前先经 refresh 备份，异常可回滚。承接关系双向在位（v1.4.8 ⑩-2 声明「将在 v1.5.2 移除」，本章为落地点） |

### 验收标准

- [ ] `doctor --refresh` 一键修复可用（备份 → 重置 → diff 报告）
- [ ] 修复动作记 decision-log（谁在何时重置了什么）
- [ ] 备份可回滚（最近 3 份自动保留）
- [ ] `@sofagent/evolve` workspace 引用已移除（全仓零命中），`@sofagent/evolve` 为唯一入口（v1.4.8 跨版承诺收口）
- [ ] `npm test` 全绿

---

## 八、约束导出与证据链外部可验（24 条规则 → 机器可读 + 密码学可验）

> 📋 **AIR §2.5 四缺口最后一块**（2026-09-01 拍板排期本版）：②workflow 容器 ③ontology_import ④ruleset 市场均已交付，仅剩 ①约束导出——24 条审计规则硬编码在 TS 里，外部（训练管线）无法机器可读消费。与第三章「audit 数据对外」同属审计模块对外开放面：**数据面（history 可查）+ 规则面（ruleset 可导出）**。v1.2.9 ruleset JSON 格式已定义机器可读格式，导出通道半成熟；v1.4.x 后训模块已交付，训练侧消费方就位。
>
> 📋 **2026-09-16 并入验签工具**（用户拍板）：与「规则可被外部消费」同属约束对外可消费面——审计证据链目前只能由 sofagent 自身校验，作为举证材料的可信度依赖维护方中立性；独立验签器把可信根从「信任 sofagent」换成「信任密码学」，且三仓共用（平台层 PR 审阅举证 / 模型层训练信号溯源 / 开源用户合规举证）。**边界**：只读校验，无写权限、不加载引擎代码。

### 交付

| 组件 | 能力 |
|------|------|
| **规则导出 MCP tool** | MCP `ruleset_export`：24 条默认规则 + 已加载扩展规则导出为标准 JSON（与 v1.2.9 `--ruleset-path` 加载格式同构——导出格式即加载格式，双向可逆） |
| **规则语义元数据** | 每条规则附训练消费元数据（rule_id + 检测意图 + 违规样例 + 严重级别）——训练管线可直接把「约束」构建为 reward 信号或 SFT 语料 |
| **版本与指纹** | 导出物带规则集版本号 + 内容指纹（对齐 v1.4.1 可复现指纹——训练任务引用规则集版本可追溯） |
| **审计留痕** | 每次导出进 audit history（谁在何时导出了哪个版本的规则集） |
| **独立验签工具** | 单文件零依赖的哈希链校验器（`tools/verify/verify-chain.mjs`，裸 node 可跑）——输入 `history.jsonl` + 链头指纹，独立重算并校验 HMAC 链完整性；第三方（审计师 / 监管 / 法院技术顾问）无需安装 sofagent 即可举证。**输出三态**：链完整 / 某条断裂（给出条目号与期望值）/ 链头不匹配 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/mcp/src/tools/ruleset-export.ts` | 新建 | `ruleset_export` tool（默认规则 + 扩展规则 → JSON + 元数据） |
| `engine/audit/src/rules/export-metadata.ts` | 新建 | 规则训练消费元数据（意图/样例/级别，随规则定义维护） |
| `engine/audit/src/__tests__/ruleset-export.test.ts` | 新建 | 单测（导出格式与加载格式双向可逆 / 元数据完整性 / 版本指纹） |
| `tools/verify/verify-chain.mjs` | 新建 | 独立验签器（单文件零依赖，裸 node 可跑；含 `--selftest` 合成回归） |
| `tools/verify/README.md` | 新建 | 验签器说明（输入格式 / 三态输出 / 第三方举证用法） |

### 验收标准

- [ ] `ruleset_export` 导出 24 条默认规则 + 已加载扩展（JSON 格式）
- [ ] 导出物可被 `--ruleset-path` 重新加载（双向可逆验证）
- [ ] 每条规则含训练消费元数据（rule_id/意图/样例/级别）
- [ ] 导出物带版本号 + 内容指纹
- [ ] 独立验签器三态可复现：链完整 / 断链（给出条目号与期望值）/ 链头不匹配
- [ ] 验签器零依赖——不 import 任何引擎包，`node tools/verify/verify-chain.mjs --selftest` 自检通过
- [ ] 篡改注入验证：改动任一 history 条目 ⇒ 验签器报断链并定位条目号（防「验签器恒真」假绿）
- [ ] 每次导出审计留痕
- [ ] `npm test` 全绿

## 九、双规则引擎统一（2026-09-08 排入 · 消除 ARCHITECTURE 悬空承诺）

> 📋 **背景**：`engine/rules/`（tool-level 规则，3 条）与 `engine/audit/src/rules/`（git-diff 规则，24 条）历史上并行维护，均含 secret-leak 检测，存在行为不一致风险。v1.3.0 已部分收敛（正则模式共享至 `@sofagent/core`），但**规则引擎仍是两套**，ARCHITECTURE 记载「统一排期见 ROADMAP」而 ROADMAP 此前无此单元格——属悬空承诺，本版补上落点。
>
> 统一方式：以 `ruleType` 字段在两种触发模式下复用同一套规则定义（tool-level 在调用前拦截、audit 在 commit 后审计），收敛为单一规则引擎 + 两种执行时机。
>
> **纪律外延（2026-09-08 补）**：判定规则**只有一个事实源**。企业侧若另建判定点（自建模型网关/代理做流量侧 PII 过滤与敏感度分流），必须集成本引擎的同一套规则与检测器（v1.4.9 第八章检测器插槽即可嵌入组件），**不得另起一套**——每多一处独立实现，就是一次口径分裂风险。

### 交付

| 组件 | 能力 |
|------|------|
| **单一规则引擎** | 两套规则定义合并为一套（含 `ruleType` 触发时机标记），tool-level 与 git-diff 两种执行模式共用 |
| **行为一致性回归** | 统一前后对同一输入的判定结果必须一致（回归锁，防统一过程引入行为漂移） |
| **ARCHITECTURE 同步** | 「已知技术债：双规则系统重叠」段改写为已排期指向本版 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/rules/` | 修改 | 吸收 tool-level 规则，统一规则定义与 `ruleType` |
| `engine/rules/` | 修改 | 收敛为触发时机适配层（不再独立维护规则正则） |
| `engine/audit/src/__tests__/rules-unified.test.ts` | 新建 | 单测（两套触发下判定一致 / secret-leak 行为不漂移） |

### 验收标准

- [ ] tool-level 与 git-diff 两种触发共用同一套规则定义
- [ ] 统一前后 secret-leak 等重叠规则判定结果一致（回归锁）
- [ ] ARCHITECTURE 技术债段改写为明确排期指向（无悬空承诺）
- [ ] `npm test` 全绿

---

## 十、审计规则自测 schema（Codex execpolicy 启发 · 2026-09-15 用户拍板排期本版）

> 📋 **研究收编**（OpenAI Agents API 公测 + codex 仓源码核验，2026-09-15）：[codex-rs/execpolicy](https://github.com/openai/codex/blob/main/codex-rs/execpolicy/README.md) 把「规则正负成对自检」做成**引擎级机制**——每条 prefix_rule 强制携带 `match`（必须命中的样例）与 `not_match`（必须不命中的样例）字段，**加载时即校验**（官方原话 "think of them as unit tests"）：规则写错（锚串消失/误命中）立刻爆，不靠人记。本仓「假门禁五形态」反模式（正向探针空转/锚串消失静默跳过/门禁块吞退出码）一直靠纪律级 checklist 防（回归清单「自检/守卫正负成对」条目）——本章把该纪律升级为 schema 强制。与第九章天然衔接：**双规则统一后的单一规则定义正是携带正负样例的最佳载体**（一套定义，两种触发时机，样例同源）。

### 交付

| 组件 | 能力 |
|------|------|
| **规则 schema 扩展** | 规则定义新增 `examples: { match: string[], not_match: string[] }` 必填字段——每条规则强制声明「必须命中」与「必须不命中」的输入样例（静态定义类规则可豁免，豁免须显式标记） |
| **加载时断言** | 规则集加载时逐条跑样例断言：match 样例未命中 / not_match 样例误命中 → 拒绝加载并报规则 id（fail-closed，对齐 execpolicy "validated at load time"） |
| **FAIL 报文替代建议** | forbidden/FAIL 级规则报文强制携带修复建议字段（对齐 execpolicy「forbidden 必附 justification 替代建议」——如 "Use `jj` instead of `git`"）；b-fix 轮次可直接消费，降低审查-修复循环轮数 |
| **多规则裁决语义** | 同一输入命中多条规则时取最严（forbidden > warn > allow，对齐 execpolicy "strictest severity across all matches"）——裁决语义显式化并配单测 |
| **导出联动** | 第八章 `ruleset_export` 的训练消费元数据（违规样例字段）与本章 `examples` 同源——导出即正负样例对，AIR 训练侧消费零转换（SFT 对/reward 判据直接用） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/rules/` | 修改 | 规则定义加 `examples` 字段（24 条默认规则逐条补正负样例） |
| `engine/audit/src/rule-loader.ts`（或等效加载入口） | 修改 | 加载时样例断言 + fail-closed 拒载 |
| `engine/audit/src/__tests__/rule-selftest.test.ts` | 新建 | 单测（正样例命中/负样例不命中/锚串删除后拒载/最严裁决） |

### 验收标准

- [ ] 24 条默认规则每条含 match/not_match 样例（或显式豁免标记）
- [ ] 加载时断言生效：注入「锚串被删」的规则集 → 拒绝加载非 0 退出（故障注入实证）
- [ ] FAIL 级报文携带替代建议
- [ ] 多规则命中取最严有单测锁
- [ ] `ruleset_export` 导出物含正负样例（与第八章联动验收）
- [ ] `npm test` 全绿

---

## 十一、A24 交付物落点审计规则（+1 条 · 2026-09-15 用户拍板）

> 📋 **研究收编**（Codex `/workspace/outputs` 不可变产物模式）：Codex 沙箱里交付物只能写 outputs 目录——「交付物落盘零号铁律」（统一落约定目录、不擅自新建文件夹）从用户纪律升级为**产品机制**（A 类节点级可判——路径前缀匹配，事前拦截）。归属 A 类 8 条同族：违规载荷就在 tool 调用入参（路径），可精确归因。

### 交付

| 组件 | 能力 |
|------|------|
| **A24 交付物落点** | diff 中新增的交付物类文件（报告/文档/产物）落点须在约定目录白名单内（如 `~/Desktop` 交付面或 workflow 声明的 outputs 目录），越界即 FAIL 并附替代路径建议（对齐第十章 FAIL 报文纪律） |
| **白名单声明面** | workflow/任务声明允许的交付目录（默认空 = 全不检，opt-in——对齐 G10 授权白名单模式：默认空 fail-closed，声明即启用） |
| **正负样例出生即带** | A24 作为第十章自测 schema 落地后的**首个新规则**，出生即携带 match/not_match 样例（新规则新纪律，不留旧账） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/rules/rule-a24-deliverable-path.ts` | 新建 | 交付物落点检测（白名单路径前缀匹配） |
| `engine/audit/src/rules/assemble.ts` + `index.ts` | 修改 | A24 注册进规则集（全量 24→25 条） |
| `engine/audit/README.md` + `docs/SECURITY.md` | 修改 | 规则表同步（⚠️ SECURITY 是文档级 SSOT——「24 条」全仓落点随本规则升级为「25 条」，涉及 ARCHITECTURE/README/门禁断言同批改，改动清单开发时核定） |

### 验收标准

- [ ] A24 规则落地（白名单 opt-in，默认空不检，声明后越界 FAIL）
- [ ] 出生即带 match/not_match 正负样例（与第十章联动）
- [ ] FAIL 报文附替代路径建议
- [ ] 全仓「24 条」计数落点同批升级「25 条」（SECURITY SSOT + ARCHITECTURE + README + 门禁断言，逐处核对清单随开发回写）
- [ ] `npm test` 全绿

---

## 十二、能力面使用率治理（技能 SkillOps 五维 + 工具使用率遥测 · 2026-09-15 / 09-16 用户拍板排期本版）

> 📋 **研究收编**（Google《Towards a Systems Foundation for Agentic Skills》arXiv:2608.29596）：技能库存储借鉴 OS 虚拟内存思想，SkillOps 提出五维技能库健康度治理——**剪除低价值技能、偿还「技能技术债」**。本仓现状：SKILL 库只进不出（instinct→skill 单向晋升），无退役机制——技能熵只增不减。综述同时实证「技能效用相当脆弱」（SkillsInWild 3.4 万技能测量）+「弱模型进化技能对强模型负迁移」（WikiSkill 4B 技能把 Gemini-3.5-Flash 从 50.5% 拉到 18.1%——v1.4.8 modelScope 门控已防）。与第八章 ruleset_export / 第十章自测 schema 同属「规则与技能的治理面」主题。
>
> 📋 **2026-09-16 并入工具使用率遥测**（用户拍板）：同一治理面从技能扩到工具——104 个 MCP tools 目前**无调用遥测**，静态引用密度只能证明文档完备、不能证明被调用（本仓 104 tools 盘点实测：`orchestrator/workflows`、`task/plans` 等子系统零运行时痕迹，但无法据此判断其余工具的冷热）。退役与加码决策需要真实调用数据，故补工具面使用率。

### 交付

| 组件 | 能力 |
|------|------|
| **五维健康度指标** | 按综述 SkillOps 框架落五维（候选：使用频率 / eval 分数趋势 / modelScope 覆盖面 / 依赖健康（引用的工具/接口是否还在）/ 年龄与版本漂移）——`skill-health report` 输出，Dashboard 可见 |
| **低价值技能退役** | 五维连续 N 期低于阈值 → 退役候选清单（进 FDE 人工确认，不自动删——对齐「只提示不阻断」）；退役技能归档不物理删（可回滚） |
| **技能技术债台账** | 每技能记「上次 eval 验证时间 / 当前分数 vs 历史最优 / 待还债清单」——与 v1.4.5 skill-impact 台账同源扩展 |
| **工具使用率遥测** | 每次 MCP tool 调用追加一行到 `~/.sofagent/data/tool-usage.jsonl`（**tool 名 + 时间戳 + 会话标识**，不含调用参数与内容——隐私边界）；**纯本地存储、无任何上报路径**（对齐数据主权铁律「记忆/日志/决策记录永不离开本地」）；遥测默认开、配置可关 |
| **工具使用率盘点** | `sofagent stats --tools` 输出三视图：被调用 / 零调用 / 频次分布——为工具退役与加码提供数据依据（与五维使用频率同源消费） |
| **与 evolve 门控联动** | dream-cycle 晋升面照常，本项补**出口**：晋升进来就有健康度追踪，降级/退役有据 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/evolve/src/skill-health.ts` | 新建 | 五维指标计算 + 退役候选生成 |
| `engine/evolve/src/cli.ts` | 修改 | `skill-health report` 子命令 |
| `engine/evolve/src/__tests__/skill-health.test.ts` | 新建 | 单测（五维计算 / 阈值判定 / 退役候选 / 归档可回滚） |
| `engine/mcp/src/usage-tracker.ts` | 新建 | 调用埋点与 jsonl 追加（本地路径 + 无上报；含关闭开关） |
| `engine/mcp/src/server.ts` | 修改 | tool 调用分派点接 usage-tracker |
| `engine/mcp/src/__tests__/usage-tracker.test.ts` | 新建 | 单测（落盘字段完整性 / 不含内容字段 / 开关生效 / 无网络路径断言） |
| `engine/core/src/cli/stats.ts` | 修改 | `--tools` 三视图输出 |

### 验收标准

- [ ] 五维健康度指标可计算（`skill-health report` 输出）
- [ ] 退役候选清单生成（人工确认制，非自动删）
- [ ] 退役技能归档可回滚
- [ ] 技能技术债台账落盘（上次验证/分数/待还债）
- [ ] tool 调用落 `tool-usage.jsonl`（tool 名 + 时间戳 + 会话标识；**断言无参数/内容字段**）
- [ ] `sofagent stats --tools` 三视图可用（被调用 / 零调用 / 频次分布）
- [ ] 遥测开关生效（关闭后零写入）；代码面无网络上报路径（对齐数据主权铁律）
- [ ] `npm test` 全绿

---

## 十三、审计结论失效语义（Codex Guardian 启发 · 2026-09-16 用户拍板排期本版）

> 📋 **研究收编**（codex-rs [guardian/](https://github.com/openai/codex/tree/main/codex-rs/core/src/guardian) 源码核验）：Codex 给审查结论定义了一套**失效条件**（`GuardianReviewReason`）——`FreshRequired`/`StaleScore`/`AuthorizationChanged`/`IncompatibleCompaction`/`ElevatedRisk`/`ScoringFailure`。本仓现状：审计结论（history.jsonl）与 HITL 授权一经产生即视为永久有效，下游消费方无法判断「这条结论还能不能信」。与第十章自测 schema 同属「审计模块自我完善」主题：第十章保证规则**加载时**是对的，本章保证结论**消费时**还是对的。
>
> ⚠️ **马鞍边界（四问过筛已过，边界在此）**：只吸收「失效原因词汇表」与判定逻辑——属共享账本层豁免（词汇表不是执行器）。**不吸收** Guardian 的 reviewer pool / 同步审查会话 / 预热机制（那是审查的马场：换 fresh-eyes 或任何外部审查器，本失效语义价值不受损）。

### 交付

| 组件 | 能力 |
|------|------|
| **失效原因词汇表** | 审计结论 schema 新增 `invalidationReason` 字段（枚举：授权变更/压缩不兼容/风险升级/分数过期/需新鲜结论），对齐 Guardian 六原因语义取子集 |
| **失效判定钩子** | 三个事件触发失效标记：①授权或白名单配置变更（AuthorizationChanged）②压缩/摘要事件发生（IncompatibleCompaction——对应 L2 工具输出总结、加载链压缩）③后续轮次风险升级（ElevatedRisk——同一对象后续被更严规则命中） |
| **下游消费语义** | 带失效标记的结论不作为后续判定的输入（fresh-eyes 审查输入、HITL 免检依据、治理 KPI 统计均须过滤）——「过期结论不当新证据用」 |
| **留痕不删除** | 失效结论保留原文 + 失效标记（对齐 HMAC 链只追加不删改——失效是标记不是抹除） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/decision-schema.ts` | 修改 | 结论 schema 加 `invalidationReason` 枚举字段（可选，缺省=有效） |
| `engine/audit/src/invalidation.ts` | 新建 | 三钩子：授权变更/压缩事件/风险升级 → 标记既有结论失效 |
| `engine/audit/src/__tests__/invalidation.test.ts` | 新建 | 单测（三触发路径/失效结论被下游过滤/原文保留） |

### 验收标准

- [ ] 失效原因词汇表落地（schema 字段 + 枚举值）
- [ ] 三触发钩子生效：授权变更/压缩事件/风险升级 → 既有结论标记失效
- [ ] 失效结论被下游过滤（fresh-eyes 输入/HITL 依据/KPI 统计）
- [ ] 失效结论原文保留（HMAC 链不破坏）
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.2 的什么 |
|------|------|
| v2.0.0 | UI 层审计可行性报告（若结论为 go 则排实做）；OWASP 覆盖率进度（10/10 路线延续）；约束导出通道（AIR 训练信号消费——模型层后训练引用规则集版本）；双规则统一后的单一引擎（v2.x 规则市场与跨设备审计的单一事实源） |
