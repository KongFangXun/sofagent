# v1.5.2 开发日志 — 审计模块 · 对外开放面（MCP audit 数据对外 + 约束导出与证据链外部可验）

> 📋 **已排期。** 前置依赖：v1.5.1（事件总线——审计事件订阅推送复用其出站面）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.2 第三章（MCP audit 数据对外）与第八章（约束导出与证据链外部可验）——两章同属「审计模块对外开放面」，内容零改写** · 原依据链：2026-08-29 二次收编（探索方向「MCP 暴露 audit 数据对外」迁入，用户拍板前移 v1.5.x）· 2026-08-31 版本号顺延（原 v1.5.3 改号 v1.5.2，前序版本号撤销引起）· 2026-09-01 收编约束导出通道（规划文档 §2.5 四缺口最后一块——24 条规则导出为机器可读训练信号）· 2026-09-16 并入独立验签工具与 decision 链双覆盖（用户拍板）· **2026-09-16 章数上限收敛**：SMB 场景审计扩展 / UI 层审计前置评估 / OWASP 补条 / 能力面使用率治理四章后移（按[排期准入判据](../../ROADMAP.md#排期准入判据)——超限章优先后移，不退回探索方向表；用户拍板），经地基期拆版落于 [v1.5.11](./v1.5.11.md) / [v1.5.12](./v1.5.12.md)

## 定位

> 🔍 **审计模块 · 对外开放面**——审计对外可消费的两面：**数据面**（history 可查——MCP `audit_query` 只读 + 事件订阅推送）与**规则面**（ruleset 可导出——机器可读 JSON，导出格式即加载格式双向可逆），外加**举证面**（独立验签工具——第三方无需安装 sofagent 即可验 HMAC 链）。本版是审计模块「从内部审计走向对外可举证」的收口。

---

## 一、SMB 场景审计扩展（→ 已后移 v1.5.11）

> 📋 **2026-09-16 后移**（按[排期准入判据](../../ROADMAP.md#排期准入判据)「每版交付章上限 10 章」，超限章**优先后移到后续版本**（不退回探索方向表）。完整规格见 [v1.5.11 第一章](./v1.5.11.md)。

---

## 二、UI 层审计前置评估（→ 已后移 v1.5.11）

> 📋 **2026-09-16 后移**：同一判据。完整规格见 [v1.5.11 第二章](./v1.5.11.md)。

---

## 三、MCP audit 数据对外（自探索方向迁入 · 2026-08-29 用户拍板）

> **形态归属**：`[主干]`（对外接口面·举证能力）｜ L1 关档（对外暴露面应默认关，安全面 opt-in）
>
> 📋 **探索方向表迁入**（DataFlow 启发）：v1.3.6 已排期 ontology/workflow 暴露面；audit 数据对外暴露（读 history.jsonl / decision-log 的 MCP 接口）作为 meta-harness（v1.3.9）的延伸能力——外部 SIEM / 商业平台可订阅审计流。

### 交付

| 组件 | 能力 |
|------|------|
| **audit 查询 tool** | MCP `audit_query`：按时间/规则/exitCode 过滤读 history.jsonl（只读，HMAC 链零破坏）+ decision-log 因果链查询（消费 v1.4.4 causedBy 字段） |
| **订阅推送** | 审计事件流对外订阅（复用 webhook 三态推送通道，对齐 v1.5.1 事件总线出站面） |

### 验收标准

- [ ] audit_query 只读查询可用（时间/规则/exitCode 过滤）
- [ ] 只读验证：查询前后 history.jsonl 字节级一致
- [ ] `npm test` 全绿

---

## 四、FDE 进场记忆目录工程化（→ 已前移 v1.4.5 第八章）

> 📋 **2026-08-31 拍板前移**：FDE 进场记忆目录工程化（session 目录 + session-stop 自动捕获 + 跨 session 恢复）已整体前移 [v1.4.5 第八章](../v1.4/v1.4.5.md#八fde-进场记忆目录工程化自原-v153-第四章前移--2026-08-31-用户拍板)（SSOT）——FDE 六引擎 v1.4.2 已交付，FDE Harness 层主题延续到 v1.4.5 自然收口，纯工程化无硬依赖。

---

## 五、OWASP Agentic Top 10 补条（→ 已后移 v1.5.11）

> 📋 **2026-09-16 后移**：**归属修正**——本章原文自述「**审计场景扩展版**顺手补 1-2 条 ASI 规则」，本就属于场景扩展主题。完整规格见 [v1.5.11 第三章](./v1.5.11.md)。

---

## 六、约束导出与证据链外部可验（24 条规则 → 机器可读 + 密码学可验）

> **形态归属**：`[主干]`（举证面·独立验签工具）｜ L1 关档（导出面可关，链本体不受影响）
>
> 📋 **约束导出通道缺口（规划文档 §2.5 四缺口最后一块）**（2026-09-01 拍板排期本版）：②workflow 容器 ③ontology_import ④ruleset 市场均已交付，仅剩 ①约束导出——24 条审计规则硬编码在 TS 里，外部（训练管线）无法机器可读消费。与第一章「audit 数据对外」同属审计模块对外开放面：**数据面（history 可查）+ 规则面（ruleset 可导出）**。v1.2.9 ruleset JSON 格式已定义机器可读格式，导出通道半成熟；v1.4.x 后训模块已交付，训练侧消费方就位。
>
> 📋 **2026-09-16 并入验签工具**（用户拍板）：与「规则可被外部消费」同属约束对外可消费面——审计证据链目前只能由 sofagent 自身校验，作为举证材料的可信度依赖维护方中立性；独立验签器把可信根从「信任 sofagent」换成「信任密码学」，且三仓共用（平台层 PR 审阅举证 / 模型层训练信号溯源 / 开源用户合规举证）。**边界**：只读校验，无写权限、不加载引擎代码。
>
> 📋 **2026-09-16 验签器扩双链**（用户拍板）：现状是审计数据面**双链只写一验**——history.jsonl 有 `--verify-chain` + doctor 校验通道，decision-log.jsonl（决策因果链，十万条级）100% 带链字段写入但零消费校验（`checkDecisionChainDetailed` 已实现并经 public-api 导出，生产消费方为零）。验签器出生即双链：同一次校验覆盖 history + decision 双文件，顺带补齐 decision 链的机内校验入口（`sofagent audit --verify-chain` 同批扩展）。

### 交付

| 组件 | 能力 |
|------|------|
| **规则导出 MCP tool** | MCP `ruleset_export`：24 条默认规则 + 已加载扩展规则导出为标准 JSON（与 v1.2.9 `--ruleset-path` 加载格式同构——导出格式即加载格式，双向可逆） |
| **规则语义元数据** | 每条规则附训练消费元数据（rule_id + 检测意图 + 违规样例 + 严重级别）——训练管线可直接把「约束」构建为 reward 信号或 SFT 语料 |
| **版本与指纹** | 导出物带规则集版本号 + 内容指纹（对齐 v1.4.1 可复现指纹——训练任务引用规则集版本可追溯） |
| **审计留痕** | 每次导出进 audit history（谁在何时导出了哪个版本的规则集） |
| **独立验签工具** | 单文件零依赖的哈希链校验器（`tools/verify/verify-chain.mjs`，裸 node 可跑）——输入 `history.jsonl` + 链头指纹，独立重算并校验 HMAC 链完整性；第三方（审计师 / 监管 / 法院技术顾问）无需安装 sofagent 即可举证。**输出三态**：链完整 / 某条断裂（给出条目号与期望值）/ 链头不匹配 |
| **decision 链双覆盖** | 验签器同批支持 `decision-log.jsonl`（决策因果链）——同一工具校验双链；机内 `sofagent audit --verify-chain` 同步扩展 decision 链校验（消费既有 `checkDecisionChainDetailed`，消除「只写不校验」通道缺口） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/mcp/src/tools/ruleset-export.ts` | 新建 | `ruleset_export` tool（默认规则 + 扩展规则 → JSON + 元数据） |
| `engine/audit/src/rules/export-metadata.ts` | 新建 | 规则训练消费元数据（意图/样例/级别，随规则定义维护） |
| `engine/audit/src/__tests__/ruleset-export.test.ts` | 新建 | 单测（导出格式与加载格式双向可逆 / 元数据完整性 / 版本指纹） |
| `tools/verify/verify-chain.mjs` | 新建 | 独立验签器（单文件零依赖，裸 node 可跑；含 `--selftest` 合成回归） |
| `tools/verify/README.md` | 新建 | 验签器说明（输入格式 / 三态输出 / 第三方举证用法） |

### 验收标准

- [ ] `ruleset_export` 导出 24 条默认规则 + 已加载扩展（JSON 格式）
- [ ] 导出物可被 `--ruleset-path` 重新加载（双向可逆验证）
- [ ] 每条规则含训练消费元数据（rule_id/意图/样例/级别）
- [ ] 导出物带版本号 + 内容指纹
- [ ] 独立验签器三态可复现：链完整 / 断链（给出条目号与期望值）/ 链头不匹配
- [ ] 验签器零依赖——不 import 任何引擎包，`node tools/verify/verify-chain.mjs --selftest` 自检通过
- [ ] 篡改注入验证：改动任一 history 条目 ⇒ 验签器报断链并定位条目号（防「验签器恒真」假绿）
- [ ] decision 链双覆盖：验签器 + `--verify-chain` 均校验 `decision-log.jsonl`，篡改注入同批验证（双链各测一例）
- [ ] 每次导出审计留痕
- [ ] `npm test` 全绿

---

## 七、能力面使用率治理（→ 已后移 v1.5.12）

> 📋 **2026-09-16 后移**：治理面（技能 + 工具双面），且工具使用率遥测需一段数据积累期才能产出可用结论，后置更合理。完整规格见 [v1.5.12 第一章](./v1.5.12.md)。

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.2 的什么 |
|------|------|
| v2.0.0 | 约束导出通道——训练管线侧消费训练信号（模型层后训练引用规则集版本） |

> 📋 **章数上限收敛的后移记录（2026-09-16）**：SMB 场景审计扩展 / UI 层审计前置评估 / OWASP Agentic Top 10 补条 / 能力面使用率治理四章按[排期准入判据](../../ROADMAP.md#排期准入判据)移出本版——经地基期拆版，前两章与 OWASP 落 [v1.5.11](./v1.5.11.md)，能力面使用率治理落 [v1.5.12](./v1.5.12.md)。
