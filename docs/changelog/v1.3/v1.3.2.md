# v1.3.2 开发日志 — 多设备协同第一刀：Agent 身份码 + 跨设备审计聚合

> ⚠️ **规划中，尚未实现。** 以下内容为版本目标规划，不代表当前可用能力。

> 状态：规划中 · 孔放勋 · 2026-08-01
> 前置依赖：v1.2.5（Agent 身份码轻量版已在企业 SubAgent 落地）；v1.1.8 安全联邦（加密配对 + 联邦查询）
> 设计文档：[多设备协同设计](../guides/multi-device-collab.md)（开发前由架构师产出）

## 定位

v1.3.2 是**多设备协同系列的第一刀**——先给每个 Agent 一个独立、可验证的身份，再让跨设备的审计轨迹可聚合。这两项是后续 L2 团队协作协议（v1.3.3）和组织能力市场（v1.3.4）的**地基**：没有身份就没有"谁在协作"，没有聚合就没有"协作得怎么样"。

> **版本瘦身来源**：原 v1.2.5 规划的多设备协同七大项过重，本系列拆分如下——v1.3.2 身份码+审计聚合 → v1.3.3 L2 协作协议 → v1.3.4 L3 能力市场 → v1.4.0 权限体系+代理网关 → v2.x 归因引擎。

## P0：Agent 独立身份码 + KYA 完整版

- **身份码签发**：每个 Agent 启动时签发加密签名身份（Ed25519 keypair），绑定委托人 / 约束版本 / 责任声明
- **KYA（Know Your Agent）完整版**：v1.2.5 是轻量版（企业 SubAgent 注册即带身份），v1.3.2 升级为完整版——身份可跨设备验证、可审计撤销
- **身份与审计绑定**：每次审计记录带签发者身份，`[sofagent]` 签名升级为"身份 + 引擎"双签名

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/identity.ts` | 新建 | 身份码签发/验证（Ed25519） |
| `engine/core/src/identity-store.ts` | 新建 | 身份注册表（本地 data/ 存储） |
| `engine/audit/src/audit-record.ts` | 修改 | 审计记录增加 agentId 字段 |
| `engine/mcp/src/tools/agent-identity.ts` | 新建 | MCP `agent_identity` tool（查自己/查他人身份） |

## P1：跨设备审计轨迹聚合

- **审计轨迹带身份跨设备合并**：设备 A 的审计记录 + 设备 B 的审计记录，按 agentId 合并为一条完整轨迹
- **复用安全联邦通道**：加密配对 + 联邦查询（v1.1.8 已有），审计轨迹走联邦合并而非新造通道
- **冲突裁决**：同一 Agent 在两台设备的审计记录，以 HMAC 验签 + trust 优先级裁决（复用联邦 merge 策略）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/federation/audit-merge.ts` | 新建 | 跨设备审计轨迹聚合 |
| `engine/daemon/src/inspectors/audit-trail.ts` | 新建 | 审计轨迹聚合巡检（@daily） |
| `engine/mcp/src/tools/audit-trail.ts` | 新建 | MCP `audit_trail` tool（按 agentId 查完整轨迹） |

## 验收标准

- [ ] 同一 Agent 在两台设备上的审计记录可按 agentId 合并为完整轨迹
- [ ] 身份签名伪造的审计记录被拒绝（验签失败 → 丢弃 + WARN）
- [ ] 联邦通道加密：轨迹聚合走 AES-256-GCM，不出现明文跨设备传输
- [ ] `agent_identity` / `audit_trail` 两个 MCP tool 可用

> 详细开发 Prompt：`~/Desktop/sofagent-dev-prompts/fde-multidevice-v1.3.2-prompt.md`

## 与后续版本的依赖

| 后续版本 | 依赖 v1.3.2 的什么 |
|---------|-------------------|
| v1.3.3 L2 团队协作协议 | 身份码（协作主体要有身份） |
| v1.3.4 L3 组织能力市场 | 身份码 + L2 协议（市场里交易的是有身份的 Agent 能力） |
| v1.4.0 权限体系 + 代理网关 | 身份码 + 审计轨迹（权限判定依据） |
