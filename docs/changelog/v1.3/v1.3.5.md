# v1.3.5 开发日志 — 自进化与运维闭环（MCP 覆盖度审计缺口补全）

> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 前置依赖：v1.3.1（Agent 身份码 = A/B 实验归属溯源；审计日志 = 破坏性操作留痕）+ v1.3.3（Refine 循环引擎 = A/B 实验的消费方）

> 状态：已排期 · 2026-08-03
> 来源：MCP 覆盖度审计 13 个缺口 tool 的最后四项（P1+P2），版本归属用户已拍板

## 定位

v1.3.5 落位 v1.3.x 弹性预留区，主题是**自进化与运维闭环**——MCP server（engine/mcp）已注册 23 tools + 4 resources，但自进化链路（ab-test）与运维链路（daemon 快照）在引擎侧早已就绪，唯独缺 MCP 入口：Agent 看得见「审计结果」，却摸不到「进化的方向盘」和「回退的安全绳」。本版补上这 4 个 tool，让自进化与运维两条闭环在 MCP 侧完整可用。

## P0：自进化闭环（ab-test ×2）

`engine/ab-test` 已有 `runABTest` / `decidePromotion` 实现，缺的只是 MCP 暴露：

| tool | 所属包 | 一句话 |
|------|--------|--------|
| `run_ab_test` | ab-test | 发起 A/B 对比实验（current vs candidate + golden-set），打通自进化闭环 |
| `promote_ab` | ab-test | A/B 晋升决策执行，**强制人审** |

## P1：运维闭环（daemon 快照 ×2）

daemon 快照机制（`snapshot.ts`）已落地，补上查询 + 恢复两个 MCP 入口：

| tool | 所属包 | 一句话 |
|------|--------|--------|
| `snapshot_list` | daemon | 列出审计快照时间线（只读查询） |
| `snapshot_restore` | daemon | 恢复到指定快照，**强制人审**（破坏性操作） |

## 🔴 P0 安全：vitest critical 漏洞链升级（fresh-eyes run-02 发现 · 2026-08-14）

**npm audit 实测**：8 漏洞（4 moderate + 3 high + **1 critical**）。

| 项 | 详情 |
|----|------|
| critical | GHSA-5xrq-8626-4rwp（CVSS 9.8）——Vitest UI server 任意文件读取 + 执行，影响 vitest <3.2.6 |
| 中招包 | audit `^1.6.0` / mcp `^1.6.0` / rules `^3.0.0`——全部 <3.2.6 |
| 连带链 | esbuild（GHSA-67mh-4wv8-2f99）→ vite ≤6.4.2 → vite-node → vitest，`npm audit fix --force` 统一升 vitest@4.1.10 解决 |
| 风险面 | Vitest UI server 仅开发时监听，CI/headless 跑测试不启 UI——**实际暴露面有限，但依赖树合规扫描（企业采购/安全审计）会被 critical 直接标红**，必须升 |
| breaking | vitest 1.x/3.x → 4.x 是 breaking change，升级后全量 `npm test` 验证（当前 2235 测试） |

**交付**：三包 vitest 统一升 `^4.1.10` + 全量测试跑绿 + `npm audit` 漏洞归零（或残余项逐条评估记录）。

## 安全边界（用户已背书）

1. 破坏性操作（`promote_ab` / `snapshot_restore`）MCP 可触发但**强制 human-confirm 语义**——无人审确认不执行，全程审计留痕
2. daemon 类 tool 只做**只读查询**（`snapshot_list` 只读）——start/stop 等进程控制永不 MCP 化（防恶意 Agent 滥用）

## 验收标准

- [ ] `run_ab_test` 可发起 A/B 对比实验（current vs candidate + golden-set）并返回实验结果
- [ ] `promote_ab` 仅在人工确认后执行晋升决策，未确认时挂起不执行
- [ ] `snapshot_list` 返回审计快照时间线（只读，不改变任何状态）
- [ ] `snapshot_restore` 仅在人工确认后执行恢复，未确认时挂起不执行
- [ ] 4 个 MCP tool 全部走审计留痕（D1-D5 / 运行时审计日志）
- [ ] **SKILL.md 登记本版 MCP tools（`run_ab_test` / `promote_ab` / `snapshot_list` / `snapshot_restore`）—— check-version.sh 工具数校验通过**
- [ ] `npm test` 全绿

---

## 🔒 双闸验证（P2 · 运维闭环增强）

> 📖 **来源**：v1.3.0 运行时审计只做「执行前 gate」，双闸补「执行后副作用复查」。

### 定位

v1.3.0 的 wrapToolCall middleware 是「执行前 gate」（拦截危险工具调用）。双闸验证补「执行后副作用复查」——工具执行完后，复查实际产生的副作用是否符合预期（如：写文件操作是否真的只写了预期文件、没误改其他文件）。前后双闸 = 执行前拦截 + 执行后复查。

### 交付（P2）

- wrapToolCall middleware 新增 `postToolCall` 钩子（v1.3.0 只有 pre）
- 副作用复查规则集：文件写入范围复查 / git 操作影响范围复查 / 网络外联目标复查
- 复查异常 → 写运行时审计 WARN + 中断后续工具调用

### 验收标准

- [ ] `postToolCall` 钩子存在并在每次工具执行后触发
- [ ] 至少 3 条副作用复查规则（文件 / git / 网络）
- [ ] 复查异常写审计 WARN 并中断

---

## 😴 Agent 疲劳度检测（P2 · 运维闭环增强）

### 定位

Agent 跑长循环时上下文窗口会被污染、决策质量会衰减——「疲劳」不是 metaphor，是可观测的信号（重复调用同一失败工具 / 忽略前文约束 / 输出质量下降）。本项给运维闭环补一个健康指标。

### 交付（P2）

- 疲劳信号采集：同一工具连续失败次数 / 上下文窗口占用率 / 输出与历史相似度（查重）
- 疲劳度评分 → 写 daemon-health.json（@hourly 采集）
- 疲劳度超阈值 → daemon 触发自动 `/compact`（v1.2.7 已有）或建议重启

### 验收标准

- [ ] 3 个疲劳信号可采集（连续失败 / 窗口占用 / 输出相似度）
- [ ] 疲劳度评分写 daemon-health.json
- [ ] 超阈值触发 compact 或重启建议

---

## 🪜 分级降级梯队（P2 · 韧性设计 · OpenFDE 启发）

> 📖 **来源**：OpenFDE 分级降级——console → TUI → spec 逐级降级，workflow never stops。

### 定位

sofagent 当前的降级是二态（全功能 / crash）。本项补分级降级梯队——当某个能力不可用时，逐级降级而非整盘崩溃：full（全部规则 + LLM 审计）→ rules-only（纯 git-diff 规则，LLM 不可用时）→ minimal（只跑 A1-A11 核心安全规则）→ safe-stop（全部规则都跑不动时，安全停止不破坏）。workflow never stops = 总有一级能兜底。

### 交付（P2）

- 降级触发器：LLM 不可用 / 审计引擎超时 / daemon 崩溃
- 三级降级：full → rules-only → minimal → safe-stop
- 每次降级写审计日志（记录降级原因 + 降级到哪级 + 持续多久）

### 验收标准

- [ ] 3 个降级触发器可检测（LLM / 审计引擎 / daemon）
- [ ] 三级降级状态机正确（full → rules-only → minimal → safe-stop）
- [ ] 每次降级写审计日志

---

## 📝 decisions.jsonl 完整版（P2 · 意图审计扩展 · OpenFDE 启发）

> 📖 **来源**：OpenFDE decisions.jsonl——每次判断落 `{kind, moment, why, spec_ref}`。v1.3.0 已交付 emitDecision（决策审计 MVP），本项扩展为完整版。

### 定位

v1.3.0 的 `emitDecision()` 只在关键节点（HITL / 审计 FAIL）触发。decisions.jsonl 完整版补「判断时刻日志」——Agent 每次做选择（走哪条路 / 选哪个方案 / 为什么跳过某步）都落 `{kind, moment, why, spec_ref}`，让意图审计从「只记关键决策」扩展到「记全部判断时刻」。

### 交付（P2）

- 扩展 emitDecision 触发点：从关键节点扩展到 Agent 的每次方案选择
- `kind` 分类枚举（route / select / skip / retry / escalate）
- decisions.jsonl 查询接口：按 kind / 按 moment / 按 agentId 回溯

### 验收标准

- [ ] emitDecision 触发点扩展到方案选择时刻
- [ ] kind 枚举至少 5 种（route/select/skip/retry/escalate）
- [ ] decisions.jsonl 可按 kind / moment / agentId 查询

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.3.5 的什么 |
|---------|-------------------|
| v1.3.7 权限体系 + v1.3.8 代理网关 | 晋升/恢复类破坏性操作的风险分级与权限判定（场景驱动） |
