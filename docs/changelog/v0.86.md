# v0.86 开发日志

> 2026-06 · 运行时加固 + 设计约束补充
>
> v0.85 完成了定位校准（治理层→纪律层），v0.86 在这个地基上补两件事：
> 一是 engine.md 运行时逻辑改进（读写型分流），二是 7 条学习笔记的设计约束落地。
>
> 来源：2026-06-22 Get 笔记两篇（Anthropic Cloud Code 协作结构 + 产品负责人 Cat 法则）+ 2026-06-23 Logan Kilpatrick 文章。

---

## 概述

v0.85 是「不动代码改定位」的版本。v0.86 反过来——定位稳了，回头补运行时和设计。

核心问题来自 Anthropic Cloud Code 的实践数据：**不是所有复杂任务都该拆成多 Agent**。

- 写为主任务（代码生成/修改为主）：单 Agent 配优质上下文反而更稳——交接冲突代价 > 并行收益
- 读为主任务（查多处代码、分析多份数据）：多 Agent 并行正常拆解

当前 engine.md A4 ComplexityScorer 只按复杂度分数分模型（Pro / Flash），不分任务读写类型。v0.86 补这个判断。

```
v0.85 定位校准完成
   ↓
回头看 engine.md 编排逻辑
   ↓
发现：所有 🔴 复杂任务无条件走 ao compose 拆解
   ↓
但 Anthropic 实践证明：写型复杂不该拆
   ↓
v0.86：ComplexityScorer 加读写型判断
```

---

## 一、交付物清单

| # | 交付物 | 类别 | 涉及文件 | 状态 |
|:--:|------|:----:|---------|:----:|
| 1 | **读写型复杂任务分流** | 运行时逻辑 | sofagent/engine.md A4 段 | 待开发 |
| 2 | **7 条学习笔记文档补充** | 设计约束 | ROADMAP + ARCHITECTURE + daemon-design.md | ✅ 已提交 `672f995` |

---

## 二、读写型复杂任务分流（P0）

### 背景

Anthropic Cloud Code 团队的实践结论：

> 读为主任务（查资料、扫代码）适合多 Agent 并行；写为主任务（代码生成/修改为主）单 Agent 配优质上下文反而更稳。交接冲突代价 > 并行收益。

当前 engine.md A4 ComplexityScorer 的路由逻辑：

```
总分 ≥ 0.5 → Pro 模型（ao compose 完整编排）
总分 < 0.5 → Flash 模型 或 默认编排（降级）
```

问题：一个"重写整个模块的 API"的写型复杂任务，分数 ≥ 0.5，会走 ao compose 拆成多个子 Agent——但子 Agent 之间会产生文件修改冲突。

### 改动方案

在 A4 ComplexityScorer 公式后、路由决策前，加任务类型判断：

```
任务类型判断（在 ComplexityScorer 分数计算后）：

写型复杂（代码生成/修改为主）：
  → 不拆子 Agent
  → 走单 Agent 高质量上下文模式（Pro 模型 + 完整上下文）
  → 理由：交接冲突代价 > 并行收益

读型复杂（查多处代码/分析多份数据）：
  → 正常拆解（ao compose 完整编排）
  → 理由：并行收益 > 协调成本

判断依据：任务描述中"修改/重写/重构/迁移"类动词占比 > 60% → 写型
```

### 验收标准

- engine.md A4 段新增任务类型判断逻辑
- 判断逻辑不替代 ComplexityScorer 的分数计算，是在分数之后追加一层
- 写型复杂任务的 Agent 行为：不调用 ao compose，直接走单 Agent 模式
- 新增 `task_type` 字段写入 `{SOFAGENT_DATA}/orchestrator/` 供后续参考

### 约束

- 只改 engine.md A4 段，不碰其他段落
- 不改 ComplexityScorer 公式本身（子任务数×0.4 + 跨领域数×0.3 + 预估 token×0.2 + 含代码/报告×0.1）
- 不改 rules.md 优先级（模型偏好始终最高）

---

## 三、7 条学习笔记文档补充（已完成）

> 来源：2026-06-22 Get 笔记两篇 + 2026-06-23 Logan Kilpatrick 文章分析。

### 笔记来源

1. **多 Agent 协作系统深度解析**（Anthropic Cloud Code 四种协作结构 + 避坑指南）
2. **AI 时代产品管理革命**（Anthropic Cloud Code 产品负责人 Cat 的 7 大核心法则）
3. **Logan Kilpatrick 文章**（Google DeepMind，2026-06-23——模型会吞掉外部脚手架）

### 7 条落地汇总

| # | 建议 | 落点 | 改了什么 |
|:--:|------|------|---------|
| 1 | 协同协议补冲突裁定 | ROADMAP v2.x | 两个 Agent 产出分歧时，路由器或独立评判 Agent 裁定 |
| 2 | 协议补交接三字段 | ROADMAP v2.x | 交接必填：改了什么 / 为什么改 / 影响范围 |
| 3 | 路由器补并发上限 | ROADMAP v2.x | 单设备 ≤ min(4, CPU−2)，超过排队 |
| 4 | 读写型复杂分流 | ROADMAP v1.x + v0.86 开发 | 写型不拆子 Agent（本日志 §二） |
| 5 | 审计误报率约束 | ROADMAP v0.9 | discipline-check.sh 误报率 <10%，否则缩小范围 |
| 6 | 加载链瘦身审查 | ARCHITECTURE | 每次大模型发布后检查铁律是否已被模型内置能力覆盖 |
| 7 | daemon 从提醒到行动 | daemon-design.md §十一 | 远期方向：提醒→熔断→同步→行动，daemon.json 预留 action 字段 |

### 提交记录

```
672f995 docs: 7 条学习笔记落地——v2.x 协同补 3 条 + 审计误报率约束 + 加载链瘦身 + daemon 远期方向
```

---

## 四、Logan Kilpatrick 文章分析（背景记录）

> 2026-06-23，Google DeepMind 开发者关系负责人 Logan Kilpatrick 公开判断：**"每一行外部脚手架，都是对模型无能的妥协。这种红利顶多还有 12 个月。"**

### 对 sofagent 的影响判断

| sofagent 功能 | 会被模型吞吗 | 理由 |
|---|:---:|---|
| 编排引擎 | ✅ 最先死 | 模型原生 tool calling + planning 越来越强 |
| Hook 机制 | ✅ 平台标准化 | OpenClaw/WorkBuddy 会做原生 Hook API |
| 断路器 / 步数闸 | ✅ 模型自带 | 后训练会内置"知道什么时候该停" |
| 纪律层核心 | ⚠️ 防线在松动 | 能力 ≠ 纪律。模型能先读但不保证每次都先读 |
| 提交时审计 | ❌ 吞不掉 | 让模型审计自己 = 利益冲突 |
| 跨平台通用性 | ❌ 吞不掉 | Google 优化 Google，OpenAI 优化 OpenAI |
| 本地数据治理 | ❌ 吞不掉 | 模型厂盈利模式是数据上云，反商业逻辑 |

### 结论

v0.85 的方向判断和 Logan 的预判高度一致：
- 编排引擎降级 ✅（最先被吞）
- 纪律层定位 ✅（防线还在）
- 提交时审计 ✅（吞不掉）
- 设备化方向 ✅（模型厂三个"不做"）

---

## 五、不做什么

| 不做的事 | 为什么 |
|---------|--------|
| 不改 ComplexityScorer 公式 | 公式本身没问题，问题在公式后缺任务类型判断 |
| 不碰 SKILL.md 铁律 | 铁律是契约层，v0.86 只改 engine.md A4 |
| 不做 v0.9 的 45 组实验 | 实验执行是 v0.9 的事，v0.86 只补运行时逻辑 |
| 不新增脚本 | 读写型分流是 engine.md prompt 级逻辑，不需要新脚本 |

---

## 六、验证

### P0 读写型分流

- engine.md A4 段有任务类型判断逻辑
- 写型复杂任务不触发 ao compose
- 读型复杂任务正常走 ComplexityScorer → ao compose

### 文档一致性

- ROADMAP v1.x 有读写型分流条目 ✅（已提交）
- ROADMAP v2.x 有冲突裁定 + 交接字段 + 并发上限 ✅（已提交）
- ROADMAP v0.9 有误报率约束 ✅（已提交）
- ARCHITECTURE 有加载链瘦身审查 ✅（已提交）
- daemon-design.md 有远期方向 ✅（已提交）

---

## 七、文件改动清单

| 文件 | 改了什么 | 状态 |
|------|---------|:----:|
| sofagent/engine.md | A4 段加读写型任务类型判断 | 待开发 |
| ROADMAP.md | v2.x 补 3 条 + v0.9 补误报率 + v1.x 加读写型条目 | ✅ |
| ARCHITECTURE.md | 加载链瘦身审查段 | ✅ |
| docs/daemon-design.md | §十一 远期方向 | ✅ |

---

> v0.86 是小版本——补运行时逻辑 + 落地设计约束。大动作（实验执行、审计 MVP）留给 v0.9。
