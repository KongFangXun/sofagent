# v1.4.5 开发日志 — 训练引擎 · 服务与持续（推理服务 + 持续后训练 + 合规扫描）

> 📋 **已排期。** 前置依赖：v1.4.1-1.4.4（训练引擎地基/数据评估/运行需求/信号部署）+ v1.3.6（model_register / model_switch）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-10（v1.4.1-1.4.5 拆分第五版 · 生命周期补全）

> 🔗 **训练 Agent 化（2026-08-23 拍板，详见 v1.4.1）**：本版持续后训练的触发/回退由训练 Agent 决策（数据回流评估 → 触发/回退保护），train serve/合规扫描走引擎（确定性）。

## 定位

> 🚀 **训练引擎 · 服务与持续**——补齐训练引擎生命周期的**最后两环 + 一道合规闸**：训练完的模型要**跑起来**（推理服务）、企业数据要**持续回流**（飞轮闭环）、训练集要过**合规扫描**（合规红线代码化）。四版（v1.4.1-1.4.4）把"训得出来"做完了，本版把"用起来 + 持续转"补上——**训练引擎到这一版才真正闭环**。

---

## 一、训练推理服务（train serve · 部署后跑起来）

> 📋 **2026-08-10 补充（生命周期审查新发现）**：v1.4.4 只到"部署（model_register / model_switch）"——但**部署后谁拉起推理服务？** `model_register` 注册了权重，却没有"启动 vLLM / Ollama 服务并健康检查"的环节。缺了它，权重是"注册了但没跑起来"的死文件。

### 交付

| 组件 | 能力 |
|------|------|
| **train serve** | 启动推理服务：从权重目录拉起 vLLM / Ollama / OpenAI 兼容端点（复用 v1.3.2 `client_type: 'openai-compatible'` 插槽） |
| **健康检查** | 服务就绪探测（/health）+ 启动失败自动重试（对齐 v1.3.1 错误处理指数退避） |
| **服务生命周期** | 启动 / 停止 / 重启 / 状态查询——与 model_switch 联动（切模型时自动拉起新权重服务） |
| **serve 审计** | 每次服务启停记 `train_serve` 事件（谁启的、哪个模型、哪个节点在用） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-serve.ts` | 新建 | 推理服务生命周期（拉起 vLLM/Ollama + 健康检查） |
| `engine/mcp/src/tools/train-serve.ts` | 新建 | MCP `train_serve` tool（启动/停止/状态） |
| `engine/audit/src/writer.ts` | 修改 | 增加 `train_serve` 事件类型 |
| `engine/orchestrator/src/__tests__/train-serve.test.ts` | 新建 | 单测（拉起 + 健康检查 + 失败重试） |

### 验收标准

- [ ] `train serve` 从权重目录拉起推理服务（vLLM/Ollama）
- [ ] 健康检查就绪探测；启动失败指数退避重试
- [ ] 与 model_switch 联动（切模型自动拉起新权重服务）
- [ ] 每次启停记 `train_serve` 审计事件
- [ ] `npm test` 全绿

---

## 二、持续后训练（飞轮闭环的"持续"环）

> 📋 **2026-08-10 补充（生命周期审查新发现）**：「持续后训练」——非 FDE 路径下企业数据回流，让模型"越用越懂企业 workflow"——在 v1.4.1-1.4.4 没有落地点。训练引擎有了"训一次"（train-job），缺"定期增量训"（持续后训练）。

### 交付

| 组件 | 能力 |
|------|------|
| **增量训练任务** | 基于新回流数据（企业运行数据 / 人工纠正记录）定期触发增量训练——复用 train-job 编排（v1.4.1）+ 数据管道（v1.4.2） |
| **调度策略** | 触发条件：数据量达到阈值 / 定时（@weekly）/ 人工触发——对齐 daemon 定时任务模式 |
| **回退保护** | 增量训练后 eval 分数不低于基线 → 晋升；否则回退旧权重（复用 v1.4.4 产物注册衔接 + 回滚） |
| **飞轮数据源** | 复用 worklog（v1.3.9 节点耗时 + 人工介入记录）+ decision-log + llm-calls（v1.4.4 语料导出同源） |

> 🚧 **边界声明（2026-08-26 补）**：本版持续后训练 = **数据回流 + 触发策略 + 回退保护**（规则驱动的增量再训）。**不延伸至**：① 自然语言指令直接转权重更新（on-policy self-distillation）② 在线交互实时训练（online RL）——这两条是 continual learning 的权重级路线，属商业模型层范围，不在开源训练引擎内实现（训练引擎只提供数据回流管道与再训编排，权重级能力由商业层消费）。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/tasks/continuous-training.ts` | 新建 | 持续后训练调度（daemon 定时任务，对齐 decision-memory @daily 模式） |
| `engine/orchestrator/src/train/train-continuous.ts` | 新建 | 增量训练编排（数据阈值 / 定时 / 人工触发） |
| `engine/orchestrator/src/__tests__/train-continuous.test.ts` | 新建 | 单测（触发条件 + 回退保护） |

### 验收标准

- [ ] 数据阈值 / 定时 / 人工三种触发方式可用
- [ ] 增量训练复用 train-job + 数据管道（不重复实现）
- [ ] 回退保护：eval 分数低于基线 → 回滚旧权重
- [ ] `npm test` 全绿

---

## 三、训练数据合规扫描（train compliance · 合规红线代码化）

> 📋 **2026-08-10 补充（合规缺口）**：v1.4.2 有"训练入口脱敏"（redactor 语义脱敏），但缺**合规扫描**——训练集进训练前的合规检查（个保法 / 敏感字段 / 数据来源标记）。合规红线（仅以脱敏聚合基准对外，不落个体级数据）需要一个代码化落点：**训练集进训练前先过合规闸**。
>
> 🔗 **与 v1.4.4 脱敏的边界**：两者用同一套检测能力（redactor 的 PII / 敏感字段红名单），但**流向不同**——v1.4.4 脱敏在**语料导出时**做（数据离开企业边界前，去掉可识别信息）；本版合规扫描在**训练管线入口**做（训练提交前，检查数据集是否可安全训练）。两道闸针对不同风险：导出闸防"泄漏出去"，训练闸防"不该训的数据进了训练"。检测能力共用，处置逻辑独立。

### 交付

| 组件 | 能力 |
|------|------|
| **合规扫描** | `train compliance <dataset>`——扫描训练集：个人可识别信息（姓名/手机号/身份证）/ 敏感字段（健康/财务）/ 企业专有名词——复用 v1.4.4 redactor 的红名单检测 |
| **合规报告** | 扫描结果 → 合规报告（发现项 + 严重度 + 处置建议：脱敏 / 剔除 / 人工确认）——写入训练集版本（v1.4.2） |
| **合规闸门** | 严重级发现 → 阻断训练提交（数据不处理完不让训）——对齐质量闸门模式 |
| **数据来源标记** | 训练集记录数据来源（企业提供 / 合成 / 公开语料）——合规可追溯 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-compliance.ts` | 新建 | 合规扫描 + 报告 + 闸门 |
| `engine/orchestrator/src/train/dataset-version.ts` | 修改 | 记录合规扫描结果 + 数据来源标记 |
| `engine/mcp/src/tools/train-compliance.ts` | 新建 | MCP `train_compliance` tool |
| `engine/orchestrator/src/__tests__/train-compliance.test.ts` | 新建 | 单测（PII 扫描 + 闸门阻断 + 来源标记） |

### 验收标准

- [ ] `train compliance` 扫描 PII/敏感字段/企业专有名词（复用 redactor 检测）
- [ ] 合规报告写训练集版本（发现项 + 严重度 + 处置建议）
- [ ] 严重级发现阻断训练提交（数据不处理完不让训）
- [ ] 训练集记录数据来源（合规可追溯）
- [ ] `npm test` 全绿

---

## 四、FDE 训练交付包（train-deliverable · 产品闭环收口）

### 定位

> 📋 **2026-08-10 补充（产品闭环收口）**：v1.4.3 把训练引擎定位为「FDE Agent 的核心功能点」，加了后训练 workflow 模板。但 FDE 离场时交的「训练运维包」到底包含什么？v1.4.2 有 `train report`（一次训练的报告），但 FDE 交付训练能力，交的应该是**一套可自运转的东西**：训练配置模板 + 数据管道配置 + eval 基线 + 运维手册（怎么续训、怎么回滚、出问题找谁）。现在这个「交付包」概念散落在各版本，没有收口。没有交付包定义，v1.4.5 ② 持续后训练企业自己跑不起来——给了引擎但没给说明书。本版作为生命周期补全版，收口这个交付物。

### 交付

| 组件 | 内容 |
|------|------|
| **train-deliverable 标准内容** | FDE 离场交付的训练运维包，包含：① 训练配置模板（v1.4.3 模板库实例化版）② 数据管道配置（v1.4.2 数据源连接 + 字段映射）③ eval 基线（v1.4.2 首次训练的 Benchmark 分数冻结）④ 运维手册（续训触发条件 / 回滚步骤 / 故障排查 / 联系方式）⑤ 权重清单（当前生产权重 + 历史版本 + 回滚点） |
| **交付包生成** | `train deliverable <enterpriseId>` → 打包上述内容为 `train-deliverable-<enterpriseId>-<date>.zip`（含 manifest 清单 + HMAC 签名） |
| **交付包校验** | 企业收到交付包后 `train deliverable verify` → 校验完整性（manifest 逐项核对）+ 环境兼容性（train doctor 跑一遍） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-deliverable.ts` | 新建 | 交付包生成（聚合配置/基线/手册/清单）+ 打包 + 签名 |
| `engine/mcp/src/tools/train-deliverable.ts` | 新建 | MCP `train_deliverable` tool（生成/校验） |
| `engine/orchestrator/src/__tests__/train-deliverable.test.ts` | 新建 | 单测（交付包完整性 + manifest 核对 + 环境兼容校验） |

### 验收标准

- [ ] `train deliverable` 生成标准交付包（配置模板 + 数据管道 + eval 基线 + 运维手册 + 权重清单）
- [ ] 交付包带 manifest + HMAC 签名
- [ ] `train deliverable verify` 校验完整性 + 环境兼容性
- [ ] 运维手册含续训/回滚/排查/联系方式（企业可自运转）
- [ ] **SKILL.md 登记本版 MCP tools（`train_serve` / `train_compliance` / `train_deliverable`）—— check-version.sh 工具数校验通过**
- [ ] `npm test` 全绿

---

## 五、训练产物归档与保留策略（磁盘治理 · 企业级标配）

### 定位

> 📋 **2026-08-10 补充（磁盘治理）**：训练跑多了会堆积大量历史 checkpoint 和旧训练集——一个 14B 模型的 checkpoint 几十 GB，跑十次几百 GB。v1.4.1 ⑤ 有 `train cleanup`（覆写销毁），v1.4.5 ④ 交付包含「权重清单（历史版本 + 回滚点）」——但**保留多少个回滚点？旧 checkpoint 留多久？过期谁清？** 没有定义。不补 = 磁盘逐渐塞满，手动清容易误删生产权重。

### 交付

| 组件 | 能力 |
|------|------|
| **保留策略配置** | `train-retention.json`：默认保留最近 N 个 checkpoint（默认 5）+ 当前生产权重 + eval 基线对应训练集 + FDE 交付包标记的回滚点——其余标记为可归档 |
| **自动归档** | daemon 定时任务（@weekly）：扫描超保留期的 checkpoint / 旧训练集 → 压缩归档到 `data/train/archive/` + 记审计（归档不删除，压缩冷存） |
| **过期清理** | 归档超过二次保留期（默认 90 天）→ `train cleanup` 覆写销毁（走 v1.4.1 ⑤ 覆写标准）|
| **空间预警** | 磁盘占用超阈值（默认 80%）→ 告警 + 建议清理（列出最大的可归档项）|

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/retention-policy.ts` | 新建 | 保留策略解析 + 归档标记 |
| `engine/daemon/src/tasks/train-archive.ts` | 新建 | 定时归档任务（@weekly）|
| `engine/orchestrator/src/__tests__/retention-policy.test.ts` | 新建 | 单测（保留策略 + 归档标记 + 过期清理） |

### 验收标准

- [ ] `train-retention.json` 保留策略可配（默认：最近 5 checkpoint + 生产权重 + eval 基线 + 回滚点）
- [ ] daemon @weekly 自动归档超期 checkpoint / 旧训练集（压缩冷存 + 审计）
- [ ] 归档超 90 天覆写销毁（走 v1.4.1 覆写标准）
- [ ] 磁盘超 80% 告警 + 清理建议
- [ ] `npm test` 全绿

---

## 六、训练引擎 Quickstart（端到端示例 · 拉新门槛）

### 定位

> 📋 **2026-08-10 补充（首次使用门槛）**：v1.4.x 有 16 个 MCP tools，工具链完整但**没有一个「5 分钟跑通一次训练」的 quickstart**。企业第一次用，面对 train env init / train doctor / train dry-run / train analyze / train_submit 不知道从哪下手。v1.4.3 后训练 workflow 模板是 FDE 视角；企业 IT 自己想快速试，需要的是一份**端到端示例文档**——「给你一个 10 条数据的 CSV，从 train env init 到 train serve 跑通，每一步的命令和预期输出」。这是拉新用户的门槛——没这个，潜在用户看完 16 个工具名就走了。

### 交付

| 组件 | 内容 |
|------|------|
| **quickstart 文档** | `docs/guides/train-quickstart.md`：端到端示例（10 条 CSV → 安装环境 → 体检 → dry-run → 选型 → 提交 → 监控 → eval → 部署 → serve），每一步给命令 + 预期输出截图/片段，失败有排查指引 |
| **示例数据集** | `docs/guides/examples/quickstart-data.csv`（10 条合成数据，无敏感信息，可直接跑）|
| **示例配置** | `docs/guides/examples/quickstart-job.json`（最小可跑的 job.json 模板，@mlx-node/trl 个人 Mac 阶段 0 验证）|

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `docs/guides/train-quickstart.md` | 新建 | 端到端示例文档（5 分钟跑通）|
| `docs/guides/examples/quickstart-data.csv` | 新建 | 10 条合成示例数据 |
| `docs/guides/examples/quickstart-job.json` | 新建 | 最小 job.json 模板 |

### 验收标准

- [ ] 按 quickstart 文档从零跑通一次训练（env init → serve 全链路）
- [ ] 示例数据无敏感信息（合成数据，可直接跑）
- [ ] 每一步有命令 + 预期输出 + 失败排查指引
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.5 的什么 |
|---------|-------------------|
| v1.4.6+（若排期） | train serve 是云端交付的模型运行底座；持续后训练是飞轮闭环的完整落地 |
| v2.x | meta-harness 承接多个模型的 serve 生命周期；Dashboard 展示推理服务状态 |
| **模型层（商业侧）** | ① train serve = 专属模型部署后"跑起来"的最后一环（交付完整性）② 持续后训练 = 「企业数据持续回流」的开源规则驱动版（非 FDE 路径数据回流）③ 合规扫描 = 合规红线（"不落个体级数据"）的代码化闸门 |

> 📖 行业对标：train serve 对齐 vLLM / Ollama 服务生命周期管理；持续后训练对齐 daemon 定时任务模式（decision-memory @daily 同款）；合规扫描对齐合规红线（仅脱敏聚合，不落个体级数据）。
