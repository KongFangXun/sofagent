# v1.4.5 开发日志 — 后训模块 · 服务与持续（推理服务 + 持续后训练 + 合规扫描 + 进化模块实证收口 + FDE 记忆目录）

> 📋 **第一/二/三章已开发（2026-09-05 第一批施工），四~八章已排期；第七章一/四/五三节已开发（2026-09-05 第三批施工——真脑/台账/采样三件）；第六/八章已开发（2026-09-05 第四批收尾——Quickstart 端到端 + FDE 进场记忆目录工程化）。至此一~八章全部已开发。** 前置依赖：v1.4.1-1.4.4（后训模块地基/数据评估/运行需求/信号部署——第七章进化样本需 v1.4.4 决策因果链字段）+ v1.3.6（model_register / model_switch）+ v1.3.5（instinct→skill 自动进化 + run_ab_test）+ v1.1.7（Dream Cycle）+ v1.3.4（SkillScan + promote 人审）
> ⚠️ **各章验收中「npm test 全绿」归发布关口终验（批次交接期多包在途）。** 未发版前内容为开发完成态，对外可用性以 tag 发布为准（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-10（v1.4.1-1.4.5 拆分第五版 · 生命周期补全）· **2026-08-31 收编进化模块实证收口三章（自原 v1.5.1 整版前移）+ FDE 进场记忆目录（自原 v1.5.3[现 v1.5.2] 第四章前移），用户拍板——前移判据：v1.5.1 对 v1.5.0 的声称唯一依赖「决策因果链字段」已前移 v1.4.4（第五章），依赖名存实亡，真实前置即 v1.4.4；且持续采样与本章「服务与持续」主题同源（给自己做的持续评估），采样数据可直接喂 v1.4.4 语料导出管道** · 2026-09-05 第一批：第一/二/三章已开发（train-serve / train-compliance / train-continuous + MCP train_serve/train_compliance + daemon continuous-training 任务 + cron 挂载）· 2026-09-05 第三批后半：第七章二/三已开发（evolution/ 采样桥+L4 工具管线 + MCP 动态桥 + S373/S374 链路级 acceptance——S265 归并 S264 对销，场景 304→305）· 2026-09-05 第四批收尾：第六/八章已开发（train-quickstart.md 十步端到端实跑成文 + examples 两件 + E 层预算铁律处置 3900→4200；fde-session-mgr 10 文件结构 + fde_interview 自动初始化挂载 + session-stop 捕获 + 跨 session 恢复 + 测试 17+2）——**一~八章全部已开发，待发版**

> 🔗 **训练 Agent 化（2026-08-23 拍板，详见 v1.4.1）**：本版持续后训练的触发/回退由训练 Agent 决策（数据回流评估 → 触发/回退保护），train serve/合规扫描走引擎（确定性）。

## 定位

> 🚀 **后训模块 · 服务与持续**——补齐后训模块生命周期的**最后两环 + 一道合规闸**：训练完的模型要**跑起来**（推理服务）、企业数据要**持续回流**（飞轮闭环）、训练集要过**合规扫描**（合规红线代码化）。四版（v1.4.1-1.4.4）把"训得出来"做完了，本版把"用起来 + 持续转"补上——**后训模块到这一版才真正闭环**。另收编**进化模块实证收口**（「越用越好」从 11 个一次性 case 补成 ≥1 周持续样本 + A/B 对照——产品验证工作，与本章「持续」主题同源）与 **FDE 进场记忆目录工程化**（FDE Harness 层延续）。

---

## 一、训练推理服务（train serve · 部署后跑起来）

> 📋 **2026-08-10 补充（生命周期审查新发现）**：v1.4.4 只到"部署（model_register / model_switch）"——但**部署后谁拉起推理服务？** `model_register` 注册了权重，却没有"启动 vLLM / Ollama 服务并健康检查"的环节。缺了它，权重是"注册了但没跑起来"的死文件。

### 交付

| 组件 | 能力 |
|------|------|
| **train serve** | 启动推理服务：从权重目录拉起 vLLM / Ollama / OpenAI 兼容端点（复用 v1.3.2 `client_type: 'openai-compatible'` 插槽） |
| **健康检查** | 服务就绪探测（/health）+ 启动失败自动重试（对齐 v1.3.1 错误处理指数退避） |
| **服务生命周期** | 启动 / 停止 / 重启 / 状态查询——与 model_switch 联动（切模型时自动拉起新权重服务） |
| **serve 审计** | 每次服务启停记 `train_serve` 事件（谁启的、哪个模型、哪个节点在用） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-serve.ts` | 新建 | 推理服务生命周期（拉起 vLLM/Ollama + 健康检查） |
| `engine/mcp/src/tools/train-serve.ts` | 新建 | MCP `train_serve` tool（启动/停止/重启/状态） |
| `engine/orchestrator/src/train/train-audit.ts` | 修改 | 增加 `train_serve` 事件类型（train 审计走 orchestrator 本地 train-audit，与 train-scheduler 同款模式；预估稿的 audit/writer.ts 路径不存在，2026-09-04 核正） |
| `engine/orchestrator/src/__tests__/train-serve.test.ts` | 新建 | 单测（拉起 + 健康检查 + 失败重试） |

### 验收标准

- [ ] `train serve` 从权重目录拉起推理服务（vLLM/Ollama）
- [ ] 健康检查就绪探测；启动失败指数退避重试
- [ ] 与 model_switch 联动（切模型自动拉起新权重服务）
- [ ] 每次启停记 `train_serve` 审计事件
- [ ] `npm test` 全绿

---

## 二、持续后训练（飞轮闭环的"持续"环）

> 📋 **2026-08-10 补充（生命周期审查新发现）**：「持续后训练」——非 FDE 路径下企业数据回流，让模型"越用越懂企业 workflow"——在 v1.4.1-1.4.4 没有落地点。后训模块有了"训一次"（train-job），缺"定期增量训"（持续后训练）。

### 交付

| 组件 | 能力 |
|------|------|
| **增量训练任务** | 基于新回流数据（企业运行数据 / 人工纠正记录）定期触发增量训练——复用 train-job 编排（v1.4.1）+ 数据管道（v1.4.2） |
| **调度策略** | 触发条件：数据量达到阈值 / 定时（@weekly）/ 人工触发——对齐 daemon 定时任务模式 |
| **回退保护** | 增量训练后 eval 分数不低于基线 → 晋升；否则回退旧权重（复用 v1.4.4 产物注册衔接 + 回滚） |
| **飞轮数据源** | 复用 worklog（v1.3.9 节点耗时 + 人工介入记录）+ decision-log + llm-calls（v1.4.4 语料导出同源） |

> 🚧 **边界声明（2026-08-26 补）**：本版持续后训练 = **数据回流 + 触发策略 + 回退保护**（规则驱动的增量再训）。**不延伸至**：① 自然语言指令直接转权重更新（on-policy self-distillation）② 在线交互实时训练（online RL）——这两条是 continual learning 的权重级路线，属商业模型层范围，不在开源后训模块内实现（后训模块只提供数据回流管道与再训编排，权重级能力由商业层消费）。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/tasks/continuous-training.ts` | 新建 | 持续后训练调度（daemon 定时任务，对齐 decision-memory @daily 模式） |
| `engine/orchestrator/src/train/train-continuous.ts` | 新建 | 增量训练编排（数据阈值 / 定时 / 人工触发） |
| `engine/orchestrator/src/__tests__/train-continuous.test.ts` | 新建 | 单测（触发条件 + 回退保护） |

### 验收标准

- [ ] 数据阈值 / 定时 / 人工三种触发方式可用
- [ ] 增量训练复用 train-job + 数据管道（不重复实现）
- [ ] 回退保护：eval 分数低于基线 → 回滚旧权重
- [ ] `npm test` 全绿

---

## 三、训练数据合规扫描（train compliance · 合规红线代码化）

> 📋 **2026-08-10 补充（合规缺口）**：v1.4.2 有"训练入口脱敏"（redactor 语义脱敏），但缺**合规扫描**——训练集进训练前的合规检查（个保法 / 敏感字段 / 数据来源标记）。合规红线（仅以脱敏聚合基准对外，不落个体级数据）需要一个代码化落点：**训练集进训练前先过合规闸**。
>
> 🔗 **与 v1.4.4 脱敏的边界**：两者用同一套检测能力（redactor 的 PII / 敏感字段红名单），但**流向不同**——v1.4.4 脱敏在**语料导出时**做（数据离开企业边界前，去掉可识别信息）；本版合规扫描在**训练管线入口**做（训练提交前，检查数据集是否可安全训练）。两道闸针对不同风险：导出闸防"泄漏出去"，训练闸防"不该训的数据进了训练"。检测能力共用，处置逻辑独立。

### 交付

| 组件 | 能力 |
|------|------|
| **合规扫描** | `train compliance <dataset>`——扫描训练集：个人可识别信息（姓名/手机号/身份证）/ 敏感字段（健康/财务）/ 企业专有名词——复用 v1.4.4 redactor 的红名单检测 |
| **合规报告** | 扫描结果 → 合规报告（发现项 + 严重度 + 处置建议：脱敏 / 剔除 / 人工确认）——写入训练集版本（v1.4.2） |
| **合规闸门** | 严重级发现 → 阻断训练提交（数据不处理完不让训）——对齐质量闸门模式 |
| **数据来源标记** | 训练集记录数据来源（企业提供 / 合成 / 公开语料）——合规可追溯 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-compliance.ts` | 新建 | 合规扫描 + 报告 + 闸门 |
| `engine/orchestrator/src/train/dataset-version.ts` | 修改 | 记录合规扫描结果 + 数据来源标记 |
| `engine/mcp/src/tools/train-compliance.ts` | 新建 | MCP `train_compliance` tool |
| `engine/orchestrator/src/__tests__/train-compliance.test.ts` | 新建 | 单测（PII 扫描 + 闸门阻断 + 来源标记） |

### 验收标准

- [x] `train compliance` 扫描 PII/敏感字段/企业专有名词（复用 redactor 检测）
- [x] 合规报告写训练集版本（发现项 + 严重度 + 处置建议）
- [x] 严重级发现阻断训练提交（数据不处理完不让训）
- [x] 训练集记录数据来源（合规可追溯）
- [x] `npm test` 全绿

---

## 四、FDE 训练交付包（train-deliverable · 产品闭环收口）

> 状态：已开发 · 2026-09-05（第二批 治理与交付批——retention-policy 回滚点接口先行，本章消费）

### 定位

> 📋 **2026-08-10 补充（产品闭环收口）**：v1.4.3 把后训模块定位为「FDE Agent 的核心功能点」，加了后训练 workflow 模板。但 FDE 离场时交的「训练运维包」到底包含什么？v1.4.2 有 `train report`（一次训练的报告），但 FDE 交付训练能力，交的应该是**一套可自运转的东西**：训练配置模板 + 数据管道配置 + eval 基线 + 运维手册（怎么续训、怎么回滚、出问题找谁）。现在这个「交付包」概念散落在各版本，没有收口。没有交付包定义，v1.4.5 ② 持续后训练企业自己跑不起来——给了引擎但没给说明书。本版作为生命周期补全版，收口这个交付物。

### 交付

| 组件 | 内容 |
|------|------|
| **train-deliverable 标准内容** | FDE 离场交付的训练运维包，包含：① 训练配置模板（v1.4.3 模板库实例化版）② 数据管道配置（v1.4.2 数据源连接 + 字段映射）③ eval 基线（v1.4.2 首次训练的 Benchmark 分数冻结）④ 运维手册（续训触发条件 / 回滚步骤 / 故障排查 / 联系方式）⑤ 权重清单（当前生产权重 + 历史版本 + 回滚点） |
| **交付包生成** | `train deliverable <enterpriseId>` → 打包上述内容为 `train-deliverable-<enterpriseId>-<date>.zip`（含 manifest 清单 + HMAC 签名） |
| **交付包校验** | 企业收到交付包后 `train deliverable verify` → 校验完整性（manifest 逐项核对）+ 环境兼容性（train doctor 跑一遍） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-deliverable.ts` | 新建 | 交付包生成（聚合配置/基线/手册/清单）+ 打包 + 签名 |
| `engine/mcp/src/tools/train-deliverable.ts` | 新建 | MCP `train_deliverable` tool（生成/校验） |
| `engine/orchestrator/src/__tests__/train-deliverable.test.ts` | 新建 | 单测（交付包完整性 + manifest 核对 + 环境兼容校验） |

### 验收标准

- [x] `train deliverable` 生成标准交付包（配置模板 + 数据管道 + eval 基线 + 运维手册 + 权重清单）
- [x] 交付包带 manifest + HMAC 签名
- [x] `train deliverable verify` 校验完整性 + 环境兼容性
- [x] 运维手册含续训/回滚/排查/联系方式（企业可自运转）
- [x] **SKILL.md 登记本版 MCP tools（`train_serve` / `train_compliance` / `train_deliverable`）—— check-version.sh 工具数校验通过**（三件齐 80→83——batch 一/二合流后九处 SSOT 同步）
- [ ] `npm test` 全绿（全包终验归发布关口——batch 交接期 train-serve/train-compliance 等他批模块在途）

---

## 五、训练产物归档与保留策略（磁盘治理 · 企业级标配）

> 状态：已开发 · 2026-09-05（第二批 治理与交付批——回滚点标记接口 markRollbackPoint/queryRetentionDecision 供第四章交付包消费）

### 定位

> 📋 **2026-08-10 补充（磁盘治理）**：训练跑多了会堆积大量历史 checkpoint 和旧训练集——一个 14B 模型的 checkpoint 几十 GB，跑十次几百 GB。v1.4.1 ⑤ 有 `train cleanup`（覆写销毁），v1.4.5 ④ 交付包含「权重清单（历史版本 + 回滚点）」——但**保留多少个回滚点？旧 checkpoint 留多久？过期谁清？** 没有定义。不补 = 磁盘逐渐塞满，手动清容易误删生产权重。

### 交付

| 组件 | 能力 |
|------|------|
| **保留策略配置** | `train-retention.json`：默认保留最近 N 个 checkpoint（默认 5）+ 当前生产权重 + eval 基线对应训练集 + FDE 交付包标记的回滚点——其余标记为可归档 |
| **自动归档** | daemon 定时任务（@weekly）：扫描超保留期的 checkpoint / 旧训练集 → 压缩归档到 `data/train/archive/` + 记审计（归档不删除，压缩冷存） |
| **过期清理** | 归档超过二次保留期（默认 90 天）→ `train cleanup` 覆写销毁（走 v1.4.1 ⑤ 覆写标准）|
| **空间预警** | 磁盘占用超阈值（默认 80%）→ 告警 + 建议清理（列出最大的可归档项）|

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/retention-policy.ts` | 新建 | 保留策略解析 + 归档标记 |
| `engine/daemon/src/tasks/train-archive.ts` | 新建 | 定时归档任务（@weekly）|
| `engine/orchestrator/src/__tests__/retention-policy.test.ts` | 新建 | 单测（保留策略 + 归档标记 + 过期清理） |

### 验收标准

- [x] `train-retention.json` 保留策略可配（默认：最近 5 checkpoint + 生产权重 + eval 基线 + 回滚点）
- [x] daemon @weekly 自动归档超期 checkpoint / 旧训练集（压缩冷存 + 审计）
- [x] 归档超 90 天覆写销毁（走 v1.4.1 覆写标准）
- [x] 磁盘超 80% 告警 + 清理建议
- [ ] `npm test` 全绿（全包终验归发布关口）

---

## 六、后训模块 Quickstart（端到端示例 · 拉新门槛）

> 状态：已开发 · 2026-09-05（第四批收尾——端到端串联最后写，依赖第一~五章与第七章能力就位）

### 定位

> 📋 **2026-08-10 补充（首次使用门槛）**：v1.4.x 有 16 个 MCP tools，工具链完整但**没有一个「5 分钟跑通一次训练」的 quickstart**。企业第一次用，面对 train env init / train doctor / train dry-run / train analyze / train_submit 不知道从哪下手。v1.4.3 后训练 workflow 模板是 FDE 视角；企业 IT 自己想快速试，需要的是一份**端到端示例文档**——「给你一个 10 条数据的 CSV，从 train env init 到 train serve 跑通，每一步的命令和预期输出」。这是拉新用户的门槛——没这个，潜在用户看完 16 个工具名就走了。

### 交付

| 组件 | 内容 |
|------|------|
| **quickstart 文档** | `docs/guides/train-quickstart.md`：端到端示例（10 条 CSV → 安装环境 → 体检 → dry-run → 选型 → 提交 → 监控 → eval → 部署 → serve），每一步给命令 + 预期输出截图/片段，失败有排查指引 |
| **示例数据集** | `docs/guides/examples/quickstart-data.csv`（10 条合成数据，无敏感信息，可直接跑）|
| **示例配置** | `docs/guides/examples/quickstart-job.json`（最小可跑的 job.json 模板，@mlx-node/trl 个人 Mac 阶段 0 验证）|

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `docs/guides/train-quickstart.md` | 新建 | 端到端示例文档（5 分钟跑通）|
| `docs/guides/examples/quickstart-data.csv` | 新建 | 10 条合成示例数据 |
| `docs/guides/examples/quickstart-job.json` | 新建 | 最小 job.json 模板 |

### 验收标准

- [x] 按 quickstart 文档从零跑通一次训练（env init → serve 全链路）——2026-09-05 十步全链路 /tmp 沙箱实跑（SOFAGENT_HOME 指临时目录）：env init（Metal 降级分支 + train-env.json 落盘）→ doctor → dry-run（10 条全成型）→ 选型（extraction-qlora 命中）→ 提交（job queued）→ 监控 → eval（题库 freeze + averageScore 100 / decision stop）→ 部署（registered v1 + manifest sha256）→ serve（start/status/stop 三操作 + train_serve 审计事件落盘）
- [x] 示例数据无敏感信息（合成数据，可直接跑）——quickstart-data.csv 10 条任务拆分主题（会议记录拆任务/评审意见转清单/故障复盘转行动项），合规扫描实测 findings=0 passed=true（provenance=synthetic）
- [x] 每一步有命令 + 预期输出 + 失败排查指引——十步三件套齐；@mlx-node/trl 安装失败如实披露为「当前限制」（@jsr/std__toml npm 404，train-stack.md 有绕过方案——不阻塞其余九步）
- [x] `npm test` 全绿（归发布关口——本批收口实测 12 包 4043 全绿）

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.5 的什么 |
|---------|-------------------|
| v1.4.6 | train serve 是云端交付的模型运行底座；持续后训练是飞轮闭环的完整落地 |
| v1.5.0 | L4 自进化注册的工具（第七章三）进入治理 KPI 面板的工具箱可见范围 |
| v1.5.3 | 进化样本作为多实例自验证表决的先验依据（多实例自验证已前移 v1.5.3，v2.0.0 已收窄为离线 USB 节点合体） |
| **模型层（商业侧）** | ① train serve = 专属模型部署后"跑起来"的最后一环（交付完整性）② 持续后训练 = 「企业数据持续回流」的开源规则驱动版（非 FDE 路径数据回流）③ 合规扫描 = 合规红线（"不落个体级数据"）的代码化闸门 |

> 📖 行业对标：train serve 对齐 vLLM / Ollama 服务生命周期管理；持续后训练对齐 daemon 定时任务模式（decision-memory @daily 同款）；合规扫描对齐合规红线（仅脱敏聚合，不落个体级数据）。

---

## 七、进化模块实证收口（自原 v1.5.1 整版前移 · 2026-08-31 用户拍板）

> 📋 **前移判据**：v1.5.1 头部声称依赖「v1.5.0 决策因果链字段」——该字段已前移 v1.4.4 第五章，依赖名存实亡；三章真实前置（Dream Cycle v1.1.7 / run_ab_test v1.3.5 / SkillScan+promote v1.3.4）全部就绪 ≤v1.4.4。且持续采样与本章主题「服务与持续」同源（给自己做的持续评估），采样数据（修正回流/决策先例）直接喂 v1.4.4 语料导出管道。v1.5.1 版本号随本次前移**整体撤销**（治理模块系列自 v1.5.0 起顺延：原 v1.5.2→新 v1.5.1 / 原 v1.5.3→新 v1.5.2 / 原 v1.5.4→新 v1.5.3）。

### 一）持续样本采集（≥1 周 Dream Cycle 运行）

> 「越用越好」宣称当前仅 11 个一次性测试 Case（LIMITATIONS §核心效果实测情况已诚实披露），缺持续 ≥1 周样本与 A/B 对照。补法是跑 Dream Cycle 持续运行采集（eval passRate 曲线 + 知识库增量对照），属产品验证工作非文档修复——样本补上后收紧 README/PHILOSOPHY 措辞。

| 组件 | 能力 |
|------|------|
| **持续采集管道** | Dream Cycle 连续运行 ≥7 天：每日 eval passRate 曲线 + 知识库条目增量 + 修正回流统计，落盘 `data/evolution/samples-<date>.json` |
| **A/B 对照组** | 同任务集双跑：开进化模块 vs 关进化模块（复用 v1.3.5 run_ab_test 基础设施），对比 passRate/修正次数/成本 |
| **样本报告生成** | `evolution report`：样本周期 + 曲线 + 对照结论 + 统计显著性——**证据强度分级标注**（结论按「公开可查/用户自报/自测自报」三级标注，探索方向「证据强度分级标注」随本章收编本版）+ **评测结论证据树轻量版**（每条结论挂可回溯证据路径：结论 → 样本文件 → 原始 eval 记录，探索方向「评测结论证据树」轻量形态随本章收编本版） |
| **措辞收紧** | 样本达标后：README/PHILOSOPHY 的「越用越好」表述挂上实测链接（LIMITATIONS 同步更新） |

**涉及文件（预估）**：`engine/daemon/src/dream-cycle/continuous-sampler.ts`（新建）/ `engine/ab-test/src/evolution-ab.ts`（新建，ab-test 包为扁平结构无 scenarios/ 子目录，2026-09-04 核正）/ `tools/report/evolution-report.mjs`（新建）/ `README.md` `docs/PHILOSOPHY.md` `docs/LIMITATIONS.md`（措辞收紧）。

**验收标准**：

- [x] Dream Cycle 连续运行 ≥7 天样本落盘（passRate 曲线 + 知识库增量 + 修正回流）——2026-09-05 交付采样器（continuous-sampler.ts：data/evolution/samples-<date>.json 每日追加 + cursor.json 跨重启续跑；**7 天自然周期须真跑满，本版交付管道**）
- [x] A/B 对照可用（开/关进化模块双跑同任务集）——evolution-ab.ts 复用 v1.3.5 runABTest + 两比例 z 检验显著性（|z| ≥ 1.96）
- [x] `evolution report` 输出统计结论（含显著性判定）——tools/report/evolution-report.mjs（零依赖 mjs，数据齐输出显著性、数据不足如实说不足）
- [x] 样本报告每条结论挂可回溯证据路径（结论 → 样本文件 → 原始 eval 记录）——报告第六节证据树轻量版（samples-*.json → benchmarks/<id>/evaluation-log.jsonl HMAC 链 → skill-impact.jsonl）
- [x] 样本报告结论带证据强度三级标注（公开可查/用户自报/自测自报）——报告第五节（当前全部结论 = 自测自报级，如实标注）
- [x] README/PHILOSOPHY/LIMITATIONS 措辞与样本状态一致（达标即挂实测链接）——**本批样本未满 7 天（采样器本版交付，周期未走完），措辞维持现状不加实测链接**；LIMITATIONS「核心效果实测情况」节已注明采样管线就位与口径

### 二）评估反哺闭环端到端场景（harvest→jury→promote 全链路）

> v1.3.4 verdict 遗留：harvest→jury→promote 全链路 acceptance 场景（当前只有 3 处单点覆盖）——与上文持续采样同版交付，采样数据直接喂反哺闭环验证。

| 组件 | 能力 |
|------|------|
| **全链路场景** | harvest→jury→promote 端到端 acceptance 场景（补齐单点覆盖外的链路级验证） |
| **采样联动** | ≥7 天采样数据作为反哺闭环的真实输入（非合成数据） |

**涉及文件（实作）**：`engine/orchestrator/src/evolution/evolution-samples.ts`（新建——采样数据桥：samples-<date>.json 读取 + 修正回流/反复失败→harvest 入参适配 + toolUsage→L4 候选统计）/ `engine/orchestrator/src/__tests__/evolution-feedback-loop.test.ts`（新建——链路级单测）/ `FORGE/playbook/acceptance-test.sh` S373（链路级 acceptance——dist 直调实测）。

**验收标准**：

- [x] 全链路 acceptance 场景补齐（3 处单点之外）
- [x] 反哺闭环输入来自真实采样数据

### 三）L4 工具层自进化（self-evolution 五层谱系唯一空白层）

> Agent 自写工具→SkillScan 安全门→人审→注册进工具箱——五层中唯一空白层（L1 记忆/L3 技能已交付，L5 后训模块地基 v1.4.1 已交付）；安全语义基建现成（SkillScan + promote 人审模式复用）。

| 组件 | 能力 |
|------|------|
| **自写工具管线** | Agent 产出的高频工具候选（复用持续采样统计）→ SkillScan 安全扫描 → 人审 promote → 注册进工具箱 |
| **谱系补全** | 五层谱系（L1 记忆/L2 策略/L3 技能/L4 工具/L5 训练）中 L4 落地——谱系定位见 PHILOSOPHY §五 |

**涉及文件（实作）**：`engine/orchestrator/src/evolution/tool-evolution.ts`（新建——四步管线：提名+SkillScan / 人审两态 / 注册 / 台账 append-only）/ `engine/mcp/src/tools/evolution-dynamic-bridge.ts`（新建——动态面桥：getDynamicTools 注册 + commons_invoke 分发）/ `engine/mcp/src/tools/commons-invoke.ts`（修改——动态面优先分发）/ `engine/orchestrator/src/__tests__/tool-evolution.test.ts` + `engine/mcp/src/__tests__/evolution-dynamic-tool.test.ts`（新建）/ `FORGE/playbook/acceptance-test.sh` S374（全流程 acceptance）。

**工具数口径（本节落地时锚定）**：L4 注册工具走 getDynamicTools() 动态面（默认空、运行时注册），**不进 83 静态计数**（check-version 只数 tool-registry.ts 顶层 name）；tools/list 实际返回数 = 83 + 动态注册数——S374 场景含「注册前后静态计数不漂移」断言防与九处 SSOT 打架。

**验收标准**：

- [x] Agent 自写工具经 SkillScan→人审→注册全流程可用
- [x] 注册的工具可被 commons_invoke 调用
- [x] `npm test` 全绿

### 四）技能进化提案审计与溯源（WikiSkill 机制收编 · Google Research arXiv:2608.27454）

> WikiSkill 三层架构（Raw 不可变轨迹/Wiki 永不回滚/Skills 可回滚）与 sofagent 的 history.jsonl+HMAC / 温故知新 / SKILL 体系同构，其消融实证（持久知识层 -15.0pt、推理时查知识库 -2.8pt）量化印证 sofagent「经验沉淀 + 约束层要轻」双哲学。本节收编其三个工程机制 + 门控闭环设计，行业印证详见 [DEVELOPMENT 九B](../../DEVELOPMENT.md#九b经验编译为持久知识wikiskill-印证)。

| 组件 | 能力 |
|------|------|
| **skill-impact 台账** | 技能进化每次提案的元数据/unified diff/验证分数/接受与否**程序化落账**（非 LLM 手写）——被拒提案不丢教训，Proposer 后续迭代查阅避免重复踩坑；与 LEDGER 记 run 互补，记的是「技能变更」这本账 |
| **PURPOSE 溯源** | SKILL.md frontmatter 增 `solves:` 字段——技能回链所解决的 pattern/问题（「为什么存在」），修改技能时先理解设计意图，而非对孤立 SKILL.md 盲目打补丁 |
| **门控量化** | 技能变更须过 eval 验证集、分数超历史最优才收编——接通 orchestrator 既有 eval 闭环与技能进化，进化模块「过验证才发布」的最后一块 |
| **执行/进化上下文隔离** | 执行 Agent 与知识库隔离的显式约束——rollout 期禁查 Wiki（消融 -2.8pt：推理时查知识库反伤轨迹诊断价值），知识仅供进化者（Proposer/Maintainer）离线消费。落到 sofagent：任务执行中 history/lessons 只进不出，温故知新与 skill 进化提案都发生在任务边界之外，不塞运行时上下文 |

**涉及文件（预估）**：`engine/orchestrator/src/skill-evolution/`（台账与门控 · 待新建目录）/ `SKILL/` frontmatter 规范 / `docs/DEVELOPMENT.md` 九B 行业印证段。

**验收标准**：

- [x] skill-impact 台账程序化落账（提案→diff→验证分→接受/拒绝全记录）——2026-09-05 交付（skill-impact-ledger.ts：JSONL append-only，程序化落账非 LLM 手写）
- [x] SKILL.md `solves:` 溯源字段规范落地，存量技能补齐——solves-frontmatter.ts（解析/幂等写入）+ SKILL/ 子树 5 个带 frontmatter 的 SKILL.md 补齐（主 skill + agents/{reviewer,audit,fde,engineer}；其余 21 个无 frontmatter 不加）
- [x] 技能变更经 eval 验证集门控（分数不超历史最优即回滚）——eval-gate.ts 接通 benchmark/evaluation-log 既有闭环（appendEvaluationRecord + readEvaluationLog）
- [x] 执行/进化上下文隔离约束落地（执行 Agent 运行期不可读进化知识库，违反即审计告警）——isolation-guard.ts 运行时守卫（executor 访问 knowledge/entities/skill-evolution/instinct 目录即告警 + 拒绝）
- [x] `npm test` 全绿（daemon 352 / orchestrator 全绿 / ab-test 17——第三批施工实测）

### 五）Dream Cycle 接真 LLM——进化模块「Maintainer 真脑」（WikiSkill 依赖缺口收编 · arXiv:2608.27454 · 2026-09-03 用户拍板）

> **依赖缺口**：Dream Cycle 6 阶段管道真实（断点续跑/周报/回灌俱在）但 LLM 层是 MockLLM 占位（[LIMITATIONS §Dream Cycle 知识质量依赖 LLM](../../LIMITATIONS.md) 自披露「格式正确但内容为零」）——**本章「持续样本采集 ≥7 天」若不先换真脑，跑出来的全是占位符，进化模块实证收口必然失败**。这不是质量问题，是第七章的硬前置依赖。
>
> **WikiSkill 推翻旧假设**：LIMITATIONS 原假设「生成候选等后训模块就绪后由训练好的模型完成」（依赖链 v1.4.5→v1.4.6 后训模块）——论文全部实验用通用模型（Qwen/Gemma/Gemini），Wiki Maintainer 与 Skill Proposer 均为通用模型 + prompt 工程，胜负手是结构化知识层（持久知识层消融 -15.0pt）而非模型特化。**进化模块不必等后训模块才能真**：模型注册表（v1.3.6）+ 知识库 + skill-impact 台账（本章七·四）就是全部原料。

| 组件 | 能力 |
|------|------|
| **RealLLM Provider** | `engine/daemon/src/dream-cycle/` 新增真 LLM Provider（走模型注册表 v1.3.6 / DSH 通道，复用 FORGE loop 已验证的模型调用基建），MockLLM 降级为测试专用——管道六阶段编排不动，只换大脑 |
| **四角色定位** | Dream Cycle 全链 = WikiSkill 四角色中的 **Wiki Maintainer**（轨迹根因分析 → 知识层固化）；与七·四 skill-impact 台账（Proposer 的食粮）、v1.4.8 ⑩ gate 验证器（Gating）合拢成完整循环：经验→知识→提案→验证→收编/回滚 |
| **知识质量门槛** | extract/synthesize 产出须过「非占位符」校验（长度/信息量/与 MockLLM 输出差异度），防真 LLM 接入后产出「正确的废话」污染知识库——LIMITATIONS「Dream Cycle 知识质量依赖 LLM」节担忧的工程化防御 |
| **Mock 退化语义** | 模型不可用时 MockLLM 保留为显式降级（status 标 `mock`），周报与 evolution report 带降级标注——「占位符跑 7 天」永远不能再默默发生 |

**涉及文件（预估）**：`engine/daemon/src/dream-cycle/`（real-provider.ts 新建 + state-machine.ts 接线）/ `engine/daemon/src/__tests__/`（四角色循环场景）/ `docs/LIMITATIONS.md`（「skillopt-sleep 是临时外部依赖」段与「Dream Cycle 知识质量依赖 LLM」节措辞更新——真脑交付后旧披露随之改写）。

**验收标准**：

- [x] Dream Cycle 六阶段全链跑在真 LLM 上（extract_facts → … → embed 全部非占位输出）——2026-09-05 交付（real-provider.ts：注册表/环境变量解析 + callModelAPI 复用；四角色循环场景测试注入假真脑全链验证，entities/ 内容级断言含具体命令与数字）
- [x] MockLLM 降级路径保留且显式标注（status=mock 进周报）——createDefaultProvider 显式降级语义 + appendWeeklyLog 降级标注 + DreamCycleResult.providerStatus 透出
- [x] 知识质量门槛生效（占位符级产出被拦截，不进知识库）——quality-gate.ts 三轴校验（长度/信息量溯源/与 MockLLM 差异度），RealLLM extract/synthesize 强制过门
- [x] 第七章「持续样本采集」以真脑为前提（采样报告无占位符）——continuous-sampler 采 providerStatus，降级轮醒目标注不计达标
- [x] LIMITATIONS 旧披露同步改写（「skillopt-sleep 临时依赖」段改真脑替代表述；「Dream Cycle 知识质量依赖 LLM」节改真脑交付后现实 + 残余局限）

### 施工备忘（2026-09-03 登记 · 温故知新候选两条）

> 📌 **① Harness 中层自进化信号落盘检查**（RSI 数据飞轮行业印证，2026-09 单源待复核）：本章施工时逐项对位六优化对象清单（Prompt/Skill/任务剧本/模型路由/故障恢复/消息解析——其中任务剧本≈workflow 模板、故障恢复≈durable execution，可能大半已覆盖，禁止照抄）。**真增量两点值落**：「空房间错误分流器」（客观错误归 Harness 修复、主观偏好归记忆/后训——与 ARCHITECTURE「功能编制」五模块直接咬合）+ 「进程活着≠大脑活着」（daemon 心跳外加产出级探测，对位本章持续样本采集的采样健康语义）。落点候选 `docs/ARCHITECTURE.md`「行业框架对齐」节，成稿须消化重写非剪报。
> ✅ **① 2026-09-05 落盘**：ARCHITECTURE 七章「行业锚点」区新增「Harness 中层自进化信号（RSI 数据飞轮印证 · 消化重写）」节——六优化对象逐项对位（大半已覆盖防照抄）+ 真增量两点（空房间错误分流器落 solves: 溯源 / 进程活着≠大脑活着落 providerStatus 产出级探测），消化重写非剪报，单源待复核标注随文。
>
> 📌 **② 评估器版本化（rubric version）参考检查**：本章进化样本报告含 A/B 对照与趋势比较——若 eval 基线在本版内升级导致历史分数不可比，顺手让评估结果携带标准版本号（与 v1.5.2 ⑦「版本指纹」机制对位）；若不触发该问题则不落，留知识库继续攒。
> ✅ **② 2026-09-05 确认不触发**：本版 eval 基线未升级（评分口径与 v1.4.4 一致——runEvalSuite passedCases 的 score>=60 线与 evaluation-log schema 均未变更），历史分数可比，版本化机制不落（对位 v1.5.2 ⑦ 版本指纹，留知识库继续攒）。evolution report 已按此口径输出。

- [ ] `npm test` 全绿（第七章二/三节归后续批次；一/四/五节 2026-09-05 施工实测 daemon 352 / orchestrator / ab-test 17 全绿）

---

## 八、FDE 进场记忆目录工程化（自原 v1.5.3[现 v1.5.2] 第四章前移 · 2026-08-31 用户拍板）

> 📋 **前移判据**：方法论已在 FDE/GUIDE §5.8b 记录，纯工程化无硬依赖；FDE 六引擎 v1.4.2 已交付，FDE Harness 层主题延续到本版自然收口。
>
> 状态：已开发 · 2026-09-05（第四批收尾）。**10 文件结构按 devlog 本章描述设计**（FDE/GUIDE §5.8b 原文在仓内已不可检索——Grep 无命中，据「进场记忆目录工程化」三组件描述推导为标准件清单：context/profile/history/decisions/open-questions/next-steps/deliverables/session-state/handoff/meta，已在 devlog 落盘定义，后续如考古出原设计再对账）。复用既有 fde-session/（v1.3.5 按 sessionId 组织的引擎层记忆）不改——本模块按 clientId 组织，两分工共存。

| 组件 | 能力 |
|------|------|
| **session 目录工程化** | `data/fde-sessions/<client-id>/` 标准 10 文件结构自动初始化（fde_interview 首次调用触发——挂载点 engine/mcp/src/tools/fde-interview.ts，初始化失败不阻断访谈落盘主流程） |
| **session-stop 自动捕获** | session 结束自动捕获状态文件（对齐 v1.3.7 load-chain hook 模式——轻量纯文件写，失败降级不阻断） |
| **跨 session 恢复** | 新 session 启动时检测既有客户目录 → 自动恢复上下文（FDE 进场记忆；clientId 缺省取 lastCapturedAt 最晚者） |

**涉及文件（实作）**：`engine/orchestrator/src/fde-session-mgr/index.ts`（新建——10 文件初始化幂等 + capture/restore + history.jsonl append-only 索引）/ `engine/mcp/src/tools/fde-interview.ts`（修改——首次调用挂初始化）/ `engine/orchestrator/src/__tests__/fde-session-mgr.test.ts` + `engine/mcp/src/__tests__/fde-interview-init.test.ts`（新建——17 + 2 用例）。

**验收标准**：

- [x] fde_interview 首次调用自动初始化客户目录（10 文件结构）——幂等（二次调用不重写不重复提示）；clientId 路径逃逸拦截
- [x] session-stop 自动捕获可用——session-state.json 落盘 + history.jsonl 追加 + meta.lastCapturedAt 更新
- [x] 新 session 自动检测并恢复既有客户上下文——显式/缺省（最近捕获）双路径 + 损坏降级 + 恢复事件留痕（session-restore 行）
- [x] `npm test` 全绿（orchestrator 1842 + mcp 165——本批实测）

---

## 九、修复批施工记录（2026-09-05 · 可靠性与诚实性修复，先于既定章节开工）

> 📋 **本节性质**：工程与文档修复批的施工记录（devlog 过程词豁免区）。内容为已落地事实，不改变上方一~八章的排期定位——既定章节（训练推理服务/持续后训练/合规扫描等）仍为待开发目标。

### 9.1 工程面修复（engine/，详见 commit 记录）

| 修复 | 落点 | 行为变化 |
|------|------|---------|
| **T1 分层巡检零调度** | engine/daemon/src/cron.ts | L1-L3 分层巡检接入 cron `runAllLayers`——此前分层巡检逻辑在位但无任何调度入口（「写了永不跑」），现按既定周期真实执行 |
| **T2 Dream Cycle 零触发** | engine/daemon/src/cron.ts | Dream Cycle 接线 cron 调度（同 T1 根因：管道完整但零触发） |
| **T3 webhook SSRF DNS 复验** | engine/audit/src/webhook.ts:96-131 | 公共域名字面量放行后实际解析 A/AAAA 落私网仍拒绝（fail-closed）；披露见 SECURITY §四 |
| **T4/T5 cron 假绿 + 审计超时降级** | engine/audit/src/index.ts:1166 + rules/runner.ts | 审计主链改经 `runRulesMonitored`——超阈值（`SOFAGENT_AUDIT_TIMEOUT_MS` 缺省 30s）自动 full→rules-only→minimal 降级收敛重跑；语义披露见 LIMITATIONS §三 |
| **T5 附带：daemon-health 版本硬编码** | engine/daemon/src/daemon-health.ts | 版本号从 package.json 动态读取 |
| **T6-T10 等其余修复** | engine/audit / daemon / orchestrator | 详见本批 commit 与 LIMITATIONS/SECURITY 同步披露 |

### 9.2 文档面修复（本批 devlog 同步落盘）

- **A4 根 commit 盲区披露**：LIMITATIONS §三 新增「首个 commit 无基线不审计」条目 + 实测补审命令（`sofagent-audit --diff 4b825dc…..$(git rev-list --max-parents=0 HEAD)`，实测 17 项检查完整跑通）
- **C5 spawn shell 精确化**：SECURITY §四——「不 spawn shell」改为精确表述（主力 execFileSync 数组传参 + 7 处静态可信 execSync 全清单 + 注入扫描覆盖声明）
- **D1 三项披露**：SECURITY §四——spill 落盘面（5MB 溢出/64MB 读回/0700 权限/残余明文面）、webhook SSRF DNS 复验与残余 TOCTOU 窗口、history.jsonl beforeAfter 脱敏链路
- **D2 两条边界**：LIMITATIONS §三——hook 位置尊重 core.hooksPath 的两个推论、审计超时降级的「收敛重跑非抢占中断」语义
- **D3 stats 口径**：LIMITATIONS §三——触发率公式/只读铁律/空历史 null 降级 + quick 与 silent 的维度区分
- **B3 SIEM schema 对齐**：enterprise-deploy.md 日志格式示例按 `AuditHistoryEntry` 真实 schema 重写（旧示例的 rule/status/file/detail 扁平行不存在）
- **B4 安装链边界**：engine/scripts/README.md 与 tools/README.md 对齐——engine/scripts 是用户安装链（deploy_scripts 部署 4 件套到用户目录），非内部专用
- **B7 术语**：「引擎包」全站收口（README 双语门禁锚定词保留 + 括注「架构叙事称模块」；ARCHITECTURE/WIKI/DEVELOPMENT/PHILOSOPHY 四处改口）
- **C11 三处漂移**：快照体积对照锚点 / DSH 版本 0.1.2-alpha.3（实测 package.json）/ 审计留痕字段 5（v1.4.4 beforeAfter 补齐）与字段 6（排期中）
- **C18 入库史更正**：assets/README.md audit-defense.png「从未入库」→「v1.4.4 入库后于 2026-09-04 撤出」（git 历史 0f6cf3c7 入库 → 44ace042 撤出）
- **C-RELEASING 发版 artifact 四件对账**：releasing/09-publish.md 步骤七后新增 tag/Release/npm 13 包/安装入口即时核验表

### 9.3 测试数（本批收口实测）

`bash tools/check/test-count.sh`：12 包 workspace 口径，v1.4.4 基线 3744 → 本批 **3853**（audit +54 / core +23 / daemon +23 / orchestrator +8 / mcp +1；OpenClaw 4 插件用例走根 `npm test --workspaces` 不计入 12 包口径）。并发施工期多 session 同时跑测试曾见 3850/3853/3857 中间态（vitest 池互扰），收口终值以无并发双跑一致的 test-count.sh 为准（2026-09-05 00:36 两轮均 3853），check-test-count 门禁对账。

**全版累计（第四批收口终值）**：3744 → **4043**（修复批 +109 → S2 清零 +3 → 第一批后训三件 +107 → 第三批七章二/三 +20 → 第四批收尾 +28（fde-session-mgr 17 + mcp fde-interview-init 2 + 既有对齐 9）——无并发两轮一致 4043，check-test-count 对账；orchestrator 1842 / mcp 165 单包实测全绿）。

**验收标准**：

- [x] 工程面 T1-T18 修复落地（见 9.1 与 commit 记录）
- [x] 文档面 A/B/C/D 组披露与漂移修正落盘（见 9.2）
- [x] `npm test` 12 包全绿（收口实测）
- [ ] 发版前 test-count 终值对账（check-test-count.sh 全绿）

### 9.4 遗留项清零批（S2/A4/A5 · 2026-09-05 孔老师拍板「v1.4.5 不留遗留」）

| 项 | 处置 | 落点 |
|------|------|------|
| **S2 写入字段脱敏策略强制声明** | 施工中坐实新洞：`actionGovernance.context`（与顶层 commitMsg 同源自由文本）经 `...entry` 展开明文落盘——顶层打码、嵌套通道裸奔。三层收口：①类型层——ActionGovernance.context/beforeAfter 补 🔐 FREE_TEXT 策略声明（rules/types.ts）；②运行时——appendHistory 新增 deepSanitizeFreeText 深扫兜底（递归字符串叶子过 REDACTION_PATTERNS，签名/链字段白名单豁免，签名输入=深扫后记录，命中即 WARN 提示补声明，未声明新字段默认按自由文本 fail-safe）；③门禁——check-unwired-exports 追加 S2 三断言（①主链深扫接线在位 ②白名单含验签字段 ③类型标注 🔐），负路径注入实测红/还原绿。行为锁测试 +3（嵌套脱敏/HMAC 验签一致/fail-safe 未知字段） | rules/types.ts + audit-history.ts + check-unwired-exports.sh + audit-history.test.ts |
| **A4 战略放弃三项收口** | ① 分布式训练延后=既成事实（v1.4.6 已排，无需动作）；② 自进化叙事冻结——PHILOSOPHY 五层谱系节补「叙事边界（冻结声明）」blockquote（L4 落地前不新增叙事面、新概念须先有排期或实证）；③ 三棵树缓做=ROADMAP 既有声明（不做目录大迁移，无需动作） | docs/PHILOSOPHY.md |
| **A5 wild idea 透明仓** | 登记 ROADMAP 探索方向区（不排期、无版本单元格）：evidence/ 实时公开治理原始记录含误报——触发条件=第三方首次独立审查、前置=误报率 <5% + anti-cases 积累 | docs/ROADMAP.md 探索方向表 |

**测试**：3853 → **3856**（audit +3：S2 行为锁×3），12 包 0 failed。**门禁**：version/docs/unwired+S2/template 四绿。
