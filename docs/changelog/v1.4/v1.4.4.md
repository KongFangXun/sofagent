# v1.4.4 开发日志 — 训练引擎 · 信号与部署闭环（语料 + 权重 + 注册 + 对比 + 决策因果链 + CI 加固 + 存量升级 + spec-first 硬禁令 + 审查收编批）

> 📋 **已排期。** 前置依赖：v1.4.1（checkpoint 管理 / train done 事件）+ v1.4.2（训练集版本 / eval 报告）+ v1.4.3（沙箱）+ v1.3.6（model_register / model_switch / 公开格式约定）+ v1.3.2（Trace 任务级轨迹 / FDE 梳理辅助）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-10（v1.4.1-1.4.4 拆分第四版）· 2026-08-26 语料导出补「规则 → reward/verifier 映射」（第一章）——语料（样本）到 RL 训练（reward 配方）的最后一公里，River/ScaleRL 调研收编，用户拍板落盘 · **2026-08-31 收编「六轮审查 17 项修复批」（第九章，用户拍板——fresh-eyes 29 视角报告桶一全量 + 桶三裁决 4 项落位，零上下文复验 5 处证据漂移修正）**

> 🔗 **训练 Agent 化（2026-08-23 拍板，详见 v1.4.1）**：本版评估/晋升是训练 Agent 的决策点（训练质量评估 → 决定晋升/回滚），权重注册走 model_register（治理层）；语料导出五源 = 训练 Agent 的决策原料。

## 定位

> 🚀 **训练引擎 · 信号与部署闭环**——训练引擎的**两端出口**：往训练管线出**训练信号**（语料导出三件套），往生产环境出**可部署产物**（企业专属模型本地权重部署 + 产物→注册自动衔接），外加**多基座对比**（选型数据支撑）。训练引擎到这一版闭环。

---

## 一、训练语料导出三件套（规则 + 方法论 + 样本 · 训练信号）

> 🔗 **2026-08-10 归集**：原 v1.3.2「训练语料导出 MVE」+ 原 v1.3.6「训练语料导出三件套」合并为本章（MVE 与完整版同版交付，不再拆分）。依赖 v1.3.0 decision-log（已交付）+ v1.3.1 llm-calls/evaluation-log + v1.3.2 Trace 任务级轨迹（样本富矿）。

开源侧已有丰富的现成语料——GUIDE 方法论、SKILL 加载链、24 条审计规则、25+ 案例——本版把它们全部机器可读化，成为可喂给训练管线的训练信号：

| 语料 | 来源 | 交付 |
|------|------|------|
| **规则定义** | 24+3 条审计规则（TS 代码内聚 · 口径定义见 [表下注 A](#表下注-a243口径定义)） | 序列化导出（JSON/YAML 双格式）+ 版本化 + HMAC 签名 |
| **FDE 方法论** | `FDE/GUIDE.md`（五要素/三问判定/量化公式）+ SKILL 加载链 | 结构化导出（markdown → JSON 摘要 + 原文）+ **结构化锚点维护机制**（`<!-- METHODOLOGY: five-elements -->`，GUIDE 更新后重导即同步） |
| **带标签审计样本** | 五源（落点实测见 [表下注 B](#表下注-b五源落点实测2026-09-01)）+ Trace 轨迹 | 聚合导出（脱敏 + 按任务分组 + 标签）——**runtime-audit 含工具调用 + FAIL/PASS/HITL 判定，是 RL「行为 → 奖励信号」对应数据（⚠️ 现仅 FORGE 侧实现）；fde-session 含 FDE 进场梳理的关键决策+理由（自举：训练自己的 FDE 模型的最佳语料，见 v1.4.1 训练 Agent 化）；Trace 轨迹提供任务级完整序列** |

> #### 表下注 A：「24+3」口径定义
>
> **2026-09-01 核对 `engine/audit/src/rules/index.ts`（规则注册权威处）后补。** 该「24+3」自 2026-08-10 沿用至今从未定义，而仓库并存多种计数，导致本章验收第 1 条「规则零遗漏」不可判定。源码真相：
>
> | 口径 | 数值 | 构成 | 出处 |
> |------|:--:|------|------|
> | `defaultRules` | **17** | A1-A11 + A18-A23（quick 默认启用） | `rules/index.ts:43-64` |
> | `extendedRules` | **7** | E1/E2/E4 + A14-A17（需 `extendedRulesEnabled`） | `rules/index.ts:72-81` |
> | `rules`（全量） | **24** | 17 + 7（README「完整 24 条 = 17 默认 + 7 扩展」同此） | `rules/index.ts:84` |
> | 永久跳号编号位 | **3** | **A12 / A13 / E3**——A12 供应链安全、A13 文件权限于 v0.99.4 并入 A11；E3 于 v1.2.5 并入 A11（行数维度）。三者已无独立实现 | `rules/index.ts:32`、`:55`、`:75` |
>
> **本版取「24+3」= 24 条已实现规则 + 3 条跳号编号位（A12/A13/E3）**——这是源码证据下唯一自洽的读法：跳号数恰为 3，且三处跳号在同一文件内被显式写明、「+3」这个加数才成立。导出须覆盖 **27 个编号位**（跳号 3 条以 `status: 'merged-into-A11'` 占位登记），使「零遗漏」可对**编号空间**对数而非仅对数组对数，避免 3 个已知合并位被静默丢掉。
>
> ⚠️ **待用户拍板**（不代为决定）：若拍板为「只导 24 条实现、跳号不占位」，则本章验收第 1 条同步改为「24 条全部可导出」，且导出的编号空间出现 A12/A13/E3 三个空洞需在格式里显式声明为 `skipped`（而非静默缺省）。
>
> #### 表下注 B：五源落点实测（2026-09-01）
>
> | 源 | 状态 | 实际落点 |
> |------|:--:|------|
> | decision-log | ✅ 引擎侧在位 | `engine/audit/src/decision-log.ts` → `data/audit/decision-log.jsonl` |
> | llm-calls | ✅ 在位（**异名**） | 实现文件为 `engine/core/src/llm-call-trace.ts`（落盘 `data/audit/runtime/llm-calls.jsonl`，见 `:160`）——规划文档习称 `llm-calls`，全仓无同名实现文件，施工勿按名找 |
> | evaluation-log | ✅ 引擎侧在位 | `engine/orchestrator/src/benchmark/evaluation-log.ts` → `data/<project>/benchmarks/<benchmark_id>/evaluation-log.jsonl`（注意在 **orchestrator** 包，不在 audit 包） |
> | runtime-audit | ⚠️ **仅 FORGE 侧实现** | `FORGE/src/audit-middleware.mjs:111` → `data/audit/runtime/<repo-hash>/runtime-audit.jsonl`（实测本机 `a325d4ba64c8/runtime-audit.jsonl` 有数据）。**`engine/` 侧无实现**——`docs/ARCHITECTURE.md:481` 明载「引擎侧 data-sovereignty 审计日志仍全局，**已移排 v1.4.7** 复用 FORGE 方案补齐」 |
> | fde-session | ✅ 引擎侧在位（尚无数据） | `engine/orchestrator/src/fde-session/index.ts`（v1.3.7 交付 5 #2，286 行）→ `data/fde/sessions/<sessionId>/context.md` + `meta.json`；本机实测 sessions 目录为空，即模块在位但尚未产生样本 |
>
> **对施工的直接影响（不代为拍板，列选项由用户定）**：
> - **runtime-audit 一源有两个选项**：① **读 FORGE 产物**——sample-aggregator 直接解析 `data/audit/runtime/<repo-hash>/runtime-audit.jsonl`（零新增代码，但语料面只覆盖跑过 FORGE 自托管 SubAgent 的仓库）；② **等 v1.4.7 引擎侧补齐**后再纳入，本版五源降级为四源。
> - **llm-calls / fde-session 两源无阻塞**，仅需按上表真实路径接线（前者异名、后者尚需触发产生数据）。

**样本是训练信号里最值钱的部分**——真实的 FAIL/通过案例 = 带标签的「什么是对的」，对应 self-recording improvement（模型协作产生 trace → 用 trace 训练）。

### 规则 → reward/verifier 映射（样本到 reward 配方的最后一公里 · 2026-08-26 补）

> 📋 **2026-08-26 补充**：三件套导出了**样本**（什么行为被判 FAIL/PASS），但 RL 训练还需要 **reward 定义**（用什么函数给行为打分）。sofagent 的天然优势：24+3 条审计规则的判定逻辑本身就是机器可执行的 verifier——**规则导出（第一件）从「给人看的规则定义」升级为「可挂进训练循环的 reward 骨架」**。

| 组件 | 能力 |
|------|------|
| **规则 → reward 骨架导出** | 每条审计规则导出时附带 `reward_hint` 段：判定函数签名（输入=工具调用序列/产物文本，输出=PASS/FAIL 三态）+ 严重度权重 + 可判定性标注（机器可判 / 需人审 / 启发式） |
| **verifier 清单** | 规则导出附 `verifiers.json`——哪些规则可直接当 reward 函数（机器可判类）、哪些只能当训练后验收（需人审类）——训练管线按清单接线，不混用 |
| **与 RL 配方模板衔接** | reward 骨架 + v1.4.3 RL 配方模板（grpo/dapo/cispo）= 完整训练配置——商业侧「reward 配方」的开源对应物（开源导骨架，商业侧调权重） |

**边界（不做）**：本版只导「规则 → reward 骨架」的映射格式与清单，**不实现训练循环本身**（那是计算面，复用开源框架——v1.4.1 拍板的「轮子全复用」边界）；也不做自然语言指令直转权重（self-distillation 属商业模型层）。

### 通用脱敏管线（业务语义脱敏）

现役脱敏只有 A2 规则的**格式匹配**（AKIA / sk- / ghp_ / PEM 块）+ decision-log `sanitizeWhy`——企业业务语义脱敏（客户名/内部代号/API 响应体业务字段）无通用管线。本版新增 `engine/core/src/export/redactor.ts`：

- **规则配置**：`redact-rules.json`——正则 + 实体名 + 占位符映射（`{CUSTOMER_NAME}`, `{INTERNAL_CODE}`）
- **三类红名单**：格式类（复用 A2 检测）/ 语义类（实体名库，FDE 梳理辅助可录入）/ 结构类（API 响应体字段）
- **验收**：样本导出后 grep 原始企业专有名词 0 命中

### 合规红线（仅脱敏聚合，不落个体级数据）

| 红线 | 规则 |
|------|------|
| **不落个体级数据** | 样本导出仅脱敏聚合，可反推具体企业/具体请求的原始字段一律剥离 |
| **字段白名单** | 可导出：ruleId / ruleType / severity / token 数 / 耗时 / 失败码 / 脱敏后的文本模式 |
| **字段黑名单** | 禁止导出：原始 API 响应体 / 密钥类值 / 企业专有名词 / 用户可识别信息 / 未脱敏代码片段 |
| **导出审计** | 每次导出记 `corpus_export` 事件（谁导的、范围、签名）——导出行为本身受审计 |

### FDE 梳理 → 语料导出衔接（人工基准 = ground truth）

v1.3.2 FDE 梳理辅助产出的 workflow.yml 草稿 + 五要素 + 量化四字段，是企业 eval「访谈替代率」的人工基准（ground truth）——随语料导出（标记 `source: 'human-fde'`，人工基准样本）。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/export/rule-schema.ts` | 新建 | 规则导出格式定义（含 `reward_hint` 段——判定签名/严重度权重/可判定性标注） |
| `engine/audit/src/export/exporter.ts` | 新建 | 规则 + 样本序列化 |
| `engine/audit/src/export/reward-mapping.ts` | 新建 | reward 骨架 + `verifiers.json` 清单生成（机器可判/需人审/启发式三类分桶，2026-08-26 补） |
| `engine/core/src/export/methodology.ts` | 新建 | FDE 方法论结构化导出（锚点解析） |
| `engine/core/src/export/redactor.ts` | 新建 | 通用脱敏管线（业务语义） |
| `engine/core/src/export/sample-aggregator.ts` | 新建 | 五源 + 轨迹聚合 + 脱敏 + 标签（fde-session 第五源 · 2026-08-23 新增） |
| `FDE/GUIDE.md` | 修改 | 关键章节加结构化锚点标记 |
| `engine/mcp/src/tools/corpus-export.ts` | 新建 | MCP `corpus_export` tool |
| `engine/audit/src/cli/index.ts` | 修改 | CLI `sofagent-audit corpus export` 子命令注册（双入口的 CLI 面——audit 包 bin 面，`sofagent corpus export` 经 CLI 桥接可达，2026-09-01 审查补） |
| `engine/audit/src/__tests__/export.test.ts` | 新建 | 单测（规则零遗漏 + 锚点解析 + 脱敏 0 命中） |

### 验收标准

- [ ] 24+3 条规则全部可导出（零遗漏）——**口径以「表下注 A」为准：24 条已实现 + 3 条跳号编号位（A12/A13/E3）以 `status: 'merged-into-A11'` 占位，共 27 个编号位；若用户拍板只导 24 条，本条改为 24 条并显式声明三个 `skipped` 空洞**
- [ ] GUIDE 方法论可结构化导出（五要素/三问判定/量化公式齐全）；锚点标记生效（GUIDE 更新重导即同步）
- [ ] 样本源 + 轨迹聚合导出（脱敏 + 分组 + 标签），含 `source: 'human-fde'` 人工基准——**源数与路径以「表下注 B」为准（llm-calls 异名 `llm-call-trace.ts`、runtime-audit 仅 FORGE 侧实现，拍板前本条不可打勾）**
- [ ] **规则导出含 `reward_hint` 段 + `verifiers.json` 清单**（机器可判/需人审/启发式分桶正确——机器可判类可直接当 reward 函数接线）
- [ ] 导出结果带版本号 + HMAC 签名；脱敏 grep 企业专有名词 0 命中
- [ ] CLI `sofagent corpus export` + MCP `corpus_export` 双入口可用
- [ ] SKILL.md 登记本版 MCP tools（`corpus_export`）—— check-version.sh 工具数校验通过（79→80）
- [ ] `npm test` 全绿

---

## 二、企业专属模型本地权重部署链路

> 🔗 **2026-08-10 归集**：原 v1.3.6「本地模型部署链路」并入本版——模型注册/灰度的 **endpoint 型**能力仍在 v1.3.6 ④（云端/可寻址服务），**权重文件型**（企业专属模型交付）随训练主题归本版。

| 组件 | 能力 |
|------|------|
| **权重文件分发** | 训练产物（LoRA adapter / 量化权重）目录规范 + 版本清单——`model_register` 支持 `source: 'local-path'`（权重文件路径，非 URL） |
| **本地加载** | 从权重目录加载模型（vLLM / Ollama / OpenAI 兼容本地端点）——复用 v1.3.2 `client_type: 'openai-compatible'` 插槽 |
| **版本回滚** | 权重版本切换可回滚（git snapshot 兜底，对齐 model_switch 异常回滚语义） |

> ⚠️ **施工顺序（2026-09-01 审查定）**：本章在第三章之前——第三章 artifact-register 调 `model_register source: 'local-path'`，该 source 类型是本章交付物；先做第三章则端到端验收（自动注册权重产物）不可写。依赖链：第七章·一 → 第一章 → **第二章 → 第三章 → 第四章**。

> 🧭 **现状基线（2026-09-01 实测 · 施工前必读）**：`local-path` 的**类型位与 schema 已全面预留，实现未落地**——`engine/orchestrator/src/model-registry.ts:29` 已 `export type ModelSource = 'endpoint' \| 'local-path'`，`model-register.ts:27` 与 `tool-registry.ts:821` 已声明该取值，但 `model-registry.ts:10-11` 自陈「字段可存，但 local-path 条目**不可切换为活动模型（无可加载权重）**」；权重目录解析 / 版本清单 / 权重加载三者全仓零命中。故本章是「填实现」而非「从零设计」，勿重复造类型槽。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/model-registry.ts` | 修改 | 权重条目落盘与解析（`:29` 类型已有、`:56` endpoint 字段对 local-path 型为「权重目录占位」，补权重目录规范 + 版本清单 + 完整性校验；`:10-11` 的「不可切换为活动模型」限制随之解除） |
| `engine/mcp/src/tools/model-register.ts` | 修改 | `source: 'local-path'` 分支实装（`:9` 注释「本版只处理 endpoint 型」待改、`:58` 已解构 source、`:118` why 串已留 local-path 分支） |
| `engine/orchestrator/src/model-router-config.ts` | 修改 | 本地加载接线——复用 `:42` `provider: z.enum(['ollama', 'openai-compatible'])` 既有插槽，不新增协议 |
| `engine/mcp/src/tools/model-switch.ts` | 修改 | 权重版本回滚（`:8`/`:21` `action='rollback'` 已有，补权重版本级回滚语义，对齐异常回滚不要求人审） |
| `engine/orchestrator/src/__tests__/`（权重部署用例） | 新建 | 注册 / 加载 / 回滚三路径回归用例 |

### 验收标准

- [ ] 权重目录规范 + 版本清单落盘；`model_register` 以 `source: 'local-path'` 可注册本地权重路径（解锁 `model-registry.ts:10-11` 的「不可切换为活动模型」限制）
- [ ] 本地加载可用：权重目录 → vLLM / Ollama / OpenAI 兼容本地端点（复用 `client_type: 'openai-compatible'`，不新引入协议）
- [ ] 版本回滚可用：权重版本切换可回滚（git snapshot 兜底），与 `model_switch` 异常回滚语义一致（止损不要求人审）
- [ ] 端到端全链审计留痕：**电池厂场景**——一张 4090 + 电芯数据训出的 LoRA → 权重目录 → `model_register` 本地注册 → `model_switch` 挂载到产线校准节点，每一步进 `model_registry` 事件链
- [ ] `npm test` 全绿

> 📌 **待拍板（不代为决定）**：权重文件入册是否强制校验哈希/签名（可复用 v1.4.1 `artifact-signing.ts` 的权重 HMAC 链路）。本章设计只写了「目录规范 + 版本清单」，**未含供应链完整性校验**；若要求「无清单或哈希不匹配即拒绝注册」，需在此处补一条验收项。

---

## 三、训练产物 → 模型注册自动衔接（训练闭环的最后一步）

> 📋 **2026-08-10 补充（架构审查新发现）**：训练引擎的「训练完成」和 v1.3.6 的「模型注册」是**断的**——训练完的权重产物不会自动进 `model_register`，差一个自动衔接动作。缺了它，训练引擎闭环不完整（train → eval → 部署 三环节断裂）。

### 交付

```
train job 完成（v1.4.1）
  → eval 通过（v1.4.2 阈值判定）
  → 权重产物打包（第二章目录规范）
  → 自动调 model_register（v1.3.6 ④，source: 'local-path'）
  → model_switch 挂载到 workflow 节点（灰度比例）
  → 全程 train_job + model_registry 审计留痕
```

| 组件 | 能力 |
|------|------|
| **训练产物注册器** | train done + eval pass → 自动调 `model_register`（幂等：同权重版本不重复注册） |
| **挂载建议** | 注册后给出 `model_switch` 建议（哪个 workflow 节点可挂载）——人工确认后执行（晋升强制人审，对齐 v1.3.5） |

> 🔗 **商业平台接口预留 G1（2026-08-17 补充）**：本版「训练产物 → 模型注册自动衔接」与商业产品层的 workflow 模板注册同类机制（商业平台接口规划 §9.1 G1）——商业平台跨企业 fork 需要 `workflow_export`/`workflow_import`（workflow 模板导出/导入 + 血缘追踪），挂靠本版的注册器模式扩展：**模型可注册，workflow 模板同样可注册**（模板元数据加血缘字段，记录「此模板来自哪个企业/哪个 fork」）。**本版先打通模型注册，workflow 模板注册随商业平台 SaaS 需要再补**。

> 📖 **G1 导出口径（2026-09-01 明确 · 缺口 B）**：`workflow_export`/`workflow_import` 的「模板」= **五件套完整导出**——① workflow 定义（DAG）② 各节点 Agent 配置 ③ 节点挂载的工具清单（MCP/CLI）④ 模型绑定（modelPreference，v1.4.8 ⑧）⑤ 验收标准（merge_criteria/approver）。商业平台 §2.1「workflow 包是综合交付物」——fork 交易的就是完整结构，不是裸 DAG。血缘字段随导出物携带（来源企业/fork 链）。
>
> ⚠️ **G1 去向版本待拍板（2026-09-01 审查发现 · 不代为决定）**：上述口径已记，但 `docs/ROADMAP.md` 规划表（v1.4.4~v2.0.0 全部 11 行）**无任何版本单元格承接 G1**——它是 G1~G13 中唯一悬空的缺口（G7/G12/G13 均已落 v1.4.7 / v1.5.1）。违反 ROADMAP 自身「🔴 阻塞项占位纪律：任何阻塞项必须占据一个明确的版本单元格，不得仅写在散文备注里」。
>
> **两个候选（附推荐）**：
>
> | 候选 | 理由 | 反理由 |
> |------|------|--------|
> | **v1.4.7（推荐）** | ① 与 G13 PR 生命周期接口同批——G1 的血缘字段与 G13 的 `merge_criteria`/approver 落在同一套「workflow + 验收元数据」结构上，同批可共用一次 schema 变更；② v1.4.7 已是「商业平台接口版」，G 系列缺口本就归此；③ 与 v1.4.9 设备侧的 G9/G10/G11 同属 G 系列收口节奏 | v1.4.7 已有 G2/G4/G6/G7/G8/G13 六项 + workflow 烧进 USB，负载偏重 |
> | v2.0.0 离线 USB 节点合体 | ① workflow 烧录 USB 已在 v1.4.7，导出/导入是烧录的自然前序；② v2.0.0 收窄后只余「离线 USB 节点合体」一件核心，容量充足 | 太远（v1.5.x 尚有 4 版），商业平台 fork 需求等不及；且 G1 是**接口**（写侧），不该等到交付形态版本 |
>
> **推荐 v1.4.7**：G1 是接口面而非交付形态面，归「商业平台接口版」比归「离线 USB 节点」更合语义；且 G13 同批可摊薄一次元数据 schema 变更成本。拍板后由 ROADMAP v1.4.7 行落单元格，本条注记即作废。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/artifact-register.ts` | 新建 | 训练产物 → model_register 自动衔接 |
| `engine/orchestrator/src/__tests__/artifact-register.test.ts` | 新建 | 单测（eval pass → 自动注册 + 幂等） |

### 验收标准

- [ ] train done + eval pass → 自动调 `model_register`（权重产物）
- [ ] 幂等：同权重版本不重复注册
- [ ] 挂载建议给出（人工确认后 model_switch）
- [ ] `npm test` 全绿

---

## 四、多基座对比训练（train compare · 选型数据支撑）

> 📋 **2026-08-10 补充（训练引擎完善 · 阶段 2 选型前置）**：后训练阶段 2 有两个待测对比——**Qwen3 vs R1-Distill**、**7B vs 14B**（eval 分数 ÷ 训练/部署成本 = ROI）。本版把「同一数据多基座并行训练 → 对比报告」做成引擎能力，选型不用手跑实验。

### 交付

| 组件 | 能力 |
|------|------|
| **对比训练** | `train compare --bases qwen3-8b,r1-distill-7b` → 同一数据并行提交多个 train job（同数据 hash / 同超参模板） |
| **对比报告** | 各基座 eval 分数 + 训练/部署成本 → ROI 排序（复用 v1.4.2 train report） |
| **GPU 调度** | 对比任务走 v1.4.3 GPU 队列（串行 or 预算并发） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-compare.ts` | 新建 | 多基座对比（并行提交 + 汇总） |
| `engine/orchestrator/src/__tests__/train-compare.test.ts` | 新建 | 单测（同数据多基座 → ROI 排序） |

### 验收标准

- [ ] `train compare` 同数据多基座并行提交（同 hash / 同模板）
- [ ] 对比报告含 eval 分数 + 成本 + ROI 排序
- [ ] 对比任务走 GPU 队列（不 OOM）
- [ ] `npm test` 全绿

---

## 五、决策因果链与先例检索（Semantica 启发 · 2026-08-29 排入）

> 🔗 **参照而非依赖**（2026-08-29 拍板）：Semantica（semantica-agi/semantica，Python/MIT/5.5k+ stars）的核心设计「决策不是日志行，是一等公民图节点」值得吸收，但其 Python 栈（gensim/FAISS/Oxigraph）与 sofagent 纯 TS 零重依赖定位冲突——**只参照设计，不引入依赖**。本版把 decision-log 从「完整性很强但无因果边」升级为可查因果链的结构。

### 定位

> decision-log.jsonl 的 HMAC 链保证**完整性**（防篡改），但决策之间没有因果关联——审查者看到的是一串孤立判定。Semantica 的 `CAUSED / INFLUENCED / PRECEDENT_FOR` 三种因果边映射到 sofagent：拦截决策引用触发它的路由决策、HITL 决策引用待审的上游决策。**审计叙事从「每条自证清白」升级为「决策链可追溯」**。

### 交付

| 组件 | 能力 |
|------|------|
| **决策因果边** | `DecisionLogEntry` 加可选字段 `causedBy?: string[]`（引用前序 decisionId）+ `causalType?: 'caused' \| 'influenced' \| 'precedent_for'`——写入时由触发链路带上（拦截→路由、HITL→待审项），HMAC 计算含新字段 |
| **因果链查询** | decision-query 扩展 `traceDecisionChain(id)`——沿 causedBy 回溯至根因，输出链式叙事（A 导致 B，B 触发 C 拦截） |
| **先例检索** | decision-query 扩展 `findSimilarDecisions(query)`——按 `DecisionWhy.tags + triggeredRule` 结构化匹配（不用向量，字段已有，零新依赖）；HITL 审批界面展示「历史类似决策 + 结果」 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/decision-schema.ts` | 修改 | 加 `causedBy` / `causalType` 可选字段（旧条目无此字段照常校验——向后兼容） |
| `engine/audit/src/decision-query.ts` | 修改 | 加 `traceDecisionChain` / `findSimilarDecisions` 查询 |
| `engine/audit/src/decision-query.test.ts` | 修改 | 因果链回溯 + 先例匹配用例（实测位于 `src/` 根下，非 `__tests__/` 子目录——audit 包测试文件两种位置并存，施工勿找错） |

### 验收标准

- [ ] 旧 decision-log（无 causedBy 字段）加载与链校验不破坏（向后兼容）
- [ ] traceDecisionChain 能回溯多级因果链并输出人类可读叙事
- [ ] findSimilarDecisions 按 tags + triggeredRule 匹配，HITL 场景可展示先例
- [ ] HMAC 计算涵盖新字段（链完整性不因 schema 扩展而弱化）

## 六、CI 供应链加固三件套（Semantica 工程治理启发 · 2026-08-29 排入）

> 🔗 参照 Semantica 仓库治理实践（GHSA-j4mq 修复 + LiteLLM/Trivy 供应链攻击防御），原排 v1.4.3 候选，2026-08-29 拍板统一落本版。

### 交付

| 组件 | 能力 |
|------|------|
| **CI Action SHA 固定** | `.github/workflows/` 8 个文件的 `uses:` 从浮动 tag（`@v5`）改为完整 commit SHA（tag 留尾注释）——防第三方 Action 被入侵后窃取 release token（LiteLLM/Trivy 同款攻击面） |
| **pin 验证脚本** | `tools/check/check-action-pins.sh`——核对每个 SHA pin 与注释 tag 指向同一 commit（GitHub API 对账），纳入 CI + 发版门禁 |
| **本地面 fail-closed 自查** | dashboard serve（localhost:3780）与 Explorer 类本地服务面核查——无密钥配置时拒绝服务（503）而非静默匿名，绑定默认 127.0.0.1，`ALLOW_ANONYMOUS` 须显式 opt-in。**已发现实锤**：`tools/dashboard/serve-dashboard.mjs:687` `server.listen(port)` 未传 host——Node 默认绑 `::`（IPv6 全网卡），局域网可达，须改 `server.listen(port, '127.0.0.1')`（2026-09-01 已先行落地为 `DASHBOARD_HOST || '127.0.0.1'`，本章验收第 3 条回归确认即可） |

### 验收标准

- [ ] 8 个 workflow 全部 SHA 固定，浮动 tag 引用清零
- [ ] check-action-pins.sh 能抓到「SHA 与 tag 不一致」的坏样本（用合成坏 pin 测失败路径）
- [ ] dashboard/本地服务面自查结论落 LIMITATIONS 或 SECURITY（有网络面则修，纯本地静态则记录豁免理由）
- [ ] **dashboard 完全离线可用**（2026-08-29 已直接修复：36 图标 SVG 内嵌去 jsdelivr CDN + `bi-microscope` 用 eyeglasses 替代——上游本就无此图标，CDN 时代也是静默空白；本条目为验收固化——断网打开 dashboard 图标零丢失，与 v2.0.0 离线 USB 节点叙事对齐）


## 七、存量升级审查落本版（十六项 · 2026-08-29 用户拍板收编）

> 📋 **2026-08-29 用户拍板**：「老的功能开发好了不能一直放着，看看有什么可以更新或优化的。」升级清单（与 v1.4.3 第十一章清扫相对——清扫是删除型，本章是翻新型）自 v1.4.3 第十二章迁入本版为正式章节。同日全仓发散扫描（文档/代码/工程链/DX 多视角）追加三项消化项（四/五/六）——dashboard 去 CDN 已当场直接修复（36 图标内嵌 + microscope→eyeglasses 上游缺失替代），v1.4.3 im-bridge 死链已顺手修，不占本版条目。三轮复查再追加消化项第七项（install.sh 部署模式）。复核轮 F-15 拍板追加消化项第八项（过期承诺限时检查，机制层——承诺从注释升级为门禁盯防）。

### 本版消化项（一~十 · 发版前完成 · 2026-09-01 编号校正：原「一~九」标题与十项表体不符）

| # | 对象 | 现状证据 | 升级动作 |
|---|------|---------|---------|
| 一 | **eval 三套体系统一** | `engine/eval`（评分引擎独立包）+ `engine/orchestrator/src/benchmark/`（评测设计/执行/MLflow 导出）+ `engine/ab-test`（A/B 对照）——三套各建 runner/scorer | **本版语料导出前置统一**：eval 包为 SSOT scorer，benchmark 与 ab-test 复用其 scoring 层——语料导出五源含 evaluation-log，训练 eval 闭环要求三源同口径（本版第一章依赖） |
| 二 | **79 MCP tools 瘦描述复核** | v1.4.0「瘦描述」交付时 66 tools，现 79（v1.4.1 +1 train_submit / v1.4.2 +9 fde 六件+train 三件 / v1.4.3 +3 train_status+train_list+train_diagnose——累计新增 13 个）覆盖度未复核 | 发版前复核新增 13 个 tools 描述长度与去版本号纪律（对齐 v1.4.0 瘦描述规范；2026-09-01 审查修正：原「76/新增 10」漏 v1.4.3 三件） |
| 三 | **trimMessagesSafe 作用域审计**（自探索方向迁入 · 2026-08-29） | v1.3.4 release-gate 遗留：FORGE 消息裁剪的作用域边界未确认（跨闭包引用同款风险家族——run-12 effectiveHardLimit 同族） | 本版发版前完成作用域审计：确认裁剪函数无跨闭包引用，必要时加固 + 回归用例 |
| 四 | **13 包 engines 字段统一**（全仓扫描发现 · 2026-08-29） | `audit`/`mcp` 已声明 `engines: {node: '>=18'}`；`core`/`orchestrator`/`daemon` 等 9 包未声明——Node 16 用户装包得到运行时玄学错误而非清晰版本提示 | 13 包 package.json 统一补 `engines`（CLI 入口包优先），与根 README Node 版本要求对齐 |
| 五 | **npm 包 README 门面补齐**（全仓扫描发现 · 2026-08-29） | `audit`（366 行）/`eval`（92 行）达标；`orchestrator` 仅 10 行、`core`/`daemon`/`skillopt`/`think`/`rules` 各 9 行——npm 页面 13 包中 8 个门面近乎空白，核心包 `@sofagent/orchestrator` 尤甚 | 模板化补齐：每包「职责一句话 + 主入口 + 安装 + 文档链接」30 行内（不写长文），发版前一次完成 |
| 六 | **check-anchors 文件存在性断言**（全仓扫描发现 · 2026-08-29） | 锚点门禁只查「章节锚点」不查「链接目标文件存在」——本轮实测 664 链接中 6 处文件级死链/假阳性（v1.4.3 im-bridge 相对路径错一级为真死链，已顺手修；release SOP 模板占位符 `v{major}.{minor}` 4 处为假阳性需白名单） | `check-anchors.mjs` 加文件存在性断言（约 20 行）+ 模板占位符白名单，纳入发版门禁 |
| 七 | **install.sh 静态资源部署模式升级**（三轮复查发现 · 2026-08-29） | install.sh:713-719 只拷 `dashboard.html` + `serve-dashboard.mjs` 两文件到 `~/.sofagent/web/`——任何未来对 `docs/assets/*` 或新增静态资源的引用都会在安装态断链（v1.4.4 header logo 断链事件的根因之一，已内嵌 data-URI 当场修复但模式性风险仍在） | 部署段从「白名单两文件」升级为「tools/dashboard/ 目录整体同步（rsync 或 cp -R）」+ serve-dashboard 静态根指向 web/——新增资源零改动自动部署；同步更新 install.sh 提示文案 |
| 八 | **「将在 vX.Y.Z 移除」过期承诺限时检查**（复核轮 F-15 拍板 · 2026-08-29） | engine/ 内存在 `将在 v1.5.0 移除` 三处弃用 shim 承诺（audit CLI `engine/audit/src/index.ts:332/:693/:734`）——承诺只活在注释里，到期无人盯防即静默过期（v1.4.4 第六章 TODO(v1.4.0) 拖两版本才收口是同款事故） | check-docs（或独立定期脚本）加过期承诺检查：`grep -rn "将在 v[0-9.]* 移除" engine/` 逐条解析承诺版本，承诺版本 ≤ 当前版本且 shim 仍在即告警——与 v1.5.0 六）shim 兑现移除联动，承诺从「注释自觉」升级为「门禁盯防」 |
| 九 | **FDE 三层交付模板外置**（2026-08-30 用户拍板直接排入） | 引擎五 `distillDeliverables` 的 docLayer/skillLayer 渲染骨架硬编码在 `engine/orchestrator/src/fde/fde-quantify.ts` 字符串数组里——FDE 每个企业要定制交付物样式，现在改个模板骨架就得走包发版 | 模板骨架外置到 `FDE/templates/deliverables/`（engine 运行时读取渲染，含占位符约定）+ 模板缺失/占位符不全时 fail-closed 回退内置默认 + 模板结构断言测试——模板优化从「发版」降级为「改文档」，FDE 现场即可定制（hugozhu《FDE 的 20 个问题》「问变成查」方向：能力沉淀从代码层移到文档层，Plugin/Skill 不频繁升级也能进化） |
| 十 | **DSH 内嵌执行路径 cordis 断裂修复**（v1.4.3 发版闸门 2026-08-30 拍板记待办） | v1.4.0 拍板「CLI 桥接 = 当前正式形态」——但内嵌路径 execution-backend 走 cordis 的分支在并行 session 升级中断裂（v1.4.3 闸门 precheck 发现；本版不阻塞发版：验收口径「全 step 走 dsh」不分内嵌/CLI） | 修复内嵌路径 cordis 分支接线 + 内嵌/CLI 双路径回归用例（同一 workflow 两种执行面同结果），修复后与「CLI 桥接正式形态」口径一并复核——若双路径长期并行成本过高，评估收敛为单一路径 + 另一路径显式弃用声明 |

### 登记分发项（去向版本已定，本版只登记）

| # | 对象 | 去向 |
|---|------|------|
| 十一 | 加载链 token 预算（当前仅 persona 500 字符/knowledge 2000 字符截断——harness/src/index.ts:147，非 token 级） | v1.4.8 ④ 自动上下文压缩（触发条件依赖 token 计量） |
> 注：原编号「十」与消化项「十（DSH cordis 修复）」撞号，2026-09-01 校正为十一起顺延（十二~十六 同步 +1，正文引用同步） |
| 十二 | webhook 事件源扩展（三态推送目前仅审计场景→训练完成/治理周报/死信告警统一事件源） | v1.5.1 事件总线（webhook 入站+出站统一；v1.5.x 顺延后编号，2026-08-31 校正） |
| 十三 | daemon 巡检 28 项归组（inspectors/ 粒度参差——commons-health 与 commons-catalog-daily 合并候选） | v1.5.0 治理 KPI 面板（面板化前先归组，避免面板读 28 个源） |
| 十四 | IM 桥一等公民评估（install.sh 默认不装——FDE Harness 执行通道叙事下应重估） | v1.5.0 FDE 陪跑期补全联动（陪跑日报经 IM 推送是自然场景） |
| 十五 | 证据强度分级（VALIDATION/THANKS 有来源纪律无强度分级——按「公开可查/用户自报/自测自报」三级标注） | v1.4.5 措辞收紧联动（进化样本报告先落地三级标注——随进化引擎前移，2026-08-31 拍板） |
| 十六 | 本体混合检索评估（向量召回+图谱扩权——Semantica 实测同任务 token 38k→12k 省 60%、准确率 82.1%→89.2%） | v1.5.0 ②双时态联动评估（同一 schema 层变更） |

### 验收标准

- [x] eval 统一：三套评分同口径（benchmark/ab-test 引 eval 包 scorer，无重复实现）——**d8d03616 落地**：evalBridgeScoringFn 桥接（动态 import + 非 JSON 回退）+ ab-test v1.4.2 起直引 evalCase；零信任核查三目录零重复评分实现（grep 三维度关键词仅命中桥接注释与 SSOT 本体）
- [ ] 79 tools 瘦描述复核完成（新增 13 个对齐规范——v1.4.1×1 + v1.4.2×9 + v1.4.3×3）
- [ ] trimMessagesSafe 作用域审计结论落盘（无跨闭包引用风险或已加固）
- [ ] 13 包 engines 字段全声明且与根 Node 版本要求一致
- [ ] 13 包 npm README 门面达标（职责+入口+安装+文档链接）
- [ ] check-anchors 文件存在性断言上线（真死链清零 + 模板占位符白名单不误报）
- [ ] install.sh 部署段升级为目录同步（tools/dashboard/ 整体部署，新增静态资源零改动自动可达）
- [ ] 过期承诺限时检查上线（注入「将在当前版本移除」合成坏样本能告警，v1.5.0 shim 三处到期前门禁先红）
- [ ] FDE 三层交付模板外置（FDE/templates/deliverables/ 可定制生效：改模板不改代码，渲染输出与内置默认逐字节等价；模板坏样本 fail-closed 回退 + 结构断言测试在位）
- [ ] DSH 内嵌路径 cordis 断裂修复（内嵌/CLI 双路径回归用例通过：同一 workflow 两种执行面同结果；或已收敛单一路径 + 弃用声明）
- [ ] 分发项六项全部有明确去向版本（上表右列；2026-09-01 编号校正后为十一~十六）
- [ ] `npm test` 全绿

## 八、spec-first 硬禁令（OpenFDE 启发 · transcript 永不直驱代码 · 2026-08-30 排入）

> 📋 **2026-08-30 用户拍板**：探索方向表挂了多时的「spec-first 硬禁令（OpenFDE 启发 · 设计约束）——单一事实源：transcript 永不直驱代码，spec 才是唯一驱动」一直停在方向格。本版把断言基建已就绪（第六章 check-anchors 文件存在性断言同批交付）的条件用上，落成可执行的门禁 + 纪律双层交付。

### 定位

单一事实源纪律的工程化：**代码变更必须可追溯到 spec**（业务流声明 / 任务书 / 变更说明），conversation transcript（对话记录、访谈原文）本身永远不是改代码的依据——transcript 先沉淀为 spec（workflow.yml / fde.md / task 书），spec 才驱动实现。这与 SKILL 既有「先读再用」纪律同源，补的是「留痕可对账」这层机制。

### 交付

| 层 | 组件 | 能力 |
|------|------|------|
| **门禁层** | check-spec-first.mjs（tools/check/） | 扫 commit message / PR 描述与变更文件：涉及 `engine/*/src` 的代码提交，message 须含 spec 关联标记（`spec:` 前缀引用 workflow/fde/task 文件路径，或 `no-spec: <理由>` 显式豁免声明）；无标记无豁免即 WARN——**只提示不阻断**（新机制渐进纪律，跑一个版本观察误报率再议升级 FAIL） |
| **纪律层** | SKILL/skills/ 规范升级 | 五步循环的「实现」步前插入 spec 引用要求：Agent 改代码前先声明「本变更对应哪个 spec」——transcript 直出的口头需求须先落 fde.md/task 书再动代码；SKILL.md 加载链 L1 铁律区同步一行 |
| **对账层** | doctor 联动 | `--doctor` 输出近期 N 条 commit 的 spec 关联覆盖率（纯展示，不判 FAIL）——让「多少代码变更是 spec 驱动的」从不可见变为可运营的数字 |

### 涉及文件（预估）

| 文件 | 改动 |
|------|------|
| `tools/check/check-spec-first.mjs` | 新建（约 80 行：git log 解析 + 标记/豁免识别 + WARN 输出） |
| `SKILL/SKILL.md` | 加载链 L1 铁律区 +1 行（spec-first 一句话纪律） |
| `SKILL/skills/04-deliver.md` | 实现步前 spec 引用要求 |
| `engine/core/src/doctor.ts` | spec 关联覆盖率展示段（读 git log 纯展示） |

### 验收标准

- [ ] check-spec-first.mjs 对「有 spec: 标记」commit 零告警、「无标记无豁免」commit WARN、「no-spec: 理由」豁免不误报（三态实测）
- [ ] SKILL.md 铁律区含 spec-first 一句话纪律，执行步含 spec 引用要求
- [ ] doctor 输出 spec 关联覆盖率数字
- [ ] 自查：本仓库近 30 条 commit 跑一遍，误报率观察记录随发版检查落盘
- [ ] `npm test` 全绿

### 与 OpenFDE 的对齐边界

只借「单一事实源」纪律形态，不引入其工具链——sofagent 的 spec 载体是自有体系（workflow.yml / fde.md / task 书），检查器是自有门禁（tools/check/ 家族），与 v1.4.3 第九章 onboarding 走查的「不断头路」原则一致。

---

## 九、六轮审查 17 项收编批（fresh-eyes 29 视角 · 2026-08-31 拍板）

> 📋 **2026-08-31 用户拍板**：六轮独立审查（含团队子代理轮 + 对账轮，29 视角报告三桶视图——报告留档作者机不入库）的桶一 17 项立即修复项全部收编进本版。分桶判据：改动小、风险低、独立可验、不依赖产品决策。原台账编号保留以便溯源。**裁决阶段零上下文复验全部锚点**，5 处证据漂移已修正（#2 半拉子修复维持 P0、#32+47「daemon init 即修」不成立、#49 定性改「已实现未接线」、#73 占位实为 4 个、#16 已消解撤销）。

### 修复清单（17 项 · 按报告台账编号）

| # | 级别 | 问题 | 修复动作 | 工作量 |
|---|---|---|---|---|
| 2 | 🔴 P0 | `audit.cost.budget` 配置被静默丢弃（89e8d856 已加告警白名单，merge 透传仍缺——半拉子） | mergeWithDefaults 补 cost 透传行（config-loader.ts:600-614）+ topLevelAuditKeys（:422-426）+ 契约测试 | 一行+测试 |
| 3 | 🔴 P0 | SOFAGENT_KEY_PATH / HOME_ALLOWED_PREFIXES 未披露 | SECURITY.md 补两行威胁模型（对齐 SOFAGENT_DATA 格式） | 纯文档 |
| 58 | 🟡 P1 | webhook SSRF 对 IPv6 字面量整体失效 | isPrivateWebhookUrl（webhook.ts:32）剥方括号一行 + IPv6 测试用例 | 一行+测试 |
| 59 | 🟡 P1 | pre-commit hook 未知退出码静默放行 | 退出码白名单（0/1/2 放行、其余 fail-closed） | 小改 |
| 60 | 🟡 P1 | `--reset-chain` 幽灵指引（4 处文档推荐不存在的参数） | 二选一：实装 flag 或删 4 处指引（doctor.ts:435/:444、verify.ts:49/:65） | 小改 |
| 61 | 🟡 P1 | `--history` 幽灵旗标 + SKILL.md 悬空引用 | HANDBOOK:338 改 --timeline；SKILL:175 修工具表引用 | 纯文档 |
| 48 | 🟡 P1 | @sofagent/mcp 幽灵依赖 js-yaml（独立安装即 MODULE_NOT_FOUND） | mcp package.json dependencies 加 `"js-yaml": "^5.3.0"` | 一行 |
| 24 | 🟡 P1 | SECURITY/LIMITATIONS 引用已退役 API checkHistoryChainIntegrity | 两处（SECURITY.md:259、LIMITATIONS.md:204）改 checkHistoryChainDetailed | 纯文档 |
| 36 | 🟡 P1 | SECURITY automerge 安全声明整段失真 + 4 处活文档漂移 | 重写 §五段（@automerge/automerge@^3.4.1 实态）+ 同步 ARCHITECTURE:420、team-collab:22/:49、releasing/02-dev:65 | 文档批 |
| 12+26+33 | ⚠️ P2 | 文档数字漂移三处（281→284、76→79×2、README:59 混排） | FDE/GUIDE:949、WIKI:207、README:59 三处数字更新 | 纯文档 |
| 38+78 | ⚠️ P2 | 「工作流」术语漏网 4 处 | SECURITY:305、audit/README:289/:291、WIKI 统一改「业务流」 | 纯文档 |
| 13+44 | ⚠️ P2 | audit-history.ts 头部 v1.3.2 时代注释（两份） | 注释更新为 atomicAppendSync 实态；统一 v1.3.2/v1.3.1 编号 | 注释 |
| 14 | ⚠️ P2 | install.sh 自述冻结（941 vs 1372 行）+ @latest 浮动 | 头部行数更新；@latest 改固定版本 | 注释+一行 |
| 15 | ⚠️ P2 | SECURITY 目录图缺 keys/、automerge 版本（并入 #36 批） | 目录图补 keys/ | 纯文档 |
| 42 | ⚠️ P2 | audit.md 落盘零脱敏（history.jsonl 有双层脱敏） | audit-log.ts 接 sanitizeFreeText（REDACTION_PATTERNS SSOT） | 小改 |
| 32+47 产品半 | 🟡 P1 | daemon 守护死亡无告警（exit 78 无检测 + writeHealthFile 诞生即死 #62 同族） | 接线心跳写 + 退出码检测 + doctor 读同一路径（环境半=plist 手工修，不占代码项） | 小改 |

> 📖 **计数口径**：「17 项」= 六轮审查报告桶一台账条目数（报告留档作者机不入库）；上表按落盘批次合并呈现（#12+26+33 三合一、#38+78 二合一、#13+44 二合一、#32+47 二合一），**表行数与项数不一一对应**，溯源以台账编号为准。下文 A 批「13 项」= 唯一台账编号数（#3/#12/#13/#14/#15/#24/#26/#33/#38/#61/#70/#71/#78，合并编号展开去重）——commit `dd72c20d` message 的「9 项」系按改动文件组计，口径以本注为准。

> 附带收编 3 项顺手修（不占 17 项口径）：#20 daemon 两处 IM 推送复用 isPrivateWebhookUrl（两行接入）；#63 commit-msg hook 追加与 eval 写路径收口（与 #42 同批）；#46 accumulateWarnings 下沉 core（一行测试改 import）。**落盘状态核实（2026-09-01 实测）**：#20 已落盘（`engine/daemon/src/webhook/index.ts` + `push-target.ts` isPrivateWebhookUrl 引用在位✓）；#46 全仓 grep `accumulateWarnings` 零命中——函数名不符或未落盘，施工时先核实实际函数名再定夺；#63 待施工核验（tools/hooks/ 仅见 sofagent-precommit.sh）。另 **#37（根 `package.json` `overrides` 段 6 条本机绝对路径）是发版收编必做**——不是代码修复，但若直接提交此配置，干净机器 `npm ci` 即炸。**实证对象校正（2026-09-01 实测）**：污染不在 `package-lock.json`（`dsh-deployed` 零命中；含 dsh 的 232 个 `packages` 条目 `link` 字段全为 null），而在根 `package.json:50-55` 的 `overrides` 段——6 条 `/Users/kongfangxun/.local/share/dsh-deployed/node_modules/@deepseek-ai/cordis*`，已随 v1.4.3 公开且至今在远端 main；防复发场景 S361 原只查 lock、未覆盖 `package.json`，故漏检——**此项已由 `f5d55b26` 补面**：S361 现双文件（`package-lock.json` + `package.json`）双关键字（`dsh-deployed` + `/Users/`）同查，负例实测「注入 `dsh-deployed` 路径判 FAIL」「仅改目录名判 FAIL」非空转，本条防复发缺口已闭合。**本版收口（删除 `overrides` 整段 · 2026-09-01 已落地 `f5d55b26`）**：修法是**删除整个 `overrides` 段**（该键现已不复存在），**不是**改为 registry 版本号——实测删除前后 `npm install --package-lock-only` 重解析，`package-lock.json` diff 为 0 行、六个 cordis 包 `version` 与 `resolved` 完全一致（均 `registry.npmjs.org`、`link: false`），证明这 6 条是惰性死配置、从未生效，删除为零行为变更；反之若写死六个 registry 版本号（`@deepseek-ai/cordis` 4.0.2 / `cordis-plugin-timer` 1.1.4 / `cordis-plugin-group` 1.0.2 / `cordis-plugin-hmr` 1.0.17 / `cordis-plugin-include` 1.0.7 / `cordis-plugin-loader` 1.0.3）则等于新增恒强约束，将来 dsh 父包若要求其他 range 将触发 ERESOLVE，属引入新风险。**本条口径与代码侧均已闭环，本版无需再施工。**

### 桶三裁决归位（8 项存疑的落点）

- **落本版**：#70-core（core/package.json:7「不含审计逻辑」描述改一行）、#71-①（think-generator.ts:16 注释改 getDataDir 实态——实现站对了边，注释漂移）、#71-②（drillDshStableGuardRelease JSDoc 补「检查一~三为代码事实断言非运行时探测」定性说明）、#73-占位（4 个纯占位测试重写：cron/constraints/think-generator/inspectors——SSOT 数字走 check-test-count 既有基线流程）
- **维持桶二不动作**：#34（#57+74 锁死 v1.4.7 硬前置，接线不得先行）、#25（ROADMAP v1.4.3 状态列维持三态，发版自然消除）、#49（推荐 B：标注预留 + 接线排 v1.5.x 与 #8b 网关同批，最终归属随 v1.5.0 定稿）

### 执行进度（2026-08-31 · 三批推进）

**A 批 · 纯文档/注释类 13 项已修**（commit `dd72c20d`，2026-08-31 02:50）：#3 / #24 / #61 / #12+26+33 / #38+78 / #13（core 半复核不成立，实修 audit 版） / #14 / #15 / #70-core / #71-① / #71-②。修复中两处复核修正：WIKI「工作流」命中为黄仁勋引文（行业观点转述，术语口径注豁免不改）；README:59 版本段数字为 v1.4.2 历史快照（加历史锚标注，不改数为历史事实）。门禁：check-anchors 236/236 · check-docs 全绿 · check-version 102/102 · tsc 冒烟 · 13 文件 U+FFFD 零。

**B 批 · TDD 代码修复 4 项已随 v1.4.3 落盘**（commit `d6907083`，2026-08-31——v1.4.3 发版流程走完后当场执行，原文「排程本版开发批」已过时，2026-09-01 校正）：#2 cost 透传（config-loader mergeWithDefaults + topLevelAuditKeys + 契约测试）、#58 IPv6 SSRF（剥方括号 + 测试）、#59 退出码白名单（fail-closed + 测试）、#42 audit.md 脱敏接线（sanitizeFreeText + 测试）——**本版仅回归验证，勿重复实现**。

**C 批 · 顺手修 4 项当场执行**（2026-08-31 拍板）：#60 删 4 处 `--reset-chain` 幽灵指引（`--init --reset-chain` 从未实装；实装属新功能应走规划而非顺手加）、#36 SECURITY automerge 段重写 + 4 处活文档同步、#73 占位测试重写（4 个纯占位文件）+ #48 幽灵依赖一行。

### 验收标准

- [ ] #2 契约测试：YAML 配 `audit.cost.budget` 后 `loadEnvConfig().cost.budget` 真实可达（audit/index.ts:1258 消费点非 undefined）
- [ ] #3/#24/#36/#15 SECURITY.md 威胁模型与描述实态对齐（退役 API 引用清零、automerge 段重写、SOFAGENT_KEY_PATH/HOME_ALLOWED_PREFIXES 披露在位、目录图含 keys/）
- [ ] #58 IPv6 测试用例：`http://[::1]:3000/hook` 等 IPv6 字面量私有地址被拦截（方括号剥离后判定）
- [ ] #59 未知退出码（如 127/78）fail-closed 白名单测试
- [ ] #60/#61 幽灵旗标处置定案（实装或删除，全仓 `--reset-chain`/`--history` 残留指引清零）
- [ ] #48 mcp 独立安装冒烟（干净目录 npm install 后 import 不炸）
- [ ] #42 audit.md 落盘含 REDACTION_PATTERNS 脱敏（与 history.jsonl 同口径）
- [ ] #32+47 产品半：daemon exit 78 可被 doctor 感知（心跳文件 + 检测路径在位）
- [ ] 桶三 4 个纯占位测试重写（非同义反复断言）+ check-test-count 基线同步
- [ ] 文档批全量：数字三处（#12+26+33）、术语 4 处（#38+78）、audit-history 注释（#13+44）、install.sh 行数与 @latest（#14）
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.4 的什么 |
|---------|-------------------|
| v1.4.5 | 语料导出是持续后训练管线的底座；多基座对比是阶段 2 选型数据来源（v1.4.5 已排期——进化引擎实证收口前移件，2026-09-01 校正原文「若排期」） |
| v2.x | meta-harness 承接训练任务的多 harness 编排；Dashboard 展示训练报告 |
| **模型层（商业侧）** | ① 训练语料导出 = 训练信号前置 ② 企业专属模型本地权重部署 = 专属模型落地上线 ③ 产物注册衔接 = 训练→部署闭环（train done → eval pass → model_register）④ 多基座对比 = 阶段 2 选型（Qwen3 vs R1-Distill / 8B vs 14B ROI）的引擎化 |

> 📖 行业对标：语料导出对齐合规红线（仅脱敏聚合，不落个体级数据）；多基座对比对齐 Qwen3.8-27B 三方横向对比思路。
