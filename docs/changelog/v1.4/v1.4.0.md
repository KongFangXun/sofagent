# v1.4.0 开发日志 — Web 工作明细页（企业 AI 工作记录视图）

> 📋 **已排期。** 前置依赖：v1.3.9（worklog 聚合数据层 + `data/dashboard/worklog.json` 落盘）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-09（版本号重新启用——原 v1.4.0 内容已前移 v1.3.7/v1.3.8）

## 背景

> 🔗 **版本号重新启用说明**：2026-08-09 版本重排时原 v1.4.0（沙箱/权限/网关/加密/WAL）全部前移 v1.3.7/v1.3.8，v1.4.0 留空。本版重新启用 v1.4.0 承载新排期：**AI 工作明细的 Web 展示层**（Startwork 启发）——数据罗列层（worklog 聚合 + `worklog_query` MCP + `worklog.json` 落盘）已在 v1.3.9 排期，Web 页单独一版（v1.3.9 已承载 AST 引擎 + meta-harness 两个大交付）。

**Web Dashboard 前端已就绪**（`dashboard.html` 单文件控制台 6 页面 + `tools/serve-dashboard.mjs` 静态服务，数据模式 = tryFetch 读 `data/` 目录文件 + File System Access 选目录 + 示例数据降级）——本版在现有控制台上加「工作明细」页/区块，无需新建 Web 基建。

## 交付

### Web 工作明细区块（dashboard.html）

| 区块 | 内容 |
|------|------|
| **按 Agent 的工作明细** | 某 Agent 在时间段内：执行了哪些任务（taskId）→ 调用了哪些工具 → 结果（PASS/FAIL/被审批拒绝）→ 消耗（token/耗时/成本） |
| **按 Workflow 聚合** | 整个 workflow 的节点执行情况：哪些节点在跑/跑完/失败/人工介入——业务进度视图 |
| **按周趋势** | 节点活跃度 / 成功率 / 成本趋势（复用驾驶舱趋势底座） |
| **人工介入记录** | HITL 审批/纠正事件（谁批的、为什么）——「人参与 AI 工作」的可追溯视图 |

**数据接入**：读 v1.3.9 落盘的 `data/dashboard/worklog.json`（与现有 `daemon-health.json` 读取模式一致）；聚合数据未生成时降级示例（对齐现有 ds-sample 模式）。

### 终端 ASCII 视图（补充 v1.3.9 可选交付）

若 v1.3.9 终端 ASCII 列表视图未开发完，本版补齐（复用 v1.2.3 渲染底座，开发/FDE 自用）。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `dashboard.html` | 修改 | 新增「工作明细」区块（nav 页 + tryFetch `worklog.json` + 降级示例） |
| `tools/serve-dashboard.mjs` | 修改 | 静态服务无需改（读 data/ 文件模式已支持）；如需 `/api/worklog` 聚合接口则补 |
| `engine/daemon/src/dashboard/worklog-view.ts` | 新建 | 终端 ASCII 列表视图（v1.3.9 未交付时本版补） |

### 验收标准

- [ ] dashboard.html「工作明细」区块可读 `data/dashboard/worklog.json`（无数据降级示例）
- [ ] 按 Agent 明细 / 按 Workflow 聚合 / 按周趋势 / 人工介入记录四个视角可切换
- [ ] 与驾驶舱现有区块风格一致（复用组件，不另起样式）
- [ ] `node tools/serve-dashboard.mjs` 启动后工作明细可访问
- [ ] `npm test` 全绿

---

## 📊 可视化 DAG 画布（DataFlow 启发 · v2.x 前移）

> 📖 **来源**：DataFlow——Dashboard 补「workflow 可视图」，会话 Agent 与 DAG 画布实时同步同一 pipeline 表示。

### 定位

v1.4.0 已交付 Web 工作明细页（工作记录视图），DAG 画布是其天然扩展——工作明细页是「表格视角」（按 Agent/Workflow/周列出任务），DAG 画布是「图视角」（同一 pipeline 用节点+边可视化）。两个视角共享同一数据源（worklog.json），只是渲染方式不同。

### 交付

- DAG 渲染：读 workflow.yml + 运行时状态（worklog.json），渲染为节点+边的有向无环图
- 节点状态实时同步：running（高亮）/ done（绿色）/ failed（红色）/ hitl-waiting（黄色脉冲）
- 会话 Agent 与画布双向同步：Agent 跑到哪步 → 画布对应节点高亮；点击画布节点 → 跳到该节点的审计日志
- 拖拽布局：用户可手动调整节点位置（保存到 `.sofagent/dashboard-layout.json`）

### 验收标准

- [ ] DAG 画布渲染 workflow.yml 的节点+边
- [ ] 节点状态实时同步（running/done/failed/hitl-waiting 四态）
- [ ] 点击节点跳转审计日志
- [ ] 拖拽布局可保存

---

## 🖥️ TUI / 对话分支回溯（DeerFlow 启发 · v2.x 部分前移）

> 📖 **来源**：DeerFlow——终端 UI + 可视化面板 + 对话分支回溯。

### 定位

v1.4.0 的 Dashboard 部分（Web 工作明细 + DAG 画布）已覆盖「可视化面板」。本项补另外两部分：① 终端 TUI（给不喜欢开浏览器的开发者/FDE 用，纯终端看 dashboard）；② 对话分支回溯（依赖 v1.3.5 decisions.jsonl 完整版——Agent 做过的每次选择可以回溯查看「如果当时选了另一条路会怎样」）。

### 交付

- 终端 TUI dashboard：复用 v1.2.3 控制图渲染底座，把 Web 工作明细 + DAG 画布的核心信息在终端用 ASCII 渲染
- 对话分支树：读 decisions.jsonl，按 `moment` 构建分支树，用户可在终端浏览历史决策点 + 查看「该节点选了什么 / 放弃了什么」

### 验收标准

- [ ] 终端 `sofagent-dashboard --tui` 可渲染工作明细 + DAG 概要
- [ ] 对话分支树可按 decisions.jsonl 构建
- [ ] 分支节点可查看「选了什么 / 放弃了什么」

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.0 的什么 |
|---------|-------------------|
| v2.x Dashboard 产品化 | 工作明细 Web 页作为「Dashboard 产品化」的起点（多租户云端看板在此基础上扩展） |
| 模型层（商业侧） | 工作明细页为自优化闭环提供运行观测入口（企业客户视角） |

> 📖 行业对标：AI 工作明细启发自 Startwork（startwork.im，企业 AI 管理平台）——把「人与 AI 的工作过程」整理成业务可读的工作记录。
