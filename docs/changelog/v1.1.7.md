# v1.1.7 开发日志 — Dream Cycle 6 阶段管道 + sensitivity + v1.1.6 发布后问题修复（15 项）

> 状态：✅ 已发布 · 孔放勋 · 2026-07-15（BugFix 主线 2026-07-20 补入，新功能主线 2026-07-20 完成）
> 前置依赖：v1.1.6（conflict-check 已就绪）

## 背景

v1.1.7 是**合并版本**——同时交付两块：

- **新功能主线**：知识沉淀从「散点脚本」升级为「流水线 + 治理 + 可观测」。v1.1.6 能检测知识库的毛病了，但知识本身还是靠 daemon 两个老脚本（weekly-report / lessons-extract）散点生成。v1.1.7 用 gbrain 的 Dream Cycle 精简版（6 阶段 pipeline）替换它们，把知识沉淀从散兵升级为流水线。同时给 knowledge/ 加 sensitivity 字段，为后续联邦查询的安全过滤铺路。
- **BugFix 主线**（15 项）：v1.1.6 发布后三份独立审查（fresh-eyes review）发现 15 项问题（2 P1 + 12 P2 + 1 P3），全数修复。核心方向：文档数字/日期漂移、门禁基线错误、产品关系预期、企业部署痛点、安全文档滞后、Skill 元数据残缺、red-team 缺口

---
### 设计输入：操作型本体论的读写回路

> 来自 Palantir 操作型本体论系列研报（2026-07）的启发。

Palantir 操作型本体论的核心是一个 5 节点闭环的**读写回路**：读（数据集成）→ 逻辑（规则编排）→ 写（事务回写）→ 反馈（持续学习）→ 治理（全链路审计）。传统知识图谱只覆盖前两节点——能查询，不能操作，不能反馈，不能治理。

sofagent 的 Dream Cycle 本质上是在做一个**迷你读写回路**：

| Palantir 回路 | Dream Cycle 对应 | v1.1.7 实现 |
|------|------|------|
| 读 | Ledger 层原始数据 | think.md + audit history（已有） |
| 逻辑 | 从原始数据提取模式 | extract_facts → extract_atoms → cluster_patterns（新增） |
| 写 | knowledge/ 写入 | synthesize_concepts → knowledge/ entities/（新增） |
| 反馈 | 持续学习回路 | skillopt_backfill → fde.md 优化（新增） |
| 治理 | 全链路审计 | conflict-check + sensitivity 过滤（v1.1.6 已有 + 本版增强） |

**关键差异**：Palantir 依赖集中式 Ontology OS + 重度物化索引。sofagent 走分布式路线——每个设备维护自己的 knowledge/，联邦查询按需获取。**我们不建中央本体，我们让每个 Agent 自己长出本体**。

### 交付一：Dream Cycle 6 阶段（P0）

gbrain 21 阶段的精简版——对约束沉淀真正有用的前半段：

```
extract_facts → extract_atoms → cluster_patterns
             → synthesize_concepts（喂 @sofagent/ontology）
             → skillopt_backfill（回灌自进化）
             → embed（向量化，供未来检索）
```

- `@sofagent/daemon`：stage 状态机替换 weekly-report / lessons-extract
- `@sofagent/ontology`：加 synthesize 接口
- `@sofagent/skillopt`：加 backfill 钩子

### 交付二：sensitivity frontmatter（P1）

entity/concept 的 frontmatter 新增 `sensitivity: public | internal | restricted` 字段。联邦查询时按 sensitivity 过滤结果。

### 交付三：knowledge-health inspector（P1，新增）

> 来源：外部笔记研读（个人 LLM Wiki 治理实践）+ v1.1.6 conflict-check 范式复用。

v1.1.6 的 `conflict-check` 管「项目文件矛盾/孤儿/死链」；本交付管「知识库健康」——Ledger-Views-Policy 三层知识目录的孤立、重复、断链、陈旧、缺源。与交付一（Dream Cycle 生成 fact/atom/concept）形成「生成 + 治理」闭环，是 Palantir 读写回路「治理」节点的补强。

- **文件**：`sofagent/daemon/src/inspectors/knowledge-health.ts`（与 `conflict-check.ts` 同形态）
- **注册**：`sofagent/daemon/src/inspectors/index.ts` 加 `'knowledge-health': { enabled: true, schedule: '@weekly' }`，与 conflict-check 同节奏，跑在外层 LOOP compliance 扫查
- **检查项**（适配 sofagent 知识目录）：
  1. 孤立页：`knowledge/` 中无任何其他 entity/concept/fde.md 引用的条目
  2. 重复页：标题或 normalized key 高度相似 / embedding 近邻的 concept（可复用交付一的 `embed` 产出）
  3. 断裂链接：`knowledge/` + `think.md` / `fde.md` 中 markdown/wikilink 指向不存在目标
  4. index 过旧：`index.md` / `log.md` 的 mtime 明显早于 `raw/` 或 `knowledge/` 最新源
  5. 来源缺失：concept/claim 无 `source:` frontmatter 或回指 `raw/` 的引用
- **行为**：fail-closed、只读（仅 `readdirSync`/`readFileSync`/`existsSync`）、输出 `InspectorResult`（finding 级别为 warning，非 critical——健康是建议性的）、**只建议不自动删**（与笔记②治理纪律一致，呼应 conflict-check 的 fail-closed 只读范式）
- **与 sensitivity 的关系**：巡检报告生成时按 `sensitivity` 过滤，restricted 条目不向未授权上下文泄露
- **测试**：`sofagent/daemon/src/inspectors/__tests__/knowledge-health.test.ts`（镜像 `conflict-check.test.ts`，8 case）
- **依赖**：无新增（复用 `fs`/`path`、既有 `InspectorResult` 类型、daemon 知识目录解析）

### 交付四：知识可观测性（LUI 感知 A+B，P1）

> 来源：原 changelog「LUI 感知」段落 concretized。用户要"看见"后端三件事在跑。

**A · 可感知产物（折入交付一/二/三验收）**：
- 交付一：Dream Cycle 每次 nightly 跑完追加 `knowledge/log.md` 周报（"本周学 N 个 concept，来自 M 条 audit"）
- 交付二：sensitivity 分类结果在 `sofagent knowledge` 相关命令输出 / mock query 响应中可见（每条标注级别）
- 交付三：knowledge-health 报告落到 `knowledge/health-report.md`（与 conflict-check 同形态）

**B · 聚合命令（本交付主体）**：新增 `sofagent knowledge status` 子命令，一次性聚合「上次 Dream Cycle 时间 + 产出概览 / 知识健康状态 / sensitivity 统计」成一页可读报告。只读聚合，复用三源输出；restricted 不泄露（复用 sensitivity 过滤）。
- 文件：`sofagent/daemon/src/commands/knowledge-status.ts`（或并入现有 cli.ts，架构师阶段确认落点）
- 测试：`__tests__/knowledge-status.test.ts`（4 case：空知识库 / 仅 log / 含 health report / sensitivity 统计且 restricted 不泄露）

### 交付五：文档同步（P2 · 文档）

1. `docs/changelog/v1.1.7.md`：发布检查清单全勾选，状态改 `✅ 已发布`
2. `CHANGELOG.md` 根：v1.1.7 段正式化（参考 v1.1.6）
3. `ROADMAP.md` v1.1.7 行改 `✅ 已发布`（顺带核对 v1.1.6 行若滞后）
4. `README.md` / `README.en.md`：补充 Dream Cycle + knowledge-health + knowledge status 说明
5. `docs/ARCHITECTURE.md`：daemon inspector 清单小表加 knowledge-health
6. 版本号 bump（1.1.6 → 1.1.7）**在 releasing.md 阶段做，不在开发阶段**

### 交付六：audit ActionGovernance schema（随 T01 混入，QA Round 1 补文档）

> 来源：行业五层骨架 / Palantir Action Type 研读（A4）落地。该改动随 commit `c9f36b8`（T01 Dream Cycle）混入，与本版本新功能主线**无功能依赖**；QA Round 1（K2）检出 changelog/CHANGELOG/ROADMAP 零记载，走「补文档」路线对齐 SSOT（不拆 commit：改动向后兼容、audit 407 全绿，拆历史成本高于收益）。

审计记录从「结果」升级为「可问责的动作凭证」：

- **`ActionGovernance` 5 字段 schema**：`actor`（发起方）/ `timestamp`（ISO 8601）/ `targetEntity`（目标实体：文件路径 / 实体 ID / 资源）/ `beforeAfter?`（前后值摘要，不承载 diff 原文避免 history.jsonl 膨胀且规避 A2/A9 脱敏问题，按需从 git diff 取）/ `context?`（任务 / workflow / session）
- **`DecisionProvenance` 决策溯源组**：`who`（人类 / Agent / 系统）/ `when` / `whichDataVersion?`（知识 / 本体数据版本，FDE 知识库版本化后回填）/ `whichApp?`（app / Agent 身份）
- **兼容性**：全部字段向后兼容（可选字段），旧 history.jsonl 记录无需迁移；audit 407 测试不回归，新增 2 用例（audit-history.test.ts）锁定 actionGovernance 写入行为

**文件变更**：

| 文件 | 变更 |
|------|------|
| `sofagent/audit/src/rules/types.ts` | +46：`DecisionProvenance` / `ActionGovernance` 接口定义 |
| `sofagent/audit/src/audit-history.ts` | +3：history 记录加 `actionGovernance` 可选字段 |
| `sofagent/audit/src/audit-log.ts` | +3：log 结构同步 |
| `sofagent/audit/src/index.ts` | +28：审计写入时填充 actionGovernance |
| `sofagent/audit/src/audit-history.test.ts` | +48：2 用例锁定 actionGovernance 写入 |

---

## BugFix 主线（15 项 · v1.1.6 发布后问题修复）

> 来源：v1.1.6 fresh-eyes review 三份执行。SSOT 保持 1.1.6（未 bump），15 项均在 v1.1.7 开发期落地。

### P1 修复（2 项 · 影响诚信）

#### 问题 1 · LIMITATIONS 日期滞后 + 缺日期扫描

**问题**：LIMITATIONS.md 头部日期滞后（停留在旧日期），且 check-version.sh 无「文档头日期一致性」扫描项，导致日期漂移无法被门禁捕获。

**修复**：
- LIMITATIONS.md 头部日期同步至 2026-07-19；COMMUNITY.md / PHILOSOPHY.md / THANKS.md / guides/mcp-usage.md 同步
- check-version.sh 新增第 14 项「文档头日期一致性扫描」（`> vX.Y · YYYY-MM-DD` 格式）

**文件变更**：

| 文件 | 变更 |
|------|------|
| `LIMITATIONS.md` | 头部日期同步 |
| `tools/check-version.sh` | 新增文档头日期一致性扫描项（第 14 项） |
| `docs/COMMUNITY.md` / `docs/PHILOSOPHY.md` / `docs/THANKS.md` / `docs/guides/mcp-usage.md` | 日期同步 |

#### 问题 2 · README 无 llm-wiki-mapping 链接

**问题**：v1.1.6 新增了 `docs/llm-wiki-mapping.md`（三层映射核心文档），但 README 延伸阅读表未链接，用户无法从入口发现。

**修复**：README.md 延伸阅读表补充 `docs/llm-wiki-mapping.md` 链接。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `README.md` | 延伸阅读表补 llm-wiki-mapping 链接 |

---

### P2 修复（12 项 · 影响一致性）

#### 问题 3 · FDE/LOOP「独立产品」声称与实际耦合矛盾

**问题**：FDE / LOOP 对外称「独立产品」，但实际需 clone 主仓库才能运行（依赖 `sofagent/scripts/install.sh`），声称与事实矛盾，用户按"独立"操作会失败。

**修复**：FDE/FDE.md、LOOP/LOOP.md、LOOP/README.md 首行/相关句声明「需 clone 主仓库」；README.md 命名约定段澄清「非可独立运行的独立仓库」。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `FDE/FDE.md` | 补依赖说明 |
| `LOOP/LOOP.md` / `LOOP/README.md` | 补依赖说明 |
| `README.md` | 命名约定段澄清 |
| `docs/ARCHITECTURE.md` | 同步 |

#### 问题 4 · check-version defaultRules 计数误报（17 实为 13）

**问题**：`tools/check-version.sh` 用 `grep -cE "name:|ruleClass:"` 统计规则字段，但因 A14-A17 定义格式为 `name: 'A14 知识库越权'`（数字后有空格），带闭引号的正则 `grep -vE "name: 'A1[4-7]'"` 匹配 0 行，导致 defaultRules 误报为 17（实际 13）。

**修复**：改为**空格感知正则** `grep -vE "name: 'A1[4-7] "`（注意尾随空格），实得 13，与 `defaultRules.length` 一致。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `tools/check-version.sh` | defaultRules 计数正则修正（空格感知） |

#### 问题 5 · README「700+ 用例」漂移

**问题**：README 写「700+ 用例」，实际审计核心 405、全 workspace 737——「700+」是模糊估算，不符合 SSOT 原则。

**修复**：改为精确值「审计核心 405 用例；全 workspace 737 用例」。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `README.md` | 用例数改为精确值 |

#### 问题 6 · pre-push-check 历史 tag 扫描（NO-OP 确认）

**问题**：审查建议 pre-push-check 增加历史 tag 扫描，但经核验 v1.1.6 已有全量历史 tag 扫描（第 7 步，v1.1.5 污点 WARN 豁免），**前提不成立**，无需写代码。

**修复**：**NO-OP**（无代码改动）。既有扫描已覆盖，确认即可。

#### 问题 7 · LIMITATIONS 测试数陈旧（402/726 → 405/737）

**问题**：LIMITATIONS.md 测试数停留在 402/726（旧值），实测已为 405/737。

**修复**：改为 405/737，并注明「见 `tools/test-count.sh` 实测」。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `LIMITATIONS.md` | 测试数 402/726 → 405/737 |

#### 问题 8 · README 三产品关系 + LOOP 不可见

**问题**：README 未说明 sofagent / FDE / LOOP 三产品关系，且 LOOP 在 README 中几乎不可见，用户不知道 LOOP 是什么。

**修复**：README 企业落地段补 LOOP 说明 + 三产品关系（核心管守门 / FDE 管部署 / LOOP 管自迭代）+ 链接 LOOP/README.md。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `README.md` | 补三产品关系 + LOOP 说明段 |

#### 问题 9 · daemon 审计无集中收集（企业部署痛点）

**问题**：daemon 审计结果仅本地存储（`daemon-notice.md` + stdout），企业 IT 无法集中收集审计日志，这是企业落地的阻塞项。

**修复**：
- SECURITY.md 补 filebeat / logstash 集中收集 workaround（定时轮询 `.sofagent/audit/history.jsonl`）
- Webhook 推送标「企业采购阻塞项」（v1.2.x 才就绪）

**文件变更**：

| 文件 | 变更 |
|------|------|
| `SECURITY.md` | 补集中收集 workaround + Webhook 阻塞标注 |
| `ROADMAP.md` | 同步 |

#### 问题 10 · 竞品表 AgentLoop 表述补限定语

**问题**：README 竞品对比表中 AgentLoop 一行表述过于绝对，未体现 sofagent 的差异化定位。

**修复**：补限定语「本地不送云、不依赖云端轨迹回传」，核实非 strawman。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `README.md` | 竞品表 AgentLoop 行补限定语 |

#### 问题 11 · 跨产品 install 契约无 CI 验证

**问题**：FDE/fde-install.sh 与 LOOP/loop-install.sh 都调用 `sofagent/scripts/install.sh`（跨产品契约），但无 CI 验证该契约不被破坏——一旦主仓库改了 install.sh 路径，FDE/LOOP 安装会静默失败。

**修复**：`.github/workflows/pr-check.yml` 新增 `cross-product-contract` job，断言两个子安装脚本都引用 `sofagent/scripts/install.sh`。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `.github/workflows/pr-check.yml` | 新增 cross-product-contract job |

#### 问题 12 · red-team 缺口转回归锁

**问题**：fresh-eyes review 的 red-team 维度发现多项检测边界（A9 全角/leet 注入、history 篡改、hook 删除、非法 YAML、非 git 目录），但无回归测试锁定这些边界，未来重构可能静默回退。

**修复**：acceptance-test.sh 新增 18 个 red-team 场景（scenario 92-96 等），覆盖：A9 全角/leet 检测断言（断言 rc=2）、history.jsonl 篡改检测（hash chain 完整性）、hook 被删 → doctor 持续检测、非法 YAML config 不崩、非 git 目录友好报错、skillopt CLI 回归锁。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `tools/acceptance-test.sh` | 新增 18 个 red-team 场景 + 头注释同步 |

> **坑位说明**：scenario 中 A9 全角/leet 注入样本采用 **base64 运行时解码**生成（源码只留 ASCII blob），避免测试脚本自身被 sofagent A9 审计规则自扫拦截（"测 A9 的脚本被 A9 自己拦死"的鸡生蛋问题）。详见 [v1.1.6 开发日志 A9 自扫阻塞坑](./v1.1.6.md)。

#### 问题 13 · SECURITY USB federation 章节滞后

**问题**：SECURITY.md USB federation 章节停留在旧版本状态，未记录 v1.1.5 HMAC 签名和 v1.1.6 现状。

**修复**：章节标注 v1.1.6 状态 + 记录 v1.1.5 HMAC 签名。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `SECURITY.md` | USB federation 章节更新 |

---

### P3 修复（1 项 · 锦上添花）

#### 问题 14 · 子 Agent SKILL frontmatter 不完整

**问题**：3 个子 Agent SKILL.md（sofagent-engineer / sofagent-releaser / sofagent-reviewer）的 frontmatter 缺 `not_when:` / `emoji:` / `color:` 等字段，不完整（audit / fde 的已完整）。

**修复**：仅补这 3 个，5 文件 frontmatter 的 `not_when:` 等字段全部 ≥1。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `agents/SKILL/sofagent-engineer/SKILL.md` | 补 frontmatter 字段 |
| `agents/SKILL/sofagent-releaser/SKILL.md` | 补 frontmatter 字段 |
| `agents/SKILL/sofagent-reviewer/SKILL.md` | 补 frontmatter 字段 |

#### 问题 15 · --doctor 弃用措辞困惑

**问题**：`sofagent-audit --doctor` 的提示语用负面措辞「已弃用，将移除」，用户看了困惑（是要删了吗？迁移到哪？）。

**修复**：改为建设性措辞「建议改用功能更强的 `sofagent-core --doctor`（迁移：先 npm install -g @sofagent/core，再运行 sofagent-core --doctor）」。

**文件变更**：

| 文件 | 变更 |
|------|------|
| `sofagent/audit/src/index.ts` | --doctor 提示语改为建设性措辞 |

---

## 质量验证（新功能主线 · 已实测）

> 以下为新功能主线 5 交付落地后的门禁实测结果（commit 序列 `c9f36b8`…，SSOT 保持 1.1.6）。

- check-version: **69/69** 全绿
- pre-push-check: **17 通过 / 6 警告 / 0 失败**（6 警告均为历史 tag 污点豁免）
- test-count 全 workspace: **770 passed / 12 包**（daemon 69 / core 101 / audit 407 / ontology 5 / skillopt 7，均不回归）
- daemon 包: **69 passed**（43 基线 -2 旧脚本删 +14 dream-cycle +10 knowledge-health +5 knowledge-status）
- core 包: **101 passed**（96 基线 + 5 sensitivity）
- 新增测试数门禁全达标：knowledge-health 10 case（≥8）/ Dream Cycle 每阶段 ≥1（stages 8 + state-machine e2e 6）/ sensitivity 5 case（≥3）/ knowledge-status 5 case（≥4）
- e2e 真实 `.sofagent/knowledge/`（4 空子目录 + index.md/log.md 框架）：`checkKnowledgeHealth` → `triggered:false`；`knowledgeStatus` 优雅降级不报错
- 铁律核验：knowledge-health/knowledge-status 0 `writeFileSync`/`unlink`；头注释全 `v1.1.6 新增`

## 质量验证（BugFix 主线 · 已实测）

> 以下为 BugFix 15 项落地后的门禁实测结果（commit `3c54a32` + `b67b343`，SSOT 保持 1.1.6）。

- check-version: **71/71** 全绿（原 70 + 新增文档头日期扫描 1 项）
- pre-push-check: **17 通过 / 6 警告 / 0 失败**（6 警告均为历史 tag 污点豁免）
- audit 包测试: **405 passed**（不回归）
- workspace 全量: **737 passed / 12 包**（不回归）
- acceptance-test: **96 场景 / 109 断言全通过 / 0 失败**（原 91 + 新增 red-team 18 项，含多断言场景）
- SSOT: 15 个 package.json 版本号全为 1.1.6，无 1.1.7 泄漏

---

## 发布检查清单

### 新功能主线（已完成）

- [x] Dream Cycle 6 阶段 nightly 跑通，旧脚本清理或迁移
- [x] knowledge/ 输出 fact/atom/concept 分目录
- [x] `knowledge/log.md` 周报随 Dream Cycle nightly 生成（LUI A）
- [x] sensitivity frontmatter 写入 + query 时过滤生效，缺省 = internal
- [x] sensitivity 分类结果在 `sofagent knowledge` 输出 / mock query 中可见（LUI A）
- [x] knowledge-health inspector 注册 @weekly 并随外层 LOOP 跑通
- [x] 五项检查（孤立/重复/断链/陈旧/缺源）均有用例覆盖，只建议不自动删
- [x] 巡检报告按 sensitivity 过滤，restricted 不泄露
- [x] `knowledge/health-report.md` 随 knowledge-health 生成（LUI A）
- [x] `sofagent knowledge status` 聚合命令可读出三源状态，restricted 不泄露（LUI B）
- [x] pre-push-check 全绿

### BugFix 主线（已完成）

- [x] BugFix 15 项全部落地（2 P1 + 12 P2 + 1 P3），其中问题 6 为 NO-OP 确认
- [x] check-version 71/71（含新增文档头日期扫描）
- [x] pre-push-check 17 通过 / 0 失败
- [x] acceptance-test 96 场景 / 109 断言全通过
- [x] 3 个子 Agent SKILL.md frontmatter 补齐
- [x] acceptance-test 头注释场景数措辞精确化（「109 场景」→「96 场景 / 109 断言」）

---

## 文件变更总览（BugFix 主线）

| 文件 | 变更 | 关联问题 |
|------|------|---------|
| `LIMITATIONS.md` | 头部日期 + 测试数 405/737 | 问题 1, 7 |
| `tools/check-version.sh` | 新增文档头日期扫描 + defaultRules 空格感知正则 | 问题 1, 4 |
| `docs/COMMUNITY.md` / `docs/PHILOSOPHY.md` / `docs/THANKS.md` / `docs/guides/mcp-usage.md` | 日期同步 | 问题 1 |
| `README.md` | llm-wiki 链接 + 用例数精确 + 三产品关系 + LOOP 说明 + 竞品表限定语 | 问题 2, 5, 8, 10 |
| `README.en.md` | 同步 README.md | 问题 5 |
| `FDE/FDE.md` | 补依赖说明 | 问题 3 |
| `LOOP/LOOP.md` / `LOOP/README.md` | 补依赖说明 | 问题 3 |
| `docs/ARCHITECTURE.md` | 同步产品关系 | 问题 3 |
| `SECURITY.md` | 集中收集 workaround + USB federation 更新 | 问题 9, 13 |
| `ROADMAP.md` | 同步 | 问题 9 |
| `.github/workflows/pr-check.yml` | 新增 cross-product-contract job | 问题 11 |
| `tools/acceptance-test.sh` | 新增 18 red-team 场景 + 头注释精确化 | 问题 12 |
| `agents/SKILL/sofagent-engineer/SKILL.md` | 补 frontmatter | 问题 14 |
| `agents/SKILL/sofagent-releaser/SKILL.md` | 补 frontmatter | 问题 14 |
| `agents/SKILL/sofagent-reviewer/SKILL.md` | 补 frontmatter | 问题 14 |
| `sofagent/audit/src/index.ts` | --doctor 建设性措辞 | 问题 15 |

## 文件变更总览（交付六 · ActionGovernance，随 `c9f36b8` 混入）

| 文件 | 变更 |
|------|------|
| `sofagent/audit/src/rules/types.ts` | +46 `DecisionProvenance` / `ActionGovernance` 接口 |
| `sofagent/audit/src/audit-history.ts` | +3 `actionGovernance` 字段 |
| `sofagent/audit/src/audit-log.ts` | +3 log 结构同步 |
| `sofagent/audit/src/index.ts` | +28 写入 actionGovernance |
| `sofagent/audit/src/audit-history.test.ts` | +48 / 2 用例 |
