# v1.1.1 开发日志 — 质量加固

> ⚠️ **状态：开发中** — 此版本尚未正式发版。内容可能变更。
>
> v1.1.1 是 v1.1.0 的质量补丁版本。不发新功能。
>
> 待开发 · 孔放勋

---

## 背景

v1.1.0 完成了 12 包拆分 + 轻量多设备四件事，但发布时留下以下已知缺口：

### 开发前提

| 条件 | 状态 |
|------|:--:|
| v1.1.0 已发布 | ⏳ 待发布 |

---

### 问题 1（P1）：acceptance-test 31 场景未全量验证

**现状**：场景 1-9 PASS，场景 10 起在 git 临时仓库中偶发卡死。

**根因**：场景 10（--no-verify 绕过检测）在连续场景中创建 commit，但前序场景的 `.env` 文件残留污染了 git 状态。

**方案**：每个场景结束后增加 `git reset --hard` 清理 + `rm -f .env`。

---

### 问题 2（P1）：experience sharing 真实验证

**现状**：发布检查清单第 5 项 `[ ]`。`harness/src/index.ts` 中 `knowledge/shared/` 注入逻辑已写（C-1），但缺少：
1. 真实 think.md 数据 → lessons 提取 → shared knowledge 写入的端到端验证
2. 多设备共享流程的实证（第二台设备导入 shared knowledge 后 Agent 行为变化）

**方案**：创建最小复现用例——用测试脚本模拟"设备 A 踩坑→写 think.md→提取 lessons→设备 B 加载 shared knowledge→加载链检查注入内容"。

---

### 问题 3（P2）：lessons-missteps 周报未验证

**现状**：发布检查清单第 6 项 `[ ]`。`daemon/src/weekly-report.ts` 已写（D），但：
1. 未在实际 daemon 环境中跑 ≥2 期验证格式稳定
2. LLM 降级路径（无 LLM 时的纯文本汇总）未测

**方案**：用测试数据跑 weekly-report.ts，验证不少于 2 期输出的格式一致性。

---

### 问题 4（P2）：shellcheck info 级清零

**现状**：SC1090、SC1091、SC2016 等 info 级警告 3 处。

**方案**：逐项消除——SC1090/SC1091 加 `# shellcheck source=...` 注释，SC2016 改引号。

---

### 问题 5（P2）：pre-push-check workspace 模式再验证

**现状**：v1.1.0 发布前从单包切换为 workspace 模式（14 项），但：
1. check-version.sh 加了注释行过滤（grep -v），需要确认不影响 TS 常量和 sh 脚本检查
2. workspace `npm test` 在 CI 环境（全新 clone）下是否全绿未经实战

**方案**：全新 clone → npm install → pre-push-check，确认 14/14。

---

### 问题 6（P2）：12 包各包 test 脚本实测

**现状**：v1.1.0 新包的 `npm test` 在各包中已有对应测试，但部分包为零测试文件（如 workflow-hub 补了 1 个）。acceptance-test 烟测了 CLI --help 但没有覆盖真实的子命令功能调用。

**方案**：为新包补最小功能测试（如 ontology merge、daemon start smoke test、core doctor/verify 调用验证）。

---

### 问题 7（P2）：SOP 自我进化 — 数字同步

**现状**：releasing.md 在 v1.1.0 发布后已更新多处，但以下数字未同步：
- 阶段速查表中 pre-push-check 项数：旧 7 项 → 新 14 项
- 阶段十发布步骤：旧"双包"→ 新"12 包按依赖层分批"
- 检查项数（维度数）：旧 ~267 → 新 ~283（含 278-283）

**方案**：逐项核对 releasing.md 中的所有数字（项数、维度数、doctor 项数等）与实际一致。

### 问题 8（P2）：回归维度退役清理

**现状**：维度 21（v0.99.9 ROADMAP 叙事）和维度 28（v0.99.8 406 残留）指向 3 个大版本前的过期产物。

**方案**：删除维度 21 和 28，注释标注「v1.1.1 退役：v0.99.x 维度已过期」。

### 问题 9（P2）：CI workspace 构建顺序验证

**现状**：v1.1.0 CI 因 workspace 数组顺序错误（think 在 mcp 后）报 TS2307。已修复但未经 CI 实战验证。

**方案**：在 GitHub Actions 上重新触发一次 CI，确认 workspace 构建顺序正确（think→mcp 编译通过）。

### 问题 10（P2）：陌生视角审查结果吸收

**现状**：v1.1.0 发布后陌生视角审查尚未执行。审查结果中的 P0/P1/P2 问题需吸收进 v1.1.1。

**方案**：等审查结果回来后，将 P0 项加入本 changelog 的问题清单。

---

## 发布检查清单

- [ ] 全量 workspace build + test 零错误
- [ ] acceptance-test 31 场景全 PASS（含新包 CLI 烟测）
- [ ] experience sharing 端到端验证通过（设备 A→B 流程实证）
- [ ] lessons-missteps 周报格式跑 ≥2 期稳定
- [ ] shellcheck 零 error 零 warning
- [ ] pre-push-check 14/14 全绿（全新 clone 环境验证）
- [ ] check-version.sh 过滤逻辑未引入误判
- [ ] check-docs.sh 通过
- [ ] 所有新包至少 1 个功能测试
- [ ] ROADMAP / CHANGELOG 更新
- [ ] Release Notes 标注
