# v1.0.6 开发日志 — 编排迁移 + A/B 真实运行器 + 文档一致性修复

> 三线并行：① ao compose 迁移到 DeepAgents + A/B 测试运行器从 mock 升级为真实模型 API——自迭代闭环打通；② v1.0.5 审查发现的文档一致性、CHANGELOG 纯度、版本号对齐等全部修复；③ 审查体系自进化——回归检查清单追维 + fresh-eyes-review.md 视角任务更新。
>
> ✅ 已正式发版 · 孔放勋

---

## 背景

v1.0.5 经审查发现三个层面的问题：

**技术层**（已有计划）：
1. launcher.ts 仅 68 行 thin wrapper，compose 仍走 `execFileSync('ao', ['compose'])`，DeepAgents 一行没写
2. **A/B 测试运行器 `simulateAgentRun()` 是 mock**——直接返回 testCase.expected，导致 candidate 和 current 永远打平，自迭代闭环断了

**文档层**（本轮新增）：
3. README 规则分类描述与 index.ts 实际有三处事实性错误（称 11 git-diff 实际 13、称 A12-A13 存在实际不存在、称 2 扩展实际 6）
4. CHANGELOG 实验版条目含过程性措辞（模型名、P0/P1 标签、评分、"文档修正"标题）

**审查体系层**（本轮新增）：
5. 现有回归检查清单和 fresh-eyes-review.md 缺少对跨文档一致性、CHANGELOG 纯度的检查维度

v1.0.6 三条线并行：技术层（DeepAgents 迁移 + A/B 真实运行器）+ 文档层（一致性和纯度修复）+ 审查体系层（自进化）。

---

## 交付内容（9 件事）

> 三线并行：① 编排迁移 + A/B 真实运行器——自迭代闭环打通；② v1.0.5 审查发现的文档一致性、CHANGELOG 纯度、版本号对齐全部修复；③ 安全加固——history.jsonl 防篡改 + post-commit hook 自动检测 --no-verify 绕过。

### 1. DeepAgents compose 实现

用 `createDeepAgent()` 替换 orchestrate-compare.ts 中的 `execFileSync('ao', ['compose'])`：

```typescript
// 之前（v1.0.5 及以前）
const output = execFileSync('ao', ['compose', taskDesc], { timeout: AO_TIMEOUT });

// 之后（v1.0.6）
const agent = await launch(orchestratorAgent);
const result = await agent.compose(taskDesc);
```

**实现要点**：
- compose agent 的 systemPrompt 基于 workflow.yml 的节点定义
- 输出格式与 ao compose 一致（YAML 工作流定义）
- ao 仍作为 fallback——deepagents 未安装时自动降级到 `execFileSync('ao', ...)`
- `@deprecated` 标注从"v1.0.5 移除"修正为"v1.0.6 迁移核心逻辑，ao 降为 fallback"

### 2. Sub Agent 状态管理基础

为 Sub Agent 加上最基本的运行状态跟踪：

| 能力 | 实现 |
|------|------|
| Agent 注册表 | 从 `.sofagent/subagents/` 加载 YML 定义（v1.0.4 已有 registry.ts） |
| 运行状态 | 记录启动/活跃/停止时间戳到 `.sofagent/subagents/runtime.json` |
| 基本心跳 | launcher.ts 的 Agent 实例定期写 `lastActive` 到 runtime.json |

**不做的**：
- ❌ Agent 间消息传递（推到 v1.1）
- ❌ LangGraph 状态图（推到 v1.1）
- ❌ 长任务规划（推到 v1.1）

### 3. A/B 真实运行器：模型 API 直跑（方案 B）

v1.0.5 的 `ab-runner.ts` 中 `simulateAgentRun()` 是 mock——直接返回 expected，A/B 永远打平。v1.0.6 替换为真实模型 API 调用。

**设计思路**：Skill 优化的本质是 prompt 质量对比——candidate 和 current 的区别是 Skill 文件内容不同，不是工具链路不同。用最小 Agent（模型 + Skill prompt）就够区分，不需要完整编排引擎。

**改动文件**：`ab-runner.ts`

```typescript
// 之前（v1.0.5 mock）
async function simulateAgentRun(testCase, _agentPath) {
  return { ...testCase.expected }; // 永远满分
}

// 之后（v1.0.6 真实运行）
async function runMinimalAgent(testCase, skillPath) {
  // 1. 读 Skill 文件内容作为 system prompt
  const skillContent = readFileSync(skillPath, 'utf-8');
  // 2. 组装消息：system = skillContent, user = testCase.input
  // 3. 调模型 API（模型，复用 daemon 的 API 接入）
  // 4. 解析输出为结构化结果
  // 5. 返回给 scoreCase 评分
}
```

**关键设计**：

| 维度 | 规格 |
|------|------|
| 模型 | 复用 daemon 已有的 API 接入（模型 / 模型） |
| 温度 | temperature=0.3 固定，减少随机性 |
| 重试 | 单次 60s 超时，失败重试 1 次，再失败返回零分 |
| 公平性 | candidate 和 current 用相同的调用顺序，同一 testCase 先跑 current 再跑 candidate |
| 降噪 | 每个 testCase 跑 1 次（后续 v1.0.7 可扩为 3 次取平均） |

**eval/types.ts 补充**：TestCase 加 `input` 字段（任务描述），当前只有 `expected`。

**TestCase 示例**：
```yaml
- id: fde-onboard-01
  input: "梳理一个电商客服团队的工作流，识别 AI 节点"
  expected:
    deliverable: "workflow.yml + AI 节点清单"
    key_sections: ["渠道分析", "意图分类", "AI 节点", "人机协作"]
    constraint_check: true
```

**自迭代闭环打通后的完整链路**：

```
Agent 跑任务 → loop-evaluate 评分 → scoring.md（✅ v1.0.1）
  → daemon 检测阈值 20 条 → 24h 防抖（✅ v1.0.4）
    → skillopt-run 生成 candidate（✅ v1.0.4）
      → A/B 对比：candidate vs current 各跑 testCase 集（🆕 v1.0.6）
        → 连续胜出 ≥2 → promote 替换（✅ v1.0.5 逻辑在）
```

**不做的**：
- ❌ 工具调用（模型不调外部 API，纯 prompt 对比）——v1.0.7 接 DeepAgents 后可加
- ❌ 多轮推理（单次调用，不是 Agent loop）——同上
- ❌ 多次采样取平均——v1.0.7 可扩

### 4. README 规则分类修复 🔴 P0

**问题**：审查发现 README 第 169 行对 17 条审计规则的分类描述与 `index.ts` 实际代码有三处事实性错误：

| 维度 | README 声称 | 代码实际 | 问题 |
|------|-----------|---------|------|
| 纯 git-diff 数量 | 11 条（A1-A6,A9-A11） | **13 条**（A1-A6=6, A9-A11=3, **E1-E4=4**） | E1-E4 被遗忘 |
| 需 Agent 日志的规则 | A7-A8, **A12-A13** | A7, A8, A14, A15 | A12-A13 不存在，A14-A15 归错类 |
| 扩展规则数量 | **2** 条（A14, A15） | **6** 条（E1-E4 + A14 + A15） | E1-E4 没出现 |

相比之下 **SECURITY.md 的表述是正确的**："17 条规则（11 默认 + 6 扩展）"——说明两条文档各自维护，没有交叉校对。

**修复**（`README.md` 第 169 行）：

```
- 改前：17 条审计规则：11 纯 git-diff（A1-A6,A9-A11）+ 4 需 Agent 日志（A7-A8,A12-A13）+ 2 扩展（A14 知识库越权·事后审计提醒、A15 约束验证），覆盖密钥泄漏、越界修改、注入攻击、知识库越权等

- 改后：17 条审计规则：11 条默认（A1-A11）+ 6 条扩展（E1-E4 + A14-A15），覆盖密钥泄漏、越界修改、注入攻击、知识库越权、工程规范等。其中 13 条纯 git-diff，4 条需 Agent 日志（hybrid）
```

同时 README 的对比表格中 "17 条审计规则" 那行也需同步更新。

### 5. CHANGELOG 过程性措辞清理 🟡 P1

**问题**：CHANGELOG 的实验版条目中包含大量审查过程元信息，不符合产品 changelog 的"只写产品变更"标准。具体问题条目：

| 版本 | 行 | 问题 | 修复 |
|------|:--:|------|------|
| v1.0.2 | 26 | 标题"文档修正" | → "文档修正 + 规则对齐" |
| v0.99.9 | 45 | "经独立审查发现 14 项问题" | → 删除此句，保留功能描述（AI 知识库概念 + verify.ts 拆分 + 行业笔记 + 理论基础） |
| v0.99.8 | 49 | 标题含过程性措辞 | → "文档收尾 + FDE 架构重构" |
| v0.99.8 | 51 | "独立审查发现 20 项遗留问题，本版全部修复" | → 删除此句 |
| v0.99.6 | 59 | 标题"文档修正" | → "npm 双包发布 + 25 项修复" |
| v0.99.5 | 65 | 标题"CI 自动化 + 文档修正" | → "CI 自动化 + npm 发布" |
| v0.99.5 | 67 | "审查驱动修复 + bump-version/check-version 增强" | → 删除"审查驱动修复 + "，保留"bump-version/check-version 增强" |
| v0.99.4 | 71 | 标题"文档修正" | → "准入诚实化 + 41 项修复" |
| v0.99.2 | 81 | "两份独立十维审查驱动修复 18 个问题" | → "18 项修复（daemon 歧义根治 + 死链清零）" |
| v0.92 | 117 | "3 个 P0 安全硬伤 + 6 个 P1 工程欠债 + 5 个 P2 改进。综合评分 5.7/10" | → 删除 P0/P1/P2 标签和评分，仅保留"安全硬伤 + 工程欠债 + 改进" |

**原则**：版本标题只写功能变更，描述只写做了什么、不写怎么审查的。P0/P1/P2 标签属于开发过程，不应出现在面向用户的 changelog 中。

### 6. ROADMAP 标题日期 + 描述对齐 🟡 P1

**问题**（`ROADMAP.md` 第 4 行）：

```
- 改前：v1.0.5 · 2026-07-11（UTC）· 自动优化 + 约束验证 + 过程性措辞
- 改后：v1.0.5 · 2026-07-12（UTC）· Ontology 统一层 + Workflow Hub + 安全加固
```

两处改动：① 日期 07-11 → 07-12（发版日期）② 用实际功能摘要替代过程性措辞。

### 7. 审查体系自进化：追加回归检查项 + 更新视角任务 🟢 P2

**背景**：本轮审查发现的问题暴露了现有回归检查清单和审查 prompt 的盲区——有些问题（如 README 规则分类错误、CHANGELOG 过程性措辞泄漏）在现有检查项中完全没有覆盖。

**追加到回归检查清单的 4 个新维度**：

| 维度 | 描述 | 来源 |
|------|------|------|
| — | README 规则分类（纯 git-diff/hybrid/扩展的数量与分类）与 `index.ts` 中 evidenceMode + 数组归属完全一致 | P0：README 写错规则数、写不存在规则 |
| — | SECURITY.md 和 README 对同一数据点（规则数量/分类方式）的表述无矛盾 | P0：两文档各写各的 |
| — | ROADMAP 头部日期与 `@sofagent/audit` package.json 的 version 字段对应版本的**实际发版日期**一致 | P1：ROADMAP 日期差 1 天 |
| — | CHANGELOG 全历史不含过程性措辞（模型名、严重度标签、轮次计数等内部描述） | 多个版本标题/描述含过程性措辞 |

**更新 fresh-eyes-review.md 的 3 个视角任务**：

| 视角 | 修改 | 说明 |
|------|------|------|
| 视角三·竞品分析 | 任务 6 后加子任务 6b | "验证 README 分类描述与 evidenceMode 匹配"——原来只让数数量，没让验证分类标签 |
| 视角八·CI/自动化 | 任务 1 后加任务 1b | "验证 README 对规则分类的自然语言描述与代码中 evidenceMode 字段完全匹配" |
| 视角一·陌生人 | 任务 5 加子步骤 | "打开 README 规则描述段，逐条对照代码验证分类" |

### 8. history.jsonl 防篡改加固——环境指纹 🛡️

**问题**：hash chain 只有 prevHash 链。Agent 删中间一行 → 链断 → --doctor 能检测。但 Agent 重算整条链 → 链不断 → 不可检测。

**方案**：hash 计算中加入"环境指纹"（hostname + username + git 路径的 hash）。新条目标记 `hashVersion: 2`，旧条目（无标记）用旧算法校验（向后兼容）。不是完美方案，但把篡改门槛从"会写 JS"提高到"需要逆向 hash 算法 + 知道环境指纹机制"。

**改动文件**：`sofagent/audit/src/audit-history.ts`

### 9. post-commit hook——--no-verify 自动检测 🛡️

**问题**：`git commit --no-verify` 跳过 commit-msg hook 绕过审计。当前只能靠用户手动跑 `--doctor` 事后发现。

**方案**：新增 post-commit hook——`--no-verify` 跳过 commit-msg 但**不跳过 post-commit**。每次 commit 完成后自动检查 history.jsonl 是否有当前 commit SHA，没有就输出警告。`--init` 同时安装 commit-msg + post-commit，`--doctor` 增加 post-commit 检测项。

**改动文件**：`sofagent/audit/hooks/post-commit`（新建）、`sofagent/audit/src/commands/init.ts`、`sofagent/audit/src/commands/doctor.ts`

---

## 和现有组件的关系

| 组件 | 受影响 | 说明 |
|------|:----:|------|
| orchestrate-compare.ts | 🔧 重构 | compose 子命令走 DeepAgents，ao 降为 fallback |
| launcher.ts | ⚡ 增强 | 从 wrapper 升级为编排入口 |
| registry.ts | 不变 | YML 定义加载已在 v1.0.4 完成 |
| ao (npm 依赖) | ⬇️ 降级 | 从默认路径变为 fallback only |
| ab-runner.ts | 🔧 重构 | simulateAgentRun(mock) → runMinimalAgent(模型 API) |
| eval/types.ts | 📝 扩展 | TestCase 加 input 字段 |
| README.md | 📝 修正 | 规则分类描述修正（3 处事实性错误） |
| CHANGELOG.md | 📝 清理 | 实验版 10 处过程性措辞清理 + 1 处正式版标题修正 |
| ROADMAP.md | 📝 对齐 | 日期 07-11→07-12 + 描述去审查化 |
| fresh-eyes-review.md | 📝 进化 | 3 个视角任务更新 + 审查结束后追加 4 条回归检查维度 |
| agents/README.md | 📝 刷新 | 版本引用更新至 v1.0.5+ |
| audit-history.ts | 🛡️ 加固 | hash 计算加环境指纹 + hashVersion: 2 + 向后兼容 |
| hooks/post-commit | 🆕 新建 | --no-verify 绕过自动检测 |
| init.ts | ⚡ 增强 | --init 同时安装 commit-msg + post-commit |
| doctor.ts | ⚡ 增强 | 增加 post-commit hook 检测项 |

---

## 发布检查清单

### 编排引擎迁移（原 3 件事）
- [x] DeepAgents compose 实现完成
- [x] ao fallback 路径测试通过
- [x] Sub Agent runtime.json 状态记录
- [x] orchestrate-compare.ts @deprecated 注释修正
- [x] ab-runner.ts: simulateAgentRun → runMinimalAgent（模型 API 直跑）
- [x] eval/types.ts: TestCase 加 input 字段
- [x] A/B 测试在真实 testCase 集上跑通（candidate ≠ current 分数）
- [x] 自迭代闭环端到端验证（scoring → skillopt → A/B → promote）
- [x] 现有 ab-testing 单元测试同步更新（不再依赖 mock 行为）

### 文档一致性修复（新增 4 件事）
- [x] README.md 规则分类修正（1 处：第 169 行，3 个错误同时修）
- [x] README 对比表格中规则描述同步更新
- [x] CHANGELOG.md 实验版 10 处元信息清理（详见交付内容 5 的表）
- [x] CHANGELOG.md v1.0.2 标题"文档修正"→"文档修正 + 规则对齐"
- [x] ROADMAP.md 日期 07-11→07-12 + 描述改为实际功能摘要
- [x] agents/README.md 版本引用刷新至 v1.0.5+
- [x] fresh-eyes-review.md 3 个视角任务更新（视角一任务 5、视角三任务 6b、视角八任务 1b）
- [x] 回归检查清单追加 4 个新维度（README 规则分类 / 跨文档一致性 / ROADMAP 日期 / CHANGELOG 纯度）

### 安全加固（交付物 8-9 + 修复 1-4）
- [x] history.jsonl 环境指纹：hash 计算加 hostname+username+gitDir
- [x] hashVersion:2 版本标记 + 向后兼容（旧格式不误报）
- [x] checkHistoryChainIntegrity 逐条 hashVersion（非 firstEntry 一刀切）
- [x] post-commit hook：所有 exit 路径 → exit 0
- [x] --init 同时安装 commit-msg + post-commit
- [x] --doctor post-commit 检测（缺失 → WARN）
- [x] hooks/post-commit 中文乱码修复
- [x] audit-history.ts 死代码清理
- [x] LIMITATIONS.md A14 事后审计说明追加
- [x] 回溯兼容 + 防篡改 4 项测试全部通过

### 发版标准
- [x] bump-version 1.0.5 → 1.0.6
- [x] check-version.sh/pre-push-check.sh 全绿
- [x] npm test 全绿（480 tests）
- [x] acceptance-test.sh 16/16 全绿
- [x] OpenClaw 验收 28/28 全绿
- [x] shellcheck 零 error
- [x] SkillOpt CLI 契约修复（isSkillOptAvailable 探针 + runSkillOpt 子命令 + parseArgs --input 误判）
- [x] 审查体系维护（releasing.md 文件名引用 + regression-checklist 218-219 维度 + fresh-eyes-review 3 处更新）
- [x] git tag v1.0.6 + push
