# v0.98 开发日志

> 审计独立化：审计闭环六步 + GitHub Action 模板 + 首个持续使用案例 + bash→TS 第三波决策
>
> v0.97 完成纪律层验证后，v0.98 把审计工具从一个「检测器」升级为「持续改进系统」。核心变动：审计闭环六步（v0.96 设计，现在有真实数据了）+ 首个外部用户持续使用案例 + GitHub Action 让审计进 CI。同时决定部署层 bash 的去留。

---

## 概述

v0.98 的定位来自三条线的交汇：

1. **v0.97 的结论驱动**。如果 v0.97 的纪律层实验证实了增量，v0.98 的审计闭环就有真实数据可分析——根因分析不再是真空设计。如果 v0.97 证伪了增量，审计闭环仍然可以做——因为审计工具的价值是「事后检测」，不依赖「事前约束」的效果。

2. **Huntley 的「watch the loop」哲学**。审计工具不应该只是一次性扫描——它应该是一个持续观察 Agent 行为、发现失败模式、推动规则改进的循环系统。这六步闭环（检测→分类→根因→改进→回归→上线）就是「watch the loop」的工程实现。

3. **审查一关于审计层独立化的建议**。审计工具可以成为独立产品——不绑定 sofagent 核心、不需要 OpenClaw、不依赖 Agent 配合。GitHub Action 是这一步的标志性产物。

---

## 一、P0：审计闭环六步——从单次判定到持续改进

> 来源：v0.96 §二设计，v0.97 推迟（当时没有真实数据）。

### 六步闭环

```
检测（git diff 扫描改动）
  ↓
分类（规则匹配：A1-A11 逐条判定）
  ↓
根因（为什么这次 FAIL 了？同类问题之前出现过吗？）
  ↓
改进（规则阈值需要调吗？Agent 行为模式需要改吗？）
  ↓
回归（改进后重新审计同一批历史 diff，确认旧问题不重犯）
  ↓
上线（规则更新 + threshold 调整 + config.yml 版本化）
```

### 工程实现

| 步骤 | 产物 | 技术 |
|------|------|------|
| 检测 + 分类 | 现有功能 | v0.94 已完成 |
| 根因 | `audit/src/audit-root-cause.ts` | 聚合审计历史，按规则/文件/Agent 三个维度归因 |
| 改进 | `audit/src/config-suggestion.json` | 基于根因分析自动建议阈值调整 |
| 回归 | `audit/src/audit-regression.ts` | 用历史 diff 重跑更新后的规则 |
| 上线 | git commit config.yml | 配置变更进入版本控制 |

### 根因分析三个维度

| 维度 | 分析内容 | 输出示例 |
|------|------|------|
| 按规则 | 哪条规则触发最多？误报率如何？ | 「A3 不改越界：本月触发 23 次，其中 8 次经人工确认是误报（误报率 35%），建议调整路径匹配规则」 |
| 按文件 | 哪些文件反复被标记？ | 「src/config.ts：本月被 A3 标记 7 次——该文件可能是公共配置，建议加入白名单」 |
| 按 Agent | 哪个模型/配置下的 Agent 违规最多？ | 「GLM-5.2 + 无 sofagent：A7 盲改率 42%。GLM-5.2 + sofagent：A7 盲改率 12%。建议所有 Agent 启用 sofagent」 |

### 前置条件

- v0.97 积累了至少 30 天的审计数据（来自纪律层实验 + 日常使用）
- 审计数据量 ≥ 100 条任务日志（有足够样本做统计分析）

---

## 二、P0：首个外部用户持续使用案例

> 来源：两份审查共识——「1 个 30 天样本比 100 个一次性样本有价值」。「v1.0 发布前必须有一个非作者的外部用户使用 sofagent ≥ 30 天，并愿意公开说这东西有用。」

### 目标

| 指标 | 数值 |
|------|:--:|
| 用户数 | ≥ 1（非作者、非 AI 生成） |
| 持续使用天数 | ≥ 30 |
| 任务记录 | ≥ 20 条 task/logs |
| 审计数据 | ≥ 10 次 sofagent-audit 运行记录 |
| 公开发声 | 愿意在 EVIDENCE.md 或 GitHub Issue 中分享使用体验 |

### 理想用户画像

- 个人开发者（不是企业——企业门槛太高）
- 日常用 Agent 写代码
- 愿意花 5 分钟装 sofagent、花 30 分钟反馈
- 平台不限（OpenClaw 最好，但 WorkBuddy 用户也可以）

### 引流策略（轻量级——不做推广，只做邀请）

1. 在 sofagent 的 GitHub Discussions 发帖邀请
2. 向 v0.93 已有的 3 个第三方测试者（@cedric123123 / @liudi8785-cell / @jm4170134-droid）私信邀请
3. 在 Ralph Loop 相关社区（Reddit / V2EX / 知乎）提一句

---

## 三、P1：GitHub Action 模板

> 来源：ROADMAP 探索方向「CI/CD Gate」+ 审查一建议。

### 功能

一个 GitHub Action 模板文件，任何仓库放到 `.github/workflows/sofagent-audit.yml` 即可启用：

```yaml
name: sofagent-audit
on: [pull_request]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run sofagent-audit
        run: |
          npm install -g sofagent-audit
          sofagent-audit --diff origin/main..HEAD --ci --strict
```

### 设计原则

- **零配置**：默认行为就是最佳实践（`--ci --strict`）
- **exit code 驱动**：FAIL (exit 1) → PR 被 block；WARN (exit 2) → PR 可以合并但有标注
- **README badge**：通过后 README 显示绿色 badge
- **不绑定 sofagent 核心**：审计 CLI 独立运行，不需要 OpenClaw，不需要安装 sofagent Skill

### 产物

| 文件 | 说明 |
|------|------|
| `.github/workflows/sofagent-audit.yml` | Action 模板（放在 sofagent 仓库自身，也作为示例） |
| `docs/github-action.md` | GitHub Action 使用指南 |

---

## 四、P1：部署层 bash→TS 决策 + 执行

> 来源：bash→TS 迁移第三波（v0.96 规划，v0.97 推迟）。

### 决策背景

部署层脚本（install.sh / uninstall.sh / verify.sh / cleanup.sh / lite/install.sh）共 2,458 行。特点是：
- 需要在**安装 Node.js 之前**就能运行（否则 chicken-and-egg）
- 需要零依赖（bash 是所有 macOS/Linux 的默认 shell）
- 不涉及复杂逻辑（主要是 cp / mkdir / chmod / echo）

### 决策

**部署层保留 bash bootstrap，不迁 TypeScript。**

理由：
1. bash 在部署场景下是唯一可依赖的运行时——你不能假设目标机器有 Node.js
2. 部署脚本的逻辑足够简单——不涉及类型安全的风险
3. 保留 bash 和 Rust/C 项目保留 `Makefile` 一样——是 bootstrap 层，不是核心逻辑层
4. 迁移成本高（2,458 行 bash 重写为 TS + 引入 Node.js 依赖 + 处理 chicken-and-egg 问题）但收益低

### 执行

- 部署层脚本封存——不再做大改动，只做 bugfix
- 在 LIMITATIONS.md 标注「部署层永久保留 bash bootstrap」
- bash 重复代码债（§七 代码审计发现）——接受为已知技术债，不单独建 bash 基础设施

---

## 五、P2：FDE + AI 中台对接优化

> 来源：v0.96 编排引擎重构的后续工作。

### 审计报告 → 企业平台推送

基于 v0.97 完成的编排引擎反馈回路 + think.md 反思驱动，v0.98 做一件事：**让审计报告推到企业协同平台**。

| 平台 | 推送方式 | 优先级 |
|------|------|:--:|
| 钉钉 | 群机器人 Webhook | P1 |
| 飞书 | 群机器人 Webhook | P2 |
| 企微 | 群机器人 Webhook | P2 |

推送时机：每次 sofagent-audit 运行后，如果有 FAIL 或 WARN，推一条消息到对应群：
```
⚠️ sofagent 审计警告
任务：「修复报价计算逻辑」
A3 不改越界：2 个文件不在任务范围内
A7 不存盲改：task/logs 未找到本次任务记录
详情：[审计报告链接]
```

### 实现

- `audit/src/reporter.ts` 增加 webhook 输出模式（`--webhook dingtalk` / `--webhook feishu`）
- 不引入外部 HTTP 库——用 Node.js 内置 `fetch`

---

## 发布检查清单

### 审计闭环六步
- [ ] `audit-root-cause.ts`：3 维度归因可用
- [ ] `config-suggestion.json`：基于根因自动建议
- [ ] `audit-regression.ts`：历史 diff 回归验证
- [ ] 闭环全流程端到端跑通一次（检测→分类→根因→改进→回归→上线）
- [ ] 审计报告输出包含根因分析结果

### 首个持续使用案例
- [ ] 至少 1 个外部用户持续使用 ≥ 30 天
- [ ] 任务记录 ≥ 20 条
- [ ] 用户公开发声（GitHub Issue 或 EVIDENCE.md）
- [ ] EVIDENCE.md 更新

### GitHub Action
- [ ] `.github/workflows/sofagent-audit.yml` 模板可用
- [ ] `docs/github-action.md` 使用指南
- [ ] sofagent 仓库自身 CI 启用 sofagent-audit Action
- [ ] README 加 sofagent-audit badge

### 部署层决策
- [ ] 部署层确认保留 bash（决策文档写入 ARCHITECTURE §bash→TS 迁移）
- [ ] LIMITATIONS.md bash 重复代码债段更新
- [ ] 部署脚本 bugfix（如有）

### FDE 对接
- [ ] `reporter.ts` webhook 输出模式（钉钉优先）
- [ ] 审计报告格式优化（FDE 部署者可读）

### 收尾
- [ ] `npm test` 全量通过
- [ ] `npm run build` 编译成功
- [ ] CHANGELOG.md 索引添加 v0.98 条目
- [ ] ROADMAP.md 更新版本状态

---

## v1.0 准入条件进度追踪

| # | 条件 | 状态 | 完成版本 |
|:--:|------|:--:|:--:|
| 1 | 纪律层增量在反转实验中被证实 | ⏳ | v0.97 |
| 2 | 审计工具实现完整六步闭环 | ⏳ | v0.98 |
| 3 | 纪律层上下文成本 ≤ 窗口 5% | ✅ | v0.96 |
| 4 | OpenClaw 预装 + AO compose + Agency Agent 全链路跑通 | ⏳ | v0.98 |
| 5 | MCP server + 至少一个企业平台 webhook 跑通 | ⏳ | v0.98 |
| 6 | daemon 稳定运行 ≥ 30 天 | ⏳ | v0.99 |
| 7 | 至少 3 个外部用户 30 天使用数据 | ⏳ | v0.99 |
| 8 | install → verify → 首次任务通过率 ≥ 90% | ⏳ | v0.99 |
| 9 | 能力矩阵五个平台都有实测数据 | ⏳ | v0.97 |

> 进度：2/9 完成（截至 v0.96）。v0.97 预计完成 #1 #9，v0.98 预计完成 #2 #4 #5，v0.99 预计完成剩余全部。

---

## 推迟/砍掉清单

### 推迟到 v0.99
| 项目 | 原因 |
|------|------|
| Beta 公测 20 人 | 先有 1 个人的 30 天案例，再谈 20 个人。质量优先于数量 |

### 砍掉
| 项目 | 原因 |
|------|------|
| bash→TS 第三波（部署层全迁） | 部署层保留 bash bootstrap——是 bootstrap 层，不是核心逻辑层（见 §四决策） |
