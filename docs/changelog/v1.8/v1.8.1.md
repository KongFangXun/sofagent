# v1.8.1 开发日志 — 判定底座 · 一体机推理接入（并行约束解码 + 多问并行 + 判定档接通）

> 📋 **已排期。** 前置依赖：v1.8.0（DecisionChannel 默认实现）+ v1.5.6（本地并发槽位与判定分层编排）+ v1.4.9（一体机数据面与设备侧清单上报）。
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-09-20（判定底座施工期拆版落盘，用户拍板）。设计依据见维护者侧判定底座设计文档（仓外）。

## 定位

> 🖥️ **判定底座 · 一体机推理接入**——阶段三第二版，回答「**判定在哪跑、跑得多快**」。判定底座要在客户的私有环境（一体机、离线节点、信创全封环境）里可用，且**不新增运行时组件**——跑在既有本地推理栈上。本版同时把路由层的**判定档（`decision-model`）**从「插槽已预留」接通为「真实可达」：判定任务由判定底座直接作答，**不经过生成模型**。**路线不绑定**——本版落的是一条实现路线，契约由 [v1.8.0](./v1.8.0.md) 的 `DecisionChannel` 保证，路线选择属实现面。

---

## 一、并行约束解码（一次前向出全部字段）

> **形态归属**：`[主干]`（判定·推理实现）｜ **不可拔**（判定吞吐与延迟的实现基础）

### 交付

| 组件 | 能力 |
|------|------|
| **并行约束解码** | 同一份 `state` 下的**全部问句一次前向出答案**（KV cache 广播 + 单次前向填充全部字段），而非逐问循环解码——这是判定模型相对生成模型的结构性优势，也是延迟能进入百毫秒级的前提 |
| **类型约束解码** | 解码过程**约束在声明的回答类型内**（choice 只能出候选项、score 只能出合法档位、noul 只能是概率）——从解码层保证「类型正确」，与 v1.6.0 的 schema 层限定构成双重保证（schema 层防越界，解码层防畸形） |
| **多问批处理** | 相互独立的问句可批量并行；**有依赖的问句串行**（依赖关系由问句集声明，不自动推断——自动推断会引入不可审计的隐式行为） |
| **本地推理栈协同** | 复用一体机既有本地推理栈（**零新增运行时组件**），与 v1.5.6 的本地并发槽位管理协同：判定请求走独立槽位通道，**不与主模型抢槽** |
| **离线可用** | 无网络环境下判定底座可正常加载与推理（权重本地留存，不联络外部服务）——信创全封场景的硬要求 |
| **实现路线不绑定** | 本版落地的是**其中一条路线**（并行约束解码），但契约不绑定路线——同期生态已出现**离散扩散去噪**（答案模板写在固定长度 canvas 上、只读去噪一步读出全部答案分布）与**零微调 stock 模型**两条可行路线，且主流推理引擎侧已有扩散支持立项。**契约由 [v1.8.0](./v1.8.0.md) 的 `DecisionChannel` 保证，路线选择属实现面**——换路线不改判定头契约、不改问句集与判据格式。本版契约测试**以接口为准，不以具体路线为准**。**外部实证**：同期生态在**不足两天**内出现**六个互不相同的实现**——编码器 + 策略梯度、扩散模型的约束解码分支、因果小模型 LoRA、编码器 + 蕴含分类器、自训逐字节打分件、半参数因果模型 LoRA（可在消费级笔记本上跑）——**没有任何两个用同一架构，却全部收敛到同一条接口**（文本进、类型化概率出）。这一致的收敛方向本身即结论：**护城河不在模型架构**，在于契约、校准与判据质量——这正是本仓把路线判为实现面的依据 |
| **超词表上界的选项集分块** | 单次前向能覆盖的选项数受**基座词表单 token 数**限制（实测某基座 A–Z / a–z 合计 52 项可单次前向），而 `Choice` 的规格上限高于此。超上界时**分块打分 + 末轮归一**并保留「以上都不是」槽——**不得静默截断候选集**（截断会让模型在缺失选项里挑最优，产出一个看似高概率、实则无意义的答案）。**硬前提 · 单 token 可映射性**：零微调路线要求**每个答案标签恰好编码为 1 个词表 token**（多 token 标签须改走「逐 token 概率连乘」或「整串概率合并」两条变体，不得假设默认成立）——这是**选项集构造期的断言**，不是运行期的兜底。**词表规模是量级参照**：同级开源基座的词表在**二十万至二十六万**量级（实测两例为 201,088 与 262,208），故「答案位占一个槽」这件事在词表层面总是成立，**真正的约束是标签文本的单 token 可编码性**，与词表总量无关 |
| **不确定时的稳定化重读（可选实现）** | 逐问句看熵，超阈时带新噪声重读至上限并**取平均**——与弃权互补：弃权是**承认判不了**，重读平均是**用采样降方差**。**默认关闭**——重读增加延迟且改变概率语义（平均后的概率不再是单次前向的读数，校准时须按同口径重测），开启须显式配置并落痕。触发粒度是**逐问句**而非全局阈值 |
| **延迟与吞吐落数** | 单次判定延迟、批量吞吐、并发下的延迟劣化曲线如实落数（作为 v1.9.1 四层性能标准的输入） |
| **职责频率分层（延迟即职责边界）** | 延迟不只是性能指标，它直接决定判定层**能承担哪一层职责**——成熟形态是分层错位布置：高频控制回路（数百 Hz）与安全反射归确定性代码，符号化感知归经典 CV，**判定层只坐在最低频那一层且性质为建议**。本版的延迟曲线须同时给出「**该延迟下判定可承担的职责层级**」，并写明判定**永不进**控制回路与安全反射 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/judge-runtime.ts` | 修改 | 并行约束解码 + 批量批处理 + 依赖串行 |

### 路线选型（四道筛）

> 「架构路线没有最好，只有最适合」——但**最适合不是按性能排出来的，是按约束筛出来的**。四道筛的**顺序即优先级**：前几道是出局条件，最后一道才是偏好。

| 筛 | 出局 / 偏好条件 | 淘汰或偏好谁 |
|----|----------------|-------------|
| ① **信创与离线** | 无 GPU / 纯 CPU 环境必须可用 | **淘汰**：把「显存 ≥ 24 GB」当独木桥——把某条路线的硬门槛升格为全域门槛即失守 |
| ② **边界（不做模型托管）** | 不得为每个客户训专属权重 | **淘汰**：需要「逐客户训练」的路线——不是效果问题，是「替客户训权重＝我们在托管模型」的边界问题 |
| ③ **审计（概率来源可追溯）** | 概率须来自输出分布的读数 | **淘汰**：外挂打分头且**未校准**的实现——其输出是**分数不是概率**，须先建校准才能当概率消费 |
| ④ **一体机耦合（零新增运行时）** | 复用既有本地推理栈 | **偏好**：在既有引擎的 logprobs 能力上加适配层（零新增组件），而非依赖我方无法单方面决定的引擎侧能力 |

**选型结论（主力 / 辅助 / 条件 / 不做）**：

| 定位 | 路线 | 依据 |
|------|------|------|
| **主力** | **免训练 · 占位槽 logits + 归一 softmax** | 零训练（不承诺模型性能——边界判据四）· 零新增运行时（复用既有引擎）· 零样本泛化很高 · **概率直接是输出分布的读数，审计链最短**。附带一项边界收益：**语言能力由部署方自选的基座决定**——「中文优先」因此成为**可配置项**而非我方的模型能力 |
| **辅助（窄域）** | **小模型 + 线性打分头** | 延迟与硬件门槛最低（可在边缘 CPU 运行），只用于**极高频的少数窄判据**。**前置纪律**：打分头输出在被校准前**不得作为概率消费**（见筛③） |
| **条件启用** | **离散扩散 · 单步去噪读全部答案槽** | 免训练、开箱多语言、**双向注意力使问句互不干扰**（对「一次前向出多个独立问句」价值高）。唯一门槛是显存 ⇒ **按硬件档启用**（配 GPU 档开启，信创 CPU 档不启用），**不作全域主力** |
| **明确不做** | **为每个客户训专属权重** | 越界（见筛②）。与其效果无关——同类实测里该路线表现可圈可点；问题是**它把交付方变成模型提供者**，随之而来的是数据归属、性能承诺与训练服务 |

**一条必须写明的风险**：主力路线是**零样本强，不是校准强**。占位槽 logprob + 归一 softmax 得到的是**可比的排序分**——那个 softmax 只保证若干候选之和为 1，**不保证「报 0.8 就八成对」**。因此主力路线**必须配本仓自己的校准层**（[v1.7.1](../v1.7/v1.7.1.md) 分桶温度拟合），否则拿到的是「很像概率的排序分」。这同时是相对同期复刻件的**差异点所在**：它们大多停在「能出概率」，本仓要的是「**概率可信到能用来分流**」。
| `engine/orchestrator/src/router/slot-manager.ts` | 修改 | 判定独立槽位通道（不与主模型抢槽） |
| `engine/orchestrator/src/__tests__/judge-parallel.test.ts` | 新建 | 并行正确性 / 类型约束 / 槽位隔离 / 离线加载单测 |
| `docs/VALIDATION.md` | 修改 | 延迟与吞吐实测落数 |

### 验收标准

- [ ] 并行约束解码可用：同状态多问一次前向出全部答案（非逐问循环）
- [ ] 类型约束在解码层生效（畸形输出不可能产生，有单测）
- [ ] 独立问句并行、有依赖问句串行（依赖关系显式声明）
- [ ] 判定走独立槽位通道，**不占本地主模型槽**（行为锁）
- [ ] 离线环境可加载并推理（不联络外部服务，有断言）
- [ ] **契约测试以接口为准**：换实现路线（并行约束解码 / 扩散去噪 / 零微调）不改判定头契约与判据格式
- [ ] 超词表上界的选项集分块打分 + 末轮归一，**候选集不静默截断**（有单测）
- [ ] **零微调路线下每个答案标签的单 token 可编码性在选项集构造期断言**（多 token 标签走连乘/整串合并变体，不得静默退化）
- [ ] **换实现路线不改判定头契约与判据格式**有断言（至少两条路线的契约一致性测试）
- [ ] 稳定化重读默认关闭；开启时延迟与概率语义变化**落痕**（行为锁）
- [ ] 延迟曲线同时给出「该延迟下可承担的职责层级」；判定**永不进**控制回路与安全反射（行为锁）
- [ ] 延迟 / 吞吐 / 并发劣化曲线落数（进 `docs/VALIDATION.md`）
- [ ] **路线选型四道筛落文档**，且主力路线的**校准缺口有对应交付**——主力路线不得以「零样本强」代替「校准强」（有断言）
- [ ] 辅助路线（打分头）的输出在**未校准前不得作为概率消费**（有断言）
- [ ] 条件路线（扩散）**以硬件档为启用条件**，信创 CPU 档不受其显存门槛约束（行为锁）
- [ ] `npm test` 全绿

---

## 二、路由判定档接通（`decision-model` 从插槽到可达）

> **形态归属**：`[主干]`（路由·判定档）｜ **不可拔**（路由档位是判定与执行的接缝）

### 交付

| 组件 | 能力 |
|------|------|
| **判定档接通** | `model-router` 的 `decision-model` 档从「类型已定义、分支已显式返回空」接通为**真实可达**——判定任务路由到该档后由判定底座作答，产出类型化答案 + 概率 |
| **判定档不进自动矩阵** | 该档**不参与「敏感度 × 复杂度」自动路由矩阵**——判定任务由上游显式请求（判定类任务发起时指定），避免判定任务被自动矩阵误路由到生成档 |
| **不落云端生成分支** | 判定档与拦截档（`block`）**绝不落到云端生成分支**（既有缺陷修复的回归锁——判定任务变成生成任务会既慢又错） |
| **判定档可观测** | 判定档的调用量 / 延迟 / 失败率 / 降级次数可观测（运维可见，数据本地不出门） |
| **与本地两档的分工声明** | 明确 `decision-model`（判定档）与 `local-executor`（本地执行档）/ `local-pipeline`（本地管道档）的分工边界：**判定档出判定，执行档出行为**——三者是任务形态区分，不是模型规格区分 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/model-router.ts` | 修改 | 判定档接通 + 不进自动矩阵 + 观测字段 |
| `engine/orchestrator/src/loop/plan-node.ts` / `engineer-decide.ts` | 修改 | 判定档分支由「返回空」改为真实判定调用 |
| `engine/mcp/src/tools/router-slots.ts` | 修改 | 判定档可观测字段 |
| `engine/orchestrator/src/__tests__/model-router.test.ts` | 修改 | 判定档可达 + 不落云端的回归锁断言 |

### 验收标准

- [ ] `decision-model` 档真实可达（判定任务由判定底座作答，产出类型化答案）
- [ ] 判定档不参与自动路由矩阵（须上游显式请求）
- [ ] **判定档与拦截档绝不落云端生成分支**（回归锁断言在位）
- [ ] 判定档调用量 / 延迟 / 失败率 / 降级次数可观测
- [ ] 三档分工边界在文档在位（判定档出判定 / 执行档出行为，非规格区分）
- [ ] `npm test` 全绿

### 风险与前置假设

> ⚠️ **一体机硬件规格可能不足**。判定底座的底座规格与并行约束解码的显存/内存占用须与客户实际硬件对齐（本版落数的延迟曲线是选型依据）。若目标硬件跑不动，**退回串行解码**（延迟上升但功能可用），并把该形态明确标注为降级档而非静默降速。
>
> ⚠️ **无 GPU 环境须保底可用**。纯 CPU 推理作为**最低可用档**亦须可跑——国内已有跨平台开源复现证明无 GPU 的 PC / Mac 可离线完成判定任务（中位耗时数秒至十余秒量级）。CPU 档延迟显著更高但形态成立，标注为最低档，**不是「不可用」**——信创与全封环境的实际硬件分布决定这一档必须存在。
