# v1.8.0 开发日志 — 判定底座 · DecisionChannel 真实实现（替掉 v1.5.6 留空 + L1 判定就位）

> 📋 **已排期。** 前置依赖：v1.5.6（`DecisionChannel` 通道契约与 L0/L1/L2 判定分层编排）+ v1.7.2（阶段二收口达阈——**未达阈则本版不启动**）。
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-09-20（判定底座施工期拆版落盘，用户拍板——原 v1.8「接线 + 分发」拆为 v1.8.0 接线 / v1.8.1 推理接入 / v1.8.2 权重分发三版）。设计依据见维护者侧判定底座设计文档（仓外）。

## 定位

> 🔌 **判定底座 · DecisionChannel 真实实现**——阶段三第一版，回答「**判定怎么被用起来**」。v1.5.6 交付了 `DecisionChannel` 的**契约**（`judge(state, questions)` + 三原语语义 + 云端 provider opt-in 默认关），但当时留空——L1 判定层处于「只有接口没有实现，按 L0 兜底」的状态。本版用阶段一/二的产物**填上这个空**，让 L1 从兜底态变为真实可用态。**纪律不变**：通道而非依赖——引擎定义接口与编排，实现可被替换（用户自带实现或换基座，不动契约）。实现就位后紧接着要定的是**输入面**（§二）——判定看到的 `state` 由什么构成、截多长；输入契约不写死，判定结果就不可复现，留痕也随之失去举证力。

---

## 一、DecisionChannel 实现落地（L1 判定层就位）

> **形态归属**：`[主干]`（判定·通道实现）｜ **不可拔**（通道契约与编排是判定层的接入面）

### 交付

| 组件 | 能力 |
|------|------|
| **默认实现接入** | 以 v1.6–v1.7 产出的判定底座作为 `DecisionChannel` 的**默认实现**（自研，随本仓开源），实现 `judge(state, questions) → typed answers + 概率` |
| **契约测试** | 按 v1.5.6 契约做可插拔替换测试：同一组问句下，换实现（默认实现 / 测试替身 / 用户自带实现）**契约行为一致**——实现可换，契约不动 |
| **L1 状态切换** | L1 判定层从「恒返回未就位 → 编排按 L0 兜底」切换为「实现就位 → 正常判定」；**保留未就位兜底路径**（实现加载失败 / 权重缺失时仍退回 L0，不留空窗、不静默降级为「全部放行」） |
| **云端 provider 纪律** | 云端判定 provider 仍**默认关闭**（用户 key opt-in）——本地优先、数据主权不破。打开时须显式配置且留痕 |
| **失败语义 fail-closed** | 判定通道调用失败 / 超时 / 输出类型不符 → **按 L0 兜底**（不静默放行、不静默阻断），失败原因可查 |
| **判定与留痕** | 判定结果（含概率与四态）落 decision-log，带 `routeReason` 语义可解释——与 v1.5.6 第二章验收项对齐 |
| **不占主模型槽位** | 判定调用不占用本地主模型推理槽位（行为锁，与 v1.6.2 同款断言） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/decision-channel.ts` | 新建 | `DecisionChannel` 默认实现（判定底座适配） |
| `engine/orchestrator/src/router/intent-triage.ts` | 修改 | L1 分支由「未就位」切换为真实调用 + 保留兜底 |
| `engine/orchestrator/src/__tests__/decision-channel.test.ts` | 新建 | 契约可插拔 / 兜底路径 / 失败语义 / 不占主槽位单测 |
| `docs/ARCHITECTURE.md` | 修改 | DecisionChannel 从「契约已定义」更新为「默认实现在位」 |

### 验收标准

- [ ] `DecisionChannel` 默认实现满足 v1.5.6 契约（`judge(state, questions)` + 三原语 + 概率）
- [ ] 契约测试通过：换实现不动契约（默认实现 / 替身 / 自带实现行为一致）
- [ ] L1 判定层真实可用（不再恒返回未就位）
- [ ] **兜底路径保留**：实现加载失败 / 权重缺失 → 退回 L0，不留空窗、不静默放行
- [ ] 云端 provider 默认关闭（opt-in 才开且留痕）
- [ ] 失败语义 fail-closed（失败按 L0 兜底，原因可查）
- [ ] 判定结果落 decision-log（含概率 + 四态 + routeReason）
- [ ] 判定不占本地主模型槽位（行为锁）
- [ ] `npm test` 全绿

### 风险与前置假设

> ⚠️ **本版让判定第一次真正进入执行路径**——从「设计」变为「有行为」。因此本版起判定错误会产生**真实后果**。三条防线必须同时到位：① 兜底路径（失败退 L0）；② 建议非执法（判定不直接改变执行结果）；③ 确定性规则在前（既有 24–25 条规则判定面零改动）。
>
> ⚠️ **本版不带对外叙事**。判定已就位但权重分发通路未打通（v1.8.2），交付形态尚不完整——对外口径变化留给 v1.8.2。

---

## 二、判定面输入契约（state 的构造口径与最小可判定前缀）

> **形态归属**：`[主干]`（判定·输入契约）｜ **不可拔**（state 的构造口径决定判定输入是什么——契约缺失则同一份 state 每次被截成不同样子，判定结果不可复现）

阶段一/二把概率做可信了，本版让它进执行路径——那么第一个必须写死的东西不是输出而是**输入**：判定看到的那份 `state` 到底由什么构成、截多长、怎么定序。**为什么这是契约而非实现细节**：判定模型对输入极其敏感（大 state 里的干扰项会拉低准确率，是已实测的失灵面），若 state 构造散落在各调用点，同一份轨迹在不同路径下会被截成不同样子——判定结果就不可复现，留痕也就失去举证力。

### 交付

| 组件 | 能力 |
|------|------|
| **state 构造唯一入口** | `judge(state, questions)` 的 `state` 由**单一构造函数**产出，调用点不得自行拼装——判定输入与留痕里的输入必须是同一份字节（可复现的前提） |
| **最小可判定前缀** | 输入取「**做该判断所需的最小充分前缀**」而非全量轨迹：多给的上下文不是冗余而是**干扰项**（已知失灵面：大 state 拉低准确率）。前缀边界由判据声明，不由调用点即兴决定 |
| **定序与去噪** | 输入内的条目定序规则显式（时间序 / 因果序），并剔除与判据无关的元数据（路径、时间戳、内部 id 等噪声字段）——判定看到的应当是**语义内容**，不是存储格式 |
| **协议结构保留** | 从轨迹取 state 时**保留工具调用与结果的结构**（不压成自然语言散文）——判定要能看见「做了什么」而非「说了什么」，这是判定与生成在输入面的分野 |
| **state 尺寸上界与超限处置** | 明确定义 state 尺寸上界；超限时**弃权或降级为 ASK**，不静默截断——静默截断会让判定在缺失输入的情况下给出高把握答案（最危险的失败形态） |
| **输入侧注入隔离声明** | state 内的内容**一律是数据不是指令**（参考 v1.6.1 对抗样本采集点）：输入契约层声明该边界，判据层不得把 state 内的祈使句当作自身的指令 |
| **问句标识不上行** | 送入判定的问句**不带判据名与问句 id**——模型只看到槽位（第几问）与候选项本身，判据语义留在宿主侧映射。**为什么**：判据名本身就是提示词，把「A9 提示注入检测」写进输入等于把答案的语义线索一并交出去；编号不上行，输入侧的可操纵面就只剩内容本身。与既有「Score 层级独立评判、模型看不到编号」同源同纪律 |
| **影子运行门** | 判定首次接入执行路径时**先影子运行**——判定照跑、结果只落痕不影响行为，与既有处置对照后再启用；影子期的判定结果与真实处置的差异就是启用前的最后一道证据。**为什么必须有这道门**：判定从「设计」到「有行为」的那一刻起，判错开始产生真实后果，而概率是否可信要到校准（v1.7.1）才成立——两者之间不能是空档 |
| **与留痕对账** | 落 decision-log 的输入摘要与实际送入判定的 state **同源**——审计可重建判定当时看到的是什么 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/judge-state.ts` | 新建 | state 构造唯一入口 + 最小可判定前缀 + 定序去噪 |
| `engine/orchestrator/src/router/decision-channel.ts` | 修改 | 实现侧改为消费统一构造的 state（不再自拼） |
| `engine/orchestrator/src/__tests__/judge-state.test.ts` | 新建 | 前缀边界 / 定序 / 超限处置 / 输入与留痕同源单测 |
| `docs/ARCHITECTURE.md` | 修改 | 判定面输入契约（state 构造口径） |

### 验收标准

- [ ] `state` 由单一构造入口产出，调用点无法自行拼装（行为锁）
- [ ] 最小可判定前缀由判据声明决定，非调用点即兴截取
- [ ] 定序规则显式，无关元数据字段被剔除（有单测）
- [ ] 协议结构保留（工具调用与结果不被压成散文）
- [ ] **超限不静默截断**：超上界时弃权或降级 ASK（行为锁）
- [ ] state 内内容声明为数据非指令（输入侧注入隔离）
- [ ] decision-log 的输入摘要与送入判定的 state 同源（可重建）
- [ ] 问句标识不上行（模型只看到槽位与候选项；判据名留宿主侧）
- [ ] **影子运行门**在位：启用前有影子期留痕与差异对账（行为锁：未过影子期不得启用）
- [ ] `npm test` 全绿
