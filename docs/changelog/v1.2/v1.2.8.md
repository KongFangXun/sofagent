# v1.2.8 开发日志 — 记忆分层 + 定时任务 + 🔗 激活链 Phase 3 前半 + 🚪 release-gate-loop F 修复者 + 🔍 FORGE 全 loop 接入 audit + ⏸️ Checkpoint/Resume

> 状态：✅ 已开发 · 孔放勋 · 2026-08-07
> 激活链设计文档：[docs/guides/fde-activation-chain.md](../../guides/fde-activation-chain.md)

## 🚪 release-gate-loop 新增 F（修复者）角色

### 定位

v1.2.7 的 release-gate-loop 是线性 5 步跑完即出 PASS/FAIL。FAIL 之后人工修，修完手动重跑——闸门只发现问题，不参与修复。

v1.2.8 在 release-gate-loop 内部新增 **F（修复者，Fixer）** 角色：V 验证 FAIL 后，F 读 verdict 报告 → 定位根因 → 改代码 → 跑 audit → 回到 V 重验。V 和 F 形成**验-改循环**，直到 V 出 PASS 或达到最大轮次（3 轮）。

### 设计

```
round N:
  V 验证（5 步线性，现有）
    acceptance → regression → coverage → consolidate → verdict
    verdict = PASS → 出 loop，可以发版 ✅
    verdict = FAIL ↓

  F 修复（新增）
    step 6: f-diagnose  — F 读 verdict.md，定位根因，写修复方案到 fix-plan.md
    step 7: f-fix       — F 按方案改代码（最小改动），写 fix-summary.md
    step 8: f-audit     — driver 自动跑 sofagent-audit --diff（铁律：变更必审）
                          audit FAIL → 打回 F 重修（不进 V）
                          audit PASS → 进入 round N+1

  收敛条件：V 出 PASS / 达到 3 轮上限 → 出"需人工介入"报告
```

### 与 fresh-eyes-loop A/B 的镜像关系

| | fresh-eyes-loop | release-gate-loop |
|---|---|---|
| 主角色 | A（审查者）| V（验证者）|
| 副角色 | B（工程师）| F（修复者）|
| 触发 | 固定多轮 | verdict FAIL 触发 |
| 收敛 | 连续 2 轮 clean | V 连续 PASS / 3 轮上限 |

### 交付

| 文件 | 改动 | 说明 |
|------|------|------|
| `FORGE/src/release-gate-driver.mjs` | 修改 | 循环结构改造：verdict FAIL 后触发 F 步骤，最大 3 轮。⚠️ acceptance SKIP 鸡生蛋 bug 已在 v1.2.7 commit 6c5776d 修复（`ensureAcceptancePreRun()`），本版不重做，仅回归验证多轮循环不破坏该逻辑 |
| `FORGE/SKILL/release-gate-loop/SKILL.md` | 修改 | 监控协议更新（多轮结构 + F 步骤说明）|
| `FORGE/SKILL/release-gate-loop/prompts/f-diagnose.md` | 新建 | F 诊断 prompt |
| `FORGE/SKILL/release-gate-loop/prompts/f-fix.md` | 新建 | F 修复 prompt |
| `FORGE/models/profile.mjs` | 修改 | 新增 F 角色映射（GLM-5.2 + engineer role）|
| `FORGE/env.local.template` | 修改 | 新增 `SOFAGENT_LLM_F` 占位变量（A/B/V 已在 v1.2.7 commit 6c5776d 补齐，本版只补 F）|

> ⚠️ **架构变更**：v1.2.7 的 STEPS 无 `role` 字段（只有 V 一个角色，硬编码）。v1.2.8 显式化所有步骤的 `role`。新增 `role: null` 步骤类型（f-audit / b-audit）——driver 直接执行 `runAuditGate()`，跳过 `createReactAgent` 调用。执行引擎需增加 `role === null` 分支。

## 🔍 FORGE 全 loop 接入 audit（dogfooding 铁律）

### 定位

sofagent 的核心卖点是"审计每次变更"。但 v1.2.7 的两个 FORGE loop（fresh-eyes-loop / release-gate-loop）改代码后都只跑 `npm test`，**全程不跑 sofagent-audit**。这是纯粹的遗漏——driver 零提及 audit，loop.md 零提及，b-fix prompt 零提及。

v1.2.8 补齐：所有 FORGE loop 里改代码的步骤，改完必须跑 `sofagent-audit --diff`，audit 不通过打回重修。

### 交付

| 文件 | 改动 | 说明 |
|------|------|------|
| `FORGE/src/fresh-eyes-driver.mjs` | 修改 | b-fix 步骤后插入 audit 检查（新步骤 b-audit）|
| `FORGE/SKILL/fresh-eyes-loop/prompts/b-fix.md` | 修改 | 补充"改完代码后跑 audit"指令 |
| `FORGE/src/release-gate-driver.mjs` | 修改 | F 步骤内置 f-audit（见上方 ⑤）|
| `FORGE/src/driver-base.mjs` | 修改 | 新增 `runAuditGate()` 公共函数（两个 driver 共用）。内部自动 git commit + 跑 audit |

### audit 检查内容

F/B 改完代码后，driver 自动执行：
```bash
git add -A && git commit -m "FORGE auto-commit: <step> round-<N>"
sofagent-audit --diff HEAD~1..HEAD --silent
```
- exit 0（全过）→ 进下一环节
- exit 1（有警告）→ 记录但继续（警告不阻塞）
- exit 2（有违规）→ 打回 F/B 重修，不进 V/A-verify

> auto-commit message 带 `FORGE auto-commit:` 前缀，方便事后过滤。

## 🔗 激活链 Phase 3 前半：dag-runner 扩展 + 节点执行器

### 定位

v1.2.7 构建了 StateGraph 配置。v1.2.8 让 dag-runner 真正能跑企业 Agent，并新增 `run-enterprise` CLI 命令。

### 交付

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/dag-runner.ts` | 修改 | 扩展支持企业 Agent 执行 |
| `engine/orchestrator/src/node-executor.ts` | 新建 | 企业节点执行器 |
| `engine/orchestrator/src/cli.ts` | 修改 | 新增 `run-enterprise` 子命令 |
| `engine/orchestrator/src/__tests__/node-executor.test.ts` | 新建 | 单测 |

**CLI**：`sofagent-orchestrator run-enterprise [--workflow .sofagent/data/workflow.yml]`

> 详细开发 Prompt：`~/Desktop/sofagent-dev-prompts/fde-activation-phase3-4-v1.2.8-v1.2.9-v1.3.0-prompt.md`

## ⏸️ FORGE Driver Checkpoint/Resume（断点续跑）

### 定位

v1.2.7–v1.2.8 开发期间多次遭遇 driver 被宿主环境杀死的场景（macOS App Nap、session 过夜清理、OOM SIGKILL）。driver 设计为长时间运行进程（10–60 分钟），但宿主在 session 过期或空闲时清理后台进程，driver 被杀后只能从零重跑——之前跑完的 N 轮审查全部作废。

典型事故：run-02 在 UTC 19:04 心跳停止（session 过夜清理），Round 1 的 10 个完整产物（a-check/b-check/findings/result/summary/audit/result-verify 等）全部有效，但重跑只能从 Round 1 重新开始。

### 设计

在 driver 主循环的关键节点写 `resume-point.json` 断点文件。driver 启动时检测 `--resume` 参数，读取断点，从上次完成的位置继续执行。

```
正常执行流程：
  round 1 步骤全部完成 → 写 resume-point.json { round:1, completed:true }
  round 2 步骤全部完成 → 写 resume-point.json { round:2, completed:true }
  ...
  driver 被杀 → resume-point.json 停在最后一轮 completed

断点续跑：
  driver --resume --target vX.Y.Z
    → 读 resume-point.json → 从 round N+1 开始
    → 已完成的轮产物（findings/result/summary 等）直接复用，不重跑
```

断点文件位置：`<runDir>/resume-point.json`

断点内容：
```json
{
  "round": 2,
  "completed": true,
  "timestamp": "2026-08-07T19:04:26Z",
  "counts": { "p0": 3, "p1": 7, "p2": 5 },
  "cleanStreak": 0,
  "consecutiveDegraded": 0,
  "severityHistory": [10, 5]
}
```

### 交付

| 文件 | 改动 | 说明 |
|------|------|------|
| `FORGE/src/driver-base.mjs` | 修改 | 新增 `saveResumePoint()` / `loadResumePoint()` 公共函数（两个 driver 共用）|
| `FORGE/src/fresh-eyes-driver.mjs` | 修改 | main() 主循环插入断点写入 + `--resume` 参数解析 + 断点恢复逻辑 |
| `FORGE/src/release-gate-driver.mjs` | 修改 | 同上，V/F 多轮循环也插入断点写入 + `--resume` |
| `FORGE/src/driver-base.test.mjs` | 修改 | 新增 saveResumePoint/loadResumePoint 单测 |
| `tools/forge-smoke-test.sh` | 修改 | 冒烟测试覆盖 `--resume` 参数不崩溃 |

> 详细开发 Prompt：`~/Desktop/sofagent-dev-prompts/forge-checkpoint-resume-v1.2.8-prompt.md`

## 原有规划（DeerFlow 启发）

① **记忆事实级分层**（per-user memory.json + per-fact Markdown + `__default__` 桶）— Dream Cycle 缺事实级粒度。实现位置：`engine/core/src/memory-store.ts` + `engine/daemon/src/dream-cycle/extract-facts.ts` + `state-machine.ts`
② **Scheduled Tasks MVP**（cron+once / 暂停/恢复/触发/历史/删除）— daemon cron.ts 从占位升级为一级定时任务（LIMITATIONS §七「定时触发做不到」的解法）
③ **ToolOutputBudget 中间件化**（把 sf_read 500 行截断从单点提升为分层中间件）。实现位置：`FORGE/src/tool-output-budget.mjs`（FORGE loop 专用中间件，非通用 harness 逻辑）

## 版本范围（7 项全做）

| # | 功能 | 定位 |
|---|------|------|
| ① | 记忆事实级分层 | Dream Cycle 事实级粒度补全 |
| ② | Scheduled Tasks MVP | daemon cron 定时任务一级能力 |
| ③ | ToolOutputBudget 中间件化 | sf_read 截断从单点提升为中间件 |
| ④ | 🔗 激活链 Phase 3 前半 | dag-runner 跑企业 Agent + run-enterprise CLI |
| ⑤ | 🚪 release-gate-loop F 修复者 | 闸门从"线性"升级为"验-改循环" |
| ⑥ | 🔍 FORGE 全 loop 接入 audit | dogfooding：自己的 loop 走自己的 audit |
| ⑦ | ⏸️ FORGE Driver Checkpoint/Resume | driver 被杀后断点续跑，不丢已完成轮产物 |

## 行业对标

- ①②③ 设计启发自 [DeerFlow 2.0](https://github.com/bytedance/deer-flow)，详见 [PHILOSOPHY §十](../../PHILOSOPHY.md)
- ④ 激活链 Phase 3 详见 [激活链设计文档](../../guides/fde-activation-chain.md)
- ⑤⑥ 来自 v1.2.7 release-gate-loop run-01 实跑暴露的工程缺口

## 发布检查清单（汇总）

### 代码质量

- [x] npm test 1562/1562 全绿（audit 587 / orchestrator 379 / core 219 / daemon 187 / mcp 69 / skillopt 33 / eval 29 / rules 28 / ab-test 12 / harness 13 / ontology 5 / think 1 共 12 包）
- [x] npm run build exit 0（12 包全部编译成功）
- [x] acceptance-test 148 场景全通过（225 断言通过 / 0 失败，含 v1.2.8 新增 scenario 208-214）
- [x] bash -n acceptance-test.sh 语法通过
- [x] check-version.sh 67/67 全绿，版本号 = 1.2.8
- [x] check-test-count.sh 全绿（SSOT 1562 / 148 场景）
- [x] FORGE smoke test 11/11 全绿（6 模块加载 + 3 测试文件 + 2 dry-run）
- [x] 版本号已 bump 至 1.2.8（package.json SSOT + 13 类位置同步）
- [x] 文档头日期同步 2026-08-07（EXPECTED_DOC_DATE + 文档头）

### 功能交付（7 项功能全交付）

- [x] ① 记忆事实级分层（createMemoryStore + CRUD + search + 分层目录）
- [x] ② Scheduled Tasks MVP（createScheduler + loadTasks + nextCronTime）
- [x] ③ ToolOutputBudget 中间件化（DEFAULT_BUDGET + getStepBudget + truncateToolOutput）
- [x] ④ 激活链 Phase 3 前半（node-executor + checkHITL + resolveEnterpriseAgent）
- [x] ⑤ release-gate-loop F 修复者（f-diagnose/f-fix/f-audit + V+F 循环）
- [x] ⑥ FORGE 全 loop 接入 audit（runAuditGate driver 步骤 + role:null）
- [x] ⑦ Checkpoint/Resume（saveResumePoint + loadResumePoint + --resume + 原子写）

### 已知遗留（v1.2.9 处理）

- [ ] f-audit 步骤 `REPO_ROOT is not defined` bug（driver 步骤环境变量缺失）
- [ ] verdict PASS 时 results 中 acceptance/regression 仍显示 FAIL（状态不一致）
- [ ] f-fix worker 撞 55 次工具调用硬上限后走降级裸 LLM（v1.2.9 短任务化改造解决）
