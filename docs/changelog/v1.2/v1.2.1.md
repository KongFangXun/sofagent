# v1.2.1 开发日志 — custom/ 设计闭环 + ToolGate 运行时接入 + hook 注入 + 巡检合并 + SubAgent 可见性 L2 🔧

> 状态：规划中 · 孔放勋 · 2026-07-25
> 前置依赖：v1.2.0（物理结构大重构 + 多设备知识联邦收口）

## 背景

v1.2.0 完成了 P1 物理重构（目录重命名 / Skill 收敛 / 安装体系 / 发版工具链搬迁）+ P3 rules-engine 新包 + ToolGate 骨架。但审查发现两个"建好了没接线"的半闭环：

1. **ToolGate 出口已接好但没人用**——`createToolGate()` + `toolGate` 单例都 export 了，但 `loop/nodes.ts` 只用 `ENGINEER_TOOLS`，没调 gate。门锁挂在门框上了，开门没拧钥匙。
2. **custom/ 目录骨架就位但加载链没声明**——`custom/README.md` 写了加载顺序，但 `SKILL.md` 加载链没提 custom/，`file-deploy.sh` 也不处理 custom/ 目录。用户写了规则但 Agent 不会自动加载。

v1.2.1 补这两个闭环，外加 custom/README 从"说明书"升级为"操作手册"。

## LUI 感知

用户通过 Agent 对话感知：「我给 engineer 加了一条 custom 规则说只许改 TypeScript 文件——结果它真碰 shell 脚本的时候被 ToolGate 拦住了，告诉我『被规则拦截』。custom 写的东西真的生效了。」

## 交付项

### P0 · ToolGate 运行时接入（v1.2.0 半闭环修复）

**问题**：`tools.ts` 有 `createToolGate()` + `toolGate` 单例 + 完整的 RulesEngine 注入链，但 `loop/nodes.ts` 的 `defaultRunEngineer` / `defaultRunReviewer` 只传 `ENGINEER_TOOLS` / `REVIEWER_TOOLS` 给 DeepAgents，没有在 tool call 前过 gate。

**修复**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/tools.ts` | +28 行 | 新增 `wrapToolsWithGate(tools, gate)` 函数——将每个 tool 的 `func` 包一层 gate 检查。FAIL→返回拦截信息不执行原 func；WARN→执行但返回值前拼警告；PASS→正常执行 |
| `engine/orchestrator/src/loop/nodes.ts` | +6 行 | `defaultRunEngineer` + `defaultRunReviewer` 均调用 `createToolGate()` 创建 gate → `wrapToolsWithGate()` 包装工具集 → 传 `gatedTools` 给 DeepAgents |
| `engine/orchestrator/src/index.ts` | +1 行 | 导出 `wrapToolsWithGate` |

**验证**：全 workspace 937/937 tests 全绿（12 包），orchestrator 167 tests 全绿。全仓 grep `wrapToolsWithGate` 在运行时有 4 处调用（import + engineer gate + reviewer gate × 2）。

### P1 · custom/ README 重写

**问题**：v1.2.0 写的 `custom/README.md` 只是说明书（"这是什么"），没有回答用户核心疑问——custom/ 和 LOOP 沉淀的代码什么关系？改了代码放哪？

**修复**：`SKILL/custom/README.md` 重写，核心变化：

- 明确 **custom/ 只管规则，不管代码**——加了一张「改动类型 → 归属」对照表，把 Agent 行为规则 / 业务流程约束 / 审计规则开关 / 知识库 / 代码变更 / LOOP 沉淀六类改动的归属全部列清
- 回答"LOOP 写完的代码沉淀到哪"——进 Git commit + `.sofagent/knowledge/`，不进 custom/
- 加了 ⚠️ 当前状态标注：加载链声明和安装保护逻辑都计划在 v1.2.1 落地
- 补充 Agent 运行时加载顺序 + 升级三策略的 ⚠️ 标注

### P2 · custom/ 设计闭环补全（待实施）

**问题**：custom/ 的两个前提还没实现——

| 前提 | 设计意图 | 当前状态 | v1.2.1 目标 |
|------|---------|---------|------------|
| ① SKILL.md 加载链声明 custom/ | Agent 启动时先加载引擎层再加载 custom/ | ❌ SKILL.md 没提 custom/ | 在 SKILL.md 加载链段落声明 custom/ 加载顺序 |
| ② file-deploy.sh 安装保护 | 安全升级时只覆盖引擎层，custom/ 不动 | ❌ file-deploy.sh 不处理 custom/ | file-deploy.sh 增加 custom/ 目录的安装 + 升级保护逻辑 |

**实施任务**：

- [ ] SKILL.md 加载链段落补充 custom/ 加载声明
- [ ] `buildConstrainedSystemPrompt()` 或等价加载函数补充 custom/ 读取逻辑
- [ ] file-deploy.sh 安装时创建 custom/ 目录（如果不存在）
- [ ] file-deploy.sh 升级时跳过 custom/（安全升级策略）
- [ ] file-deploy.sh `--force` 覆盖 custom/（强制覆盖策略）
- [ ] file-deploy.sh `--merge` 三路合并 custom/（diff 合并策略）

### P3 · SubAgent 可见性 L2（worker 细粒度埋点）

**背景**：v1.2.0 的 FORGE fresh-eyes-driver 已实现 L1 可见性（轮次/步骤级，driver 编排层）。但 worker（SubAgent）内部是黑盒——A 正在读哪些文件、B 正在改哪行、token 消耗实时变化，driver 在 worker 退出前完全不可见。这是 Dashboard 实时面板（v1.2.2 L3）的数据前置缺口。

**三层可见性架构**（L1 已交付，L2 本版本，L3 在 v1.2.2）：

| 层 | 粒度 | 数据源 | 状态 |
|----|------|--------|:----:|
| **L1 driver** | 轮次/步骤完成 | `progress.jsonl` + `status.json` | ✅ v1.2.0 已交付 |
| **L2 worker** | 工具调用序列 + 模型推理心跳 | `sub-progress-A.jsonl` + `sub-progress-B.jsonl` | 🔧 v1.2.1 |
| **L3 Dashboard** | 实时面板（进度条 + 工具流 + 成本曲线） | 聚合 L1+L2 数据渲染 | 📋 v1.2.2 |

**设计**：利用 deepagents 的 middleware 机制，写一个 `ProgressMiddleware`，挂在每次工具调用（`sf_read`/`sf_write`/`sf_edit`/`ls`/`glob`/`grep`）和模型推理事件上，往 worker 自己的 `sub-progress-<role>.jsonl` 追加微事件：

```jsonl
{"ts":"...","role":"A","tool":"read","target":"README.md","phase":"start"}
{"ts":"...","role":"A","tool":"read","target":"README.md","phase":"end","duration":120}
{"ts":"...","role":"B","tool":"edit","target":"tools.ts","phase":"start","lines":"+3/-1"}
{"ts":"...","role":"B","event":"llm-chunk","tokens":523}
```

**Dashboard 能看到的实时信号**（v1.2.2 消费 L2 数据）：
- **工具调用流**：A 读了 README → grep CHANGELOG → B 改了 tools.ts 第 45 行...
- **实时成本**：usage.jsonl（已有）+ sub-progress token 事件 → 成本曲线
- **双 agent 心跳**：最后一条 sub-progress 时间戳判断是否卡死

**文件清单**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `FORGE/src/progress-middleware.ts` | 新建 | DeepAgents middleware：挂工具调用前后 + LLM 流式 chunk 事件 |
| `FORGE/src/fresh-eyes-driver.mjs` | 修改 | `runWorker()` 创建 agent 时注入 ProgressMiddleware，传入 `roundDir` + `role` |
| `FORGE/src/__tests__/progress-middleware.test.ts` | 新建 | 单测：工具调用写事件 / LLM chunk 写事件 / 错误不阻断主流程 |

**验收标准**：

| 检查项 | 通过标准 |
|------|------|
| sub-progress-A/B.jsonl 生成 | 每个 worker 启动后 3 秒内出现至少 1 条事件 |
| 工具调用事件完整性 | sf_read/sf_write/sf_edit 每次调用产生 start+end 两条 |
| LLM chunk 事件 | 模型推理期间每 ~1s 至少 1 条 chunk 事件（心跳） |
| 容错 | middleware 抛错不阻断 worker 主流程（与 L1 visibility 一致） |
| npm test | FORGE 包测试全绿 + 新增 middleware 单测 ≥5 用例 |

## 质量验证

| 检查项 | 结果 |
|------|:--:|
| npm test（全 workspace） | ✅ 937/937 全绿（12 包） |
| ToolGate 运行时调用验证 | ✅ grep `wrapToolsWithGate` 在 nodes.ts 有 4 处命中 |
| orchestrator build | ✅ exit 0 |
| custom/README 边界表 | ✅ 六类改动归属全覆盖 |

## 发布检查清单

- [x] ToolGate 运行时接入（wrapToolsWithGate + nodes.ts 调用）
- [x] custom/README 重写（规则 vs 代码边界 + ⚠️ 当前状态标注）
- [ ] SubAgent 可见性 L2（ProgressMiddleware + sub-progress jsonl）
- [ ] SKILL.md 加载链声明 custom/
- [ ] file-deploy.sh 安装保护逻辑
- [ ] pre-push-check 全绿
- [ ] npm test 全绿

## Release Notes · v1.2.1

### 🔨 核心变更

#### ToolGate 运行时接入
- **`wrapToolsWithGate()` 函数**：tools.ts 新增工具集包装器，每个 tool call 执行前过 RulesEngine 检查（v1.2.1）
- **双节点接入**：engineer + reviewer 两个 LOOP 节点均接入 ToolGate（v1.2.1）
- **拦截语义**：FAIL 阻断执行返回拦截信息；WARN 放行但返回值前拼警告；PASS 正常（v1.2.1）

#### custom/ 文档 + 设计闭环
- **custom/README 重写**：从说明书升级为操作手册，明确规则 vs 代码边界（v1.2.1）
- **custom/ 加载链声明**：SKILL.md 声明引擎层→custom/ 加载顺序（v1.2.1 待实施）
- **custom/ 安装保护**：file-deploy.sh 三种升级策略保护 custom/（v1.2.1 待实施）

#### SubAgent 可见性 L2
- **ProgressMiddleware**：DeepAgents middleware，挂工具调用 + LLM 推理事件，写 sub-progress-\<role\>.jsonl（v1.2.1）
- **worker 内部可观测**：A/B 双 agent 工具调用序列 + 模型推理心跳，不再是黑盒（v1.2.1）
- **Dashboard 数据前置**：L2 数据是 v1.2.2 Dashboard 实时面板（进度条/工具流/成本曲线）的输入（v1.2.1）

### ✅ 质量验证

| 检查项 | 结果 |
|------|:--:|
| npm test | ✅ 937/937 全绿 |
| ToolGate 运行时调用 | ✅ nodes.ts 4 处调用 |
| orchestrator build | ✅ exit 0 |
