# v1.2.6 开发日志 — 🔗 激活链 Phase 2 前半（映射表+注册扩展）+ 🔌 MCP 交付链路修补 + 📄 文档死链清零

> 状态：✅ 已开发（代码完成，开发日志阶段七定稿） · 孔放勋 · 2026-08-03
> 前置依赖：v1.2.5（激活链 Phase 1 — 企业 Agent 注册）
> 开发完成快照：npm test 1464/1464 通过（audit 571 / core 185 / orchestrator 347 / daemon 171 等 12 包）· acceptance-test 132 场景 · shellcheck 零 error · check-version.sh 65/65 · check-test-count.sh 13 处校验一致 · 版本号已 bump 至 1.2.6
> 激活链设计文档：[docs/guides/fde-activation-chain.md](../../guides/fde-activation-chain.md)

## 🔗 激活链 Phase 2 前半：映射表 + 注册扩展

### 定位

v1.2.5 Phase 1 完成了企业 Agent 注册（车厢造好挂上去了）。v1.2.6 让 workflow-parser 和 registry 认识企业 Agent 类型——为 v1.2.7 的 StateGraph 构建打基础。

### 交付

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/workflow-parser.ts` | 修改 | 扩展映射表支持 `agent: enterprise` 类型 |
| `engine/orchestrator/src/registry.ts` | 修改 | `SubAgentDefinition` 增加 hitl / hitlConfig / knowledgeDomain 字段 |
| `engine/orchestrator/src/__tests__/workflow-parser.test.ts` | 修改 | 新增 enterprise agent 映射测试 |

### 关键改动

**workflow-parser.ts**：当前映射表写死 4 个内置 Agent。新增 `enterprise` 类型，通过 `listAgents()` 动态查找注册的 YML（而非写死映射）。enterprise 节点查找失败时报错（不静默降级）。

**registry.ts**：`SubAgentDefinition` 接口新增 `hitl` / `hitlConfig` / `knowledgeDomain` 字段（全部可选，向后兼容）。同步扩展 `loadDefinition()` 解析逻辑，确保 YML 写入的新字段能被读回。

### 2A · workflow.yml 格式对齐（activate ↔ workflow-parser 共用 schema）

**背景**：实测发现两个解析器对同一份交付物期望不同 YAML 结构——`activate` 期望平铺格式（顶层 `name` / `nodes[]`），`workflow-parser` 期望嵌套格式（`workflow:` 根节点）。同一份 workflow.yml 无法同时喂给两个解析器，导致企业 Agent 注册了但编排读不到。

**交付**：`activate.ts` 读取层兼容嵌套格式（`const wf = doc['workflow'] ?? doc`），统一为嵌套格式标准；写入侧仍产嵌套。`FDE/GUIDE.md` 明确 workflow.yml 标准格式（嵌套 `workflow:` 根节点），避免 FDE 交付物与引擎期望格式漂移。

**验收**：同一份嵌套格式 workflow.yml 能同时通过 `activate` 和 `parseWorkflowYaml` 解析；activate 对平铺旧格式仍向后兼容。

### 2B · SOFAGENT_LLM 环境变量打通（编排执行前置修复）

**背景**：`resolveLLMModel()` 只认 `SOFAGENT_LLM`，为空时直接 return null 无警告。用户配了 `SOFAGENT_LLM_A` / `SOFAGENT_LLM_B`（FORGE 审查用）+ API key，但编排主链路永远跑不通——`subagent run fde` 返回"编排引擎未返回结果"。

**交付**：`resolveLLMModel` / `resolveApiKey` 增加 `SOFAGENT_LLM_A` / `SOFAGENT_LLM_B` 回退。`SOFAGENT_LLM` 显式设置时优先级不变（显式 > A/B 回退）。

**验收**：仅设 `SOFAGENT_LLM_A` + `SOFAGENT_LLM_A_API_KEY`（不设 `SOFAGENT_LLM`）时，`resolveLLMModel(null)` 返回模型实例。

## 🔌 MCP 交付链路修补

### 定位

MCP 覆盖度审计发现 13 个应 MCP 化而未做的缺口 tool，用户已拍板版本归属。v1.2.6 承接其中最紧急的四项——它们都是「能力已在、只差 MCP 暴露」的交付链路修补：两个是只读查询（实现现成），两个闭合激活链的断点。

### 交付（4 个 tool）

| tool | 所属包 | 一句话 |
|------|--------|--------|
| `daemon_status` | daemon | 查 daemon 运行状态：heartbeat、健康报告、推送队列/重试失败数、联邦 peers 概要。实现现成（`readHealthFile` / `checkDaemonHealth` 已导出）。**只读查询，不含 start/stop 进程控制** |
| `list_agents` | orchestrator | 列出已注册 SubAgent（含企业 Agent），闭合 ACTIVATE→ORCHESTRATE 发现入口。实现现成（`registry.listAgents`） |
| `list_concepts` | mcp | 列出全部 concept，与 `list_entities` 对称（实现位于 mcp-server.ts，照抄 toolListEntities 读 knowledge 目录模式） |
| `hitl_resolve` | orchestrator | 对 awaiting_human 挂起的 HITL checkpoint 写入人工决策（approve/reject）并续跑，闭合编排人工确认环 |

### 安全边界（用户已背书）

1. daemon 类 tool 只做**只读查询**——start/stop 等进程控制永不 MCP 化（防恶意 Agent 滥用）
2. 联邦 peers 只在 `daemon_status` 内含**只读概要**——pair/unpair 配对操作不 MCP 化

## 📄 文档死链清零（scenario 164 门禁前置）

**背景**：ROADMAP.md + LIMITATIONS.md 从根目录搬进 docs/（commit 8e6472b）后，后续提交 282b1c3 声称「补全搬家后的全量文档链接」，但未碰 docs/ROADMAP.md 和 docs/LIMITATIONS.md——2026-08-03 实测全仓 md 死链仍有 74 处。

**死链分布**：`docs/ROADMAP.md` 43 处、`docs/LIMITATIONS.md` 8 处、`docs/changelog/releasing.md` 6 处、`docs/changelog/v1.0/v1.0.0.md` 2 处（历史文档，修或加白名单）、其余 15 处散落在各 changelog / FORGE playbook / SKILL 文档。

**修复方法**：docs/ 下文件互链用 `./xxx.md`，指向根目录文件用 `../README.md`。修完用 scenario 164 同款脚本回归，期望 `TOTAL_BROKEN=0`。

**验收**：`TOTAL_BROKEN=0`（或历史冻结文档已加白名单并注明）；acceptance scenario 164 通过。

## Deployment Sizing 表格（README 企业 IT 参考）

**交付**：README 新增 Deployment Sizing 三档表格（个人/中型/企业级），明确 CPU/内存/磁盘/并发 Agent 数量，企业 IT 问"几核几 G"能直接答。

**三档规格**：
- 个人（1-3 Agent）：1 核 / 512 MB / 500 MB
- 中型（5-10 Agent）：2 核 / 1 GB / 2 GB
- 企业级（10+ Agent）：4 核 / 2 GB / 5 GB+

**数据来源**：各包 dist 实测（daemon 1.2M / mcp 340K / audit 1.2M / orchestrator 1.1M / core 1.0M），运行时数据实测（~94 MB），daemon 心跳 5min 间隔 + maxRetries=3。

## 项目文档同步清单（D6 · 阶段二交付）

| 功能点 | 应在文档 | 当前覆盖 | 状态 |
|------|---------|---------|:--:|
| resolveAgent enterprise 动态查找 | ARCHITECTURE.md（L120 ORCHESTRATE）+ activation-chain.md（L93 版本表） | ✅ 两处已标注 v1.2.6 | ✅ |
| registry hitl/knowledgeDomain 字段 | activation-chain.md（L93） | ✅ 已标注 | ✅ |
| MCP 4 tool（daemon_status 等） | ARCHITECTURE.md（L146 mcp 包条目）+ acceptance-test scenario 192 | ✅ 包条目已有，场景已补 | ✅ |
| 2A workflow.yml 格式兼容 | FDE/GUIDE.md（3 处 workflow.yml 描述） | ✅ 已覆盖 | ✅ |
| 2B SOFAGENT_LLM 四级回退 | activation-chain.md 或 DEVELOPMENT.md | ✅ DEVELOPMENT.md L233 已补 | ✅ |
| 文档死链清零 | acceptance-test scenario 197 | ✅ 场景已补 | ✅ |
| Deployment Sizing | README.md | ✅ 已写入 | ✅ |
| ROADMAP v1.2.6 条目 | ROADMAP.md（L8-18） | ✅ 已更新 | ✅ |

> D6 归属原则：详细机制在 activation-chain.md（权威设计文档），ARCHITECTURE.md/ROADMAP.md 一句话 + 链接引用。2B 环境变量回退链已在阶段七补入 DEVELOPMENT.md L233。

## BugFix 批次（fresh-eyes 审查 + 人工补充）

- 版本号全线 bump（package.json + TS/Shell/PS1/SKILL/docs 共 62+ 处）
- install.sh 文案更新 + FDE/GUIDE.md 链接修复
- releasing.md 版本号 bump 防跳过门禁（步骤 1b + D5 升级）

## 阶段四：审查体系合并更新（commit `e417a41`）

| 交付 | 落点 | 说明 |
|------|------|------|
| regression 维度 70 | `regression-checklist.md`（阶段二 D4 已写入） | MCP tool 注册三处一致性（v1.2.6 新盲区） |
| 维度声称修正 | `regression-checklist.md` L25（49→50 项） | 阶段二加维度 70 时漏改标题声称数 |
| acceptance-test 瘦身 | `acceptance-test.sh`（1612→1600 行） | Tier 2 深度瘦身：删 3 个版本分隔注释块 + 合并口径说明（阶段五修场景 197 后回涨至 1604，警戒线已上调至 1650） |
| FORGE/lessons 死链修复 | `FORGE/lessons/index.md` L49 | 锚点 `#thinking-only-模型特殊处理` → `#thinking-模型特殊处理`（GitHub 锚点规则不保留"-only"） |
| fresh-eyes-review | 无变更 | Tier 3 决策树判定：本版本预料外发现均为可精确描述问题，已入 regression + acceptance，无需校准视角 |

> Tier 1 防膨胀自检全绿：regression 1041≤1050 | acceptance 1600≤1600（阶段五修 bug 后 1604，警戒线已上调 1650） | fresh-eyes 264≤350。声称一致性全绿：regression 50=50 | acceptance 132=132 | check-docs 全部通过。

## 阶段五：验收脚本误报修复（commit `1242bae`）

release-gate-loop 报场景 197 + 场景 120 失败，审查判定均为**验收脚本自身 bug**（非产品 bug），修复后全绿：

| 场景 | 根因 | 修复 |
|------|------|------|
| 197（文档死链清零） | bash grep+sed 清洗 bug：`sed 's/]((//'` 删不掉 `](` 单括号 → target 残留前缀 → 381 处假阳性 | 替换为场景 164 同款 node 实现（精确正则 + `path.resolve`），死链 381→0 |
| 120（README 零 token） | 精确匹配 `grep -q "零 token"` 断在 README 措辞演进「零**额外** token」上（4 条混合规则确实用 token，「零额外」更准确） | `grep -q` → `grep -qE '零(额外)?[ ]?token'` 语义匹配 |

> 教训：① 引用已有场景实现必须真复用（拷贝），不能"参考重写"——注释写"复用 scenario 164"但实际退化成 bash grep+sed。② 验收脚本对文案匹配应容忍合理措辞演进，用语义正则而非精确字符串。

## 阶段六：审查体系最终确认（commit `a3962dd`）

阶段五修 bug 后的合理副产物，2 项修复：

| 项 | 改动 | 原因 |
|----|------|------|
| acceptance 警戒线 1600→1650 | regression-checklist.md L8/L19 + releasing.md L193/L199（4 处同步） | 阶段五场景 197 bash→node 多约 8 行，1604 > 1600 警戒线 |
| releasing.md L316 失效引用清除 | 「v1.2.2 三条教训 F-39/F2/F1」+ 场景号 153-166 → 指向 fresh-eyes「5 方向」+ 删硬编码场景号 | fresh-eyes-review.md 已重构为 5 方向抽象总结，旧引用滞后 |

> 三处验证链闭环：regression 维度 70（MCP 三处注册）→ acceptance scenario 192（4 tool + 三处注册）→ fresh-eyes 5 方向（数字漂移/搬迁残留覆盖）✅

## 阶段七：check-version.sh 归档排除补全

`FORGE/archive/` 是 FORGE 内部归档目录（冻结的历史设计文档），但 check-version.sh 只排除了 `_archive/` 和 `docs/archive/`，漏了 `FORGE/archive/`——导致 `self-evolution-design.md` 版本头仍为 v1.2.5 被报不一致。修复：2 处 `[[ ... ]] && continue` + 3 处 find `-not -path` 加 `FORGE/archive` 排除。

## 发布检查清单（汇总）

### 代码质量
- [x] npm test 1464/1464 全绿（audit 571 / core 185 / orchestrator 347 / daemon 171 等 12 包）
- [x] acceptance-test 132 场景全通过（含 v1.2.6 新增 scenario 192-197）
- [x] shellcheck 零 error（tools/ + FORGE/playbook/ 全部 .sh）
- [x] bash -n acceptance-test.sh 语法通过

### 版本号一致性
- [x] check-version.sh 65/65 全绿（阶段七补 FORGE/archive 排除后 66→65）
- [x] check-test-count.sh 13 处校验一致（CHANGELOG/ROADMAP/LIMITATIONS/evidence 声称数 = 实际 1464）
- [x] 版本号已 bump 至 1.2.6（package.json SSOT + 13 类位置同步）
- [x] 文档头日期同步 2026-08-04（EXPECTED_DOC_DATE + 14 份文档头）

### 审查体系
- [x] regression-checklist 50 维度（声称=实际），1041 行 ≤1050 警戒线
- [x] acceptance-test 132 场景（声称=实际），1604 行 ≤1650 警戒线
- [x] fresh-eyes-review 264 行 ≤350，5 方向历史教训落档
- [x] 维度 70（MCP 三处注册）↔ scenario 192 ↔ fresh-eyes 5 方向 验证链闭环
- [x] releasing.md SOP 无失效引用（L316 已修）

### 功能交付
- [x] 激活链 Phase 2：resolveAgent 支持 enterprise 动态查找
- [x] registry SubAgentDefinition 扩展 hitl/hitlConfig/knowledgeDomain
- [x] 2A workflow.yml 嵌套/平铺格式兼容
- [x] 2B SOFAGENT_LLM 四级回退链
- [x] MCP 4 tool（daemon_status/list_agents/list_concepts/hitl_resolve）三处注册一致
- [x] 文档死链清零（docs/ 下 .md 互链全部可达）
- [x] README Deployment Sizing 三档表格

### 文档同步（D6）
- [x] ARCHITECTURE.md 标注 v1.2.6（ORCHESTRATE L120 + mcp 包 L146）
- [x] activation-chain.md 版本表标注 v1.2.6
- [x] FDE/GUIDE.md workflow.yml 嵌套格式说明
- [x] 2B 环境变量回退链已补入 DEVELOPMENT.md L233（四级回退说明）
