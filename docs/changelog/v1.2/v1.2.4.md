# v1.2.4 开发日志 — 知识进化（分层巡检 + skillopt 自动触发 + 失败清单 + 联邦蒸馏）🔧

> ⚠️ **规划中，尚未实现。** 以下内容为版本目标规划，不代表当前可用能力。

> ⚠️ **本文档为开发规划，尚未实现。** 以下所有代码、接口、文件路径均为设计稿，`engine/` 下尚无对应代码。
> 状态：规划中（未开发） · 孔放勋 · 2026-07-25
> 前置依赖：v1.2.0（物理结构大重构）+ v1.2.1（custom/ 闭环 + 巡检合并）

## 背景

v1.2.2 解决了数据主权，v1.2.3 解决了 Dashboard 产品化 + 编排隔离。v1.2.4 回到"让 Agent 越用越聪明"的知识进化主线。

核心思路：**失败清单 > 正向评分**。告诉模型什么做法是错的，比什么是对的，信息量更大。

## LUI 感知

用户通过 Agent 对话感知：「Agent 告诉我'检测到知识库有 3 条矛盾记录，已标记冲突；上周跑的绩效汇总 workflow 有 2 次失败，已提取失败模式写入 think.md 并优化了 Skill'。我打开 Dashboard，看到知识健康度从 72 分升到 89 分。」

## 交付项

### P0 · 分层巡检 L1/L2/L3 + 读写回路对标

**设计**：当前 daemon 巡检是扁平的。改为三级分层：

| 层级 | 频率 | 检查内容 | 现有 inspector 归入 |
|------|------|---------|-------------------|
| **L1 快速健康** | @daily | 审计历史 + daemon 状态 + WARN 累积 | audit-history / doctor-health / warn-accumulator |
| **L2 深度巡检** | @weekly | 知识矛盾 + 孤儿 + 死链 + 新鲜度 + Skill 陈旧度 | conflict-check / knowledge-freshness / knowledge-health / skill-staleness |
| **L3 联邦分析** | @monthly | 跨设备蒸馏趋势 + 失败模式聚类 + Ontology 覆盖度 | 新增 federation-distillation / failure-pattern / ontology-coverage |

读写回路对标（gbrain Compiled Truth 理念）：巡检结果写入 Views 层（派生视图），不污染 Ledger 层（原始记录）。

**文件清单**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/inspector-layers.ts` | 新建 | L1/L2/L3 分层调度器 |
| `engine/daemon/src/inspectors/federation-distillation.ts` | 新建 | L3：跨设备知识蒸馏趋势 |
| `engine/daemon/src/inspectors/failure-pattern.ts` | 新建 | L3：失败模式聚类 |
| `engine/daemon/src/inspectors/ontology-coverage.ts` | 新建 | L3：Ontology 覆盖度 |
| `engine/daemon/src/index.ts` | 修改 | 从扁平调度改为分层调度 |

### P1 · skillopt 自动触发 + 失败清单驱动

**设计**：连续 3 次同类失败 → 自动触发 skillopt。每个 Skill 失败时记录失败场景 + 原因 + 正确做法，以负面样本驱动优化。

```typescript
if (failurePattern.repeatedCount >= 3) {
  await skillopt.optimize({
    skillId: failurePattern.skillId,
    failureMode: failurePattern.description,
    source: 'auto-trigger',
  });
}
```

**文件清单**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/skillopt/src/auto-trigger.ts` | 新建 | 失败模式监测 + 自动触发 |
| `engine/skillopt/src/failure-ledger.ts` | 新建 | 失败清单管理（记录/查询/聚类） |
| `engine/daemon/src/inspectors/skillopt-trigger.ts` | 新建 | @weekly：检查失败模式→触发 skillopt |
| `engine/skillopt/src/index.ts` | 修改 | 导出 autoTrigger + FailureLedger |

### P1b · Dashboard 历史趋势 + 任务统计（v1.2.2 延续）

**背景**：v1.2.2 交付了 `sofagent-dashboard`（终端日报告 + 实时面板），产出每日数据快照（`data/dashboard/daily-*.json`）。到 v1.2.4 已积累至少 2 周数据，可以做**历史趋势对比**。同时 v1.2.2 补了 `taskId` 追溯链——每条审计记录可追溯到具体任务，Dashboard 可以做**任务级统计**。

**交付**：

| 功能 | 数据源 | 刷新周期 | Dashboard 展示 |
|------|--------|---------|------|
| **周对比** | `daily-*.json` 聚合 | 每周一自动 | 本周 vs 上周：云端调用 / 本地执行 / 违规数 / 任务数 |
| **月趋势** | `daily-*.json` 聚合 | 每月 1 日自动 | 数据流向月度变化曲线 + 违规类型分布变化 |
| **任务成功率** | `history.jsonl`（含 taskId）| 每日聚合 | 今天 / 本周 / 本月任务成功率 + 失败任务列表（可追溯到具体任务） |
| **最常违规规则 TOP5** | `history.jsonl` | 每日聚合 | 展示"最近 7 天最容易犯的 5 条规则"——帮助用户针对性地加强约束 |

Dashboard 数据消费关系：

```
data/dashboard/daily-*.json   ← daemon @daily 生成单日聚合
     ↓
data/dashboard/weekly-*.json  ← daemon @weekly 按日聚合生成周报（含同比）
     ↓
sofagent-dashboard --trend     ← 终端渲染历史趋势视图（周对比 / 月趋势 / 任务统计）
```

**文件清单**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/inspectors/trend-aggregator.ts` | 新建 | @weekly：读 `daily-*.json` → 生成 `weekly-*.json`（含上周对比） |
| `engine/daemon/src/inspectors/task-stats.ts` | 新建 | @daily：读 `history.jsonl`（含 taskId）→ 聚合任务成功率 |
| `engine/daemon/src/index.ts` | +2 行 | 注册 trend-aggregator + task-stats inspector |
| `tools/sofagent-dashboard.sh` | +80 行 | 新增 `--trend` 模式：周对比柱状图 + 月趋势线 + 任务统计表 |

### P2 · conflict-check CLI + 联邦蒸馏

**设计**：独立 CLI 供用户手动检查 + 联邦蒸馏。

```bash
sofagent audit conflict-check [--fix]
sofagent audit federation-distill --peers device-a,device-b
```

**文件清单**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/cli/conflict-check.ts` | 新建 | 矛盾检测 CLI（可选 --fix 修复） |
| `engine/audit/src/cli/federation-distill.ts` | 新建 | 联邦蒸馏 CLI（CRDT 合并 + 矛盾标记） |
| `engine/audit/src/index.ts` | 修改 | 导出 CLI 命令 |

### P3 · Graph Engine Checker 扩展 + 受控循环升级

**⑧ 多类型 Checker 图节点**：当前 audit 节点只做文件系统审计。新增三个 inline Checker 作为独立图节点（在 audit 之后、reviewer 之前）：
- `format-checker`：输出是否符合预期 schema（JSON/Markdown/表格字段完整性）
- `fact-checker`：引用的文件/数据源是否真实存在、内容是否匹配
- `source-validator`：信息来源可信度评分（内部 trusted vs 外部 unverified）

Checker 发现的失败模式自动喂给 P1 skillopt 失败清单。

**⑨ 受控循环升级**：当前 engineer→audit→FAIL→engineer 是单一重试圈。升级为三种循环模式：
- "补信息后继续"：audit 发现证据不足→不直接 FAIL→回到 plan 补子任务
- "降级通过"：audit WARN 时不是直接过，标记后 reviewer 带警告展示
- "循环守卫"：每种循环有独立 `maxRetries` + `degradationThreshold` + `humanHandoffTrigger`
扩展 `buildLoopGraph` 条件边 + `LoopGraphState` 新增循环守卫字段。

### P0b · eval + ab-test 补全（半成品包激活）

**背景**：`@sofagent/eval` 和 `@sofagent/ab-test` 是 v1.2.0 拆出来的两个半成品——评分函数（`evalCase`）完整可用，但整条流水线从未接通：无 golden set、无 CLI、无数据持久化。v1.2.4「知识进化」的核心是"失败清单驱动优化"，而失败模式的数据源就是 eval 跑出来的质量指标。eval 不通，进化引擎的"读 eval 看哪个节点退化"就是空话。

**交付**：

| 能力 | eval | ab-test |
|------|------|---------|
| **golden set 测试集** | 用 21 条审计规则做 seed（每条 2-3 个用例，~50 条） | 复用 eval golden set |
| **CLI 入口** | `sofagent-eval`（package.json 加 bin） | `sofagent-ab-test`（已有 bin，接通 golden set） |
| **数据持久化** | `data/eval/history.jsonl`（append-only） + `data/eval/reports/*.md` | `data/ab-test/history.jsonl` + `data/ab-test/reports/*.md` |
| **进化引擎接通** | think-generator 读 `eval/history.jsonl` → 检测 passRate 下降趋势 | — |
| **Dashboard 消费** | v1.2.2 Dashboard 读 `eval/history.jsonl` → 质量趋势 | v1.2.2 Dashboard 读 `ab-test/history.jsonl` → A/B 对比 |

**data/ 数据流全景图**（eval + ab-test 补全后的完整数据流）：

```
                        写入侧（生产者）                          data/ 目录                          读取侧（消费者）
┌─────────────────────────────────────────┐  ┌──────────────────────┐  ┌─────────────────────────────────────┐
│ @sofagent/audit (审计引擎)               │  │ audit/               │  │ @sofagent/daemon (巡检器)            │
│   每次 commit/变更 → runRules()          │→ │   history.jsonl      │→ │   warn-accumulator (WARN 聚合)      │
│   会话结束 → buildSessionReport()        │→ │   session-report.json│→ │   audit-history-analyzer (趋势)     │
│                                          │→ │   session-report.md  │→ │   qa-verify-warn-accumulator         │
├─────────────────────────────────────────┤  ├──────────────────────┤  ├─────────────────────────────────────┤
│ @sofagent/think (反思生成器)              │  │ think.md             │  │ @sofagent/harness (加载链第3层)      │
│   generateThinkEntry() 基于 diff+审计结果 │→ │   (append-only)      │→ │   buildConstrainedSystemPrompt()     │
│                                          │→ │                      │→ │ @sofagent/daemon (dream-cycle)       │
│                                          │→ │                      │→ │   extract-facts() → knowledge/       │
├─────────────────────────────────────────┤  ├──────────────────────┤  ├─────────────────────────────────────┤
│ @sofagent/eval (评分引擎) ⭐ 本次补全      │  │ eval/ ⭐ 新增         │  │ @sofagent/think (进化引擎) ⭐ 接通    │
│   runEval() 跑 golden set                │→ │   history.jsonl      │→ │   检测 passRate 下降→写 think.md      │
│   eval-reporter 持久化                    │→ │   reports/*.md       │→ │ Dashboard (v1.2.2) 质量趋势面板       │
├─────────────────────────────────────────┤  ├──────────────────────┤  ├─────────────────────────────────────┤
│ @sofagent/ab-test (A/B 框架) ⭐ 本次补全   │  │ ab-test/ ⭐ 新增      │  │ @sofagent/orchestrator (ab-scheduler)│
│   runABTest() 对比方案                     │→ │   history.jsonl      │→ │   aggregateRecent() 方案判定          │
│                                          │→ │   reports/*.md       │→ │ Dashboard (v1.2.2) A/B 对比面板       │
├─────────────────────────────────────────┤  ├──────────────────────┤  ├─────────────────────────────────────┤
│ @sofagent/daemon (守护进程)              │  │ dashboard/           │  │ Dashboard (v1.2.2)                  │
│   health-reporter → runHealthReport()    │→ │   daemon-health.json │→ │   健康面板                            │
│   dream-cycle → extract/synthesize       │→ ├──────────────────────┤  │ @sofagent/harness (加载链第4层)      │
│                                          │→ │ knowledge/           │→ │   buildConstrainedSystemPrompt()     │
├─────────────────────────────────────────┤  ├──────────────────────┤  ├─────────────────────────────────────┤
│ FORGE driver                             │  │ forge-runs/          │  │ verdict.md (人类读)                  │
│   fresh-eyes/release-gate                │→ │   <loop>/<date>/run/ │→ │                                     │
└─────────────────────────────────────────┘  └──────────────────────┘  └─────────────────────────────────────┘
```

**文件清单**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/eval/src/cli.ts` | 新建 | CLI 入口（`sofagent-eval`）：--golden-set / --report-dir 参数 |
| `engine/eval/package.json` | 修改 | 加 `bin: { "sofagent-eval": "dist/cli.js" }` |
| `engine/eval/src/eval-runner.ts` | 修改 | runEval 加 dataDir 参数，默认落 `data/eval/` |
| `engine/eval/src/eval-reporter.ts` | 修改 | 加持久化：history.jsonl（append）+ reports/*.md |
| `data/eval/golden-set.yaml` | 新建 | 21 条审计规则 seed（~50 条用例） |
| `engine/eval/src/__tests__/cli.test.ts` | 新建 | CLI 端到端测试 |
| `engine/ab-test/src/ab-runner.ts` | 修改 | runABTest 接通 eval golden set + 持久化到 `data/ab-test/` |
| `engine/ab-test/src/__tests__/golden-set.test.ts` | 新建 | A/B 端到端测试 |
| `engine/core/src/data-paths.ts` | 修改 | 加 `EVAL_DIR` / `AB_TEST_DIR` 常量 |
| `engine/core/src/index.ts` | 修改 | 导出新常量 |
| `engine/think/src/think-generator.ts` | 修改 | 进化引擎加读 `eval/history.jsonl`（passRate 趋势检测） |
| `engine/eval/README.md` | 修改 | 更新 API 文档 |
| `data/README.md` | 修改 | 加 eval/ + ab-test/ 子目录说明 |
| `docs/changelog/v1.2/v1.2.4.md` | 修改 | 本文件（打勾） |
| `ROADMAP.md` | 修改 | v1.2.4 加 eval 补全交付项 |

## 质量验证

| 检查项 | 通过标准 |
|------|------|
| L1/L2/L3 分层巡检 | 各层按频率正确执行 |
| skillopt 自动触发 | 连续 3 次失败→自动触发 |
| conflict-check CLI | 手动检测+修复 |
| 联邦蒸馏 | 多设备知识合并去重 |
| eval + ab-test 补全 | golden set 可跑 + CLI 可用 + 数据落 data/ + 进化引擎接通 |
| npm test | 全绿 |

## 开发顺序

```
P0 分层巡检（L3 失败模式分析是 P1 输入）
  → P1 skillopt 自动触发 + 失败清单
  → P2 conflict-check CLI + 联邦蒸馏
```

## Release Notes · v1.2.4

### 🔨 核心变更

#### 分层巡检 L1/L2/L3
- **L1 @daily**：快速健康（审计历史 + daemon 状态 + WARN 累积）
- **L2 @weekly**：深度巡检（矛盾 + 孤儿 + 死链 + 新鲜度 + Skill 陈旧度）
- **L3 @monthly**：联邦分析（蒸馏趋势 + 失败模式聚类 + Ontology 覆盖度）
- 读写回路：巡检写入 Views 层，不污染 Ledger 层

#### skillopt 自动触发
- **失败模式监测**：连续 3 次同类失败自动触发 Skill 优化
- **失败清单**：负面样本驱动优化（"什么做法是错的"信息量更大）

#### Dashboard 历史趋势 + 任务统计
- **周对比**：本周 vs 上周（云端调用 / 本地执行 / 违规数 / 任务数），`sofagent-dashboard --trend` 查看
- **月趋势**：数据流向 + 违规类型分布月度变化
- **任务成功率**：`history.jsonl`（含 taskId）→ 每日 / 每周 / 每月任务成功率 + 失败任务回溯
- **最常违规 TOP5**：最近 7 天最容易违反的 5 条规则（帮助用户针对性加强约束）

#### conflict-check CLI + 联邦蒸馏
- **手动 CLI**：`sofagent audit conflict-check [--fix]`
- **联邦蒸馏**：多设备知识合并去重 + 矛盾标记

### ✅ 质量验证

| 检查项 | 结果 |
|------|:--:|
| npm test | ⏳ 全绿 |
| L1/L2/L3 | ⏳ |
| skillopt 自动触发 | ⏳ |
| CLI | ⏳ |
| 联邦蒸馏 | ⏳ |
| eval + ab-test 补全 | ✅ 42/42 = 100% |

> **eval + ab-test 补全验证明细**：golden set 42 条用例（21 规则 × 2），真实 CLI 端到端通过率 100%；eval 持久化 `data/eval/latest.json` + `history.jsonl` 正确；ab-test 历史迁移 `data/ab-history.jsonl` → `data/ab-test/scheduler-history.jsonl`；think-generator 进化引擎接通 eval（读 latest.json failures → 写 think.md 反思）；convertAuditResult 三态修复（WARN 不再误转 FAIL）；package.json files 字段补 data/。trust-but-verify 发现 mock 单元测试全绿 ≠ 真实引擎匹配，经重写 golden set 从 14.3% 提升至 100%。
