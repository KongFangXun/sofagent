# v0.99.5 · CI 自动化 + 审查修复版

> 2026-07-03 初版 / 2026-07-04 审查修复 · 版本类型：基础设施修复 + 文案品质升级 + 审查驱动修复
> 审查来源：v0.99.4 发版过程中发现 npm publish 手动流程瓶颈 + landing page 文案审校

## 一句话

npm 发布从手动 OTP 升级为 OIDC Trusted Publishing 全自动。文案体系对齐 landing page，统一 tagline、流程术语和交付物表述。

## 核心交付

### CI 自动化

| 项目 | v0.99.4（旧） | v0.99.5（新） |
|------|:--:|:--:|
| 认证方式 | NPM_TOKEN secret（Granular Token） | OIDC Trusted Publishing |
| 2FA 要求 | 需要 bypass 2FA token 或不稳定地关闭 2FA | 零 2FA——OIDC 自动签发临时 token |
| 发布流程 | 手动 `npm login` → 输 OTP → `npm publish` | `git tag vX.Y.Z` → `git push` → GitHub Actions 自动发布 |
| 安全级别 | 持久 token 泄露风险 | 短期 OIDC token（自动过期） |

### 具体变更

- **release.yml**：两个 publish job 新增 `permissions: id-token: write`，移除 `NODE_AUTH_TOKEN` env
- **release.yml**：`npm ci` 后 `npm install --no-save @rollup/rollup-linux-x64-gnu || true`（解决 macOS lock 文件在 Linux CI 上漏装原生模块）
- **release.yml**：新增 `workflow_dispatch` 触发器（允许手动重跑）
- **npm 端**：`@sofagent/audit` 和 `@sofagent/mcp` 均配置 Trusted Publisher（KongFangXun/sofagent → release.yml）

### 工作流 SOP 更新

- **docs/SOFAGENT_VERSION_SOP.md**：
  - bump-version 扫描范围：`audit/src/` → `audit/src/` + `mcp/src/`（修 P1-12）
  - shellcheck 门槛：零 warning → 零 error（降 P2 warning 不阻塞发版）
  - 检查清单规则：「强制 `[x]`」→「质量验证命令可替代」
  - 新鲜度检查标责任方（工程师自测时跑）
  - 覆盖表补 `FDE/SKILL.md` 和 `mcp/src/**/*.ts`

## 质量验证

### 07-04 修复后（最终）

```bash
npm test              → 406 tests passed ✅
npx tsc --noEmit      → audit 零错误 + mcp 零错误 (strict: true) ✅
check-version.sh      → 30/30 ✅
verify.sh --quiet     → 41 项全部通过 ✅
check-docs.sh         → 核心文档未超标 ✅
npm pack --dry-run    → audit 87 文件（npm pack .js.map=0 ✅），mcp 5 文件（prepublishOnly 清理 .js.map 后发布 3 个）
optionalDependencies  → 已移除 @rollup ✅
bump-version --dry-run → 12 步全有效，43 处可替换（v0.99.5 时标注 12 步，v0.99.6 统一为 13 步） ✅
```

### 07-03 审查前（历史记录）

```bash
npm test              → 406 tests passed ✅
npx tsc --noEmit      → 零错误 (strict: true, noUncheckedIndexedAccess: true) ✅
check-version.sh      → 28/28 ✅
verify.sh --quiet     → 41 项全部通过 ✅
check-docs.sh         → 核心文档 4425 行（上限 5000）✅
npm pack --dry-run    → audit 171 文件（含 42 个 .js.map 🔴）mcp 3 文件
```

## 🔴 审查发现：v0.99.5 发版不完整问题

> **审查日期**：2026-07-03 · **审查者**：软件开发团队（齐活林 + 高见远 + 许清楚 + 严过关 + DeepSeek V4 Pro）
> **审查维度**：15 维度全方面审查（产品架构、技术架构、安装体验、代码质量、CLI/API、文字质量、语言风格、数字可信度、测试哲学、证据可信度、开源成熟度、Skill 质量、v1.0 就绪度、文档可发现性、安全合规）

v0.99.5 核心交付（OIDC Trusted Publishing + 文案对齐）已实现，但审查发现本次发版存在多处 **bump-version 覆盖不全**和**doc-vs-reality 不一致**，需在 v0.99.6 修复。

### P0 — 必须修复（阻塞 v1.0 发版）

| # | 问题 | 文件:行号 | 具体表现 | 根因 |
|---|------|-----------|---------|------|
| P0-1 | README badge 版本号未更新 | `README.md:7` `README.en.md:9` | badge 显示 `v0.99.3`，CHANGELOG 声称已同步到 v0.99.5 | bump-version.sh 未覆盖 badge URL 中的版本号 |
| P0-2 | ROADMAP "现在在哪" 未更新 | `ROADMAP.md:11` | 节标题仍是 `v0.99.4`，内容描述也是 v0.99.4 | bump-version.sh 未覆盖 ROADMAP 的「现在在哪」节 |
| P0-3 | @sofagent/mcp 版本号滞后 | `sofagent/mcp/package.json:3` | 版本号 `0.99.4`，与 audit `0.99.5` 不一致 | bump-version.sh 未覆盖 mcp/package.json |
| P0-4 | npm pack 包含 42 个 .js.map | `sofagent/audit/` | `npm pack --dry-run` 显示 42 个 `.js.map` 文件被打包 | 发布配置缺少 `.js.map` 排除规则 |
| P0-5 | "零外部运行时依赖"不属实 | `sofagent/audit/package.json:18` | 依赖 `js-yaml@^5.2.0`，另有 `@rollup/rollup-linux-x64-gnu` 为 optionalDependency | 文档声称与现实不符 |
| P0-6 | env-check.ts 版本号残留 | `sofagent/audit/src/env-check.ts:2` | 文件头仍写 `v0.99.4` | bump-version.sh 未覆盖此文件 |

### P1 — 应在 v0.99.6 修复

| # | 问题 | 文件:行号 | 具体表现 | 建议方案 |
|---|------|-----------|---------|---------|
| P1-1 | install.sh 注释版本号未更新 | `install.sh:3` | 文件头注释写 `v0.99.4` | 纳入 bump-version 覆盖范围 |
| P1-2 | mcp-server.ts VERSION 常量未同步 | `sofagent/mcp/src/mcp-server.ts:53` | `SERVER_VERSION = '0.99.3'`，且有 TODO 注释 | 改为从 @sofagent/audit shared/constants 导入 VERSION |
| P1-3 | check-version.sh 漏检 | `tools/check-version.sh` | 未检查 README badge URL 中的版本号、mcp/package.json、mcp-server.ts | 扩展检查脚本覆盖范围 |
| P1-4 | ARCHITECTURE 表格残留 v0.99.4 引用 | `ARCHITECTURE.md:52` | MCP 推送层表格中写 `v0.99.4 MCP Server` | 更新为 v0.99.5 |
| P1-5 | DEVELOPMENT.md Skill 数量描述 | `DEVELOPMENT.md:37` | 说「9 个 .md」，但当前为 10 个 Skill 文件 | 更新为准确数字 |
| P1-6 | FDE Case 证据不可独立验证 | `docs/evidence/cases/fde-*-2026-07-02/` | 4 个 FDE case 目录各只有 1 个 README.md（2-3KB），原始证据声明在「企业内网」 | 建议最少包含匿名化的工作流模板截图或脱敏节点列表 |
| P1-7 | Case 018 "持续使用 2+ 周" 时间线可疑 | `docs/evidence/evidence.md:65` | 目录和文件创建时间均为 2026-07-02，但声称已持续使用 2+ 周（意味着从 6 月中旬开始）；同时说「运行不到 1 个月」 | 核实时间线，确保声称一致 |
| P1-8 | install.sh `--quick` 模式无验证输出 | `install.sh` | 快速安装完成后缺少对用户的下一步指引（如"运行 verify.sh --quiet 确认安装成功"） | 在 --quick 模式结尾添加下一步提示 |

### P2 — 改进建议

| # | 问题 | 建议方案 |
|---|------|---------|
| P2-1 | test-utils 过薄（18 行） | 提取重复的 setup 逻辑（mock diff、mock log、mock config）到 test-utils.ts |
| P2-2 | @sofagent/mcp 零测试覆盖 | 为 MCP Server 的 4 个 tool 添加单元测试 |
| P2-3 | daemon / install.sh 零自动化测试 | ROADMAP 标注为 ⚠️ 但不够具体——建议 LIMITATIONS 写明「无自动化测试，仅手动验证」 |
| P2-4 | verify.sh 41 项缺乏中文说明 | 输出中英文混排，部分条目只用英文标签，新用户难以理解 |
| P2-5 | CONTRIBUTING bus factor 声明 | bus factor = 1（唯一维护者），建议添加 co-maintainer 邀请路径 |
| P2-6 | 卸载残留 | `uninstall.sh` 未清理 `.sofagent/` 目录下的部分子目录（需实际验证） |
| P2-7 | CODE_OF_CONDUCT 缺少执行邮箱 | 执行节说「发邮件」但未列出具体邮箱地址 |
| P2-8 | npm pack 171 文件过重 | audit 包 171 个文件偏多，可考虑排除 `dist/test/` 目录中的测试构建产物 |

### bump-version.sh 遗漏清单（对照 v0.99.3→v0.99.5）

以下文件/位置在 bump-version 时未被更新，需要扩展脚本覆盖范围：

| 遗漏位置 | 当前值 | 期望值 | 修复方式 |
|---------|--------|--------|---------|
| `README.md` badge URL | `Version-v0.99.3` | `Version-v0.99.5` | 扩展 bump-version.sh 匹配 badge URL |
| `README.en.md` badge URL | `Version-v0.99.3` | `Version-v0.99.5` | 同上 |
| `ROADMAP.md` 节标题 | `现在在哪：v0.99.4` | `现在在哪：v0.99.5` | 扩展 bump-version.sh 匹配「现在在哪」行 |
| `sofagent/mcp/package.json` | `0.99.4` | `0.99.5` | 纳入 bump-version 覆盖 |
| `sofagent/mcp/src/mcp-server.ts` | `0.99.3` | `0.99.5` | 纳入 bump-version 覆盖 |
| `sofagent/audit/src/env-check.ts` | `v0.99.4` | `v0.99.5` | 纳入 bump-version 覆盖 |
| `install.sh` | `v0.99.4` | `v0.99.5` | 纳入 bump-version 覆盖 |
| `ARCHITECTURE.md` 表格引文 | `v0.99.4 MCP Server` | `v0.99.5` | 扩展 bump-version 匹配表格内版本号 |
| `DEVELOPMENT.md` 达标声明 | `v0.99.4 全部达标` | `v0.99.5 全部达标` | 扩展 bump-version 覆盖 DEV 文件 |
| `check-version.sh` 检查范围 | 未检查以上 9 处 | 全部纳入 | 扩展检查脚本

## 涉及文件

| 文件 | 变更 |
|------|------|
| `.github/workflows/release.yml` | OIDC 权限 + rollup 容错 + workflow_dispatch |
| `SOFAGENT_VERSION_SOP.md` | bump-version 覆盖 + shellcheck 阈值 + 检查清单降权 |
| `README.md` | tagline「企业上 AI，先上缰绳再上路」+ 三样交付物表 + 四步落地 |
| `README.en.md` | badge 同步 + 文案对齐 |
| `HANDBOOK.md` | 第一段改 tagline + 十步→四阶段十二步 |
| `FDE/FDE.md` | 精简开头 + 交付物卡片 + 删除冗余状态行 |
| `FDE/SKILL.md` | 补 triggers/scenarios/not_when + 交付物表 |
| `LIMITATIONS.md` | 十步→四阶段十二步 |
| `index/` | 新建——landing page (index.html) + 图片资产，替代 images/ |
| `images/` | 删除——全部图片迁移到 index/ |

## 发布检查清单

- [x] npm test 全绿（406 passed）
- [x] check-version.sh 30/30
- [x] verify.sh 41 项全部通过
- [x] npm pack .js.map = 0，文件数 87
- [x] optionalDependencies 已移除
- [x] Trusted Publisher 配置 · audit ✅ · mcp ✅
- [x] release.yml OIDC 权限到位
- [x] SOFAGENT_VERSION_SOP.md 已更新
- [x] CHANGELOG.md 已更新
- [x] ROADMAP.md 已更新

## 审核结论

**v0.99.5 就绪。** 核心价值：今后每次 `git tag vX.Y.Z --push` → npm 自动发布，零手动操作。

**2026-07-04 补充**：审查发现的所有 P0/P1 问题已于同日修复。全仓版本号一致性 30/30 ✅，日期统一为 2026-07-04，bump-version.sh 和 check-version.sh 增强完成。406 tests 全绿，verify 41/41，npm pack .js.map=0，零依赖假话清零。
