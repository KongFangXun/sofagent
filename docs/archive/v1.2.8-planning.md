# v1.2.8 开发日志 — 记忆分层 + 定时任务 + 无人值守审批

> ⚠️ **规划中，尚未实现。** 以下内容为版本目标规划，不代表当前可用能力。

> 状态：规划中 · 孔放勋 · 2026-07-29
> 前置依赖：v1.2.7 完成
> 灵感来源：[DeerFlow 2.0](https://github.com/bytedance/deer-flow) · 字节跳动 + [OpenWorker](https://github.com/andrewyng/openworker) · Andrew Ng 团队

## 定位

**v1.2.8 聚焦记忆系统、定时任务和无人值守审批**——补齐 sofagent 在"长期记忆管理"、"自动化巡检调度"和"敏感操作批量审批"上的工程化短板。P1-P4 来自 DeerFlow 启发，P5 来自 OpenWorker 启发。

## P1：记忆事实级分层

**痛点**：当前 knowledge/ 是目录级（entity/concept/cluster），Dream Cycle 从 think.md 抽取知识时缺事实级粒度——同一条事实散落在多个 think.md，无法去重、无法衰减、无法按用户隔离。

**DeerFlow 做法**：
- 跨会话持久记忆——用户画像、偏好、积累知识
- 文件支持的记忆分离全局用户上下文和代理事实
- 每个**用户**一个 `memory.json`，每个**事实**是独立的 Markdown 文件
- `__default__` 桶保留默认记忆
- 记忆更新跳过重复事实条目（单事实仓库操作是真正增量的）
- 检索引擎在 memory 检索适配器契约后面是可选的

**sofagent 移植方案**：
- knowledge/ 增加 `facts/` 子目录——每个事实一个 `.md`（frontmatter 带 `confidence` + `last_triggered` + `source`）
- Dream Cycle 抽取事实时写 facts/，重复事实跳过（content-hash 去重）
- 30 天无触发衰减（已有 think.md 衰减逻辑，扩展到 facts/）
- 多设备联邦时，facts/ 按 sensitivity 分级（已有机制，复用）

**实现位置**：`engine/ontology/src/` + Dream Cycle pipeline

## P2：Scheduled Tasks MVP

**痛点**：LIMITATIONS §七明确写了"定时触发做不到"——daemon cron.ts 只是占位（`@weekly`/`@daily`/`@hourly` 读 watch.yml，但行为未充分验证）。FDE Agent 的"周度巡检"靠人触发，不是真定时。

**DeerFlow 做法**：
- 工作区中的一级定时任务 MVP
- 管理路径：`/workspace/scheduled-tasks`
- 支持 `once` 和 `cron` 调度
- 后台执行为非交互式运行
- `skip` 重叠行为
- 暂停、恢复、触发、检查历史、删除任务

**sofagent 移植方案**：
- daemon cron.ts 升级为一级定时任务管理
- `sofagent-audit --schedule` CLI 管理（add/list/pause/resume/trigger/delete）
- 调度配置存 `.sofagent/schedule.json`（与 watch.yml 合并或替换）
- `skip` 重叠行为（上一个还没跑完，跳过本次）
- 巡检结果写 history.jsonl（已有机制）

**实现位置**：`engine/daemon/src/cron.ts` 升级 + 新增 schedule 管理 CLI

## P3：Workspace 变更摘要

**痛点**：每次 Agent 运行后，用户/Dashboard 看不到"这次运行改了哪些文件"——得自己跑 git diff。审计日志有规则判定，但没有"文件变更清单"。

**DeerFlow 做法**：每次运行后记录创建、修改、删除的文件清单（workspace 变更徽章）。

**sofagent 移植方案**：
- 每次 audit 运行后，在 history.jsonl 的 entry 里加 `workspace_changes` 字段（created/modified/deleted 文件列表）
- Dashboard 消费这个字段做"变更徽章"（v1.2.2+ Dashboard 数据前置）

**实现位置**：`engine/audit/src/audit-history.ts`（已有 entry 结构，加字段）

## P4：ToolOutputBudget 中间件化

**痛点**：memory 记录的 sf_read 500 行截断是单点修复——只在 sf_read 工具层做。其它工具（grep/glob/bash）输出也可能爆炸。

**DeerFlow 做法**：ToolOutputBudget 中间件——所有工具输出都经过预算控制，分层截断（不是一刀切）。

**sofagent 移植方案**：
- 把 sf_read 500 行截断抽象为 ToolOutputBudget 中间件
- 所有 sf_* 工具输出经过这个中间件
- 分层策略：前 N 行全文 + 中间摘要 + 末尾 N 行（而非硬切 500 行）
- 与 v1.2.7 的 /compact 配合（工具输出先预算，再 compact）

**实现位置**：`engine/harness/src/` 新增 ToolOutputBudget 中间件 + sf_* 工具注册时包装

## P5：无人值守收件箱（OpenWorker 启发）

**痛点**：Scheduled Tasks MVP（P2）交付后，daemon 在无人值守模式下跑巡检，碰到需要审批的操作（比如修复 knowledge 矛盾、自动 commit 变更）怎么办？现在只有两条路——要么自动执行（有风险），要么阻塞等用户上线（定时任务形同虚设）。

**OpenWorker 做法**：
- 四级权限模型：`plan`（只读）→ `interactive`（默认逐操作审批）→ `auto`（白名单放行）→ `custom`（自定义）
- `allowed_commands` 命令白名单 + `auto_allow` 工具白名单——安全操作直接放行
- **无人值守收件箱**：consequential 操作（写/发/执行）不直接执行也不阻塞，暂存到收件箱等用户批量审核

**sofagent 移植方案**：
- daemon 新增审批模式：`auto`（白名单放行）/ `inbox`（暂存等审批）/ `blocked`（阻塞等待）
- `.sofagent/config.yml` 新增 `approval` 配置段：`mode` + `auto_allow`（工具白名单）+ `allowed_commands`（命令前缀白名单）
- consequential 操作（commit/push/写 knowledge/发 webhook）暂存到 `data/inbox/`（每个操作一个 JSON：操作类型/目标/diff 预览/时间戳）
- `sofagent approve --list` / `--approve <id>` / `--reject <id>` CLI 批量审核
- 与 P2 Scheduled Tasks 配合：定时任务跑出的敏感操作自动进收件箱，用户上线后批量处理

**实现位置**：`engine/daemon/src/approval.ts`（新增）+ `engine/daemon/src/cron.ts`（集成审批判断）+ schedule 管理 CLI 扩展 approve 子命令

## 质量验证

| 检查项 | 通过标准 |
|------|------|
| 记忆事实级分层 | Dream Cycle 抽取的事实写 facts/，重复事实跳过，30 天衰减 |
| Scheduled Tasks | cron+once 调度生效，暂停/恢复/触发/历史/删除全链路 |
| Workspace 变更摘要 | 每次运行后 history.jsonl 含 workspace_changes 字段 |
| ToolOutputBudget | 所有 sf_* 工具输出经预算中间件，分层截断非硬切 |
| 无人值守收件箱 | daemon 审批模式三态生效，consequential 操作暂存 inbox，CLI 批量审核链路通 |
| npm test | 全绿 |

## 开发顺序

```
P3 Workspace 变更摘要（最低成本，先做给 Dashboard 铺数据）
  → P4 ToolOutputBudget 中间件化（FORGE 长任务稳定性）
  → P1 记忆事实级分层（Dream Cycle 增强，依赖 ontology 稳定）
  → P2 Scheduled Tasks MVP（daemon 升级，独立模块）
  → P5 无人值守收件箱（依赖 P2 的定时任务底座，做审批层）
```

## Release Notes · v1.2.8

### 🔨 核心变更

#### 记忆事实级分层（P1）
- **facts/ 目录**：每个事实一个 .md，content-hash 去重，30 天衰减

#### Scheduled Tasks MVP（P2）
- **一级定时任务**：cron+once / 暂停/恢复/触发/历史/删除，skip 重叠

#### Workspace 变更摘要（P3）
- **变更徽章数据**：history.jsonl 加 workspace_changes 字段

#### ToolOutputBudget 中间件化（P4）
- **分层截断**：前 N 行全文 + 中间摘要 + 末尾 N 行，非硬切

#### 无人值守收件箱（P5）
- **daemon 审批三态**：auto（白名单放行）/ inbox（暂存等审批）/ blocked（阻塞等待）
- **consequential 操作暂存**：commit/push/写 knowledge/发 webhook 进 `data/inbox/`
- **CLI 批量审核**：`sofagent approve --list / --approve / --reject`

### ✅ 质量验证

（待开发后填写）

### 📐 行业对标

- P1-P4 设计启发自 [DeerFlow 2.0](https://github.com/bytedance/deer-flow)，详见 [PHILOSOPHY §十 · DeerFlow](../../docs/PHILOSOPHY.md#deerflow-20大厂用harness命名的活样本2026-07-行业印证)
- P5 设计启发自 [OpenWorker](https://github.com/andrewyng/openworker)（四级权限模型 + 无人值守收件箱），详见 [THANKS · OpenWorker](../../docs/THANKS.md)

---

> 本文档为规划状态，开发完成后更新 Release Notes 与质量验证。
