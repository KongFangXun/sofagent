name: verify
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 18

      - name: Setup minimal sofagent environment
        run: |
          bash install.sh --platform openclaw --quick 2>/dev/null || {
            # install.sh fallback 到手动部署
            echo "[CI] install.sh failed, fallback to manual setup"
            mkdir -p ~/.openclaw/skills/sofagent
            for f in engage.md engage-fde.md entry-gate.md task-aware.md task-closure.md loop-check.md loop-evaluate.md loop-exit.md knowledge-maintain.md; do
              [ -f "SKILL/harness/$f" ] && cp "SKILL/harness/$f" ~/.openclaw/skills/sofagent/
            done
            cp SKILL/SKILL.md ~/.openclaw/skills/sofagent/SKILL.md 2>/dev/null || true
            mkdir -p ~/.openclaw/skills/sofagent/data
            cp SKILL/harness/data/*.md ~/.openclaw/skills/sofagent/data/ 2>/dev/null || true
            cp -r engine/scripts ~/.openclaw/
            chmod +x ~/.openclaw/scripts/*.sh
            mkdir -p ~/.openclaw/hooks/sofagent-load-chain
            cp engine/hooks/sofagent-load-chain/HOOK.md ~/.openclaw/hooks/sofagent-load-chain/
            cp engine/hooks/sofagent-load-chain/src/handler.ts ~/.openclaw/hooks/sofagent-load-chain/
          }

      - name: Run verify.sh
        run: |
          bash engine/scripts/verify.sh --quiet || {
            echo "❌ verify.sh failed"
            exit 1
          }
          echo "✅ verify.sh passed"
