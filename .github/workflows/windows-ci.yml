name: windows-ci

# Note: vitest is NOT run on Windows due to rollup native module
# compatibility issues. Unit tests run on macOS/Linux CI instead.
# See ROADMAP.md v1.0 criteria #9 for details.

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: '22'
          # 缓存 ~/.npm（key 由根 package-lock.json 哈希派生，lock 变更自动失效）。
          # Windows 上收益最大——无缓存时 npm ci 是全流程最慢的一步。
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Build (workspace 拓扑序)
        run: npm run build
      - name: Smoke test (audit CLI)
        run: node engine/audit/dist/index.js --help
