# FORGE loop 模型环境变量模板（阿里百炼 Qwen Token Plan）
# 复制为 env.local 并填入真实 key 后 `source FORGE/env.local`
# 🔴 env.local 已被 .gitignore 忽略，绝不要把真实 key 提交到 git
#
# Qwen 接口：https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
# 模型：qwen3.8-max-preview（thinking-only，无需额外思考参数）

# API key（A/B 两角色共用同一个 Qwen Token Plan key）
export SOFAGENT_LLM_A_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxx"
export SOFAGENT_LLM_B_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxx"

# 占位变量（driver 启动检查要求非空，内容不影响模型选择）
export SOFAGENT_LLM_A="qwen3.8-max-preview"
export SOFAGENT_LLM_B="qwen3.8-max-preview"
