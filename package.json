{
  "version": "1.1.9",
  "private": true,
  "workspaces": [
    "engine/harness",
    "engine/ontology",
    "engine/eval",
    "engine/core",
    "engine/think",
    "engine/audit",
    "engine/orchestrator",
    "engine/daemon",
    "engine/ab-test",
    "engine/skillopt",
    "engine/mcp",
    "engine/rules-engine"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "build": "npm run build --workspace=engine/harness && npm run build --workspace=engine/ontology && npm run build --workspace=engine/eval && npm run build --workspace=engine/core && npm run build --workspace=engine/think && npm run build --workspace=engine/audit && npm run build --workspace=engine/orchestrator && npm run build --workspace=engine/daemon && npm run build --workspace=engine/ab-test && npm run build --workspace=engine/skillopt && npm run build --workspace=engine/mcp"
  }
}
