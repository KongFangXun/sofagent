# sofagent cordis-plugin-sofagent-audit bundle patch v1.4.8——注册为 DSH profile layer
- insert:
    - id: sofagent-audit
      name: 'cordis-plugin-sofagent-audit'
      inject: [settings, dynamicCordisRunner]
      config:
        seam: "tools/result + tools/pre-execute + fs/write-intent"    # 语义：工具结果留证 + 工具执行前拦截 + 文件写入意图拦截
        description: "给宿主加上审计能力——24 条 git diff 硬证据规则 + 危险操作黑名单——桥接 @sofagent/audit runRules"
