# sofagent cordis-plugin-sofagent-gate bundle patch v1.4.7——注册为 DSH profile layer
- insert:
    - id: sofagent-gate
      name: 'cordis-plugin-sofagent-gate'
      inject: [settings, dynamicCordisRunner]
      config:
        seam: "agent/turn-stopping"    # 语义：Turn 结束前停止条件判定——验收不过不放行
        description: "验收不过不放行——机器可判定验收 + 人审——桥接 @sofagent/audit runRules"
