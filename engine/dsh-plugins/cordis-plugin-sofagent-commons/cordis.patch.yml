# sofagent cordis-plugin-sofagent-commons bundle patch v1.4.8——注册为 DSH profile layer
- insert:
    - id: sofagent-commons
      name: 'cordis-plugin-sofagent-commons'
      inject: [settings, dynamicCordisRunner]
      config:
        seam: "non-seam:tool-set"    # 语义：非宿主事件接入（tool 集）——能力公地五环按需调用
        description: "给宿主加上能力公地入口——能力发布 / 发现 / 调用 / 评价 / 养护五环——桥接 @sofagent/audit loadConfig"
