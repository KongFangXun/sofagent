{
  "id": "sofagent-inject",
  "name": "sofagent 注入",
  "description": "约束注入——before_prompt_build 时注入 sofagent 四层加载链（core-rules/think.md/fde.md/knowledge），品牌色 #16B8F3",
  "version": "1.4.0",
  "icon": "https://raw.githubusercontent.com/KongFangXun/sofagent/main/assets/logo.png",
  "uiHints": {
    "brandColor": { "label": "品牌色", "placeholder": "#16B8F3" }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true, "description": "是否启用约束注入" },
      "projectRoot": { "type": "string", "description": "sofagent 项目根目录（注入四层加载链的数据源）" },
      "brandColor": { "type": "string", "default": "#16B8F3", "description": "品牌色" }
    }
  }
}
